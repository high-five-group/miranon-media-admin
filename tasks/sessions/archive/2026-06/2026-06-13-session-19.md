---
owner: marcus803
updated: 2026-06-15
review_by: 2026-09-13
status: stable
lifecycle: closed
---

# Session 19 — Staging-miljö (designsession, ingen byggfas-status-ändring)

> Doket fött vid sessionsstart per ADR-043 + L67/L68 (levande artefakter har
> landnings-kadens, inte avsluts-kadens). Fött via session-start-skillens
> create-session-doc-gren (plugin marcus-hub,
> `references/create-session-doc.md`). Del 2+ fylls vid respektive
> landnings-ögonblick (committad + pushad + CI-grön), inte vid sessionsavslut.

## Del 1 — Syfte och scope

**Session 19 — Staging-miljö (designsession).**

Designa och bygga en riktig, isolerad staging-miljö så att Fas 5.5:s vertikala
write-slice kan testas och deployas utan att röra den levande miljön. Sessionen
är **research-gated**: web-research mot förstapartsmönster (Supabase + Airtable)
→ utförande-ADR (ADR-050) → bygge. **INGEN ADR-stub skrivs i förväg.**

### Bakgrund

Session 18 pausade Fas 5.5 mitt i när deploy-verifieringen visade att orgen har
**ETT enda Supabase-projekt** — "staging" == produktion + samma Airtable-bas
(L110). Hela test-/CI-/config-infran är redan författad som om staging vore
separat; denna session bygger miljön infran antar.

### Ingångstillstånd (disk-verifierat i orienterings-passet 2026-06-13)

- Spoke-HEAD `f185cb0`, rent arbetsträd, i sync med `origin/main`.
- **Supabase:** ett projekt (`lvjsfnphlauldxqlncpl`, "miranon-media-admin",
  West EU) — empiriskt bekräftat via `supabase projects list`. staging==prod
  håller.
- **Inbakade staging-antaganden:** 6 obligatoriska `TEST_*`-secrets
  (`TEST_SUPABASE_URL`, `TEST_SUPABASE_ANON_KEY`, `TEST_USER_EMAIL`,
  `TEST_USER_PASSWORD`, `TEST_ADMIN_EMAIL`, `TEST_ADMIN_PASSWORD`) + framtida
  `TEST_REGISTRATION_RECORD_ID`; `STAGING_REQUIRED:'1'` hard-fail i `ci.yml`;
  3 skippade tester i `update-record.staging.test.ts` (rad 56/83/110) gated på
  redeploy + mutations-säker record; `config.toml`-TODO "test-*-funktioner får
  ALDRIG nå produktion"; CI saknar deploy-steg.

### Numrering (disk-verifierad i orienterings-passet 2026-06-13)

Session 19; nästa ADR-050 (högsta på disk ADR-049); nästa lesson L114 (högsta
på disk L113). DISK-VERIFIERADE, ej index-antagna.

### Scope-avgränsning

- Research **FÖRE** arkitektur (förstaparts-mönster, ≥3 referenspunkter) →
  ADR-050 → bygge.
- **ABSOLUT ingen** deploy / `db push` / PATCH mot den levande miljön under
  sessionen.
- Fas 5.5 förblir **PÅGÅENDE** (ej KLAR); byggplanens fas-tabell orörd tills
  slicen är klar.

Del 2+ fylls vid respektive landnings-ögonblick (committad + pushad + CI-grön).

## Del 2 — Förlopp och landningar

Commit-range `3a3269a` → `009a8d1`. Sessionen var research-gated: empirisk
miljö-verifiering → ADR-050 → förarbete steg 1+2 → Marcus miljö-moment.

### ADR-050 skriven + landad

[ADR-050](../../docs/decisions/ADR-050-isolerad-staging-miljo.md): isolerad
staging-miljö (separat Supabase-projekt på Pro + dedikerad Airtable-bas utan
records + env-driven `AIRTABLE_BASE_ID`). Avvisar Free+keep-alive (väg A) och
branching (väg C) som primär. Commits: `1f9d5b4` (ADR-fil) + `8445f75`
(grindfix). CI föll först på två konsistens-partners → forward-fix: README
count-gate 49→50, ADR-katalog-rad, Vale code-span på länktext. Status `Accepted`
(engelsk frontmatter-konvention — Code-override av prompt-värdet "Accepterad",
korrekt per 49-ADR-beståndet; se L114).

### Förarbete steg 1 — AIRTABLE_BASE_ID env-drivet + tabell per namn

Commit `49267b4`. `AIRTABLE_BASE_ID` läses från env med fail-fast
(`'AIRTABLE_BASE_ID not set'`), INGEN hårdkodad prod-fallback. Tabell-
adressering i de 4 EF:erna (get-events/persons/registrations + field-allowlists)
bytt från bas-unika `tbl`-id:n till tabell-NAMN (Eventplanering/Personer/
Anmälningar, live-verifierade mot basen). Airtable officiella docs bekräftade
namn-i-path (`{baseId}/{tableIdOrName}`, "interchangeable"). Filerna
`.env.test.example` och `ci.yml` medvetet EJ rörda: ingen testväg läser secreten (testerna anropar
deployade EF:er över HTTP), och `AIRTABLE_TOKEN` — samma secret-klass — finns
inte heller där. Deploy-only Supabase-secret. Se L117.

### Förarbete steg 2 — fail-closed prod-deploy-allowlist

Commit `009a8d1`. Levererar SPÄRREN mot Fas 7-skulden (test-* till prod):
`.prod-functions-allowlist.conf` (5 prod-funktioner; `test-auth` medvetet utanför),
`scripts/deploy-prod-functions.sh` (deployar endast allowlistade; ersätter
farliga bare `supabase functions deploy`), `scripts/test-deploy-prod-functions.sh`
(4 testfall, bevisar fail-closed) samt eget CI-steg. Allowlist-över-blocklista: ny
funktion = prod-exkluderad by default. Doc-bindning i `config.toml` + lessons.md
L115. Se L118 (workspace-isolering).

### Marcus miljö-moment (utfört av Marcus under sessionen)

- **Supabase uppgraderad till Pro** (löser ADR-050 T1).
- **Staging-projekt skapat:** `miranon-media-admin-staging`, AWS eu-west-1,
  Micro, Data API på, auto-expose-new-tables AV, auto-RLS av.
- **Airtable staging-bas skapad:** "Miranon Media OS - staging", UTAN records, i
  SAMMA workspace som prod (medvetet — isolering via distinkt bas-ID +
  env-config, ej workspace; se L118).
- Nya ref/bas-/tabell-ID:n återstår att läsas empiriskt (bygg-steg 3, Session 20).

### Resume-landning — bygg-steg 3+4 (staging empirisk läsning + secrets satta)

Resume-19 (2026-06-14). Secret-set sker CLI→Supabase-store och ger ingen
committbar artefakt — **denna doc-post ÄR den durabla landnings-posten** (L67).

**Bygg-steg 3 (empirisk läsning).** Staging-PAT (skapad av Marcus, lagd i
gitignore-ad `.env.staging-secrets`, läge 600) källad utan eko. Airtable
`/meta/bases` → **exakt 1 bas** (scope ren, ingen scope-avvikelse):
`apphjj8Q7lkXCMsL4` ("miranon-media-admin-staging", permission `create`).
`/meta/bases/.../tables` → 18 tabeller. Bas-ID:t är källan till sanning för
`AIRTABLE_BASE_ID` på staging.

**Schema-check (ADR-050 T4) — CLEAN.** EF-koden adresserar tabeller per NAMN
(`encodeURIComponent(tableIdOrName)`, namn-portabelt per `49267b4`):
`Eventplanering` (get-events), `Personer` (get-persons), `Anmälningar`
(get-registrations + field-allowlists). Alla tre finns i staging-basen →
**T4 LÖST** (tidigare öppen/deferrad — Postgres nära tom).

**Bygg-steg 4 (staging-secrets satta).** Via `supabase secrets set` mot
staging-ref `pqtshyierkdgwdnxuirz` (Supabase-dokumenterad metod):

- `AIRTABLE_TOKEN` + `ADMIN_EMAILS` — via `--env-file .env.staging-secrets`
  (PAT-värdet aldrig på kommandoraden → ej i `ps`/logg/transcript).
- `AIRTABLE_BASE_ID=apphjj8Q7lkXCMsL4` — inline (bas-ID är identifierare, ej
  känsligt; som prod-secret-landningen, ÅTGÄRD 2).

Verifierade via `secrets list` (namn + digest, inga värden). Throwaway-filen
`.env.staging-secrets` **raderad** efter verifiering — råvärdet lever nu enbart i
staging-secret-storen (avsedd destination). Ingen deploy (bygg-steg 5+).

### Resume-landning — bygg-steg 5 (staging-deploy, 6 EF:er)

Resume-19 (2026-06-14). EF-deploy sker CLI→Supabase-store, ingen committbar
artefakt — denna doc-post ÄR landnings-posten (L67).

**Mekanism (Chat-beslut, ADR-050 steg 5 GOVERNING):** bare
`supabase functions deploy --project-ref pqtshyierkdgwdnxuirz`. Prod-allowlist-
skriptet (`deploy-prod-functions.sh`) används INTE för staging — det är en
PROD-spärr som medvetet exkluderar `test-auth`, medan ADR-050 steg 5 vill ha
**alla 6 EF:erna inkl `test-auth` på staging** (2 staging-testfiler — `cors`,
`require-user` — anropar `test-auth`). PROD (`lvjsfnphlauldxqlncpl`) orört.

**Migrations ej tillämpligt (L115):** fyra-kanals-introspektionen bevisade NOLL
app-tabeller — all data i Airtable, Postgres bär bara managed Auth. ADR-050:s
DB-led (förarbete 1 / bygg-sekvens 2) deprioriterat medvetet; EF-deploy kräver
inget app-schema.

**Resultat:** 6 EF:er deployade till staging-ref `pqtshyierkdgwdnxuirz`, alla
`ACTIVE` version 1: create-admin-user, get-events, get-persons, get-registrations,
test-auth, update-record. `verify_jwt` per `config.toml` (test-auth=false, övriga
5=true). Pre-flight: staging hade 0 funktioner före deploy (ref-vakt grön).

**Reachability:** `test-auth` (verify_jwt=false) svarar `401`
`{"error":"Invalid or expired token"}` — funktionens egen `requireUser`-logik
kör (ej 404), nådde Supabase-auth → `SUPABASE_*`-defaults plattforms-injicerade
på staging bekräftade. De 5 verify_jwt=true-EF:erna EJ skarpt anropade (kräver
test-users som ännu inte finns — bygg-steg 6/7).

**Kvar till test-aktivering (ej denna landning):** `CORS_ALLOWED_ORIGINS`
(staging-secret, krävs av `cors.staging.test.ts`), test-users i staging-auth,
nyckel-format-verifiering (legacy JWT vs nya publishable/secret → ev.
`--no-verify-jwt`-hänsyn).

### Resume-landning — bygg-steg 6+7 (staging-migration komplett)

Resume-19 (2026-06-15). Secret-/CI-/seed-operationer ger ingen committbar
artefakt — denna doc-post + commit-trailen ÄR landnings-posten (L67).

**Bygg-steg 6 (CI-test-secrets repointade, väg b).** De 6 `TEST_*`-GitHub-
secreten uppdaterade mot staging-projektet via `gh secret set --env-file`
(struktur orörd, namn oförändrade). Marcus skapade 2 staging-auth-users i
dashboarden; `ADMIN_EMAILS` (staging-secret) satt = test-admin-mejlen.
**Live-verifierat:** CI `40 passed / 1 skipped`, inga 401 → users↔secrets
bekräftade (den faktiska password-login-checken).

**Bygg-steg 7a (CORS + deny-aktivering).** `CORS_ALLOWED_ORIGINS=http://localhost:5173`
satt på staging (härlett ur `cors.staging.test.ts:19`). Deny-testerna
(update-record rad 56 prefix + rad 83 fält-utanför-allowlist) av-skippade — ren
borttagning, förkraven (redeploy + repointade secrets) uppfyllda. CI:
`cors.staging` + 4 redan-redo-filer (airtable-filter / create-admin-user /
edge-functions / require-user) gröna.

**Bygg-steg 7b (allow-vägen + staging-data-säkerhet).** Staging-access-gap löst:
Airtable-token-scopet utökat med staging-basen (Marcus) → Code når nu staging
direkt, symmetriskt med prod. Syntetisk Anmälningar-rad seedad
(`recynkk5KWpWirv7k`, `Anmälningsavgift='Ej mottagen'`, syntetiska namn/e-post,
ingen PII). `TEST_REGISTRATION_RECORD_ID` satt + wired i ci.yml api-staging-env.
Allow-testet (rad 110) aktivt: läs ursprung (get-registrations) → mutera
(`mark-registration-fee-paid` → 'Mottagen') → läs-tillbaka-assert 'Mottagen' →
`try/finally` restaurerar ursprungsvärdet. CI: **`41 passed / 0 skipped`**;
determinism bekräftad (posten åter 'Ej mottagen' efter körning).

**ADR-050 bygg-sekvens KOMPLETT (steg 1–7).** Fas 5.5:s deny/allow-grind är
avblockerad mot en isolerad, mutations-säker staging. `staging==prod`-defekten
(L110) strukturellt stängd.

**Transparens-noteringar (Code-flaggade, intent bevarad):**

- 7b seed-verifiering kördes via Airtable-MCP read-back (ej get-registrations-EF)
  — Code saknar TEST_USER-credentials lokalt + bygg-steg 6:s credential-gräns
  förbjuder password-login. EF-läsvägen bevisades i stället av CI:s allow-test.
- Teardown via `try/finally` i stället för `beforeAll/afterAll` — Playwrights
  worker-hooks når ej den test-scopeade `request`-fixturen; samma
  restore-på-fail-garanti, empiriskt bekräftad av determinism-checken.

## Del 3 — Lessons + status

Lessons L114–L118 skördade under `## 2026-06-13 — Session 19` i
[`tasks/lessons.md`](../lessons.md) (3 UNIVERSAL: L115, L116, L118; L114 repo-
konvention; L117 Chat-self-review). ADR-050 öppna trådar: T1 LÖST (Pro), T3 LÖST
(namn-i-path), T2 LÖST (bas-ID `apphjj8Q7lkXCMsL4` läst empiriskt, resume-19).
T4: point-in-time-schemamatchen verifierad (bygg-steg 3 CLEAN), men den LÖPANDE
schema-sync-disciplinen (kadens + mekanism) FORTSATT ÖPPEN — staging saknar
migrations, Airtable saknar schema-migration. Fas 5.5 förblir **PÅGÅENDE**
(ej fas-avslut denna session).

### Bär vidare (kritiska post-merge-flaggor)

1. ~~`AIRTABLE_BASE_ID` måste sättas som **prod-secret**~~ → **LÖST** (resume-19,
   2026-06-15): satt som prod-secret mot ref `lvjsfnphlauldxqlncpl` (verifierad
   digest `a0652ca6…`, bygg-steg-5-passet). Prod-EF:erna fail-fast:ar inte längre
   på saknat fält. (Korrigering av tidigare "ej satt"-flagga — öppen rivning, ej
   tyst radering.)
2. Prod-deploy hädanefter ENDAST via
   `scripts/deploy-prod-functions.sh --project-ref <ref>` — aldrig bare deploy.
   (Gäller PROD; staging-deploy gick medvetet via bare CLI, ADR-050 steg 5.)

### Nästa konkreta steg (Session 20-ingång)

Bygg-steg 3 per ADR-050 bygg-sekvens: läs staging-projektets ref + api-keys
(`supabase projects api-keys --project-ref <staging-ref>`) + nya Airtable-basens
bas-/tabell-ID:n via MCP → producera SECRET-KARTA före något sätts. Sedan
bygg-steg 4–8.

## Del 4 — Paus-bygget (lifecycle-verb session-paus)

Denna chatt fortsatte session 19 (ADR-043 A2-kontinuum) men ägnades åt en enabling-
detour: att bygga `session-paus`-verbet så session 19 kan pausas rent. Session 19:s
egen staging-scope (bygg-steg 3-8) påbörjades INTE här. Landningar (kronologiskt):

- `1da00f3` (spoke) — ADR-051 (session-paus lifecycle-verb) + README ADR-count 50→51 + katalograd.
- `13633c2` (hub) — `claude-app-skills/session-paus/SKILL.md`.
- `87acfdd` (hub) — PI-bas lifecycle-pekare + `/session-paus` (+ `updated:`-bump).
- `cf23ebf` (spoke) — L119 (UNIVERSAL: asymmetrisk lifecycle-axel = drift-källa).
- `c1e6f19` (spoke) — session-18 additiv paus-klargörande-not (fas 2).

Provisorisk lesson-kandidat (ej hub-lyft): livscykel-tillstånd hör i ett dedikerat
frontmatter-fält, inte enbart i prosa (Marcus 2026-06-13) → session 20:s
`lifecycle:`-fält + ADR-052.

## Paushistorik — Session 19 (pausad 2026-06-14, återupptagen 2026-06-14) (ADR-051 session-paus)

Sessionen är durabelt parkerad. Återupptas via `/session-resume` på **session 19**
(fortsätter N=19, inget nytt dok). Pausen uttrycks i kroppen eftersom `lifecycle:`-
frontmatter-fältet ännu inte finns — det byggs i session 20 och retro-appliceras då
(`lifecycle: paused` här, `lifecycle: closed` på session 18).

### HANDOFF — vad resume-19 tar vid på

Session 19 scope-status: staging-miljö bygg-steg 3-8 (ADR-050) — ej påbörjat; står
vid bygg-steg 3 ingång.

Sekvens (Marcus-vald ordning):

1. SESSION 20 (ny session, nytt dok) = lifecycle-fix: `lifecycle:`-fält + ADR-052 + skill-/PI-edits + retro-applicering på 18/19. Fixa systemet först.
2. RESUME SESSION 19 → staging bygg-steg 3-8.
3. `/session-start` NY SESSION (nästa lediga N) → fortsätt Fas 5.5-arbetet som påbörjades i
   session 18; peka bakåt på session 18. (Session 18 är `lifecycle: closed` — en stängd
   session resume:as inte; fasen fortsätter via en ny sessions-yta, ADR-052/L124.)

Carry / öppna trådar (hanteras vid resume-19):

- Bygg-steg 3 ingång: läs staging-ref `pqtshyierkdgwdnxuirz` api-keys + nya Airtable-basens bas-/tabell-ID:n → SECRET-KARTA (namn→målprojekt→värde-källa; råa värden lokalt hos Code) FÖRE secrets sätts (bygg-steg 4). Airtable-PAT mot staging-basen ej skapad än.
- `AIRTABLE_BASE_ID` (=`app8uGPrVCVOm6LfD`) måste sättas som PROD-secret FÖRE nästa prod-redeploy (annars fail-fast; refaktor `49267b4`). Ej satt (prod-ref `lvjsfnphlauldxqlncpl` saknar fältet).
- Prod-deploy hädanefter ENDAST via `scripts/deploy-prod-functions.sh`.

Deferrade projektion-moment (Marcus, batcha EFTER session 20 — en gång mot slutversioner):

- RE-PASTE av PI (bas+delta) i claude.ai-projektrutan → aktiverar `/session-paus`-pekaren (+ ev. session-20-tillägg).
- LADDA UPP `session-paus`-skillen (+ ev. session-20-redigerade skills) till claude.ai (Settings → Customize → Skills) → gör `/session-paus` `/`-anropbar.
