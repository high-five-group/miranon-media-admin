---
owner: marcus803
updated: 2026-08-12
review_by: 2026-11-11
status: stable
lifecycle: active
---

# Session 105 — Fas 6.5 Aktivitetslogg (xAPI): grillning till spec, dag 1-form till Lotta

> Fött **vid sessionsstart** (ADR-043 + L67/L68), efter LÄS→RAPPORTERA och
> Marcus scope-kvittens ("Kvitterar", 2026-08-11). Landnings-kadens: varje Del
> skrivs vid sin landning, aldrig i efterhand.
>
> S102 är AKTIV i huvudkatalogen och äger den via katalogägarskaps-lappen
> (belagd levande: `lifecycle: active` + main-landningar i realtid, PR #1173
> under orienteringen). S105 arbetar därför i egen worktree per ADR-090
> beslut 2 — agent-beslut vid start, ingen eskalering.
> Bas: `origin/main` `937ffbf5`.

## Del 1 — Syfte och scope

### Syfte

Ta Fas 6.5 Aktivitetslogg (xAPI) från byggplans-scope till exekverbar spec —
designgrillning till samsyn, PRD-kort, skivor — med dag 1-leverans till Lotta
som styrande skärning. `docs/byggplan.md` § Fas 6.5 (rad 782–837) är
styrande. Seniorrådets medskick (S102): lås minsta Lotta-synliga formen
först — händelseurvalet (markera betalning, bekräfta, skicka …) + historik-
sektionens form på hem-vyn per k10-facit.

### Ordnad scope (Marcus kvittens 2026-08-11)

1. Sessionsstart-orientering (LÄS→RAPPORTERA) — klar vid födelsen.
2. `/grill-me` — designgrillning till samsyn. Kända grillpunkter utöver
   formfrågan:
   - **Lagringsytan** (divergens funnen vid orienteringen): byggplanen antar
     en Airtable `Activity Log`-tabell "per `data-model.md`" — men varken
     `data-model.md`, `schema_reference.md` eller basen bär någon sådan
     tabell. Marcus ingångstanke (2026-08-11): skapa tabellen i **Supabase**
     i stället, med migrationsmotivet. Rör `ADR-063` (basen som förstklassig
     leverabel) och AT-Max-milstolpens premiss ("Fas 6.5:s `Activity
     Log`-write" som sista Airtable-interaktionen) — prövas i grillningen,
     beslutet är Marcus.
   - **`trace_id` kontra `requestId`** — bonus-ADR:n per byggplanens DoD
     punkt 5 ("beslut tas vid sessionsstart" = denna sessions grillning).
3. `/to-prd` — PRD-kort ur grillad samsyn.
4. `/to-issues` — skivor i beroendeordning; Passionslyfts-motiven (adaptiv
   lärning, Open Badges-shape) skärs till senare skivor per seniorrådet.

### Ej i scope

- Adaptiv lärning-engine, LiveKit/Cal.com — Passionslyftet (byggplanen
  § Fas 6.5 Inte scope).
- Real-time activity-stream-visning — Fas E.
- Själva bygget av skivorna ingår inte i denna scope-kvittens; avfyras efter
  `/to-issues` i Marcus takt (dag 1-deadlinen styr).

### Numrering vid födelse (disk-verifierad mot `937ffbf5`)

ADR **110** (109 filer, sista `ADR-109`; README-registret bär 108 rader —
`ADR-109`-raden släpar i S102:s landningsyta, rörs inte härifrån) · lesson
**L512** (högsta referens L511) + 3 nummerlösa fragment · tråd **T141** ·
kort **task-196** · fälla **f47**. Re-derivera ALLTID vid mint — S102
förbrukar parallellt.

### Ingångstillstånd

- Bas: `origin/main` `937ffbf5` (CI grön; PR #1175 stod i merge-kön vid
  födelsen). Huvudträdet rent vid färsk mätning; snapshotens två M-filer
  (`scripts/create-bilagor-table.mjs`, `create-kvitton-table.mjs`) hanterade
  av S102-ägaren.
- Underlag: `tasks/sessions/bilagor/s55-hem-konvergens/` med
  `k10-facit-desktop.png` + `k10-facit-mobil.png` (historik-sektionens form)
  · `docs/features/FEATURE-ACTIVITY-LOG.md` (2026-04-05, Lotta-perspektivet
  och händelsekatalogen) · `docs/byggplan.md` § Fas 6.5.
- `src/data/activityLog/` existerar inte ännu (väntat — fasen obyggd).
- Audit-grinden grön vid start (exitkod 0, körd i exitkodssäker form).

## Del 2 — Grillad samsyn: Fas 6.5-designen (2026-08-11)

Grillning körd till samsyn — fem frågor, alla explicit kvitterade.

### Beslut 1 — dag 1-snittet: B som mål, skivat med A som mellanstation

Hem-spaltens form är k10-facit-låst och grillades inte. Länken "Se all
aktivitetshistorik ›" pekar på en **full historikvy MED filterrad**
(kategori/event/tidsperiod) — skivad som kärnvy (tidsgrupperad lista) →
filterrad (additiv skiva). Tar dag 1-klockan slut är kärnvyn en hel yta;
inget rivs i något utfall. Faktagrund: filterraden är återbruk av färdiga
primitiver (`Select.tsx`, `ToggleButtonGroup.tsx`) — bedömt +20–30 % på
vy-skivan, inte en fördubbling. Marcus: *"Bra plan."* (efter *"helst B"*).

### Beslut 2 — händelseurvalet: allt som har en mutation (~11 typer)

Lottas förtroendemotiv styr: en logg med luckor är värre än ingen logg; det
dyra är infrastrukturen, inte typ elva. Förbehåll till PRD: anteckningar
loggar ATT något antecknades — aldrig innehållet; "lade till person" ingår i
skapa-anmälan tills person-skapande får egen mutation (k10-facitens exempel
saknar egen mutation i dag). Marcus: *"Kvitterar"*.

### Beslut 3 — lagringsytan: Supabase `activity_log`, inte Airtable

Marcus: *"Absolut, utan tvekan. Inte alls viktigt att ha med detta i
Airtable så Supabase blir perfekt."* Skäl: append-only-volymen mot Airtables
radtak (grovt 20–70 000 poster/år), enklare skrivväg (EF → `insert` i
stället för Airtable-API mitt i mutationens efterspel), migrationsmotivet
(Fas E), och FEATURE-dokens egen "Framtida Supabase-version"-sektion
förutsåg formen. **Över ADR-baren ⇒ ADR mintas vid bygget** (nummer
re-deriveras vid mint). Konsekvenser i samma landning: byggplanen amenderas
ÖPPET (AT-Max-blockets premiss "`Activity Log`-write = sista
Airtable-interaktionen" + § Fas 6.5:s lagringsrad), tabellen föds i BÅDA
Supabase-miljöerna (staging + prod) med RLS, write via service-role i EF.

### Beslut 4 — `trace_id`/`requestId`: sammanslagna (bonus-ADR:n)

`requestId` ÄR korrelations-ID:t, buret i xAPI `context.extensions`
(IRI-nycklat). Mätt grund: varje användaråtgärd är i dag exakt EN mutation →
EN EF (kvittot `receipts.ts` + `actionEmail.ts` verifierade; kedjor sker
server-side inom samma request) — ett distinkt `trace_id` vore en redundant
kolumn. Ingen tracing-infra existerar (grep noll träffar på
OTel/`traceparent`/`trace_id`). Marcus pressade rekommendationen ("Är du
säker?"); den stod på mätningen, med resultat att ADR:n får en **tvådelad
omprövningstrigger**: (1) OTel/W3C Trace Context införs · (2) en
användaråtgärd börjar orkestrera flera EF-anrop från klienten. Marcus:
*"Kvitterar."*

### Beslut 5 — Passionslyfts-deferralen: strikt xAPI-konformans ÄR förberedelsen

Zod-schemat följer xAPI-statementstrukturen (actor/verb/object/context/
timestamp, IRI-nycklade verb och extensions) — det är hela Open
Badges-/adaptiv lärning-förberedelsen. Noll badge-kod, noll spekulativa
fält. Deferralen bokförs explicit i PRD:n. Spec-detaljer till PRD (ej
grillbeslut): aktörsnamn ur inloggad användare · svenska sammanfattningar på
Lotta-språket med IRI-verb under huven · läsväg via ny `get-activity-log`-EF
under EF-ribban (SECURITY-SPEC §6.10). Marcus: *"Kvitterar"*.

### Landningens sidoytor

ORDLISTA.md uppdaterad i samma landning: **Aktivitetslogg** +
**Aktivitetshistorik** kanoniserade med data/vy-distinktionen.
Research-/prototyp-flaggor ur ADR-053-triagen: inga. Nästa steg: `/to-prd`.

## Del 3 — Natt-orkestreringen: PRD → skivor → mail-låset bevisat → kedjan till första blockeringen (2026-08-11 kväll → 2026-08-12 natt)

Marcus AFK-mandat (kvällens order, verbatim i väsentligheter): publicera
alla skivor, sätt ut bygg-agenter i natt (modellval mitt, Sonnet för
välspecat), paus-proceduren när klart — och det absoluta mailförbudet:
*"det enda som du eller agenterna ABSOLUT INTE får råka göra är ju att
skicka mail till riktiga människor"*, skärpt till *"det måste finnas ett
absolut mekaniskt stopp för mailutskick, inget är viktigare än det"*.

### `/to-prd` + `/to-issues`

- STOPP per skillens regel: s55-katalogen bar facit-bilder utan manifest
  (ADR-102:s R4/R5-tillstånd). Marcus kvitterade mintningen → retroaktivt
  `tasks/sessions/bilagor/s55-hem-konvergens/facit.json` ur S55 Del 12
  (arkivdok `tasks/sessions/archive/2026-07/2026-07-06-session-55.md`).
  Facit-fynd grillningen inte sett, inarbetade: spalten "Senaste aktivitet"
  endast ≥xl · mobil via Mer (B7) · INGA ikoner · lg↔xl-gapet odefinierat,
  öppet bokfört. **Facit-grinden (`check-facit.sh`) fällde manifestet två
  gånger** (godkand saknades; kallor tom) — fix `2c51c7c6` efter
  skript-läsning + B3-markörmätning; PR `#1180`. Grinden fungerar.
- PRD **TASK-201** publicerat (CLI-allokerat nummer; skarv-kvittens:
  api-skarven + e2e-skarven, inga nya). PR `#1180`.
- Skivning efter uppmätta kodkopplingar (Marcus delegerade domen):
  grund-skivan DELAD (`supabase/migrations/` fanns inte — första
  Postgres-tabellen; disk-arbete AFK / staging-applicering med bokförd
  access-risk), beroendet spalt→kärnvy VÄNT (facit-länken kräver routen).
  **TASK-201.1–201.10** publicerade (PR `#1182`); mailförbudet inskrivet i
  201.3/201.4/201.10 (landade i `#1183`).

### Mail-låsets utvidgning och bevis (kedjans viktigaste leverans)

TASK-137-låset fanns (tre lager). Uppmätt lucka: Bash mot egna
send-*-EF:er passerade. Utvidgning: `.mail-lock-policy.conf` +2 mönster
(`functions/v1/send-`, `functions[[:space:]]+invoke[[:space:]]+send-`;
deploy medvetet omatchad), testsvit D12–D13 + A10–A11, **30/30**.
PR `#1183` → main `d532adbb`. Bevis-kedjan därefter:

1. Egen session fäller gamla mönster (live-deny, verbatim i Del 3-trailen).
2. Subagent-prob: gamla mönstret FÄLLER, nya SLÄPPER → hookar kör i
   subagent-kontext men läser `CLAUDE_PROJECT_DIR` = **launch-katalogen**
   (huvudkatalogen), vars checkout var stale.
3. Ägar-synk via SendMessage-bön till S102 (`miranon-media-admin-61`):
   de ff:ade huvudkatalogen `c716a355 → d532adbb` som service-handling.
4. Verifierande prob: **NEKAD med `functions/v1/send-`** i
   subagent-kontext. Mekaniskt stopp bevisat på alla tre kanaler.

### Byggkedjan

- **201.1 LANDAD** (Sonnet 5-agent): **ADR-110** (lagringen) + **ADR-111**
  (requestId) mintade, `src/domain/schemas/ActivityStatement.schema.ts` +
  14 api-test (rött-först tvåsidigt bevisat),
  `supabase/migrations/20260811211759_create_activity_log.sql`, byggplan
  v1.15-amendering. PR `#1185` → main `5663b456`. Orelaterad
  staging-flake flaggad: `attachment-upload-large` (TASK-196-symptomet,
  2 instanser i natt, grön isolerat).
- **201.2 STOPPAD KORREKT** på kortets eget stoppvillkor: agent-miljön
  saknar Supabase db-access HELT (sju-punktsmätning inkl. kontrollprov:
  CLI:t hänger headless utan `SUPABASE_ACCESS_TOKEN`). Fynd-kort
  **TASK-201.11** (ready-for-human), 201.2 omklassad ready-for-human.
  PR `#1187`. Ingen kringgående väg försökt.
- **201.3–201.10 blockerade** på Marcus-momentet (migrations-appliceringen).

## Paushistorik — Session 105 (pausad 2026-08-12 natt: nattkedjan blockerad på db-access; återupptagen 2026-08-12 morgon)

### TILLSTÅND

- Repo: `main` @ `5663b456` vid paus-skrivningen; CI grön per jobb på
  samtliga S105-landningar (#1176, #1177, #1180, #1182, #1183, #1185).
  PR **#1187** (201.2-fyndet) armerad i kö vid skrivningen — verifieras
  per jobb före monitor-stopp; utfallet i paus-rapporten i chatten.
- Sessionens worktree: `.claude/worktrees/s105-fas65-aktivitetslogg`,
  branch `worktree-s105-fas65-aktivitetslogg` (allt pushat/landat utom
  denna paus-commit som armeras vid skrivningens slut).
- Worktree-städning körd: 1 borttagen (201.1-agentens, landad+ren),
  2 grenar frigjorda; 201.2-agentens BEHÅLLS korrekt (gren olandad tills
  #1187 mergat).
- Huvudkatalogen: synkad till `d532adbb`+ av S102:s service-ff; bär
  S102:s ägarlapp igen (satt 21:10Z AV deras synk-operation — deras
  "släppt"-rapport var sann när den skrevs). S102 pausad. Nästa session
  prövar liveness per vanlig regel.
- Arbetsform: inget aktivt läge (`arbetsform-tillstand.sh las` — tomt).
- Heartbeat-monitorn stoppas som SISTA handling efter #1187-verifikatet.

### CARRY / ÖPPNA TRÅDAR (lesson-KANDIDATER — antecknade, EJ hub-lyfta)

1. **[UNIVERSAL-kandidat] `CLAUDE_PROJECT_DIR` är launch-katalogen för
   hela sessionen inkl. alla subagenter** — en hook-policy-ändring landad
   i main är INTE verksam förrän launch-katalogens checkout bär den.
   Samma klass som "bestäms vid sessionsstart?"-frågan i CLAUDE.md
   § hooks. Mätt med tvåstegs-differentialprob i natt.
2. **Worktree-isoleringens filverktygs-spärr är striktare än
   S97-matrisen:** Edit/Write mot egna repots huvudkatalog NEKAS numera
   (matrisen bokför bara Read-cellen). CLAUDE.md-matrisen behöver en
   uppdaterad cell — Marcus-yta eller framtida docs-skiva.
3. **Cross-session service-bön fungerar som synk-väg:** SendMessage till
   katalogägaren (S102) gav verifierad ff inom minuter — mönstret värt
   att bevara (och lappen som deras operation återsatte är en känd
   bieffekt att räkna med).
4. supabase-CLI:t hänger headless utan token (dokumenterat med
   kontrollprov i TASK-201.11 — även framtida agenters fallgrop).
5. Attachment-flaken: 2 nya TASK-196-instanser i natt (n växer; riggen
   `metrics:flake` finns när någon vill mäta).
6. FEATURE-ACTIVITY-LOG.md ägs av ingen 201-skiva (bokfört i PRD:ts
   anteckningar — hanteras vid ADR-uppföljningen eller QA).

### MARCUS-SEKVENS (beslutad väg, morgonen)

1. **Lös TASK-201.11** — applicera migrationen mot staging i din
   terminal: `! npx supabase login` → `! npx supabase link --project-ref
   pqtshyierkdgwdnxuirz` → `! npx supabase db push` (alternativt
   SQL-editorn i dashboarden med migrationsfilens innehåll). Långsiktig
   access-fråga (token i agent-miljön?) är din säkerhetsavvägning.
2. **Återuppta S105** (`session-resume`) — kedjan 201.2 (RLS-beviset) →
   201.3 ∥ 201.5 → 201.4/201.6 → 201.7/201.8 avfyras i beroendeordning.
3. Dina stående moment därefter: 201.9 (prod), 201.10 (QA),
   facit-stämplingen (`npm run facit:godkann`) för hem-spalten.

### NUMRERING ATT RE-VERIFIERA VID RESUME

Mot `5663b456`: nästa ADR **112** (110+111 förbrukade i natt) · lesson
**L512** + 3 fragment (oförändrat — nattens kandidater står ovan, ej
landade) · tråd **T141** · nästa toppnivå-kort **task-202** · fälla
**f47**. Re-derivera ALLTID — parallella sessioner förbrukar.

### NÄSTA KONKRETA STEG

Marcus-sekvensens punkt 1 (migrations-appliceringen) — allt annat är
nedströms den.

## Del 4 — Resume-dagen: fynd-uppdragen landade, TASK-196 rotorsakad, token-vägen utredd (2026-08-12 morgon)

**Resume** (`session-resume`, Marcus-order i klartext): tillstånds-
återställningen `#1191` → `cfdfa1da`. Numrering re-verifierad mot disk —
en divergens: handoffens "nästa fälla f47" var stale, disk bar redan
fälla 47 (S103) → **nästa fälla är 48**. Nattens röda Nightly
(run `31560003797`) triagerad till tre förklarade klasser: extern
länk-röta · backlog-drift på FEM S102-eran-kort (`147.10`/`176`/`177`/
`184`/`186` — inte 201.1) · attachment-flaken (TASK-196, ny instans).

**Marcus dubbelorder på morgonen:** (1) supabase-raderna körs en i taget
— `!`-vägen föll direkt på non-TTY (`LegacyLoginMissingTokenError`,
lesson-kandidat) → riktig terminal krävs; (2) GO att rotorsaka och lösa
rapporterade fynd själv/via agenter.

**Fyra bygg-agenter parallellt** (Sonnet ×3 välspecade; diagnosen avsåg
huvudmodell men bygg-agent-definitionen pinnar Sonnet — utfall Sonnet 5,
bokförd avvikelse som räckte):

1. **201.1 STÄNGD** (`#1192`→`e4e83646`): alla 6 DoD bockade mot belägg.
   Kvalificering öppet bokförd: DoD 6:s skarpa write-guard materialiseras
   först i 201.3 — stängningen vilar på det bidirektionella testbeviset.
2. **Länk-rötan** (`#1193`→`3f8a2875`): 4 äkta rötor lagade (arxiv-404:an
   fanns på TRE rader, inte uppdragets en — agenten flaggade
   undertäckningen; + Apple HIG-flytt), 5 transienter klassade med
   curl-belägg, 2 `.lycheeignore`-poster per filens en-instans-praxis.
   Lokal lychee med CI:s flaggor: 0 fel. Issue `#1189` är stående kanal —
   verifieras mot nästa gröna nattkörning.
3. **TASK-196 ROTORSAKAD** (`#1194`→`193d461c`): eventual-consistency-
   hypotesen **falsifierad** (rött-först: 5 finalize-anrop upp till 16 s
   — identiskt 400). Verklig rot, reproducerad 2/2 mot skarp staging:
   `storage.list()`-defaulten `limit:100` + obegränsat växande
   `BELAGGNING_EVENT_ID`-mapp → filen utanför de 100 första namn-
   sorterade. Förklarar full-svit-vs-isolerat-signalen riktningsmässigt.
   Fix: `storage.info(path)` (direkt-GET, immun mot mappstorlek) +
   semantisk 404-klassning. **Öppen skuld: EF:en är INTE deployad**
   (ADR-050, kräver access-token) — grönt staging-kvitto återstår.
4. **Drift-korten** (`#1196`→`4b83c8a9`): `147.10`/`186`/`184` helt
   rättade mot PR-belägg; `176`/`177` AC #3 KORREKT lämnad (belägget —
   T51/T55-trådarna — saknades på disk). Infra-fynd bokfört: grinden
   `check-backlog-closure.sh` ofullbordbar under fleet-last (2 mätningar
   >55 min, 3–5 samtidiga instanser; trolig kontention delad `.git`) —
   värt diagnos-pass vid upprepning.

**Följd-landningen** (`9e018caa`, PR `#1197`): T51/T55 stängda i fil +
register mot S102 Del 6:s go-live-belägg (Grind F, deny-probes 8/8,
delivered + Reply-To + loggrad 19:17:21) → AC #3 bockat på 176/177 →
stängnings-grinden **grön lokalt (exit 0)**. Bifynd på vägen: worktree
utan `node_modules` ger grindens fail-closed exit 2 ("noll kort") —
symlink mot huvudkatalogens är etablerad lösning (201.1-agentens och
denna sessions oberoende mätningar).

**Token-utredningen** (Marcus: "är du säker på att den saknas?"):
mätt på FYRA ytor — `printenv` (0 supabase-vars), `~/.supabase/` (ingen
access-token-fil), CI-workflows (ingen referens), env-filernas namn
(enbart projekt-klass: anon-nycklar/URL:er) — **`SUPABASE_ACCESS_TOKEN`
saknas genuint**; alla befintliga nycklar är en annan klass. Instruktion
levererad: nyckelring (`security add-generic-password`) + zshrc-laddrader
för `SUPABASE_ACCESS_TOKEN` + `SUPABASE_DB_PASSWORD`. Marcus beslut:
vill DEFINITIVT att agenter kör supabase själva; fixar tokenen i
eftermiddag. Prod-skydds-kort (deny mot prod-ref, mail-låsets klass)
FÖRESLAGET — väntar Marcus-kvittens. Ägarskaps-hooken fällde korrekt
worktree-städning av landade agent-worktrees (S102:s lapp, levande
process) — uppskjuten, inte förlorad.

## Paushistorik — Session 105 (pausad 2026-08-12 förmiddag: fynd-uppdragen landade, kedjan väntar på access-token; återupptagen 2026-08-12 eftermiddag)

### TILLSTÅND

- Repo: `main` @ `ecbbc128` vid paus-skrivningen. Sex landningar i dag,
  samtliga mergade via kön och förgrundsverifierade: `#1191` (resume) ·
  `#1192` (201.1-stängning) · `#1193` (länk-röta) · `#1194` (TASK-196-fix)
  · `#1196` (drift-korten) · `#1197` (T51/T55 + AC 3). Post-merge grön på
  varje; denna paus-commit armeras vid skrivningens slut och verifieras
  per jobb före monitor-stopp.
- Sessionens worktree: `.claude/worktrees/s105-fas65-aktivitetslogg`,
  gren `worktree-s105-fas65-aktivitetslogg`, ff:ad till `ecbbc128`; rent
  utom denna paus-landning. OBS: `node_modules` är en SYMLINK till
  huvudkatalogens (etablerad i dag för grind-körbarhet) — otrackad,
  gitignorerad, överlever i trädet.
- Worktree-städning körd via `stada-worktrees.sh --utfor`: 5 borttagna
  (nattens 201.2-agent + dagens fyra, alla landade + rena), 10 grenar
  frigjorda (165→155). Ägarlappen på huvudkatalogen är S102:s (levande
  process) — INTE vår; lämnad orörd per skriptets kontrakt.
- Migrationen `20260811211759_create_activity_log.sql` är landad som fil
  men EJ applicerad mot staging (TASK-201.11 öppen). TASK-196-fixen är
  landad som kod men EF:en EJ deployad (ADR-050) — båda väntar på
  access-token (Marcus-momentet i eftermiddag).
- Arbetsform: inget aktivt läge (`arbetsform-tillstand.sh las` — tomt,
  verifierat vid resume i morse; inget läge satt under dagen).
- Heartbeat-monitorn stoppas som SISTA handling efter denna paus-commits
  landningsverifikat.

### CARRY / ÖPPNA TRÅDAR (lesson-KANDIDATER — antecknade, EJ hub-lyfta)

Nattens sex kandidater (Paushistorik-blocket ovan) står kvar olandade.
Dagens tillskott:

1. `!`-prefixet kör non-TTY — interaktiva login-flöden (webbläsar-auth)
   kan inte köras där; mätt `LegacyLoginMissingTokenError` mot
   `npx supabase login`. Riktig terminal eller token-väg krävs.
2. Agent-definitionens modell-pin vinner över Agent-anropets avsikt när
   `model` utelämnas — bygg-agent pinnar Sonnet; diagnos-uppdraget avsåg
   huvudmodell men körde Sonnet 5 (räckte bevisligen).
3. `check-backlog-closure.sh` under samtidig fleet-last: 2 mätningar
   >55 min utan fullbordan (3–5 samtidiga instanser, trolig kontention
   delad `.git`); även utan last tog körningen >420 s i worktree —
   CLAUDE.md:s 164,60 s är en huvudkatalogsmätning, inte ett löfte.
4. Worktree utan `node_modules`: stängnings-grinden fail-closed exit 2
   ("noll kort") eftersom `BACKLOG_CMD` defaultar till lokal binär —
   symlink mot huvudkatalogens node_modules är etablerad lösning
   (två oberoende instanser i dag).
5. TASK-196-diagnosens metodfynd: rött-först-falsifiering av en
   plausibel hypotes (eventual consistency) FÖRE fix — rotorsaken var
   en annan (list-default limit:100 + växande mapp). Diagnos-före-fix
   höll exakt som kortet krävde.

### MARCUS-SEKVENS (beslutad väg, eftermiddagen)

1. **Token-momentet** (instruktion i chatten, steg 1–4): PAT →
   nyckelringen (`security add-generic-password`) för båda värdena
   `SUPABASE_ACCESS_TOKEN` och `SUPABASE_DB_PASSWORD` → laddrader i
   `~/.zshrc`. Klistra ALDRIG tokenvärdet i chatten.
2. **Återuppta S105** (`session-resume`): Code verifierar env (utan att
   läsa värden) → applicerar migrationen mot staging själv (link +
   db push) → deployar `finalize-attachment-upload` + tar staging-kvittot
   för TASK-196 → avfyrar kedjan 201.2 (RLS-beviset) → 201.3 ∥ 201.5 →
   201.4/201.6 → 201.7/201.8.
3. Dina stående moment därefter: 201.9 (prod), 201.10 (QA),
   facit-stämplingen (`npm run facit:godkann`).
4. Öppet beslut: prod-skydds-kortet (mekaniskt deny mot prod-ref i
   agent-kommandon, mail-låsets klass) — föreslaget, väntar kvittens.

### NUMRERING ATT RE-VERIFIERA VID RESUME

Mot `ecbbc128`: nästa ADR **112** · lesson **L512** + 3 fragment · tråd
**T141** (T51/T55 stängda i dag, ingen ny mintad) · nästa toppnivå-kort
**task-202** · fälla **48** (handoffens f47 var stale — 47 fanns redan
på disk, S103). Re-derivera ALLTID — parallella sessioner förbrukar.

### NÄSTA KONKRETA STEG

Marcus-sekvensens punkt 1 (token-momentet) — allt annat är nedströms
den. Vid resume: börja med env-verifikatet.

## Del 5 — Eftermiddagen: blockeringen fanns aldrig, kedjan öppnad, säkerhetslåsen byggda (2026-08-12 em/kväll)

**Resume nr 2** (`session-resume`, Marcus-order "Återuppta S105"):
tillstånds-återställningen `#1199` → `d20e0a0d`. Numrering re-verifierad;
todo-kadensraden bar stale `f47` → rättad till 48.

### Token-blockeringen var aldrig verklig

Env-verifikatet visade `SUPABASE_ACCESS_TOKEN` frånvarande, och den
slutsatsen fördes vidare — **felaktigt**. Marcus invände ("är du säker på
att den saknas?" / "varför i helvete har du inte sett att vi har den här
nyckeln redan"), och invändningen höll: nyckelringen bär posten
`Supabase CLI` (konto `supabase`) sedan **2026-03-30**. Supabase egen
dokumentation säger att tokenen lagras i *native credentials storage* och
att `~/.supabase/access-token` bara är RESERVPLATSEN — den tomma katalogen
var alltså bevis för korrekt lagring, läst som frånvaro. Nattens
`supabase link`-hängning var dessutom **lösenordsprompten**, inte ett
login-flöde; med `echo "" |` svarar den direkt.

Följd samma minut: `link` → `db push` → migrationen applicerad →
`public.activity_log` verifierad live med tre index. **TASK-201.11 stängd
som falsifierat** (`#1211` → `1de92b67`).

### Aktivitetsloggens kedja

- **201.2 landad** (`#1202` → `50afc936`). Genuint fynd i bygget:
  `service_role` saknade GRANT trots `BYPASSRLS` — ny migration
  `20260812143131_grant_service_role_activity_log.sql` ger `select, insert`
  **medvetet utan `update`/`delete`**, vilket gör 201.1:s "append-only"
  strukturellt sant. RLS-bevis tvåsidigt i
  `tests/api/activity-log-rls.staging.test.ts`.
- **201.5 landad** (`#1215` → `71eba715`): `get-activity-log`-EF,
  `fetchActivityLog` i adaptern, hookarna `useActivityLogHistory` +
  `useLatestActivity`, 15 kontraktstester. Keyset-paginering bevisad mot
  skarp staging. `SupabaseAdapter`-stubben är dubbel-källa-mönstret
  (34 `NOT_IMPLEMENTED` av 188 rader), inte ett glapp.
- **201.3 i luften** (`#1216`, SHA `eac19311`): två röd-varv lösta —
  `check-langa-streck` på ett loggmeddelande, sedan hermetik-vakten
  (7 acceptance-sviter, `OmockadRequestError`) löst med en **central**
  `log-activity`-handler i fixturvärlden. Acceptance 189/7 → **197/0**.
  Stängde på köpet sitt eget bokförda gap ("bekräfta anmälan" saknade
  e2e) och byggde det negativa testet (500 fäller inte mutationen).
  Merge-konflikt mot 201.5 löst med behåll-båda.
- **201.4** bär nu två överförda skulder (`#1218` → `6195f8d2`):
  `EVENT_ID_EXTENSION_IRI` emitteras inte än, och hermetik-mocken måste
  täcka den utökade ytan FÖRE instrumentering.

### Säkerhet — incident, research, lås

Bygg-agenten självrapporterade att `supabase projects api-keys` skrev ut
stagings legacy `service_role`-JWT i klartext **utan** `--reveal`.

Research-passet
([`docs/research/supabase-hemlighetsexponering-forhindrande-2026-08-12.md`](../../docs/research/supabase-hemlighetsexponering-forhindrande-2026-08-12.md),
landad `#1211`) fann det avgörande: **legacy-nycklarna går inte längre
att rotera.** Supabase verbatim: *"it is no longer possible to rotate the
legacy anon, service and JWT secrets."* Verifierat av orkestreraren mot
källan. Den exponerade nyckeln kan alltså aldrig ogiltigförklaras —
migrering till nya nyckelsystemet är därmed GOLV, inte förbättring.
Kort **TASK-204** mintat (`#1217` → `ef940cda`).

**TASK-203** landade det mekaniska försvaret (`#1212` → `9cb64ed1`):
deny-hook mot hemlighets-utskrivande kommandon + prod-ref-låset,
45 tvåsidiga testfall. Låset blev bredare än specen — bygg-agenten
flaggade öppet. **Marcus valde A** (`#1213` → `4a0ff6d6`): låset lämnas
brett, `201.9` förblir HITL. Öppen skuld: hookarnas skarpbevis kan inte
tas i byggsessionen.

**TASK-202** landade åtkomstregistret + `npm run atkomst:diagnos`
(`#1203`), med lesson-fragmentet
`tasks/lessons.d/mat-atkomsten-inte-forklaringen-som-rakar-passa.md`.

### TASK-196 stängd — skulden betald som bonus

Fixen var landad som kod (`d4328f01`) men EF:en **aldrig deployad**,
blockerad på den access som visade sig finnas. Deployad mot staging;
länkat projekt verifierat före operationen. Matchat n=3: **3/3 röda före,
3/3 gröna efter** (15 passed × 3). Kortet Done (`#1223`, i luften).
Prod-deployen är Marcus-moment per beslut A.

### Två trådar mintade

- **T141** (`#1201` → `0c00932f`, uppdaterad `#1205`): `/add-dir` KRYMPER
  filåtkomsten. A→B→A-mätt över 30 prov — `~/Desktop` bröts när
  `~/Downloads` lades till i workspace och återkom när posten togs bort,
  medan den tillagda katalogen förblev nekad hela tiden. TCC uteslutet
  (VS Code har Full Disk Access). `~/Documents` återkom först när posten
  togs ur FILEN. Tre falsifierade förklaringar på vägen, inklusive två
  egna mätfel (`head -20`-avkortningen; ett falskt negativt från
  `ls … >/dev/null && echo || echo`).
- **T142** (`#1221` → `177d17a4`): CI hämtar sina grindverktyg från nätet
  vid varje körning. **Sju** fällningar denna eftermiddag (shellcheck ×2,
  lychee ×3, actionlint, Vale), samtliga i nedladdningssteget, ingen i
  vår kod. Tre låg i merge_group och konsumerade armeringen tyst.
  Tråden väljer ingen väg — en eftermiddags data räcker inte.

### Öppen designkollision (Marcus-beslut, ej taget)

Facit-bilden `k10-facit-desktop.png` visar **långt tankestreck (—)** som
separator i alla fyra aktivitetsrader. Grinden `check-langa-streck`
förbjuder det, och policyns `_readme` citerar Marcus 2026-08-09: *"ALLA
15 långa bindestreck i användarsynlig text MÅSTE bort"* — undantagslistan
tömdes då. Två av Marcus egna beslut kolliderar. Tre vägar bokförda
(kort bindestreck · undantag i policyn · mittpunkt `·`, som redan är
appens separator i samma vy). **Måste avgöras före 201.7 byggs.**

## Paushistorik — Session 105 (pausad 2026-08-12 kväll: kedjan öppnad, hem-spalten tre skivor bort; återupptagen 2026-08-12 kväll)

### TILLSTÅND

- Repo: `main` @ `177d17a4` vid paus-skrivningens början. **Tolv
  landningar** i dag efter Del 4: `#1199` `#1201` `#1202` `#1203` `#1205`
  `#1211` `#1212` `#1213` `#1215` `#1217` `#1218` `#1221`. Samtliga
  förgrundsverifierade.
- **I luften vid paus:** `#1216` (201.3, SHA `eac19311`, armerad
  `17:45:44Z`, konflikt löst) och `#1223` (TASK-196-stängningen, SHA
  `0830b0c7`, omkörning startad efter `T142`-klassens sjunde instans).
  **Båda ägs av nästa resume** — verifiera per jobb, armera om vid behov.
- Sessionens worktree: `.claude/worktrees/s105-fas65-aktivitetslogg`,
  gren `worktree-s105-fas65-aktivitetslogg`. `node_modules` är en SYMLINK
  till huvudkatalogens (otrackad, överlever i trädet).
- Arbetsform: inget aktivt läge (`arbetsform-tillstand.sh las` — tomt).
- Staging-miljön ändrad av denna session: migrationen applicerad,
  `finalize-attachment-upload` deployad. Prod ORÖRD.
- `~/Downloads`-åtkomsten är fortfarande NEKAD; `~/Desktop` och
  `~/Documents` återställda. Se T141 för nästa prov.

### CARRY / ÖPPNA TRÅDAR (lesson-KANDIDATER — antecknade, EJ hub-lyfta)

Nattens sex och förmiddagens fem står kvar olandade (Paushistorik-blocken
ovan). Eftermiddagens tillskott:

1. **[UNIVERSAL-kandidat] En förklaring som passar symptomet är inte
   verifierad förrän den också förklarar fallen där symptomet UTEBLEV.**
   TCC-raden passade perfekt och var irrelevant; beläggen som motbevisade
   den fanns tillgängliga hela tiden och lästes först efter tredje
   invändningen från Marcus.
2. **En hängning är inte ett felmeddelande.** Ett headless-kommando som
   blockerar har inte sagt VARFÖR — kör om med styrd stdin före slutsats.
3. **Verifiera att CI-körningen matchar PR:ens `headRefOid` före
   `gh run rerun`.** Jag körde om en föråldrad körning och
   concurrency-gruppen avbröt då körningen för faktisk HEAD — nettot blev
   att jag sköt sönder det jag väntade på.
4. Mätformen `ls DIR >/dev/null 2>&1 && echo OK || echo NEKAD` gav ett
   FALSKT negativt. Fånga exitkod och stderr separat.
5. `head -20` på en TCC-fråga dolde raden som falsifierade hela hypotesen.
   Avkorta aldrig en fråga vars fullständighet är själva beviset.
6. Lagringshygien skyddar en nyckel som ligger stilla — inte en nyckel
   som ett verktyg hämtar och skriver ut. Två olika skyddsproblem.

### MARCUS-SEKVENS (öppna beslut, inget brådskande)

1. **Tankestrecket i hem-spalten** — blockerar `201.7`, se Del 5 sista
   stycket. Tre vägar bokförda; orkestreraren lutar åt mittpunkt (`·`).
2. **Nyckelmigreringen (TASK-204)** — golv, inte förbättring. Förslag:
   eget pass EFTER hem-spalten, av risk-skäl (samma Supabase-projekt som
   kedjan bygger mot). Ej beslutat.
3. **`~/Downloads`** — provet i T141 kräver omstart av Claude Code.
4. Stående: `201.9` prod (HITL per beslut A), `201.10` QA,
   facit-stämplingen för hem-spalten.

### NUMRERING ATT RE-VERIFIERA VID RESUME

Mot `177d17a4`: nästa ADR **112** · lesson **L512** + **4 fragment**
(TASK-202 la till ett) · tråd **T143** (T141+T142 mintade i dag) · nästa
toppnivå-kort **task-205** · fälla **48**. Re-derivera ALLTID — parallella
sessioner förbrukar.

### NÄSTA KONKRETA STEG

1. Verifiera `#1216` och `#1223` per jobb; armera om vid utsparkning
   (`T142`-klassen sparkade ut tre PR:er i dag).
2. När `#1216` landat är **`201.6` fri** (dep 201.3 + 201.5 båda klara) —
   spawna bygg-agent för kärnvyn.
3. `201.7` (hem-spalten) därefter — **men först Marcus tankestreck-beslut**.
