---
owner: marcus803
updated: 2026-06-20
review_by: 2026-09-20
status: stable
lifecycle: active
---

# Session 25 — Inc 4 (kall /arch-audit mot Fas 6a) + Fas 6b (Events-domän)

> Doket fött vid sessionsstart per ADR-043 + L67/L68 (levande artefakter har
> landnings-kadens, inte avsluts-kadens). Fött via session-start-skillens
> create-session-doc-gren (plugin marcus-system,
> `references/create-session-doc.md`). `lifecycle: active` satt explicit vid
> födelse (skill-ägt, ADR-052 beslut 3 + create-session-doc steg 8). Del 2+ fylls
> vid respektive landnings-ögonblick (committad + pushad + CI-grön), inte vid
> sessionsavslut.

## Del 1 — Syfte och scope

### NULÄGE / scope

Session 25 bär två moment i **kvitterad ordning: Inc 4 FÖRE Fas 6b.** Föregående
session (24, process/governance, ingen produktkod) institutionaliserade
kvalitetsstandard + arkitektur-audit på hub-nivå och lämnade handoff:
"Session 25 → Inc 4 (/arch-audit mot Fas 6a KALLT) + Fas 6b". Marcus har kvitterat
ordningen Inc 4 först, sedan Fas 6b.

**(a) Inc 4 — kall /arch-audit mot Fas 6a.** Kör den nyformaliserade arkitektur-
fitness-auditen (ADR-058) KALLT mot Fas 6a för att validera ADR-058-kontraktet i
praktiken: reproducera och korrigera 14-vs-15-driften, samt dogfooda skillens
discovery + flow (hittar audit-mekanismen rätt artefakter, följer den sitt eget
rapport-kontrakt?). Styrande: ADR-058 + arch-audit-skillen, med mätstickorna
ADR-057 (lager-oberoende-invariant) / ADR-056 (list-paginerings-port, dubbel-källa)
/ SECURITY-SPEC §6.10 (EF-ribba per Edge Function) / KVALITETSDEFINITIONER. Inc 4
kan generera **ADR-059** om audit-driften kräver en kontrakts-ändring — numret hålls
reserverat men inget skrivs i förskott (STOPPA om arkitektoniskt dyker upp).

**(b) Fas 6b — Events-domän** (byggplan §4 Fas 6-tabellen). Bygger ut Events:
`/event-lista` (fetchEvents) + `/event/$eventId/info`-vy + Närvaro-flik. Edge
Functions `get-event` + `get-attendance`, deployade per M4 — varje deploy följer en
namngiven UI-konsument. Per-sub-fas-kontrakt: registrera ev. operation i
`field-allowlists.ts` + deny/allow-test + vy-Playwright-baseline. **Inget nytt ADR
förväntat i 6b** — STOPPA-OCH-FRÅGA om något arkitektoniskt dyker upp. 6b ärver
Fas 6a:s mönster (single-record-EF + 404-mall, `_shared/coerce.ts`,
list-paginering via `fetchAirtablePage`, cursor-conformance-test, field-allowlists).

### EJ I SCOPE

- Inga andra moment än Inc 4 + Fas 6b. Audit-körningen och bygget utförs INTE i
  detta orienterings-pass (det enbart förberedde marken och födde doket).
- Inget ADR skrivs i förskott; ADR-059 hålls reserverat för ev. Inc 4-driftfynd.

### NUMRERING (disk-verifierat vid födelse, ej index-antaget)

- Session: senaste landad = 24 (live) / 15 (arkivets topp). Detta = **Session 25**.
- ADR: senaste på disk = ADR-058. Nästa ledig = **ADR-059** (reserverad, ej skriven).
- Lesson: senaste på disk = L150. Nästa ledig = **L151**.

### INGÅNGSTILLSTÅND (snapshot vid födelse)

- HEAD: `1291509` (Session 24 /session-end, closure bake-in), arbetsträd rent,
  i sync med `origin/main`.
- arch-audit: plugin `marcus-system@marcus-hub` **v1.4.0** installerat + aktivt;
  Code-halvans `arch-audit`-skill + `scripts/arch-fitness-check.sh` (områdena i–iii)
  på plats i installerad version. Inc 4 kan köras som planerat.
- Fas 6a exit-artefakter bekräftade på disk (mönster 6b ärver): `get-person`
  single-record-EF med 404-mall (`fetchAirtableRecord`, `{error}`-body);
  `_shared/coerce.ts` (`scalarString`/`stringArray`/`selectName`);
  `_shared/field-allowlists.ts` (per L139); `tests/api/cursor-conformance.ts`;
  cursor-encode/decode i `_shared/cursor.ts`. Precisions-not: `fetchAirtablePage`
  (ETT anrop/sida) bor i `_shared/airtable-client.ts`, inte cursor.ts — redan
  konsumerad av list-EF `get-persons`.

## Del 2 — Inc 4: kall arch-audit mot Fas 6a (validering av ADR-058-mekanismen)

Första landningen. Inc 4 körde marcus-system-pluginets `arch-audit`-skill (v1.4.0)
KALLT mot Fas 6a Persons-domänen — valideringskörningen av audit-mekanismen mot en
redan klar fas. LÄS + BETYGSÄTT, ingen kod rörd.

**Utfall: alla fem fitness-områden GODKÄNDA, noll AVVIKELSE.**

- **i — Lager-oberoende (ADR-057):** GODKÄNT. 0 UI-import av konkret adapter, 0
  `callEdgeFunction` utanför `src/data/`, DI-switch på en rad, port-paritet 15==15==15
  (iface + båda adaptrar).
- **ii — Swappbarhet (ADR-056):** GODKÄNT. DI-switch närvarande; `dataSource`-modulen
  direkt-importeras endast av kompositions-roten (`src/router.ts`).
- **iii — EF-ribba (§6.10):** GODKÄNT. Scope-EF:erna `get-person` / `get-persons` /
  `update-record` bär full ribba (requireUser, corsHeadersFor, generateRequestId,
  mapErrorToResponse = 2/2/2/2). Kända omdömes-undantag (`create-admin-user`,
  `test-auth`) klassade, ej ribb-brott.
- **iv — Över-engineering-vakt:** GOLVET HÅLLET. Branschstandard/säkerhet/a11y aldrig
  skuret (EF-ribba, server-side deny/allow `update-person-note`→`['Anteckningar']`,
  detaljvy + lista + write-path alla a11y 11/10). Ingen spekulation över golvet i
  6a:s Persons-kod — cursor-porten (`ListParams`/`PersonsPage`) är golv för ADR-056,
  ej generalisering "ifall".
- **v — Ärliga betyg:** 11/10/10 på vy-ribban (Tillgänglighet 11, Teknik 10,
  Återanvändbarhet 10). Inget oförtjänt 11.

**Fas 6a är arkitektoniskt förstklassigt klar mot sina deklarerade mätstickor.**

### Dogfood-validering (uttalat Inc 4-mål)

Session 23:s ad hoc-audit räknade fritext **"14 metoder"** där disken bar **15**.
Den kalla körningen räknade iface-metoder mekaniskt + jämförde per adapter
(15==15==15) → **"14" avslöjat som ett räknefel, inte en verklig drift som
åtgärdades efteråt**. Det fasta mekaniska ADR-058-kontraktet gör den drift-klassen
omöjlig (skriptet räknar deterministiskt; fritext-glidning kan inte uppstå).
**ADR-058-mekanismen validerad mot kall körning.**

### Icke-blockerande observation (iv-B) — avförd av Chat

`DataSourceAdapter`-porten deklarerar metoder vars UI-konsument tillhör senare faser
(`fetchAttendance`, `fetchWaitlist`, `sendEmail`, `fetchMailLog` m.fl.). Detta är
**INTE 6a-spekulation**: ADR-057 kräver att en datakälla implementerar HELA
interfacet, så komplett port + inkrementellt tillkommande vyer är den avsedda
designen — ej kod byggd "ifall". Noterad, ej eskalerad.

### Lessons

`L151 [UNIVERSAL]` skriven (tasks/lessons.md): ett fast mekaniskt audit-kontrakt
eliminerar fritext-drift-klassen. Hub-lyft sker vid session-end (ej nu).

### Status

Ingen kod rörd, inget ADR-059 påkallat (kontraktet höll). **Inc 4 KLAR.**
Nästa: Fas 6b (Events-domän).

## Del 3 — Fas 6b L1: route-struktur C1 + /event-lista + spec-reconciliering

Andra landningen (första produktkoden i Session 25). Fas 6b L1 deploy-oberoende —
ingen EF rörd, ingen deploy (get-event/get-attendance byggs L2/L3). Tre atomiska
commits (L23) + sessionsdok.

**Föregående orienterings-pass utlöste en STOPPA** (route-kollision: `$eventId.tsx`
var en redan byggd Betalnings-vy, ej placeholder) + en **capacity-divergens-STOPPA**
(URL-STATE-SPEC hade capacity, STATE-STRATEGY inte — inverterat mot promptens
beskrivning). Marcus kvitterade **väg C1** (nested routes) + **B′** (behåll capacity,
bygg i världsklass). Båda inbakade i L1.

### Landningar

- **Commit 1 — route-struktur C1** (`5b70dc1`). `$eventId` leaf→katalog via `git mv`
  `$eventId.tsx` → `$eventId/betalning.tsx` (historik bevarad; innehåll orört utom
  createFileRoute-path). Nya platshållare `$eventId/index.tsx` (info, default-route,
  fylls L2) + `$eventId/narvaro.tsx` (fylls L3). `_authenticated` bär Outlet → ingen
  layout-route. Route-trädet genererar rent (3 routes resolvar). mark-paid-e2e:
  `/event/$id` → `/event/$id/betalning` (rad 78/125/152).
- **Commit 2 — /event-lista** (`8b058a3`). `EventsList` mot LIVE `fetchEvents` via
  router-context-DI (ADR-055), **klient-side** filter/sort (get-events tar inga
  params → stabil query-nyckel). `?status=upcoming|past|all` (datum-baserat filter,
  UX-val flaggat) + `?sort=date|name|capacity` (B′), nuqs history push. capacity =
  `anmaldBelaggning` (andel) fallande, null sist (6a-disciplin). Beläggning som
  TEXT alltid ("N av M platser"/"Fullt"/procent vid capacity-sort), null-säker.
  A11y 11/10: React Aria Select-kontroller, aria-live bekräftar omsortering,
  role=status loading, rad-länkar → `/event/$eventId`. e2e: baseline + capacity +
  past + axe 0 (4 gröna lokalt mot `.env.test`).
- **Commit 3 — spec-reconciliering** (`4d5d9b2`). URL-STATE-SPEC + STATE-STRATEGY:
  event-detalj `?tab=` → separata routes (data per route); konvergens på
  `date|name|capacity` (STATE-STRATEGY fick capacity). byggplan :623 flik→route-språk
  (matchar :667). Daterade reconc-noter (additivt, ej tyst rivning — L30).

### Verifiering

tsc -b + biome (0 errors) + vite build gröna. markdownlint + Vale + frontmatter
gröna. e2e: event-list 4/4 gröna; mark-paid 4/4 (en parallell-flake i global
`[data-mm-announcer]` passerade 3/3 isolerat — känd timing, ej URL-relaterad).

### Status

Inget nytt ADR (C1 = spec-reconciliering, ej arkitekturbeslut). **Fas 6b L1 KLAR.**
Nästa: L2 (info-vyns riktiga innehåll + a11y, kräver get-event-deploy) → L3 (närvaro).

## Del 4 — Fas 6b L2: get-event EF + deploy + coerce-klass-fix + info-vy

Tredje landningen — deploy-landning. get-event-EF byggd + deployad, info-vyn
realiserad. **Ett coerce-klass-fynd avbröt mitten** (kvitterat scope-tillägg, väg A).

### Coerce-klass-fyndet (kvitterat: fixa nu, väg A)

get-event-conformance mot **skarp staging-data** avslöjade att `EventSchema.parse`
föll: Airtable levererar formel-/procent-fält som blir NaN (0/0, osatt maxPlatser)
som **OBJEKT** `{specialValue:"NaN"}`, inte tal. Mappningens råa `?? null`/`?? 0`
släppte objektet → `z.number()`-parse avvisade → ETT sådant event sänkte hela
`z.array(EventSchema).parse` → list-laddningen kraschade. **Latent i deployade
get-events** (dess conformance var en smoke-test som aldrig `.parse():ade` skarp
data; L1:s e2e var mockad). **Samma klass som Session 23 "Ort"** (L5b) — Airtable
levererar fält i form domänen inte väntar; coercion måste vara konsekvent över EF:er.
Stängd via `scalarNumber` (aritets-familjen) i BÅDA mappningarna. Lesson **L152**
[UNIVERSAL] skriven. Testtäcknings-lucka noterad: list-EF-smoke-test validerar inte
skarp Airtable-output (notering räcker, ej egen tråd).

### EF + deploy

- **get-event** (single-get-mall): speglar get-events mapEvent för EN rad + ärver
  get-person:s 404-kontrakt (`fetchAirtableRecord` → null → HTTP 404
  `{error:'Event not found'}`). Full §6.10-ribba 2/2/2/2. LÄSER bara.
- **Staging-deploy** (T12-disciplin, explicit `--project-ref pqtshyierkdgwdnxuirz`,
  PROD `lvjsfnphlauldxqlncpl` orört): get-event v1→**v2** + get-events v4→**v5**
  ACTIVE (båda redeployade efter coerce-fixen). Verifierat via `functions list`.
- **T12 bet igen:** lokal `.env.test` `TEST_SUPABASE_URL` pekar på PROD → conformance
  kan ej köras mot staging lokalt (gick read-only mot prod, ofarligt). S-beviset
  körs i CI (staging-secrets). Evidens till **befintlig T12-tråd** — inget nytt
  registrerat. Staging-event verifierades finnas via Airtable MCP (3 ZZ-History-
  backing-event "Resor i medvetandet 1/2/3") → ingen seedning behövdes.

### Klient: fetchEvent + info-vyn

- **fetchEvent aktiverad** (`EventSchema.parse`, single; ADR-026). Supabase-stub-
  paritet bevarad (ADR-057, arch-audit-område i intakt).
- **EventDetail** (berikad operations-ÖVERSIKT, ej duplicerade listor): identitet +
  beläggnings-överblick (text/Fullt/procent, null-säker) + betalnings-sammanfattning
  (siffror) + länk → /betalning (byggd) + länk → /narvaro (platshållare L3) + not
  "Anmälda kommer i 6c" (ingen död länk — **valt: utelämna 6c-länk**). A11y 11/10
  speglar PersonDetail (fokus→h1, aria-live, role=alert 404+fel, document.title).

### Commits + verifiering

- `8fadfac` get-event EF (steg A) · staging-deploy v1 (steg B) ·
  `6d4220a` coerce-klass-fix + båda mappningar + fetchEvent .parse() + conformance +
  regression (steg B2/B3/C) · `6b379df` info-vy + e2e (steg D).
- Grindar: tsc -b + biome + vite build gröna. api-pure coerce 13/13 (scalarNumber +
  EventSchema-paritet). e2e 16/16 (8 info-vy inkl. axe 0/404/fokus/namnlöst/fullt/
  null-säker + 4 list + 4 mark-paid). get-event/get-events staging-conformance = CI.

### Status

Inget nytt ADR (coerce-klass-fix är en bugg-stängning, ej arkitekturbeslut).
**Fas 6b L2 KLAR.** Nästa: L3 (närvaro-vyn — kräver get-attendance-deploy).

---

## Del 5 — Fas 6b L3: get-attendance EF + deploy + närvaro-vyn (sessions-grupperad LÄS-vy)

Fjärde landningen — deploy-landning (ny EF-katalog). get-attendance byggd + deployad,
närvaro-vyn realiserad. **Ett namn-fynd utlöste STOPPA-grind i mitten** (kvitterat
VÄGVAL A).

### STOPPA-fyndet: Deltaganden saknar läsbart person-namn (kvitterat: VÄGVAL A)

LÄS-passet + **live-MCP-verifiering** (prod-bas `app8uGPrVCVOm6LfD`, `describe_table`
och 3 skarpa rader) falsifierade ingångshypotesen att Deltaganden-lookupen `Person`
bär namnet. Båda person-fälten — `Person` (`fldF9DORzkIBcRT7F`, lookup från
Anmälan.Person) OCH `Person (länk)` (`fldiU06kbTxSafkm4`, A11-länk) — returnerar
**record-ID:n**, inte namn (lookup av ett *länkfält* ger ID:n, ej primärvärdet). Inget
av de 47 fälten bär ett person-namn. Vyn kräver läsbara namn (Gunilla-princip) +
AttendanceSchema saknade namn-fält → **två STOPPA-grindar samtidigt**. Marcus/Chat
kvitterade **VÄGVAL A**: batch person-namn ur **Personer.Namn** (primärfält-formel
`TRIM(Förnamn & " " & Efternamn)` → "Ej tillgängligt" graciöst för namnlös) +
additiv `personNamn`-utökning av schemat. B (Anmälningar.Namn) förkastat (namnlös →
`" "`); C (klient-mappning) förkastat (kopplar vyn till hela person-listan, bryter
EF-självförsörjning/lager-gräns).

### Filter-fyndet (CI-rött → väg D, record-ID-batch från event-hållet)

Första deploy (väg "filtrerad lista") använde `buildLinkedRecordFilter('Event', eventId)`
ärvt från get-registrations. **CI-conformance (skarp staging-data) fällde det:** filtret
returnerade noll rader. Code-grävning till grundorsaken (verifierat mot prod OCH staging,
direkt formel-test via MCP): `FIND(recordId, ARRAYJOIN({Event}))` matchar länkens
**primär-display (eventlabel)**, inte record-ID → matchar ALDRIG på ID. **Klass-bugg** —
trasigt var helst ett länk-ID-filter byggs, **latent även i deployade get-registrations**
(dess `eventId`-filter saknar staging-test, aldrig kört mot skarp länk-data). Registrerad
som **T15** (paused; get-registrations-fix hör till 6c). Enhetstesterna verifierar
formel-SYNTAX, aldrig match-SEMANTIK mot riktig data — luckan conformance fångade
(jfr L152 NaN, L5b 403≠404).

Marcus/Chat förkastade både label-filter (väg A, eventlabel = formel-skör, fälla 17/18)
och `Event (ID)`-fält (verifierat **dött**: formeln ger radens EGNA id, ej eventets — data-model
§3.4-claim fel) och hämta-alla-1500 (väg B, tungt). **VÄG D landad:** spegla get-person:s
record-ID-batch men **från event-hållet**.

### EF + deploy (väg D)

- **get-attendance** (record-ID-batch): (1) `fetchAirtableRecord('Eventplanering', eventId)`
  → 404 `{error:'Event not found'}` om okänt (ärver get-event/get-person-kontraktet),
  saknat eventId → 400. (2) eventradens **`Närvaro (records)`-länk** (`fldwyD1cheb759YN1`,
  multipleRecordLinks → Deltaganden; live-verifierat populerat + symmetriskt med
  Deltaganden.Event) ger Deltaganden-record-ID:n. (3) chunkad `OR(RECORD_ID()=…)`-batch
  mot Deltaganden (`ATTENDANCE_BATCH_SIZE`-env, ceil(N/50), noll N+1/trunkering — get-person-
  mall). (4) **namn-batch oförändrad** (väg A): andra record-ID-batchen mot Personer.Namn →
  `Map<personId,namn>` fäster `personNamn`. `buildLinkedRecordFilter` ANVÄNDS EJ → get-attendance
  kringgår T15-klassen helt. `personId` ur `Person (länk)`; session/status via `selectName`;
  `avstamt`/`noteringar` via `scalarString`. Full §6.10-ribba 2/2/2/2. LÄSER bara.
- **Staging-deploy** (T12, explicit `--project-ref pqtshyierkdgwdnxuirz`): get-attendance
  **ACTIVE v6** (deploy → `ATTENDANCE_BATCH_SIZE=2`-secret + unset superseded
  `ATTENDANCE_NAME_BATCH_SIZE` → redeploy så env bootas). **PROD `lvjsfnphlauldxqlncpl`
  orört:** curl-verifierat staging `401` (live+auth-gate) vs prod `404` (ej deployad).
- **Chunk-merge:** record-ID-batchen speglar get-person:s redan bevisade chunk-merge;
  ZZ-fixturens event har 1 deltagande/event → merge-gränsen exerceras ej skarpt här
  (medvetet ej multi-seedat: re-bevisar identisk logik = över-engineering). Secret satt
  så vägen tas så snart ett event har >2 deltaganden/personer.

### Kontrakts-utökning + klient

- **AttendanceSchema + Attendance.ts**: additivt `personNamn: string|null`
  (symmetriskt, parallell sanningskälla). Kontrakts-regressionstest (api-pure, L142):
  parse med sträng/null/"Ej tillgängligt" passerar, saknat fält failar.
- **fetchAttendance aktiverad** (`z.array(AttendanceSchema).parse`, lista; ADR-026).
  Endast `eventId` skickas; session/status klientside. Supabase-stub-paritet bevarad
  (ADR-057).
- **EventAttendance** (sessions-grupperad LÄS-vy): grupperar per session (Dag 1/Dag 2/
  Föreläsning, fast ordning, bara sessioner med rader). Per grupp "{n} av {total}
  närvarande" (lynchpin = Närvaropoäng-mängden Närvarande+Deltog online). Person-NAMN
  (aldrig rec-ID) + status som TEXT. Kommande "Ej avstämt" = förväntat (ingen fel-yta),
  tom-state vänlig, `personNamn` null → "Namn saknas". **INGEN markera-knapp/write**
  (write = framtida slice, egna deny/allow-test). A11y 11/10 speglar EventDetail
  (fokus→h1, aria-live, aria-busy, role=alert, document.title, tillbaka→info-vyn
  `/event/$eventId`).

### Commits + verifiering

L3-bygget: `0e688a4` get-attendance EF (steg A, väg "filtrerad lista") · staging-deploy
(steg B) · `e8ff852` AttendanceSchema/-modell `personNamn` + regressionstest (kontrakt) ·
`c3fa0d5` fetchAttendance `.parse()` + conformance (steg C) · `2ee7a7d` närvaro-vyn + e2e
(steg D) · `c09a67f` Del 5-doc.

FIX-landning (CI fällde filtret): `ffbe3e0` get-attendance via record-ID-batch (väg D) +
redeploy v6 + conformance uppdaterad (404/400) · `5f10c9a` T15-registrering · denna
Del 5-uppdatering (filter-fynd + väg D + markdownlint-fix rad 242 + lessons).

- Grindar (fix-landning): tsc -b + biome + vite build + tsr generate gröna. api-pure
  **90/90** (5 AttendanceSchema-kontrakt + coerce 13). e2e narvaro/info/person-detail/
  shell gröna (vyn oförändrad). markdownlint **0** (rad 242 `+`-listmarker-fix),
  lifecycle OK.
- get-attendance staging-conformance (väg D): event m. deltaganden → record-ID-batch
  släpper rätt rader + parse + **personNamn LÄSBART end-to-end** (namn-batchen nåddes
  FÖRST NU — det trasiga filtret gav tidigare noll rader så namn-batchen aldrig
  exercerades), okänt eventId → 404, saknat → 400, anon → 401 = **CI = S-bevis** (lokalt
  går mot prod pga `.env.test`→prod, T12).

### Status

Inget nytt ADR (VÄGVAL A = hämtnings-väg + additiv kontrakts-utökning; väg D = mall-
återanvändning som kringgår en bugg-klass; T15 = tråd — inget arkitekturbeslut).
**Fas 6b L3 KLAR.** Nästa: L4 (per byggplan).

### Lessons (Session 25-H2-kandidater)

- **buildLinkedRecordFilter länk-display≠record-ID** — `FIND(recId, ARRAYJOIN({länk}))`
  matchar länkens primär-display, ej record-ID; enhetstest av formel-SYNTAX bevisar ej
  match-SEMANTIK → skarp conformance fångar (T15-klass, jfr L152, L5b).
- **get-attendance speglar get-person record-ID-batch FRÅN EVENT-HÅLLET** (`Närvaro
  (records)`-länk) → kringgår klass-buggen helt; record-ID = enda tillförlitliga
  filter-nyckeln (formel/lookup/display-match är alla sköra: `Event (ID)`=egen-ID-fälla,
  label-formel-släp, Eventkey-kollision).
