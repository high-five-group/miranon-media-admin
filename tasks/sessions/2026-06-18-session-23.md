---
owner: marcus803
updated: 2026-06-18
review_by: 2026-09-18
status: stable
lifecycle: active
---

# Session 23 — Fas 6a Persons-domän (strangler-fig start)

> Doket fött vid sessionsstart per ADR-043 + L67/L68 (levande artefakter har
> landnings-kadens, inte avsluts-kadens). Fött via session-start-skillens
> create-session-doc-gren (plugin marcus-system,
> `references/create-session-doc.md`). `lifecycle: active` satt explicit vid
> födelse (skill-ägt, ADR-052 beslut 3 + create-session-doc steg 8). Del 2+ fylls
> vid respektive landnings-ögonblick (committad + pushad + CI-grön), inte vid
> sessionsavslut.

## Del 1 — Syfte och scope

Fas 6a — Persons-domänen, FÖRST i strangler-fig-sekvensen (byggplan §4 Fas 6,
6a → 6e). Sliceen bygger Persons-rummet på Fas 5.5:s mutation- och DI-mall.

Scope (Fas 6a per byggplan §4 Fas 6):

1. Wira `/personer`-listan till befintlig `fetchPersons` (adaptern anropar redan
   `get-persons` EF; routen är idag en placeholder).
2. `/personer/$personId`-detaljvy + NY `get-person` single-record-EF +
   `fetchPerson` `.parse()`-aktivering (ADR-026, runtime-validering vid datagräns).
3. Minimal write "uppdatera notering" → ny operation i `field-allowlists.ts` +
   1 deny-test + 1 allow-test, gated på Synk-gate 2-handshake för notering-fältet.

Styrande: docs/byggplan.md §4 Fas 6 + ADR-016 (mutation-mall) + ADR-026
(Zod `.parse()` vid EF-deploy) + ADR-049/050/055 (Fas 5.5-arv: fält-val, isolerad
staging, router-context-DI). Inget nytt ADR förväntat — STOPPA om något
arkitektoniskt dyker upp.

Per-sub-fas-kontrakt (byggplan §2 Fas 6-raden): registrera operation i
`field-allowlists.ts` + deny/allow-test grönt + vy-Playwright baseline.

Inte scope: Fas 6b–6e (Events, Registrations, Hem-aggregering, Mer); andra
mutationer; realtime/polling (Fas 6d). Inga EF-deploys i förskott (M4 — varje
deploy följer en namngiven UI-konsument).

### Landning 1 (enabling-detour, Marcus-beslut)

BYGGPLAN-LÄTTLÄST-v3-driftfix FÖRE Fas 6a-bygget. Session 22:s `/session-end`
missade ADR-025:s lättläst-uppdatering vid fas-avslut (do-confirm-glapp, besläktat
med T03/T04): `docs/specs/BYGGPLAN-LÄTTLÄST-v3.md` (updated 2026-06-12) ligger ett
fas-steg efter `docs/byggplan.md` (v1.11, 2026-06-17) — visar fortfarande "Fas 5.5
… är nästa" trots att Session 22 stängde Fas 5.5. Detta är Gunilla-dokumentet och
måste vara i synk. Rättas före Fas 6a-koden.

### Numrering (disk-verifierad i orienterings-passet 2026-06-18)

Session 23; nästa ADR-056 (högsta på disk ADR-055 — ej väntad i 6a); nästa lesson
L140 (högsta på disk L139). DISK-VERIFIERADE, ej index-antagna.

### Ingångstillstånd (disk-verifierat, orienterings-pass 2026-06-18)

- Spoke-HEAD `9d42801`, rent arbetsträd, i sync med origin/main.
- `fetchPerson` (enskild) stubbad: `throw new Error('Not deployed yet — see Fas
  6a')` (src/data/adapters/AirtableAdapter.ts:95). `fetchPersons` (lista)
  implementerad — anropar `get-persons` EF (rad 50–60).
- `field-allowlists.ts`: en operation registrerad —
  `mark-registration-fee-paid` → { Anmälningar, ['Anmälningsavgift'] }. Ingen
  person-operation. Allowlisten styr endast writes via `update-record`.
- Routes: `/personer` är 11-radig placeholder (ingen loader/fetchPersons-wiring);
  inget `/personer/$personId`-skelett.
- `supabase/functions/`: `get-persons` (lista) finns; ingen `get-person`
  (enskild). Befintliga get-EF:er är alla list-typ — single-get-mönstret är nytt.
- Staging-/deploy-infra: staging-tester + `scripts/deploy-prod-functions.sh` +
  `.prod-functions-allowlist.conf` (innehåller `get-persons`) finns. Deploy-status
  i prod ej verifierbar från disk.
- `AIRTABLE_BASE_ID` refereras i `_shared/airtable-client.ts` (env); prod-secret-
  status ej verifierbar från disk (kräver Supabase-secrets-koll).

Del 2+ fylls vid respektive landnings-ögonblick.

## Del 2 — Förlopp och landningar

- **Dok fött** (`623116a`) — Del 1 fött vid sessionsstart, `lifecycle: active`.

- **Landning 1 — BYGGPLAN-LÄTTLÄST-v3-driftfix** (`b29ace9`, CI-grön run `27769296754`,
  2026-06-18). Enabling-detour (Marcus-beslut): Gunilla-dokumentet låg ett fas-steg efter
  byggplan.md (Session 22:s `/session-end` missade ADR-025:s lättläst-uppdatering). Speglade
  Fas 5-avslutets mönster exakt (commit `194e242`): status-blockquote + tidslinje-emoji +
  in-place "Så gick det"-stämpel på Fas 5.5 + versionshistorik. §5/§6/§7-strukturdriften
  **inte** rörd (precedens-troget) → flaggad och defererad som **T09**.

- **Steg 0 + Landning 2 — Personer-lista** (T09 `3db9a07`; feature `de210ba`, CI-grön run
  `27771672331`, 2026-06-18). Wirade `/personer` (var placeholder) till `fetchPersons` via
  router-context-DI (ADR-055). Kolumnval mot FAKTISK PersonSchema/get-persons-mappning
  (utelämnade "Nästa event"/"Återkommande?" som kräver EF-ändring). nuqs `?q`/`?page`,
  klient-slice-paginering (medveten kompromiss — defekt flaggad). 4 e2e (page.route-mock) + axe 0.

- **ADR-056 — list-paginerings-port (cursor, dubbel-källa)** (`9868326`, CI-grön run
  `27773817942`, 2026-06-18). Skriven **Proposed** → Marcus Gate-2. Cursor-baserad port på
  DataSourceAdapter; Airtable offset-token-wrapper byggs nu, Postgres keyset/seek låst Fas E;
  useInfiniteQuery + "Ladda fler" (a11y 11/10). Research-grundad (6 alternativ A–F kartlagda).

- **Steg 0 + Landning 3 — cursor-port end-to-end** (ADR-flip `e2e026c`; T10 `d1dfdd7`;
  T11 `d77a111`; cursor-port `83f55f9`, CI-grön run `27775247396`, 2026-06-18). ADR-056
  flippad **Accepted** (Gate-2). Ersatte L2:s klient-slice med cursor-porten: opak codec
  (`_shared/cursor.ts`), `fetchAirtablePage` (ETT anrop/sida, bredvid full-walk),
  `get-persons` cursor+pageSize, `listPersons`-port, `useInfiniteQuery` + "Ladda fler" med
  fokus-behållning + aria-live. STATE-STRATEGY §2/§3 reconcilierad (öppen rivning).
  T10/T11 registrerade.

## Del 3 — Landning 4 steg 1 (deploy)

**2026-06-18 — cursor-`get-persons` deployad till staging.** Bare CLI
(`supabase functions deploy get-persons --project-ref pqtshyierkdgwdnxuirz`),
ADR-050 steg-5-mönstret — **inte** prod-allowlist-skriptet (det är en PROD-spärr).
EF-version **3 → 4**, STATUS **ACTIVE** (UPDATED 2026-06-18 18:16:42 UTC). Carry-
artefakterna buntades med vid deploy (asset-listan bekräftade `_shared/cursor.ts` +
`_shared/airtable-client.ts`, plus errors/cors/auth/airtable-filter). **PROD
(`lvjsfnphlauldxqlncpl`) orört.** Deployen är ej committbar → denna post ÄR
landningen (L67).

> ⚠️ **Körnings-CONFORMANCE-overifierad tills steg 2.** Deployen bekräftar att
> koden ligger ACTIVE i staging-storen — INTE att cursor-porten beter sig korrekt
> mot skarp staging-data (multi-sida-vandring, opak cursor↔offset, sista-sida-null).
> Port-conformance-batteriet (Landning 4 steg 2, egen prompt) verifierar det.

## Del 4 — Landning 4 steg 3 (seed + conformance-harness)

**2026-06-18 — cursor-porten skarp-verifierad mot staging.** Steg 1:s deploy
(Del 3) är nu körnings-conformance-bevisad.

**Seed (write mot staging-Personer, ingen prod-data, ingen PII).** 5 permanenta
syntetiska records skapade via Airtable-MCP `create_record` mot
`apphjj8Q7lkXCMsL4`/`Personer` (`tbl6ZyCm3V026iFTU`). `Namn` är ett **formelfält**
(härlett ur `Förnamn`+`Efternamn`, bekräftat mot data-model.md §3.3) → ej direkt
skrivbart; väg A (ratificerad) seedar via de skrivbara käll-fälten: `Förnamn`=
`ZZ-Conformance`, `Efternamn`=`Person 01..05` → beräknat `Namn`=
`ZZ-Conformance Person 01..05`. `E-post`/`Telefon` tomma (ingen PII). Första
`create_record` löste F2-scope-residualen: **bas-nivå write bekräftad** (lyckades,
ej 403). Record-ID: `recUvh5WonUaE9FN0`, `reciDDkiza1geqG05`, `recYtWHL9z7KPCUKd`,
`recFWtbfbbMMVEadb`, `recaz7wT10AH6fBlI`. Read-back bekräftade exakt 5 i asc-ordning.
Records bor **permanent** (conformance-fixtur; städa ej — basen är "tom" = ingen
prod-data, ej absolut tom; jfr data-model.md "radera aldrig Personer").

**Search-isolering (FAS A de-risk).** EF:en bygger
`OR(SEARCH(LOWER(term), LOWER({fält})), …)` över Namn/E-post/Telefon/Ort —
case-insensitiv substring. Prefixet `ZZ-Conformance Person` isolerar exakt de 5.

**Återanvändbar harness + instans.**
[`tests/api/cursor-conformance.ts`](../../tests/api/cursor-conformance.ts) —
kanonisk `assertCursorPortConformance({ callPage, fixturePrefix, expectedNames,
pageSize })`, källa-agnostisk (6b/6c ärver). Asserterar ADR-056-kontraktet:
cursor-vandring till `nextCursor===null`, exakt pageSize/icke-sista-sida, opak
sträng-cursor, ackumulerad ordning, sida-1-bär-ej-allt (paginering, ej
full-walk-slice).
[`tests/api/get-persons.staging.test.ts`](../../tests/api/get-persons.staging.test.ts)
— instans: skarp HTTP mot get-persons (auth `getValidUserJWT` → password-grant),
`pageSize=2` mot 5 records → sid-sekvens **[2,2,1]**, `nextCursor` [≠null,≠null,null].
Klient-a11y dupliceras ej (L3:s mock-e2e täcker det).

**CI:** run `27783202181` grön — **API tests (staging) 42 passed** (tidigare 41
→ +1 = nya conformance-testet, skarpt mot live-EF + fixtur; STAGING_REQUIRED=1,
0 skip). Commit `85c625a`.

## Paushistorik — Session 23 (pausad 2026-06-18 efter Fas 6a Landning 3, återupptagen 2026-06-18)

Session-PAUS (ADR-051/052), **inte** avslut: samma session (23) återupptas via
`/session-resume` i färsk chatt. Ingen finalisering — ingen arkivering, lessons-hub-sync,
CHANGELOG, status-stängning eller nummer-increment.

**NULÄGE:** Fas 6a (Persons-domän, strangler-fig första domän). Landning 1 (lättläst-driftfix),
Landning 2 (Personer-lista) och Landning 3 (cursor-port end-to-end) KLARA, alla CI-gröna.
ADR-056 (list-paginerings-port, cursor, dubbel-källa) **Accepted**. Senaste commit cursor-port:
`83f55f9`, run `27775247396`.

**CARRY (bär in vid resume):**

- Cursor-EF:en (`get-persons` paged) är REVIEW-verifierad men **CI-OVERIFIERAD körningsmässigt**
  — ingen deno-check i CI, testad via page.route-mock. Riktig exekvering bevisas FÖRST vid
  staging-deploy (Landning 4). Anta den inte körnings-bevisad.
- Port-kontrakt `{ persons, nextCursor }` via opak codec (`_shared/cursor.ts`, envelope
  `{o:offset}` base64). Fas E keyset-cursor återanvänder samma envelope → identiskt
  klient-kontrakt. `ListParams` generisk; `PersonsPage` ej generisk `CursorPage<T>` än
  (6b avgör — YAGNI).
- `fetchAirtablePage` duplicerar ~15 rader från full-walk `fetchFromAirtable` (medvetet, hedrar
  RÖR-INTE-guarden; framtida helper-extraktion ej brådskande).
- A11y-mönstret för "Ladda fler" (fokus-behållning via ref+återfokus efter settle, status-rad
  vid sista sidan, aria-live antal) är det 11/10-mönster 6b/6c ska ärva.
- Klarspråks-förklaringen sidnummer→"ladda fler" hör in i lättläst-byggplanen → faller under
  **T09** (lättläst-svep); substans finns i ADR-056, härledbar.

**ÖPPNA TRÅDAR (i registret):** T09 (lättläst legibility-svep + klarspråks-paginerings-
förklaring), T10 (dubbel-källa-conformance + paritets-grind, Fas E), T11 (Proposed i
§Format-enum). Plus byggplan-backloggar (staging-trådar, hub-sync L103–L139, oarkiverad
sessionsdok-backlogg 16–22) — Marcus-beslut, ej Fas 6a-blockerande.

**NÄSTA KONKRETA STEG:** Landning 4 = staging-deploy av cursor-EF (`get-persons`) +
port-nivå-conformance-batteriet (Airtable-beteende på port-nivå = framtida Supabase-grind),
mot riktig staging-data. **KRÄVER Marcus miljö-moment** (deploy via
`scripts/deploy-prod-functions.sh`-mönstret mot staging; `AIRTABLE_BASE_ID`-secret-koll).
Därefter återstår i Fas 6a: Landning 5 detaljvy `/personer/$personId` + `get-person`-EF
(första single-get-mall), Landning 6 write-operation (`Personer.Anteckningar`
`fldWGlNr3ujRHo85w` via `field-allowlists` + deny/allow).

**Provisoriska lessons-kandidater** (antecknade, EJ hub-lyfta — skördas vid avslut):

- A11y: `useInfiniteQuery`-"Ladda fler" tappar fokus när knappen kort blir `disabled`
  (webbläsare blur:ar disabled-element) → kräver explicit ref+återfokus efter settle, och
  fokusflytt till status-rad när knappen unmountas vid sista sidan. Ren `useInfiniteQuery`
  ger inte 11/10 gratis.
- Cursor-opacitet: en namngiven envelope (`{o:token}` base64) frikopplar backend-token-formen
  från klienten OCH ger versionerbarhet — bättre än rå base64 av token.
- Medveten duplicering kan vara rätt val när en explicit RÖR-INTE-guard skyddar en delad
  konsument (full-walk-funktionen) — hedra guarden, defer helper-extraktion.

Del 3+ (resume-förlopp) fylls vid återupptagandet.
