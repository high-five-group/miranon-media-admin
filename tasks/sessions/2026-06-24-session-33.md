---
owner: marcus803
updated: 2026-06-24
review_by: 2026-09-24
status: stable
lifecycle: active
---

# Session 33 — Fas 6e Mer-fliken

> **Fött vid `/session-start`** (skapande-gren, ADR-043 beslut 4) med
> `lifecycle: active`. Del 1 bär scope verbatim från Chat-orienteringen; Del 2+
> föds vid respektive landning enligt landnings-kadens (L67/L68) — skrivs INTE
> i förväg.

## Del 1 — Syfte och scope

### Syfte och scope

Fas 6e — Mer-fliken (byggplan §4 Fas 6-tabellen). Tre ytor + skal, alla mot Lottas språk ("Intresserade", ej "Leads" — lessons-låst):

(a) **Intresserade (LÄS)** — ny get-leads EF + fetchLeads `.parse()`-aktivering + `/mer/intresserade`-vy. Data: Hämtade erbjudanden/Engagemang-domänen (personer med leads/touchpoints, ej deltaganden). Vy ärver Personer-listans mönster.

(b) **Maillogg (LÄS)** — ny get-mail-log EF + fetchMailLog `.parse()`-aktivering + `/mer/maillogg`-vy. Data: Utskickslogg/Bulkutskick. Listmönster.

(c) **Skicka mail (WRITE)** — ny send-email EF (direct-Resend per ADR-015 + Resend Idempotency-Key-header + DISTINKT partial-failure-status mot data-model §F.3/Fälla 29) + sendEmail + formulär (AddRegistrationModal-mönstret från 6c).

(d) **Inställningar + logga ut** — skal i Mer-landningen.

Per-operations-kontrakt (ärvt): send-email post-send Airtable-PATCH (mail-prick-timestamp) registreras i `field-allowlists.ts` med deny+allow-test; EF-ribba (requireUser/CORS/requestId/mapError) på alla tre nya EF; vy-Playwright-baseline + axe 0 per vy. Mönster-arv: single-record/list-EF-mallar, `_shared/coerce.ts`, cursor-conformance, ADR-016 mutation-mall, ADR-026 `.parse()`.

Styrande: `docs/byggplan.md` §4 Fas 6 + ADR-015 (send-email direct-Resend) + ADR-059 (idempotens-defer, Airtable-pathen — RÖRS EJ) + ADR-016/026/049/050/055/057.

### Ordnad scope (PRELIMINÄR landnings-struktur — slutlig skärning sätts av Chat per L-design)

- **L0 — doc-landning** (enabling, ingen app-kod): byggplan.md + BYGGPLAN-LÄTTLÄST-v3.md — lås 6e-scope (a–d), revidera 6e-estimat uppåt (0,5→~1,5 sess, multi-landning), LÄGG IN ny sub-fas 6f "Skapa nytt event" (write-vertikal mot Eventplanering, egen framtida session), fixa LÄTTLÄST-v3 stale-drift (T09 — den ligger 4 sub-faser efter). FULLT Fas 6 fas-avslut flyttas till efter 6f. todo.md uppdateras.
- **L1 — Intresserade** (get-leads + vy).
- **L2 — Maillogg** (get-mail-log + vy).
- **L3 — Skicka mail** (send-email write-EF + formulär; trolig atomisk split EF/vy).
- **Avslut** — `/arch-audit` (ADR-058) + `/session-end`. SESSIONSGRÄNS, EJ fas-avslut (Fas 6 öppen mot 6f).

### Inte i scope

- Fas 6f (Skapa nytt event-BYGGE) — formaliseras i doc men byggs i egen session.
- fetchEngagements — förblir post-Gate-4B-defer (ej 6e), stub orörd.
- Realtime/polling (Fas E), Aktivitetslogg (Fas 6.5).
- Inget nytt ADR väntat UTÖVER ev. additiv ADR-015-uppdatering för Idempotency-Key. STOPPA om något arkitektoniskt dyker upp (ADR-053-triage).

### Deploy-disciplin (T34-guard, in i scope)

Alla EF-deploys i denna session sker EXPLICIT mot staging-ref (aldrig bare `supabase functions deploy`). Prod orörd; prod-allowlist-skriptet ej använt (de nya EF:erna är prod-exkluderade by default — fail-closed). Verifiera live staging-ref mot disk vid deploy-ögonblicket, anta inte från index.

### Ingångstillstånd (disk-verifierat vid födelse, 2026-06-24)

- **HEAD:** `cb270cd` — "Session 32 /session-end: sessionsdok Del 2–5 + lessons L180–L184 + BUILD-LOG + todo; lifecycle: closed [T30]".
- **Träd-status:** rent, `main` i sync med `origin/main` (ingen drift sedan orienterings-passet).
- **Numrering (disk-verifierad, ej index-antagen):** session 33 (senaste landade = 32); nästa ADR = **062** (högsta på disk ADR-061); nästa lesson = **L185** (högsta på disk L184).
- **6e-stubbar intakta** i `src/data/adapters/AirtableAdapter.ts`: `fetchLeads` (rad 199–201), `sendEmail` (rad 224–226), `fetchMailLog` (rad 237–239) — alla `throw new Error('Not deployed yet — see Fas 6e')`. (`fetchEngagements` rad 212–213 förblir post-Gate-4B-defer, orörd.) SupabaseAdapter speglar alla som `NOT_IMPLEMENTED`-stubbar.
- **Inga 6e-EF deployade:** `supabase/functions/` innehåller INGEN av get-leads / get-mail-log / send-email (fri ingång för L1–L3).
- **`/mer/index.tsx`:** statisk länklista — endast Väntelista + `<p>Fler vyer byggs i Fas 6e.</p>`. Inga mer-undervyer utöver `index.tsx` + `vantelista.tsx`.

## Paushistorik — Session 33 (pausad 2026-06-24, återupptagen 2026-06-24)

> Ren paus mitt i L1, EJ avslut. Samma scope fortsätter → återupptas via
> `/session-resume` i färskt fönster med nr 33 bevarat. INGEN finalisering:
> ingen arkivering, ingen lessons-hub-sync, ingen CHANGELOG-release, inget
> nummer-increment. `lifecycle: paused` + denna rubrik → grind-konsistens.

### HANDOFF

**STATUS:** Fas 6e pågår, L1 Intresserade mitt i. L0 (doc-grund) + L1 Landning 1/1.5/2 landade CI-gröna på `main`. Återupptas vid L1 Landning 3.

**LANDAT (commit-trail, disk-sanning):**

- **L0 doc-grund:** `32bcaaa` (6e-scope-lås + 6f-formalisering + T09-fix) + `2f39a48` (grand-totals 3,5→5,5 / 16,5→18,5) + `4ed6790` (provenans-stämpel).
- **L1 Landning 1:** `bf82911` (get-leads EF + IntresseradSchema + 2 ZZ-Lead-fixturer seedade staging + proof-gate; api-staging 87 passed).
- **L1.5:** `78ad1c6` (winback/avhoppad-docs i 3 styrande docs — hur-systemet-funkar.md livscykel-rad + byggplan §4-not + LÄTTLÄST-v3 du-form).
- **L1 Landning 2:** `16e328f` (föräldralösa Lead-domänen raderad + `fetchIntresserade` adapter; port-paritet ADR-057; 0 kvarvarande referenser).

**NÄSTA KONKRETA STEG (vid resume):**

- **L1 Landning 3 — vy `/mer/intresserade` + e2e.** Mönster-arv: `src/routes/_authenticated/personer/index.tsx` + `queryKeys.persons`-grenen (`src/queries/keys.ts`). A11y 11/10: `h1="Intresserade"` + fokus→h1 post-async, aria-live data-anländning, aria-busy, fel via `role="alert"` (MessageBox, ingen dubbel-announcer L138), TOM-TILLSTÅND (vänlig text, aldrig tyst), tangentbordsnåbara rader, namnlös-fallback ("Namnlös"/e-post, aldrig krasch — data-model Fälla 22). Per rad: namn/fallback + `allaHamtningar` ("vad de nappat på") + `antalHamtningar` + senaste interaktion. Mer-landningen (`mer/index.tsx`) får länk till `/mer/intresserade`. Ny `queryKeys`-gren. e2e: lead-lista, namnlös-fallback (med/utan e-post), tom-tillstånd, loading, fel, fokus+announcer, axe 0 (`page.route`-mock, regex ej get-persons-kollision).
- **HOUSEKEEPING (i Landning 3-veva):** registrera winback-tråd ("winback/återaktiverings-bucket (avbokade-aldrig-deltagit) — segmentering", status `paused`, blockerar ej; kopplar Scenario 4) i `tasks/threads/` + index. SEDAN korrigera `byggplan.md` §4 Fas 6e-notens winback-tråd-PLATSHÅLLARE ("Se registrerad tråd, tasks/threads/ …", skriven i `78ad1c6`) till faktiskt tråd-ID.
- **DÄREFTER (efter L1 klar):** L2 Maillogg (get-mail-log EF + läsvy) → L3 Skicka mail (send-email write-EF direct-Resend + Idempotency-Key + distinkt partial-failure-status + formulär). Avslut: `/arch-audit` (ADR-058) + `/session-end` (SESSIONSGRÄNS, ej fas-avslut — Fas 6 öppen mot 6f).

**INGÅNGSTILLSTÅND (verifiera mot disk vid resume):**

- `fetchIntresserade()` live i `AirtableAdapter`; `IntresseradSchema` exporterad (`src/domain/schemas/index.ts`).
- get-leads deployad staging (`--project-ref pqtshyierkdgwdnxuirz`, prod-exkluderad fail-closed); anon smoke → 401.
- ZZ-Lead Person 01 (`recSsbzqUxxvjKavd`) + 02 (`recbXfJuC8kjHr0Cd`) = PERMANENTA staging-fixturer (vardera 1 länkad Engagemang → `Antal hämtningar`=1, 0 Anmälningar). RÖR EJ.
- `main` HEAD = `16e328f` (verifiera).

**CARRY / öppna trådar:**

- **Winback-tråd** — att registreras i Landning 3-veva (ej registrerad än, per ren-paus-disciplin).
- **byggplan winback-platshållare** — att korrigeras med tråd-ID i samma veva.
- Befintliga: **T34** (CLI prod-länkning — deploy-disciplin explicit staging-ref hållen genom hela L1) `paused`. **T09** STÄNGD i L0.

**LESSONS-KANDIDATER (capture per paus, EJ hub-lyft — ADR-051):**

- **Seed-fixtur-mönstret för filter-conformance:** en syntetisk lead (Person + länkad Engagemang) gör en annars-tom syntetisk staging-bas filter-bevisbar; `COUNTA(Engagemang)` uppdaterades rent 0→1 vid länk (ingen automation-kaskad). Skarp inklusions-/exklusions-conformance kräver seedad data — syntax-grön ≠ semantik-korrekt (L154-klass).
- **Filter-redundans-bevis via relations-kardinalitet:** `{Antal anmälningar (totalt)} = 0 ⟹ Totala deltaganden = 0` eftersom ett Deltagande är "en rad per Anmälan × Session" (data-model rad 74/107; A3 kräver `Anmälan.Person`). En klausul kan bevisas redundant ur datamodellens kardinalitet i stället för att läggas till defensivt.
- **Föräldralös-domän-radering vid omtolkning:** när ett låst domän-omtolkning (Lead→Intresserad) gör en befintlig typ föräldralös, verifiera konsument-grep FÖRST (0 utanför adapter-trion) → radera rent i stället för att böja; egen typ slår böjd typ när domänerna skiljer sig.
