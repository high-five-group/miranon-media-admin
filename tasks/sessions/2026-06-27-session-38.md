---
owner: marcus803
updated: 2026-06-27
review_by: 2026-09-27
status: stable
lifecycle: active
---

# Session 38 — Fas 6f: Skapa nytt event (create-event write-vertikal)

> **Fött vid `/session-start`** (skapande-gren, ADR-043 beslut 4) med
> `lifecycle: active`. Del 1 bär scope verbatim från Chat-orienteringen; Del 2+
> föds vid respektive landning enligt landnings-kadens (L67/L68) — skrivs INTE
> i förväg.

## Del 1 — Syfte och scope

### Syfte

Session 38 bygger Fas 6f — **create-event write-vertikal** mot Airtable-tabellen
Eventplanering. "Skapa nytt event"-handlingen: repots nästa write-vertikal efter
6g:s segment-write. Egen ADR (066) bär kontraktet. Följer det etablerade
write-vertikal-mönstret (field-allowlists + operationKey + deny/allow-test + EF +
Idempotency-Key).

### Scope (PROVISORISK — låses av forensiskt pre-pass)

> Scopet nedan är provisoriskt och **låses av den första landningen** (forensiskt
> pre-pass, TASK 2). Pre-passet binder create-event-kontraktet mot live
> Eventplanering-schema + befintligt write-vertikal-mönster FÖRE en rad 6f-kod
> (L191). Avvikelser mellan disk-referens och live-schema, eller obevisade
> premisser, justerar scopet innan kod skrivs.

- create-event write-vertikal mot Eventplanering — ett rad-write som skapar ett
  nytt event från appen.
- Egen ADR-066 (create-event-kontraktet: målfält, skrivbarhets-klassning,
  minimal krav-fält-set, idempotens).
- Återanvänder det färskaste write-vertikal-mönstret (save-segment, 6g L3;
  jämförelse mot markera-betalning, 5.5).

### Första landningen — forensiskt pre-pass (TASK 2, READ-only)

Låser create-event-kontraktet mot live Eventplanering-schema + befintligt
write-vertikal-mönster FÖRE en rad 6f-kod (L191). Skriver ingen 6f-kod, ingen
ADR, ingen field-allowlists-ändring, ingen EF, ingen deploy. Chat låser
kontraktet + designar ADR-066 + write-vertikalen NÄSTA prompt, mot fynden.

### Ej i scope (explicit)

- 6e retro-audit (T38) — eget förkrav FÖRE FULLT Fas 6-avslut, ej här.
- Fas 6h Mail — efter 6f.
- Prod-deploy av 6g-EF:erna (compute-segment / save-segment / get-segments) —
  medveten separat handling, STAGING-only nu.
- FULLT Fas 6 fas-avslut.

### Numrering (disk-verifierad)

Session 38; nästa ADR = 066; nästa lesson = L202. Föregående dok = session-37
(lifecycle: closed). DISK-VERIFIERAD detta pass, ej index-antagen.

### Ingångstillstånd (carries)

- **6g-EF:er STAGING-only** — compute-segment / save-segment / get-segments
  deployade till staging, EJ prod (medveten separat handling).
- **Trådar paused** — T16 / T34 / T35 / T36 / T37 / T38 alla `paused` i
  tasks/threads/README.md.
- **Lessons L193–L201 ej hub-lyfta** — pending efter FULLT Fas 6.

## Del 2 — Förlopp och landningar

### Landning L0 — doc-grund (ADR-066 + data-model create-avsnitt + register)

**Planerat:** lägg doc-grunden för Fas 6f create-event-vertikalen — ADR + referens-uppdatering, ingen EF/schema/allowlist/deploy (de är L1).

**Faktiskt:**

- **Forensiskt pre-pass** (READ-only) låste create-event-kontraktet mot live STAGING-schema (`tblVE3UKWl1CKrphV`) + write-vertikal-mönstret (create-registration 6c L4 / save-segment 6g L3). Fynd: `EventKey`/`Event-nr` system-genererade (formel-på-autoNumber) → sätts aldrig; **AVVIKELSE A** — `data-model.md`:s Eventplanering write-fält-tabell var UPDATE-orienterad (saknade create-essentiella identitets-/datum-fält).
- **STEG 0' — prod-introspektion** (READ-only, auktoriserad; PROD `app8uGPrVCVOm6LfD`, noll mutation): **N=50 riktiga event, 0 test-rader exkluderade**. Utbildning 46/46 (100 %) + Föreläsning 4/4 (100 %) bär sessionsmall → **GREN A vald** (Eventtyp KRÄVS vid create rakt av, ingen Typ-carve-out). Den föregående staging-baserade STEG 0 STOPPADE korrekt (staging bar bara 3 syntetiska Utbildnings-event, 0 Föreläsning → otillräckligt) → väg-omval till prod-READ.
- **COMMIT A** (`1bf4ad4`): [ADR-066](../../docs/decisions/ADR-066-skapa-event-write-vertikal-idempotens.md) (Accepted, Fas 6f) — sex beslut (ett rad-write/ingen kaskad; server-side create-set + allowlist-SSOT; idempotens via Airtable-nativ upsert på nytt `Idempotensnyckel`-fält; pessimistisk create; Eventtyp required GREN A; Månad/år härledd). Web-research-grundad (Stripe/IETF/TanStack/Airtable — `performUpsert`/`fieldsToMergeOn` + IETF-draften live-verifierade). count **65→66** (grind grön, L190-svep). README-index + README.md:128-bump i samma commit.
- **COMMIT B** (`ae2e974`): `data-model.md` create-avsnitt (Eventplanering — create-fält, live-belagt, skilt från UPDATE-tabellen → AVVIKELSE A durabel, L189); §Kända fällor **36** (Månad/år-drift, **AVVIKELSE B**, maximerings-kandidat T16) + **37** (Idempotensnyckel L1-schema); T16-not vidgad.
- **COMMIT C** (denna): BUILD-LOG Session 38-post + denna Del 2.

**Grindar:** markdownlint 0, Vale 0, `check-adr-count.sh` 66==66 — gröna före varje commit. Pre-commit-hook auto-bumpade `updated:` på governing docs (README/data-model). **STOPPA-grindar:** ingen utlöst — inget antaget-skrivbart create-fält visade sig formel (EventKey/Event-nr system-genererade by design); ingen kaskad; AVVIKELSE A var lucka (fångad), ej motsägelse.

**Carry till L1:** schema-mutation `Idempotensnyckel` staging→prod + create-event-EF + allowlist-post + staging-tester. Merge-fältets live-skrivbarhet = explicit STOPPA-grind i L1.

### Landning L1 — create-event write-vertikal (server + schema)

**Planerat:** schema-fält + EF + allowlist + staging-tester; formulär/adapter/e2e = L2. PROD orörd.

**Faktiskt:**

- **COMMIT A — schema `Idempotensnyckel` STAGING-only** (`3d0ea52`): fält skapat via MCP (6g-L3-mekanism) i STAGING Eventplanering (`fldOWoh4WR5zG6XgQ`, singleLineText). Live-verifierat: ej beräknat, tomt på alla 3 befintliga rader. **PROD-fält medvetet ej skapat** → hård prod-deploy-förutsättning registrerad §Kända fällor 37 (L192). Ingen STOPPA (typ + tomhet bekräftad).
- **COMMIT B — EF + allowlist + STAGING-deploy** (`d3b696e`): ny `create-event`-EF (egen katalog, `Deno.serve`, ej generisk update-record); säkerhets-kontrakt speglat create-registration/save-segment EXAKT (405/401/400/`{error}`+requestId/mapErrorToResponse). `fields` byggs server-side ur typade inputs; system-genererade (EventKey/Event-nr) + spegel/länk aldrig; Månad/år härledd ur Startdatum; Eventtyp required (GREN A). Idempotens-mekanism: ny `upsertAirtableRecord`-helper (`performUpsert.fieldsToMergeOn:['Idempotensnyckel']`, `typecast:false`) → 201 create / 200 replay. Allowlist-post `create-event` (inkl. `Idempotensnyckel`). **Manuell validering, ej Zod** — EF-lagret använder manuell deny-by-default (create-registration/save-segment); Zod bara i klient/adapter (ADR-026). AVVIKELSE från prompt-direktivets "Zod", löst per kodbas-konsistens (samma princip prompten åberopar för Idempotency-Key). Deployad STAGING `pqtshyierkdgwdnxuirz` (explicit `--project-ref`, T34-neutralisering), **PROD orörd**.
- **COMMIT C — staging-tester** (`20433f9`): `tests/api/create-event.staging.test.ts`, **7/7 grönt mot live STAGING**. KÄRNAN bevisad live: färsk UUID→201 created, **samma UUID→200 replay, SAMMA record-ID, noll dubblett** (upsert skapar mot tomt merge-fält + idempotent retry, L189). Write-bevis: Månad/år härlett (`September 2026`), Eventtyp-länk→`Sessionsmall:[Dag 1, Dag 2]`, EventKey/Event-nr födda. Säkerhet (405/401/400) + input-deny + UUID-format-deny gröna.

**STEG 0-fynd (premiss-brott, löst):** staging Eventformat var TOMT (0 records; prod har 3) → inget "riktigt" Eventformat-record-ID fanns. Löst genom att seeda en permanent sentinel-fixtur via MCP (`recclDd7hUQsfxoVs`, `ZZ-create-event-test-format`) + dokumenterad env-override/fallback i testet → ingen ny CI-secret krävs. Lärdom **L202** [UNIVERSAL].

**STOPPA-grind (hård, STEG 3): EJ utlöst** — merge-mekanismen fungerar (färsk UUID skapar, inte patchar tom rad; Airtable accepterar `Idempotensnyckel` som `fieldsToMergeOn`). Bevisat live, ej bara i CI.

**Städning:** alla sentinel-events från lokala körningar MCP-raderade (Event-60..65). CI-körningar sentinel-ackumulerar per ADR-060.

**Carry till L2:** adapter (`callEdgeFunction` + EventSchema-parse) + skapa-event-formulär (Eventformat-options-fetch + pessimistisk UI, ADR-066 b4) + e2e (axe 0). **PROD-handling (bundlad, separat-auktoriserad):** prod-fält `Idempotensnyckel` + create-event-EF-prod-deploy = EN atomisk handling — prod-fältet är HÅRD förutsättning (EF:en skriver det), får ej glömmas (§Kända fällor 37). Staging Eventformat-fixturen (`recclDd7hUQsfxoVs`) är en staging-test-beroende (ZZ, ADR-060-tolererad).

### Landning L2 — create-event klient-vertikal (adapter + formulär + e2e)

**Planerat:** avsluta create-event-vertikalen klient-side — adapter + formulär + e2e. PROD orörd.

**Faktiskt:**

- **STEG 0 (READ-only):** sökte kodbasen — **ingen** befintlig EF/adapter exponerar Eventformat-poster (`get-events` returnerar Eventplanering, ej Eventformat). Vägval: **NEJ → bygg minimal `get-event-formats` read-EF** (STEG 1).
- **COMMIT A** (`8a88b38`): `get-event-formats` read-EF (speglar get-segments) — listar Eventformat (rec-id + Namn) för Eventtyp-dropdownen (ADR-066 b5, ingen hårdkodad options-lista). Deployad STAGING (explicit `--project-ref`, T34), **PROD orörd**. Staging-test 3/3 grönt (returnerar sentinel-fixturen).
- **COMMIT B** (`412daf0`): CreateEvent-schemas (EventFormat/CreatedEvent/CreateEventInput) + port-metoder (createEvent/getEventFormats) + AirtableAdapter-impl + Supabase-stubbar (port-paritet, ADR-056) + query-key `events.formats`. **CreateEventForm** (`/mer/skapa-event`, nav-länk i mer/index): pessimistisk mutation (ADR-066 b4 — 201+200 båda framgång → invalidera + navigera till skapat event; värden bevarade vid fel; ingen optimistisk insert); idempotency-key lazy-genererad EN gång per öppning (b3, aldrig per klick); Eventtyp/Eventformat required ur get-event-formats-fetch (b5); Event/Typ-options härledda ur get-events (självväxande, ingen stale-lista). a11y speglar SegmentBuilder. **Manuell validering, ej Zod-i-EF** (kodbas-konsistens). Form-justering: react-aria Button `onPress` (ej `type="submit"`) + label "Eventformat" (avkollidera "Typ"-substring) — se L203.
- **COMMIT C** (`cfd274e`): e2e `tests/e2e/skapa-event.staging.test.ts` — **4/4 grönt** (page.route-mockad, ingen riktig staging-write → ingen städning): happy path (fyll→submit→pessimistisk navigation till skapat event), required-validering (tom Eventtyp→submit blockeras klient-side, ingen EF-anrop), axe 0.

**DoD lokalt:** typecheck/biome (0 errors, 204 filer)/build/api-pure 141/api-staging create-event 7/7 + get-event-formats 3/3/e2e skapa-event 4/4 — gröna.

**Lärdom:** **L203** [UNIVERSAL] — substring-kollision i e2e-selektorer (`getByRole name` default substring: "Typ" ⊂ "Eventtyp") + route-mock-regexar (`/get-event/` fångar `get-events`/`get-event-formats`) → ankra alltid. Plus react-aria Button-submit-sido-regeln.

**STOPPA-grindar:** ingen ny. STEG 0 entydigt (NEJ). PROD orörd genomgående.

**Fas 6f-status:** create-event-vertikalen är nu **funktionellt klar på staging** (server + schema + klient + e2e). **ÅTERSTÅR FÖRE 6f-deklaration:** 6f arch-audit (ADR-058, Chat dirigerar separat) + prod-deploy (fält + EF, bundlad separat-auktoriserad handling). **SESSIONSGRÄNS, EJ fas-avslut** (Fas 6 öppen mot 6e-retro/6h) → ingen arkivering/CHANGELOG/phase-end-verify/hub-lyft.
