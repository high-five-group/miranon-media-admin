---
owner: marcus803
updated: 2026-07-12
review_by: 2026-10-12
status: stable
lifecycle: active
---

# Session 65 — T76-piloten: parallella batch-pipelines (pilot-bygge → skarp pilot på partitionen 8.1–8.3 ∥ 9.1/9.3)

> Fött vid `/session-start` (skapande-gren, ADR-043 beslut 4) med
> `lifecycle: active`. Del 1 bär scope från sessionsstart-orienteringen
> (författad på Code-ytan; Marcus-kvitterad 2026-07-12 i delegerad
> senior-form — "Jag vilar på dig som senior här, jag kvitterar det du
> kvitterar" — på öppningsordern "Vi kör /session-start och riktar in
> oss på T76-piloten", ordagrann matchning mot S64-handoffens
> föreslagna ingång). Del 2+ föds vid respektive landning enligt
> landnings-kadens (L67/L68) — skrivs INTE i förväg.

## Del 1 — Syfte och scope

### Syfte

Ta tråden T76 (parallella batch-pipelines i samma repo) från grillad
samsyn (kanonisk plats: tråd-kortet, 5 Marcus-kvitterade beslut) till
BEVISAD pilot: bygga orkestrerings-formen — EN orkestrator,
inline-skript kring oförändrad `/do-work`, worktree-isolering per
pipeline, staging-semafor, kortlivad branch + draft-PR + merge-commit
vid grönt, drain-halt — och köra piloten skarpt på partitionen
pipeline A = task-8-skivorna (8.1–8.3) ∥ pipeline B = task-9-skivorna
(9.1/9.3). Marcus kvalitetsram för sessionen: bästa tänkbara sätt i
linje med branschledande kunskap — effektivt, felfritt och säkert.
Web-research-disciplinen tillämpas på designen (T71/T76-researchens
återbruk + kompletterande verifiering av harness-mekaniken mot
faktiskt tillstånd).

### Kvitterat scope (ordnat)

1. **Förberedelse-läsningen:** ADR-071 i sin helhet (styrande ADR) +
   `/work-batch`-skillen + `/do-work`-skillen + de fem skivkorten
   (8.1/8.2/8.3/9.1/9.3) i sin helhet + harness-mekanikens
   worktree-stöd verifierat empiriskt + kompletterande web-research
   där designen kräver det.
2. **Pilot-designen med STOPPA-grind.** Öppna punkter in i designen:
   (a) pilot-formens kort-antal — T76-kortets äldre "2×1"-rad vs
   S64-handoffens partition 3∥2; (b) port-allokering per worktree +
   färsk-server-disciplinen (T76-checklistans "batch 4 FÖRE
   piloten"-rad är överspelad av S63-vägvalet — porten löses i
   orkestrerings-skriptet); (c) kollisionsytan 8.2↔9.1
   (design-system-specen) — sekvensering eller isär-partition;
   (d) staging-semaforens form; (e) drain-mekanikens abort-koppling.
   Design läggs fram med rationale → Marcus-kvittens FÖRE avfyrning.
3. **Allowlist-förkravet:** `gh pr create`/`gh pr merge` in i
   spoke-allowlisten som granskningsbar diff, Marcus-kvitterad.
4. **Piloten skarpt** på partitionen per kvitterad design:
   granskningsfärdig-läge för UI-skivor per ADR-071 (Done-flip =
   Marcus), tvåstegs-stängning per kort (K61.1/T75), halt-semantik =
   drain.
5. **Vid bevisad pilot (om sessionen räcker) — EN bevis-landning:**
   pilot-ADR:n (nästa lediga nummer; amenderar ADR-071 med
   parallell-formen + B-switchen färdigspecad + drain + semafor) ·
   `/work-batch` 1.14.0 (parallel-läge + B-flagga) · T71-radens öppna
   revidering · T46 go-live-kartans switch-post · parkerade termer →
   SYSTEMET.md §0 (T76-kortets sekvens punkt 3).

### Ej i scope

TASK-5/6 (delar `playwright.config.ts` → sekventiell batch 4 i
Marcus-takt, utanför piloten) · 8.4/8.5 + 9.2 (blockade; 8.4 plockas
INTE även om dess beroenden blir Done under batchen — explicit
partition gäller) · 9.4 + QA 4.6 (`ready-for-human`, Marcus-takt;
QA 9.4 efter pilot) · hub-lyftet L267–L272 (nästa hub-sync-moment) ·
T77 (notis-centret) · bas-maximeringen (T16) · Fas 6-closeout-poster.

### Numrering (disk-verifierad vid födelsen)

Nästa ADR **073** (`check-adr-count` 72==72, exit 0) · lesson **L273**
(sista = L272) · fälla **45** (sista = 44, data-model §Kända fällor) ·
tråd **T78** (T77 högst på disk). Allt matchar S64-slutsummeringen.
Session 64 `closed`; inget arkiveras (ej fas-avslut, ADR-023/041).

### Ingångstillstånd

`main` @ `e4bd9c5` (rent träd, synkat med origin, audit-ci PASSED, CI
grön per jobb på HEAD, run 29189261622). Backlog-läget (CLI-läst):
**TASK-8** + skivorna 8.1–8.5 `To Do` (8.1/8.2/8.3 `ready-for-agent` =
pipeline A) · **TASK-9** + skivorna 9.1–9.4 `To Do` (9.1/9.3
`ready-for-agent` oblockade = pipeline B; 9.2 ←9.1; 9.4
`ready-for-human`) · TASK-4.6 `ready-for-human` · TASK-5/6
`ready-for-agent` (batch 4-kandidater, UTANFÖR piloten). Enda pausade
sessionsdoket är S56 (dess KVAR övertogs öppet av S61 — bokfört fynd,
ingen åtgärd). Relevanta lärdomar in i sessionen: L266 (fynd skrivs
för nästa utförare — substratet bär kontinuiteten mellan agenter) ·
L270 (grinda på exit-kod) · L271/L264 (tidszons-disciplin) · L272
(computed-assertioner slår pixel-titt; stale dev-moduler) · L263
(tvåstegs-stängningen) · L265 (`gh run list` med headSha-match, aldrig
`--commit`-filtret).

## Del 2 — Pilot-designen kvitterad: A/A, max-kort 5 (2026-07-12)

> Marcus-kvittens i delegerad senior-form: "Du är senior, säger du A/A
> så kör vi A/A." på design-STOPPA:ns fyra frågor — form A (fasat
> schema 3∥2), allowlist-diffen, 8.1→Done (A), avfyrningsorder
> max-kort 5. Ordern är batch-kvittot (ADR-071 beslut 1). Detta Del
> är designens KANONISKA plats (chat-STOPPA:n är efemär).

### Empiriska verifieringar före designen (fakta, ej antaganden)

1. **Worktree-minimal-testet** (Workflow, 2 parallella agenter,
   read-only): distinkta worktrees under `.claude/worktrees/`, EGEN
   branch per agent (`worktree-wf_<id>-<n>`), båda på färsk main-HEAD
   (`5513c3d`), rent träd, `.git` som fil, backlog-CLI körbart från
   worktreen; auto-städning bekräftad efteråt. Luckor: `node_modules`
   saknas (npm ci krävs per worktree, ~500 MB, disk 404 GB ledigt) och
   gitignorade `.env.local`/`.env.test` följer inte med (kopieras
   lokalt vid setup — hemligheter når aldrig trailen).
2. **CI-mekaniken:** concurrency-gruppen är per PR-nummer
   (`github.event.number || github.sha`) → parallella PR:er krockar
   inte; `pull_request → main` triggar även drafts; **Test+Build kör
   `api-staging` + e2e-staging mot staging-miljön** → två samtidiga
   runs = TASK-6-kollisionen på CI-nivå (se förfining nedan). Main
   saknar branch protection → merge kräver inte up-to-date-branch.
3. **Playwright-portrisken:** `reuseExistingServer: !CI` → kvarglömd
   server på 5173 ger en annan worktrees tester FEL KOD
   (TASK-5-klassen) → ledig-port-pre-flight i semaforen.

### Designen (kvitterad form A)

- **Topologi:** ETT orkestrerings-uppdrag i Code-sessionen; max 2
  pipelines; frisk do-work-agent per kort i EGEN worktree
  (Workflow-substratet, samma väg som minimal-testet); central
  kort-UTPEKNING före spawn (aldrig auto-plock i pipelines).
- **Fasat schema (kollisionsytor hanteras deterministiskt av faser,
  inte konflikthantering i stunden):** fas 1 = **8.1 EXKLUSIVT**
  (timing-mätningens validitet: ingen samtidig last) → fas 2 =
  **8.3 ∥ 9.1** (disjunkta: persist/lockfil ↔ primitiv/demo/spec) →
  fas 3 = **8.2 ∥ 9.3** efter 9.1-merge (spec-/demo-kollisionen
  8.2↔9.1 upplöst — 8.2 appendar på mergad main; 8.2↔9.3 disjunkta).
  Ordinal-avvikelsen (A kör 8.1→8.3→8.2) öppet bokförd: ordinal är
  plock-prioritering; inom Marcus-partitionerad batch schemalägger
  orkestratorn för kollisionsfrihet; inga hårda beroenden bryts.
- **Pipeline-agentens form (pilot-delta mot do-work, öppet
  deklarerat):** kortet utpekat (inte plockat) · leverans-commit på
  kort-branch (`git switch -c task/<id>`) + push (INTE main) · steg 5b
  (stängningen) FLYTTAT till orkestratorn. I övrigt do-work oförändrad
  (steg 2–4, AC/DoD-bockar i leverans-commiten, schema-tvingad
  statusretur: ID, utfall, commits, defekter, fynd, TDD-bevis).
  Deltat kodifieras i pilot-ADR:n + skill 1.14.0 EFTER bevis.
- **Orkestratorns ansvar (seriellt per kort — förfining post-STOPPA,
  öppet bokförd):** `gh pr create --draft` → CI-vakt per jobb
  (headSha-match, L265) → vid rött: remedierings-agent i kortets
  worktree (max 1 varv, annars halt) → `gh pr merge --merge`
  (merge-commit, aldrig squash — SHA-bevisen) → main-CI-vakt →
  OBEROENDE disk-verifiering (kort-tillstånd via CLI, path-scope,
  AC/DoD) → stängnings-bokföring på main → worktree-städning.
  **Motiv för serialiseringen:** CI:s staging-steg (verifiering 2) gör
  samtidiga PR-runs till samma contention-klass som TASK-6 — hela
  PR→CI→merge→main-CI-kedjan är därför EN orkestrator-ägd kritisk
  sektion per kort; agenternas implementationsfaser (väggklockans
  dominant, 24–109 min/kort S61) förblir parallella. Flytten av
  PR-skapandet från agent till orkestrator ingår i förfiningen (inom
  kvitterad ram: samma semafor-princip på en yta som upptäcktes efter
  STOPPA:n).
- **Semafor + portskydd:** mkdir-baserat fillås i scratchpad (utanför
  repot) som agenterna tar runt ALLT som startar server eller rör
  staging (`test:api`, Playwright-sviter, 8.1:s mätning); pre-flight i
  låset: 5173 ledig. Löser TASK-5- och TASK-6-klasserna med EN
  mekanism utan att röra `playwright.config.ts`.
- **Drain-halt (samsynens beslut 5, kodat):** pipeline-fel → kortet
  To Do med avbrotts-not (do-work:s abort-väg); den friska pipelinen
  kör klart PÅGÅENDE kort, inget nytt startas; rapporten pekar ut
  halten. Hårda gränser: max-kort 5 (Marcus-satt i ordern) · aldrig
  samma kort ×2 · kill-switch · omstart idempotent via
  backlog-tillståndet.
- **Mål-tillstånd:** 8.2/8.3/9.1/9.3 → granskningsfärdiga (In
  Progress + öppen design-review-grind; Done-flipp = Marcus; 8.3 hör
  hit — persist ÄNDRAR upplevt laddläge, L269-klassen). **8.1 → Done
  direkt** (kvitterat A): read-only-mätskiva utan produktkodsdiff —
  design-review-raden "per skiva med UI-yta" EJ TILLÄMPLIG, bokförs
  öppet i final summary.
- **B-switchen:** ej i skarp Lotta-drift → v1-formen (merge vid grönt,
  granskning mot main). Switch-posten byggs i bevis-landningen.
- **Allowlist-diffen** (denna landning, egen commit): `gh pr
  create/merge/view/list` · `git worktree/switch/branch` · `npm ci` ·
  `npm install` LÅST till ADR-072:s två TanStack-paket
  (`react-query-persist-client`, `query-sync-storage-persister`) ·
  `npm run test:a11y` · `cp` låst till `.env.local`/`.env.test` från
  huvudrepots path · `lsof` · `mkdir`/`rmdir` (fillåset).
- **Rapport:** batch-rapport per /work-batch steg 4 + pilot-metrik
  (parallell väggklocka vs summerad korttid, semafor-väntetider,
  merge-konflikter [förväntat 0], worktree-setup-tid, first-pass-CI).

### Ärliga gränser in i piloten

Drain-vägen är obeprövad (ADR-071:s ärliga gräns står — den triggar
bara vid fel); semafor-kön kostar minuter (accepterat — se
motiv ovan); branschgrund = T71-researchen (S61-omverifierad,
Anthropic worktrees + Copilot/Codex isolering-plus-PR-serialisering)
plus dagens lokala minimal-test — lokal empiri slår dokumentation.

**NÄSTA: semafor-wrappern i scratchpad → fas 1 (8.1 exklusiv).**
