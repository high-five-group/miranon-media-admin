---
owner: marcus803
updated: 2026-08-05
review_by: 2026-11-05
status: stable
lifecycle: paused
---

# Session 96 — Work-batchen: heartbeat-mekaniseringen och T95:s första våg

> Fött **vid sessionsstart** (ADR-043 + L67/L68), efter LÄS→RAPPORTERA och
> Marcus kvittens av nummer + scope ("1. Ja 96 blir det på denna session.
> 2. Kvitterar scop.", 2026-08-02). Landnings-kadens: varje Del skrivs vid
> sin landning, aldrig i efterhand.
>
> **Parallell arkitektur:** S93 (appen) kör i eget terminalfönster och äger
> **huvudkatalogen** — Marcus kvitterade vid sessionsstarten att den är
> "fullt aktiv och levande". Denna session kör därför i **egen git-worktree**
> (`.claude/worktrees/s96-work-batch`, gren `docs/s96-fodelse`) hela vägen —
> detektera+fråga per ADR-090, kvitterat i sessionsstartens utbyte.
> S92 (färgsystemet) står pausad parallell.

## Del 1 — Syfte och scope

### Syfte

Exekvera S95:s kvitterade vägval: den mekanisering Marcus beordrade efter
`L443` först, därefter T95-spårets första byggvåg. Sessionen är en
**work-batch** — orkestreraren håller tråden och landnings-svepet, skivorna
byggs av bygg-agenter per ADR-089:s tier-policy.

### Ordnad scope

1. **`TASK-119` — trevägs-heartbeaten.** Marcus mekaniserings-order, kortet
   satt `priority: high` i S95:s stängning. Heartbeat-svepet är i dag
   handstartad konvention med envägs-historik; tre mätta felmoder samma kväll
   (envägs-nyckling blind för rött · blind för DIRTY · armering-är-inte-minne).
   Åtgärd: repo-skript med trevägs-snapshot (main-SHA · röda check-rollups ·
   DIRTY-mängd), konfigdrivet per `.conf`-konventionen, `CLAUDE.md`
   § Landning pekar på skriptet som stående form. **Går först** — den vaktar
   resten av batchens landningar.
2. **T95-batchen, sju kort i tre startkedjor.** `TASK-126.1` + `TASK-126.2` +
   `TASK-127.1` parallellt → därefter `TASK-126.3` · `TASK-126.4` ·
   `TASK-127.4` · `TASK-127.5`. Samtliga `ready-for-agent`.
3. **`TASK-127.2` — prototyp-passet (login + accept, tvåfas).**
   `ready-for-human`, Spår B:s grind. Körs bredvid batchen i Marcus takt, inte
   i den.
4. **Grind 0-panelen** (Marcus-moment, inte agent-arbete): Vercel-konto +
   DNS-trion när som helst; SMTP/OTP **efter** `TASK-127.4`
   (panel-checklistan).

### Ej i scope

- **Appen** (S93, aktiv parallell — äger huvudkatalogen).
- **Färgsystemet** (S92, pausad parallell).
- **Bas-maximeringen/AT-Max** (post-Fas-6-milstolpe, ADR-063).
- **`task-120`** (go-live-klassen) — väntar Marcus GO.
- **Hub-lyftet `L433`–`L443`** — tas vid hub-sync-moment.

### Numrering vid födelse (disk-verifierad)

Session **96** (sista dok `session-95`) · nästa ADR **092** (tre källor
eniga: 91 filer == 91 katalograder == sista fil `ADR-091`) · nästa lesson
**L444** (sista `L443`) · nästa tråd **T114** (sista `T113`) · nästa kort
**task-128** (sista heltal `TASK-127`) · nästa fälla **f47** (hypotes — ej
mekaniskt avläsbar ur `data-model.md`). Kollisionsregim mot parallella S93:
båda sessionerna re-deriverar mot disk före varje minting och committar
direkt (T67 guardrail 1 + ADR-090 + `check_active_branches`-flaggans tre
kända hål, `CLAUDE.md` § Kortnummer).

### Ingångstillstånd

- `main` på `1b2e24c5` == `origin/main` (medvetet `merge --ff-only` från
  `b269b2d9` efter `fetch --prune` — S95-stängningen inhämtad, aldrig blind
  `git pull`), rent träd.
- CI grön på `main` (`1b2e24c5`, tre runs `success`); **noll öppna PR:er**;
  `audit-ci` grön (exit 0).
- Fas-läge: **Fas 6 🟡 PÅGÅR**. Fas 7:s deploy-bit är öppet framdragen på
  Marcus beslut (T95 Grind 0, ADR-091 Vercel Pro) — avvikelsen är bokförd i
  byggplanens fas-tabell, inte tyst.
- **FYND vid start, registrerat ej åtgärdat:** worktree- och gren-hygienen har
  vuxit tillbaka — 11 worktrees varav 2 `prunable`, 68 lokala grenar varav 55
  med `origin: gone` (S91 rapporterade 55→51 efter städning).
- Väntande Marcus-moment utanför scope: Update-klicket i claude.ai ·
  `task-120`-GO.

## Del 2 — Nattens batch-order och den bokförda halt-first-avvikelsen

> Skriven vid sin landning: Marcus order gavs 2026-08-02 kväll inför AFK-natt,
> och avvikelsen bokförs FÖRE exekvering — inte i efterhand.

### Ordern

Marcus, verbatim: *"Jag vill att om vi får stopp så ska agenten rapportera
till dig så får du lösa det genom att kanske skicka ut research-agenter eller
liknanade, minta trådar eller nya fynd-kort liksom, sen går vi vidare med det
som går att gå vidare med. Ditt omdöme kan vi lita på. … Jag vill att vi kör
klart så mycket vi kan köra klart."* Bekräftad med *"Då kör vi så!"* efter
att smalningen nedan lagts fram.

### Vad avvikelsen faktiskt är

`/work-batch`-skillens kontrakt lyder under rubriken *"hårda externa gränser
— aldrig agent-omdöme"*: **"Halt-first. Batchen stannar vid FÖRSTA
STOPPA-/abort-utfall … Ingen skip-and-continue, ingen stopp-klassning via
agent-omdöme."**

Halt-first lägger en filt över samtliga fyra rutor i ADR-053:s alltid-på
triage. Ordern lyfter filten och låter ADR-053 gälla också inuti batchen:

| ADR-053-ruta | Nattens hantering |
|---|---|
| Blockerar + i scope | hantera nu — enabling-detour, egen landning |
| **Blockerar + utanför scope** | **oförändrad: STOPPA, eskalera till Marcus** |
| Blockerar ej + värdefullt | tråd-registret / fynd-kort, vidare |
| Blockerar ej + lågvärde | förkasta explicit, aldrig tyst |

**Smalningen (Code:s, Marcus-kvitterad):** rutan *blockerar + utanför scope*
undantas från undantaget. Arkitektur- och scope-beslut stannar oavsett —
inte av lydnad mot kontraktet, utan för att fångst-raterna i konstitutionen
(self-review ~9 % · transparens-rapport ~64 % · Marcus-pushback ~27 %) gör
just den klassen till den svagaste. Formen blir: **parkera den KEDJAN, inte
batchen**, skriv upp beslutsunderlaget (alternativ · avvägning ·
rekommendation) för morgonens avgörande, låt övriga kedjor löpa.

### Fordonsbytet — och varför

Undantaget gör `/work-batch` till fel fordon: att fyra skillen och sedan
bryta just den rad dess kontrakt kallar hård extern gräns vore oärligt mot
kontraktet. Nattens körning sker därför som **direkta bygg-agenter per kort**
under oförändrad DoD, oförändrade grindar och oförändrad tvåstegs-stängning.
Avvikelsen bokförs i varje uppdragstext, aldrig tyst.

Bieffekt som löser en mekanisk knut: skillen bär
`disable-model-invocation: true` och kan inte fyras av Code. Den direkta
formen kräver inget skill-kommando från Marcus.

**Ingen ADR mintas för detta.** En engångsavvikelse klarar inte ADR-baren
(svår att återställa · överraskande utan kontext · verklig avvägning).
Bär den frukt tas den som ADR senare, med nattens data som underlag.

### Nattens plan

Sju kort är taket, och det är grafen som sätter det — inte tiden:

- **Startbara direkt:** `TASK-126.1` · `TASK-126.2` · `TASK-127.1`
- **Avblockeras löpande:** `126.2`→`126.3` · `126.1`→`126.4` ·
  `127.1`→`127.4` + `127.5`
- **Rörs INTE:** `127.3` · `127.6` (deps bär `TASK-127.2`, prototyp-passet =
  HITL) och därmed transitivt `127.7` · `127.8` · `127.9`. Båda korten säger
  ordagrant *"formen följer prototyp-facit"* — bygge före facit vore
  kastarbete.
- **HITL som väntar Marcus:** `126.5` · `127.10` (QA) · `127.2` (prototyp) ·
  Grind 0-panelen (Vercel-konto · DNS-trion · SMTP/OTP efter `127.4`).
- Kort vars DoD bär mänsklig grind landar i **granskningsfärdig-läge**:
  kod klar, CI grön per jobb, AC bockade, `In Progress` med not —
  Done-flippen är Marcus.

`TASK-119` (heartbeat-mekaniseringen) går först och ensam: den bygger det
svep batchens landningar lutar sig mot, och att köra den inuti batchen den
ska vakta vore fel ordning.

## Del 3 — Nattens bokföring

> Skriven under natten, inte i efterhand. `TASK-126.2` var fortfarande ute hos
> sin agent när denna Del landade; dess utfall amenderas in vid sin landning.

### Taket var tre kort, inte sju — och skälet är spec, inte tid

Orkestreraren rapporterade vid batch-planeringen ett tak om **sju** kort. Det
var **fel**, och felet upptäcktes först när `TASK-127.1` skulle stängas.

Samtliga skivor i BÅDA T95-spåren bär identiska DoD-grindar som bara Marcus
kan bocka: `126`-spåret `#5` (enhetsverifikat iPad/Mac-Safari/Chromium efter
Grind 0) och `#6` (Gunilla-principen); `127`-spåret `#5` (prototyp-godkännande),
`#6` (rundturs-e2e mot staging) och `#7` (go-live-grinden). `/to-issues`
stämplade PRD:ns spår-grindar på varje barn.

Följden: ingen skiva kan flippas `Done` av en agent, och do-work:s
plockbarhets-filter (`SAMTLIGA dependencies Done`) gör därmed nedströmskorten
oplockbara. **Detta är inte en defekt** — det är work-batch-skillens uttalade
mekanik: *"Beroende-kedjor körs därmed i granskningsvågor — en design-miss
propagerar aldrig förbi sin våg."* Nattens våg är därför de tre föräldrarna;
`126.3` · `126.4` · `127.4` · `127.5` väntar på Marcus grindar.

### Landat — tolv PR:er, samtliga merge_group-verifierade

| PR | Innehåll | Commit → merge |
|---|---|---|
| #612 | S96-dokets födelse | `eb49ee1f` → `6ccfeef7` |
| #615 | Del 2 — batch-ordern + halt-first-avvikelsen | `d5aed8d1` → `0a5984aa` |
| #614 | **`TASK-119`** heartbeat-mekaniseringen | `89fa7587` → `1b79220f` |
| #617 | `TASK-119`-stängningen (**Done**) | `5a4fb625` → `9cbddab0` |
| #618 | `TASK-128` fynd | `01be8cbd` → `e44fca46` |
| #620 | **`TASK-127.1`** — `ADR-092` + `ADR-093` + SECURITY-SPEC-rivningen | `6fbb6290` → `c39ff5d3` |
| #622 | `TASK-129` fynd | `ece81d94` → `2cbf4052` |
| #621 | **`TASK-126.1`** manifest-kompletteringen | `531ebe75` → `b27d6c52` |
| #623 | `127.1` granskningsfärdig | `53f584ce` → `ef7dc65c` |
| #624 | `TASK-130` fynd | `a6839db9` → `9e018c0d` |
| #625 | `T114` trådregistrering | `32fb591c` → `3c06fc9c` |
| #626 | `126.1` granskningsfärdig + `128`:s evidens | `3f0bb82a` → `01d94b8a` |

Post-merge grön på samtliga batch-slut-SHA:n. Noll röda larm-ärenden efter
`#619`.

### Kortens läge

- **`TASK-119` — Done.** Enda kort som flippades i natt; det saknar
  människo-grind i sin DoD.
- **`TASK-126.1` · `TASK-127.1` — `In Progress`, granskningsfärdiga.** Kod
  landad, CI grön per jobb, AC bockade, människo-grindarna öppna.
- **`TASK-126.2`** — ute hos agent vid denna Dels landning.

### Fyra fynd registrerade — samtliga etikettlösa

Etikettlösheten är avsiktlig: oplockbara tills Marcus klassar dem.

- **`TASK-128`** — heartbeat-svepets kandidat-larm fyrar på KÖADE PR:er.
  Mätt **sju** gånger under mekanismens första natt (rättat 2026-08-03 —
  raden sade "åtta"; kortet räknar upp instanserna `#614`, `#617`×3, `#621`,
  `#623`, `#624` = sju, och den räknade siffran slår den obelagda).
  `isInMergeQueue` finns
  i samma GraphQL-query och hade tystat varje falsklarm; skriptets egen
  A3b-rubrik nämner fältet utan att använda det. **Operativ kostnad:**
  bruset tvingade fram en höjning av svep-intervallet 90 → 300 s.
- **`TASK-129`** — `ADR-091`:s CSP-rivning pekar på rad 120–134, men den
  falsifierade nonce-designen bär 27 förekomster över fem sektioner,
  inklusive Fas 7-checklistan som är själva instrumentet rivningen skjuter
  arbetet till. Plus en stale `admin.miranon.se` utanför deferralens scope.
- **`TASK-130`** — PRD `task-126` och `TASK-126.4` AC#3 antar att
  preview-skarven är en CI-grind. Den anropas **aldrig** av CI.
- **`T114`** (tråd, rad) — orkestrerarens landningsvakt är blind för
  post-merge-rött på `main`.

### Avvikelser att kvittera

1. **Halt-first-undantaget** — Marcus order, bokförd i Del 2. Rutan
   *blockerar + utanför scope* undantogs från undantaget; den rutan
   utlöstes aldrig i natt.
2. **Sekvens-avvikelse, orkestrerarens egen.** Del 2 sa att batchen startar
   när `TASK-119` är **landad**. De tre startkorten avfyrades när den var
   **armerad och köad**. Ingen av dem berodde på den. Bokförs, inte
   bortförklaras.
3. **Hemvist-divergens `TASK-126.1` AC#3** — grinden lagd i `ci-suite.yml`
   Pure+Build i stället för den namngivna preview-skarven, godkänd av
   orkestreraren som avsikt-över-bokstav. **Marcus bör kvittera den
   bedömningen**; den är gränsfallet mellan tolkning och scope-beslut.
4. **Två röda post-merge passerade obemärkta** (`9aef0fef` = S93:s #613,
   `1b79220f` = S96:s #614), båda på `event-detail.staging.test.ts:1069`,
   båda transienta. Repots larm fungerade; orkestrerarens vakt läste fel
   signalklass. Registrerat som `T114`.

### Väntar på Marcus

Prototyp-passet `TASK-127.2` · QA-korten `126.5` + `127.10` · Done-flipparna
på `126.1`/`127.1` · Grind 0-panelen (Vercel-konto · DNS-trion · SMTP/OTP
efter `127.4`) · klassning av de fyra fynden · kvittens av avvikelse 3.

## Del 4 — `TASK-126.2` stoppade, och det var rätt av den att göra det

> Skriven vid stoppet. Amenderar Del 3:s öppna post.

### Utfallet

`TASK-126.2` (InstallPrompt) **byggdes klart och landade inte.** PR
[#628](https://github.com/high-five-group/miranon-media-admin/pull/628)
står öppen, röd och armerad; gren
`task-126.2-install-prompt-bibliotekskomponent`, commit `5b28b6ca`.

Bygget i sig håller. Agenten levererade 11 acceptance-tester + 5 a11y-tester,
dokumenterade fyra röd→grön-rundor, **fångade ett eget falskt-positivt test**
genom att riva ut sin fix och observera att testet ändå blev grönt, och lät ett
oberoende review-pass hitta en äkta cross-instance-bugg (två instanser delade
en förbrukad engångshändelse) som fixades och testades.

### Blockeraren — en meta-grind, inte ett fallerat påstående

`Test suite / Acceptance (hermetisk)` föll efter 7m12s. **164 tester
passerade.** Det som fällde är `scripts/hermetik-sjalvtest.mjs`, som flaggar
alla 11 nya tester:

> *"Testet överlever utan fixturens svar och bevisar därför inget om appens
> databeteende."*

Acceptance-klassen är **definierad** av fixtur-beroende — vakten är VILLKOR
för klassens existens, inte ett tillägg ([ADR-080](../../docs/decisions/ADR-080-acceptance-klassen-hermetisk-utbrytning.md)
beslut 3). `InstallPrompt` läser `navigator.userAgent`, `matchMedia` och
`beforeinstallprompt`; den har inget databeteende. Att dess tester överlever
utan fixturen är **korrekt av naturen**. Vakten gör rätt — placeringen är fel.

Och placeringen kom ur specen: PRD `task-126` § Testbeslut säger ordagrant
*"Install-ytans externa beteende … testas i acceptance-skarven — hermetiskt,
utan staging."* Agenten följde den exakt.

### Varför orkestreraren inte löste det

Ingen undantagsväg finns: vakten bär ingen scope-config, ingen exclusion-lista,
och `.hermetik/` är tom. Skriptets eget felmeddelande gör resolutionen binär —
testet hör inte hemma i klassen, eller skrivs om så det konsumerar fixturens
svar.

Båda utgångarna är **kontraktsbeslut**: den ena flyttar en testklass-gräns,
den andra urholkar en vakt `ADR-080` gjorde konstitutiv. Klassat som
**blockerande + utanför scope** — nattens enda instans av den ruta som
undantogs från Marcus halt-first-undantag (Del 2). Kedjan parkerad, batchen
opåverkad, beslutet skrivet upp i **`TASK-131`** med tre alternativ och
orkestrerarens rekommendation (A — egen klass; vakten är konstitutiv by
design och B träffar exakt det `ADR-080` skyddar).

### Nattens verkliga facit

Två skivor landade granskningsfärdiga (`126.1`, `127.1`), ett fyndkort kördes
till Done (`TASK-119`), en skiva stoppade på ett spec-konflikt (`126.2`).
**Fem** fynd registrerade etikettlöst: `TASK-128` · `129` · `130` · `131` +
tråd `T114`.

Stoppet är inte nattens misslyckande. Det är den mekanism som fungerade: en
vakt fällde något som såg grönt ut lokalt, och gränsen mellan tolkning och
beslut hölls.

## Del 5 — Morgonens genomgång: en rättelse, en klassning, ett beslut, en deadlock

> Skriven vid sin landning (PR #637 + #638), 2026-08-03.

### Rättelsen — nattens transient-bedömning var falsk

`T114`:s rad påstod att de två röda post-merge-körningarna var *"transienta"*.
Falskt. Slutsatsen drogs ur att post-merge på `9cbddab0`, `e44fca46` och
`c39ff5d3` var gröna — men de körningarna **instansierade aldrig** jobbet
`Staging (API + E2E)`: mina landningar var docs-only, och klassningen skippar
staging för den filklassen. **En slutsats drogs ur körningar som aldrig körde
testet** — `T110` klass A, mätning med ett instrument som ser en form men inte
alla.

Faktiskt förlopp, verifierat per körning:

| Körning | Utfall |
|---|---|
| Nattnätet `0a1d13b1`, 08-02 04:28Z | grönt (sista före regressionen) |
| `9aef0fef` (S93:s #613) | rött → ärende #616 |
| `1b79220f` (S96:s #614) | rött → ärende #619 |
| Nattnätet `6251d95e`, 08-03 04:35Z | rött → ärende #636 |
| **`ca350397` (S93:s #639), 08-03 10:48Z** | **grönt — löst** |

Överlämnades till S93 som hypotes, inte slutsats. **S93 fixade den samma
förmiddag** (`06a387ac` *"fix(event-detail): återställ K6 null-döljning"*).
Regressionsfönstret `0a1d13b1..9aef0fef` innehöll deras hållplats-våg, vilket
hypotesen pekade på — men orsaken fastställdes av dem, inte av denna session.

### Klassningen av de fem fynden (Marcus delegation)

Skälet står nedskrivet på varje kort, så klassningen går att granska:

| Kort | Klass | Skäl |
|---|---|---|
| `TASK-128` | `ready-for-agent` · medium | fixen empiriskt verifierad, inget beslut kvar |
| `TASK-129` | `ready-for-human` · medium | ADR-091:s räckvidd är ett ADR-beslut |
| `TASK-130` | `ready-for-human` · medium | AC-/PRD-ändring är spec |
| `TASK-131` | → `ready-for-agent` · high | efter beslut A; se nedan |
| `TASK-132` | `ready-for-human` · high | DoD-text är spec |

### Beslutet — `TASK-131` alternativ A

Marcus valde **A**: datalösa webbläsartester får en egen, explicit namngiven
klass. `scripts/hermetik-sjalvtest.mjs` och acceptance-kontraktet lämnas
**orörda** — `ADR-080` byggde vakten som villkor, och ett undantag i vakten är
ett undantag i klassens definition. B och C öppet förkastade. Kortet
omklassat till `ready-for-agent` med sex specificerade arbetspunkter;
**arbetet utförs i nästa resume**.

### Deadlocken — `TASK-132`

Verifierad mot kortfilerna: `127.1` Done kräver `127.9`:s rundturs-e2e
(DoD-punkt 6) → `127.9` kräver `127.5` → `127.5` kräver `127.1` Done. `/to-issues`
stämplade PRD:ns spår-grindar på varje barn, och i detta spår sluts en cirkel.
Prototyp-passet `127.2` löser en **annan** blockering och bryter inte denna.
126-spåret är inte cirkulärt men bundet till Grind 0 via `126.1`:s DoD #5.

Rättningsförslag i tre punkter finns i kortet; DoD-text är spec, så valet är
Marcus. Andra ordningens fråga noterad: rättas bara korten återkommer mönstret
vid nästa spår — rotorsaken sitter i `/to-issues`.

## Paushistorik — Session 96 (pausad 2026-08-03, återupptagen 2026-08-03)

### TILLSTÅND

- `main` på **`d6f3ae84`**, rent träd. Denna session arbetar i egen worktree
  `.claude/worktrees/s96-work-batch` — **S93 äger huvudkatalogen och är
  fortfarande `active`** (ADR-090).
- **Staging-regressionen är LÖST** av S93 (`ca350397`, post-merge success
  08-03 10:48Z). **Men larm-ärendena `#616`, `#619` och `#636` står kvar
  ÖPPNA** — de stängs enligt CONTRIBUTING § Nattnätet endast med
  rotorsaks-fix eller öppet skriven motivering. Fixen finns nu; stängningen
  hör till S93 som äger ytan.
- **`#628` öppen, RÖD + DIRTY**, armerad. Bär `TASK-126.2`:s färdiga
  bibliotekskomponent (gren `task-126.2-install-prompt-bibliotekskomponent`,
  commit `5b28b6ca`). Vägen framåt är specificerad i `TASK-131`.
- **Fyra dependabot-PR:er öppna och OARMERADE:** `#632` `#633` `#634` `#635`.
  Denna session rörde dem inte.
- Worktree-städning utförd vid pausen: 3 agent-träd bort, 6 grenar frigjorda
  (93 → 87 lokala grenar). 12 worktrees kvar, samtliga utanför
  harness-scope eller med olandat arbete.

### CARRY / ÖPPNA TRÅDAR

- `TASK-132` (high) — deadlocken. **Hävstången: låser upp fyra kort.**
- `TASK-131` (high) — beslut A taget, arbetet kvar.
- `TASK-129` · `TASK-130` (medium) — väntar Marcus-beslut.
- `TASK-128` (medium) — plockbar av agent.
- `T114` — vaktluckan, rättad rad; åtgärdsriktningen ej beslutad.
- **Lesson-KANDIDAT (ej hub-lyft):** `tasks/lessons.d/bokforing-pa-kort-med-olandad-pr.md`
  — nummerlöst fragment per ADR-081, kandidat för `[UNIVERSAL]`.
- **Lesson-KANDIDAT (ej nedskriven):** slutsats dragen ur körningar som
  aldrig körde testet — se Del 5 § Rättelsen. Nära `T110` klass A.

### MARCUS-SEKVENS (beslutad väg framåt)

1. `TASK-132` — välj var varje grind hör hemma. Låser upp `127.4`, `127.5`,
   `126.4` via Done-flippar på `127.1` och `126.1`.
2. `TASK-131` — den nya testklassen byggs; `#628` löses ut.
3. **Grind 0** (Vercel-konto + DNS-trion) — Marcus-moment, oberoende av 1–2,
   ligger på kritiska linjen för hela "riktig app"-målet.
4. `TASK-127.2` — prototyp-passet. Full effekt först efter punkt 1.

### NUMRERING ATT RE-VERIFIERA VID RESUME

Disk-verifierat 2026-08-03 på `d6f3ae84`: nästa ADR **094** (93 filer == 93
README-rader == sista `ADR-093`) · nästa lesson **L444** (sista `L443`, plus
**1 nummerlöst fragment** i `tasks/lessons.d/`) · nästa tråd **T115** (sista
`T114`) · nästa kort **task-133** (sista `132`) · nästa fälla **f47**
(hypotes). **S93 kör parallellt och kan förbruka nummer** — re-derivera mot
disk före varje minting, och committa direkt.

### NÄSTA KONKRETA STEG

Återuppta med `session-resume` (S96 behålls). Första handling: svep
landningsläget, kontrollera om S93 stängt `#616`/`#619`/`#636`, och ta
`TASK-132` — det är kortet som får spåret att röra sig igen.

## Del 6 — Resumen: deadlocken bruten, och rotorsaken var inte den vi trodde

> Skriven vid sin landning, 2026-08-03. Sessionen återupptagen efter Marcus
> kvittens av worktree-valet + väntan på paus-landningen; `lifecycle` åter
> `active`, paus-rubriken omvandlad till historik-form.

### Ingångsläget vid resume — tre divergenser mot handoffen

| Handoffen sa | Faktiskt läge, verifierat |
|---|---|
| `#616`/`#619`/`#636` ev. stängda av S93 | **alla tre ÖPPNA** (plus äldre `#469`, `#464`) |
| `main` på `d6f3ae84` | paus-landningen **PR #641 stod ännu i kön**; landade `11:15:26Z` som `d761a7a1` under resumens LÄS-fas |
| heartbeat-svepet är stående form | **inget svep kört** |

Numreringen re-verifierades mot disk och höll exakt (ADR **094** · lesson
**L444** + ett nummerlöst fragment · tråd **T115** · kort **task-133**):
S93:s två commits i fönstret rörde endast deras eget sessionsdok.

### `TASK-132` — rättningen utförd, med en förenkling och en skärpning

**Förenklingen:** kortets punkt 2 sa att spår-grindarna skulle FLYTTAS till
PRD-korten. De låg redan där — `task-126` bar `#5`+`#6`, `task-127` bar
`#5`+`#6`+`#7`, identiska med barnens. Ingreppet blev en ren **borttagning
från barnen**; PRD-korten är orörda.

**Skärpningen:** `#5` togs bort även från `127.3`/`127.6`, trots att de är
"login- och accept-skivorna" grinden namnger. `dependencies` bär redan
kopplingen, och deps är det `do-work` faktiskt läser. Att behålla en grind av
exakt den klass som orsakade deadlocken, som redundans, vore att spara felet
i miniatyr.

**Behållna skiv-grindar, fyra:** Gunilla-principen på `126.3` · enhetsverifikatet
på `126.5` · skarp-inbjudan-spärren på `127.10`. Övriga elva skivor bär nu
enbart de fyra universella.

**Upplåst:** `127.1` och `126.1` hade `#1`–`#4` bockade och blev Done-bara —
båda flippade. Det gör `127.4`, `127.5` och `126.4` plockbara.
**Kortets egen räkning var fel:** det sa "låser upp fyra kort"; faktiskt utfall
är **tre**. Differensen är `126.3`, som hänger på `TASK-131`:s klassfråga.

### Rotorsaken — kortets formulering var för bred, och rättades

Kortet skrev att `/to-issues` stämplade spår-grindar på barnen. Sant, men inte
felet. **Mätt över hela `backlog/`:** `task-1`, `4`, `8`, `9`, `17`, `18`,
`19`, `36`, `54` och `59` bär ALLA identiska extra-DoD-poster på samtliga barn
— tio familjer, noll problem. Stämplingen är designat beteende.

Det som skiljer T95 är grindarnas **grammatik**. Tidigare spår-grindar är
predikat över skivans EGET arbete: *"per skiva med UI-yta"* · *"varje BERÖRD
facit-punkt"* · *"varje FLYTTAD fil"* · *"körnings-ID:n citerade PÅ KORTET"*.
En skiva utan UI-yta uppfyller design-review-grinden vakuöst. T95:s grindar
refererar i stället **en systerskivas leverabel** (`#6` ÄR `127.9` — den enda
äkta cykeln) eller **en händelse utanför repot** (`efter Grind 0`,
`före DMARC-posten satt`), som per konstruktion aldrig kan uppfyllas av kod.

Verklig rotorsak: **T95 är första deploy-bundna spåret, och första där
e2e-grönt både är spår-grind OCH egen skiva.** Skillen skiljer inte klasserna.
Registrerat som **`T115`** med åtgärdsriktningen — en regel om vad en spår-grind
får REFERERA, inte "sluta stämpla" (det river tio familjers granskningsvågor).
Hub-ändring, tas vid hub-sync-moment.

### Del 6-amendering — `TASK-128` landad, och agenten fångade orkestrerarens fel

`TASK-128` byggd och pushad som PR #645 (`2d6bad0e`): `isInMergeQueue` tillagd
i svepets GraphQL-query som sjätte TSV-kolumn, kandidat-villkoret kräver nu
`inqueue==false`, testsviten 22 → 24 fall (27 assertioner). Tvåsidigt bevis
levererat: `T9b`/`T9c` röda mot orört skript, gröna efter fixen.
`CLAUDE.md` § Landning fick en not om att automatiserad klassning ska fråga
`isInMergeQueue` — med den kvarstående ambiguiteten (`false` betyder
fortfarande ANTINGEN aldrig-armerad ELLER utsparkad) ärligt bevarad.

**Orkestrerarens fel, fångat av agenten:** uppdragstexten påstod *"åtta mätta
instanser"* och källmärkte talet till kortets beskrivning. Kortet säger **sju**
och räknar upp dem. Talet kom i själva verket från Del 3 ovan, som bar samma
fel — nu rättat på plats. Agenten byggde på kortets siffra, inte uppdragets,
per ADR-086:s regel att källan slår uppdragstexten.

Klassen är `T110` B — en referens som skickades vidare utan att ha lästs på den
källa den tillskrevs. Fångad av mottagarens premiss-pass, vilket är exakt vad
ADR-086 finns för. Noterat som ytterligare en instans i den empiri `T110`
samlar; ingen ny tråd mintas för det.

**Agentens egen registrering, ej förkastad tyst:** `scripts/test-heartbeat-svep.sh`
anropas inte av CI (noll träffar i `.github/workflows/`). Det är hur `TASK-119`
avsiktligt lämnade den, och agenten förkastade posten explicit i stället för att
bygga om den — rätt bedömning, samma klass som `TASK-130`:s preview-skarv men
med motsatt rätt svar eftersom ingen AC påstår att den ÄR en grind.

## Del 7 — T95-vågen landad och prototyp-konvergensen inledd

> Skriven vid paus-landningen, 2026-08-03 kväll.

### Landat denna resume — fjorton PR:er

`TASK-132` (deadlocken) · `TASK-128` (heartbeat-larmets falskpositiv) ·
`TASK-131` (webbläsarbeteende-klassen + **`ADR-094`**, `#628` stängd) ·
`TASK-127.4` (auth-mallarna) · `TASK-127.5` (invite-EF:en) · `TASK-126.4`
(skärmbilderna) · `TASK-130`-beslutet · `ADR-092`-amenderingen ·
`ADR-091`-domännoten · scroll-byggkravet · tre prototyp-varianter + skarven ·
lessons-fragmentet.

### Prototyp-passet: divergens klar, konvergens pågår

Tre varianter byggda av tre oberoende agenter med var sin tilldelad
designhållning. **Marcus valde B för båda skärmarna.** Konvergensen har gått
tre omgångar; den sista gjordes **direkt i prototypkoden** efter att Marcus
underkände tempot: *"Vad är det som tar sådan tid???? Så här kan vi inte hålla
på vid iteration!!!"*

Han hade rätt, och skälet var inte CI — dev-servern är lokal med HMR under en
sekund. Overheaden var **agent-spawnarna**: 10–30 minuter per varv för tio
sekunders arbete. Delegering passar självständiga uppdrag; konvergens lever på
snabba varv. Formen bytt, inte för att agenten misslyckades — den stoppade
agenten hade byggt rätt sak, bara inte hunnit verifiera.

### Fyra fel jag gjorde, bokförda

1. **Två källmärkningsfel i uppdragstexter** — "åtta mätta instanser" (kortet
   sade sju) och "DMARC per ADR-091" (den ADR:n har noll DMARC-träffar). Båda
   fångade av agenternas premiss-pass. `T110` klass B, två instanser.
2. **`autoMergeRequest: null` lästes som "ej armerad"** utan disambiguering —
   exakt det fel `TASK-128` just fixade i skriptet.
3. **Delade ytor bara för KORTFILER, inte kodfiler.** Två parallella agenter i
   samma spår rörde `supabase/config.toml` respektive `playwright.config.ts` →
   två merge-konflikter, båda lösta för hand. Upprepades efter första gången.
4. **Överskrev en kortnot med bokstaven `x`** via ett slarvigt kedjat
   kommando. Upptäckt vid verifiering, återställd.

### `check-staging-preflight-wiring` fällde två agenter

Vakten kräver att varje Playwright-projekt klassas i sin policy. `TASK-131`
och `TASK-126.4` la var sitt nytt projekt och fälldes båda. Inte agenternas
miss — kravet står i en policyfil ingen kort-agent naturligt läser, och
utlöses av en helt vanlig handling.

## Paushistorik — Session 96, paus 2 (pausad 2026-08-03 kväll, återupptagen 2026-08-03 kväll)

### TILLSTÅND

- `main` på **`47916946`**, rent träd. Sessionen arbetar i egen worktree
  `.claude/worktrees/s96-work-batch`; S93 äger huvudkatalogen (verifierat rent).
- **`#670` öppen och armerad** — konvergens-omgången i prototypkoden. Landar
  via kön.
- **Fyra dependabot-PR:er öppna och OARMERADE** (`#632`–`#635`). Marcus beslut
  2026-08-03: de rör inte scopet (verifierat — axios/morgan har noll importer,
  web-vitals rör en fil, `audit-ci` exit 0). Tas efter inbjudan.
- **Larm-ärendena `#616`/`#619`/`#636` står kvar ÖPPNA** — S93:s yta.
- **Worktree-städning utförd:** 23 → 10 worktrees, 14 → 3 agent-träd (de tre
  kvarvarande tillhör andra sessioner). Städskriptet bekräftar 0 kvar i scope.
- **Dev-servern på port 5174** kör prototypen ur denna worktree. 5173 hålls av
  en annan sessions agent-worktree med `--strictPort`.

### CARRY / ÖPPNA TRÅDAR

- **Prototyp-konvergensen är MITT I ETT VARV.** Åtta av nio ändringar inne.
  Kvar: texten *"Frågor? Marcus står till ditt förfogande 24/7"* har ingen
  hemvist — den skulle ersätta en login-fotnot som togs bort med resten av
  login-texten. **Väntar Marcus besked om var den ska bo.**
- **Scroll-kravet overifierat** på mobil och accept-sidan. Bokfört som
  byggkrav på `TASK-127.3`/`127.6` (`#668`).
- **Bildens beskärning** — login centrerar fotot i sitt naturliga format, vilket
  ger tomma fält över och under. Marcus har inte sagt om det duger.
- `TASK-129` (ADR-091:s CSP-räckvidd) — enda fyndet ej delegerat; en ADR:s
  räckvidd är arkitektur.
- **Oflippade kort trots landad kod:** `126.4` och `127.4` står `To Do` med
  bockade AC; `127.5` står `In Progress`. Stängnings-commits saknas.
- Två lesson-kandidater i `tasks/lessons.d/` (nummerlösa per ADR-081).
- `T115` registrerad (`/to-issues`-grammatiken) — hub-ändring, ej utförd.

### MARCUS-SEKVENS (beslutad väg framåt)

1. **Konvergensen färdigt** — fotnots-texten, sedan scroll-verifiering.
2. **Grind 0** — Vercel Pro + DNS. Kan köras PARALLELLT, se nedan.
3. Agent-vågen på de plockbara korten.

### VIKTIGT INFÖR NÄSTA RESUME — Grind 0 blockerar mindre än antaget

Marcus antog att ordningen är konvergens → Grind 0 → agenter. **Verifierat mot
korten: agenter kan bygga NU, parallellt med Grind 0.**

- **Plockbart utan Grind 0:** `TASK-126.2` (InstallPrompt — testklassen finns
  sedan `TASK-131` landade), därefter `126.3`.
- **Väntar på PROTOTYPEN, inte Grind 0:** `127.3` + `127.6` → sedan `127.7`,
  `127.8`, `127.9`.
- **Grind 0 blockerar ENDAST:** `126.5` och `127.10` (QA på riktiga
  enheter/skarpt) samt go-live.

### NUMRERING ATT RE-VERIFIERA VID RESUME

Disk-verifierat 2026-08-03 på `47916946`: nästa ADR **095** (94 filer, sista
`ADR-094`) · nästa lesson **L444** (sista `L443`, plus **2 nummerlösa
fragment**) · nästa tråd **T116** (sista `T115`) · nästa kort **task-133**
(sista `132`) · nästa fälla **f47** (hypotes). S93 kör parallellt och kan
förbruka nummer — re-derivera mot disk före varje minting.

### NÄSTA KONKRETA STEG

Återuppta med `session-resume` (S96 behålls). Första handling: starta
dev-servern på 5174 ur denna worktree, fråga Marcus var
*"Frågor? Marcus står till ditt förfogande 24/7"* ska bo, och avsluta
konvergensvarvet. Konvergensen görs DIREKT i prototypkoden — inte via
bygg-agent.

## Del 8 — Andra resumen: processfelet bakom konvergens-tempot

> Skriven vid resumens tillstånds-landning, 2026-08-03 kväll.

### Marcus frågade rätt fråga först

Innan något prototyparbete: *"Tror du att du som orkestrerare gjorde fel i
förra resumen ELLER är det fel någonstans i vår standardprocedur? … Är det
inte normalt sett så att vi göra alla ändringar/iterationer och committar
först när jag är nöjd?"*

**Svaret var båda, i orsaksordning — och hans hypotes var korrekt.**

1. **Orkestrerar-felet:** konvergens-varv delegerades till bygg-agenter trots
   att skillen säger *"Iterera med Marcus i webbläsaren tills HELT nöjd"*
   (`UI.md` rad 148). En interaktiv loop är inte ett självständigt uppdrag.
2. **Processhålet, det djupare:** skillen beskrev VAD konvergensen är men
   inget om KADENS — vem kör, i vilken worktree, när committas, när landar
   det. Hålet fylldes med default-maskineriet. Och **även efter formbytet**
   kördes ändringen genom branch + PR + kö (`#670`) utan skäl.

**Rotorsakskedjan:** agentens isolerade worktree är enda vägen för ändringen
att nå Marcus dev-server, så delegeringsvalet SKAPADE kö-kostnaden. Inte två
oberoende fel — ett som utlöste det andra.

**Mätt** (`TASK-127.2`, fyra varv): `#664` 15:01→15:16 (15 min i kön) ·
`#666` 15:21→15:41 (20 min) · `…-omgang3` övergavs utan PR · `#670` 16:17.
Plus spawn-overhead. **Noll av varven behövde landa** — skillen kräver EN
commit i hela fasen (`[PROTOTYPE]`-SHA vid svar-fångsten).

### `T116` registrerad och åtgärdad samma pass

Kadensen kodifierad i marcus-system-pluginet **1.27.0** (hub-commit
`450c628`, hub-PR `#16`): `SKILL.md` § Standard-formen på UI-grenen **punkt
5** — varvet körs av aktören som sitter med Marcus i dev-serverns worktree ·
lokal commit per varv · push + PR EN gång när Marcus är nöjd · skarven mot
divergens hålls skarp (där ÄR delegering rätt form). `UI.md` fick pekare +
antimönster. Spoke-trådrad landad i `#672`. Plugin uppdaterat till 1.27.0 i
samma landning per distributions-praxisen.

Tråden hålls `paused` för den **kvarstående klassfrågan**, inte för åtgärden:
*vilka andra interaktiva loopar kör vi genom landnings-maskineriet av vana?*

### Tre egna fel i detta pass, bokförda

1. **Rörde huvudkatalogen tre gånger** trots att S93 ägde den — två
   `ff-merge` och en gren skapad där (`docs/t116-konvergens-kadens`). Ingen
   skada (rena träd, rena fast-forwards), men fel form mot ADR-090. S93 har
   sedan dess pausat (`#673`).
2. **Läste `autoMergeRequest`/`mergeStateStatus` som fakta** på hub-`#16` och
   rapporterade `UNKNOWN` som ett verkligt tillstånd — PR:en var redan
   `MERGED` sedan flera minuter. Exakt den fälla `CLAUDE.md` § Landning
   varnar för: vakt-event är väckarklocka, aldrig fakta.
3. **Hoppade över resumens steg 6 och tre LÄS-steg** — se nedan.

### Resumens egen procedur brast — och grinden var grön

`session-resume` steg 6 (tillstånds-återställningen) utfördes ALDRIG vid
återupptagningen. Sessionen stod `lifecycle: paused` medan tre PR:er landade
i den. Även tre LÄS-steg hoppades över: `lessons.md`, `BUILD-LOG.md` och
`byggplan.md` (alla lästa i efterhand när Marcus frågade).

**`check-lifecycle.sh` var grön hela tiden** — den prövar KONSISTENS mellan
fältet och rubriken, och `paused` + `PAUSLÄGE`-rubrik är ett giltigt par.
Den kan inte se att sessionen faktiskt arbetar. Samma klass som `T116`: en
grind som prövar formen, inte sanningen. Trådregistrets egen text säger det
rakt ut — *"Ingen av dem prövar om en rads INNEHÅLL är sant."*

**Fångad av Marcus**, inte av mig och inte av grinden: *"har du gjort
tillstånds-flipp och standardproceduren för session-resume?"*

### Läget vid denna landning

- `main` på `f636aadc` (S93:s paus `#673` landad — **huvudkatalogen är fri**).
- Dev-servern kör på **5174** ur `s96-work-batch`, gren
  `proto/s96-konvergens-varv2` grundad på `main` — alltså MED `#670`:s
  konvergensändring, vilket paus-lägets server saknade.
- Marcus har sett prototyp B och ska kommentera.
- **Fotnots-frågan står fortfarande obesvarad:** var ska *"Frågor? Marcus
  står till ditt förfogande 24/7"* bo?

## Del 9 — Prototyp-konvergensen: sjutton varv och tre research-pass

> Skriven vid prototyp-passets landning, 2026-08-03 kväll.

### Marcus dom vid start

*"I förra resumen byggdes prototyperna och de byggdes så dåligt och slarvigt
så det liknar inget. Väldigt besviken."*

Han hade valt variant B, men iterationerna hade gjort den sämre. Passet
började alltså i en underkänd utgångspunkt — och den nya kadensen från
`T116` (varv körs direkt i koden, lokal commit per varv, push först när
Marcus är nöjd) var det som gjorde sjutton varv möjliga på en kväll.

### Vad som revs, i tur och ordning

Varje rivning var Marcus beslut; skälen bokförda i respektive commit.

1. **Varianterna A och C** — han hade valt B.
2. **Prototyp-headern** → vy-axeln in i railen som generisk tredje axel.
   *"En prototyp-yta får inte bära en egen liten app ovanpå det som ska
   bedömas."*
3. **1200 px-ramen** — satt i varv 2 för att ge skärmarna samma bredd,
   riven i varv 6 när den i stället klippte den varma fonden.
4. **Fotot på Roger och Lotta** — *"testa hur det blir och rensa bort saker"*.
5. **Punktlistan + bekräfta-lösenord-fältet** — research-grundat.
6. **Tvåspalts-skalet inklusive den delade rubriklinjen** — varv 4:s
   linjeringsarbete gick i graven, vilket var rätt: det löste ett problem
   som försvann.
7. **Logotypen, i tre former** — favicon + textrader, fri mot fonden, i
   pill (vit respektive i toningens familj). *"Det är inte viktigt just nu."*

**Textmängden gick 95 → 47 ord.** Branschsnittet i research-passet är ~17.

### Tre research-pass, två med avgörande utfall

- **Aktiveringssidor, branschmönster** (`#676`). Sex live-mätta sidor +
  GitLabs produktionskod + NN/g + GOV.UK. Gav den mätbara kärnan i Marcus
  "KAOS": vi låg på 95 ord mot ett snitt på 17. Och det strukturella svaret
  på H1/H2-frågan: fem av sex sidor har EN rubriknivå, och vår H1+H2 bar
  samma faktum två gånger.
- **Scrollbar-gutter-fonden** (`#677`). Domen: **det går inte.** Chromium
  målar aldrig `background-image` i gutter-ytan — belagt spec-gap
  (`w3c/csswg-drafts#8099`), oberoende bekräftat i `twbs/bootstrap#40659`.
  Tretton kandidater mätta, inklusive två vars element bevisligen täckte
  hela viewporten och ändå fick tomma kanter. Båda mina egna försök stod i
  den matrisen som mätta misslyckanden — de var inte klumpiga utan omöjliga.
- **Fokusring vid musklick** (`#679`). Domen: **ja, och bredare än frågan
  ställdes.** Ring vid musklick på skrivytor är webbläsarnas egen
  spec-dokumenterade heuristik (CSS Selectors L4 § 9.4), inte en stilistisk
  uppfinning; 9/9 live-mätta produkter gör det, och tre designsystem
  kodifierar plain `:focus` på textfält i kompilerad CSS. **Rekommendationen
  gäller varje textfält på appens `<Input>`-primitiv** — vårt undantag är
  rätt riktning men för smalt. Passets oväntade fångst: appens `/login`
  (plain HTML-`input`) visar ring redan idag; det är `base.css`:s
  `[data-rac]`-regel, byggd för popover-dropdowns, som tystar ringen på ALLA
  React-Aria-skrivytor. Den breda ändringen är INTE gjord — produktionskod
  utanför passets scope, bokförd i `T117`.

### Rännstenen: ett belagt nej och ett kamouflage

Marcus fångade de vita kanterna **tre gånger** innan de var lösta, och jag
hade fel varje gång:

1. Jag mätte 1578 av 1600 och avfärdade det som mätbrus efter att en
   skärmdump såg hel ut. Mätningen var korrekt.
2. `w-[100vw]` gav rätt mått i headless men kringgick rännstenen.
3. `scrollbar-gutter: auto` för auth-vyerna fungerade — och **införde ett
   hopp**. Marcus direktiv, absolut: *"INGET får hoppa i denna app under
   några omständigheter."* Rivet.

Lösningen blev ett **kamouflage-lager**: platt `background-color` under
gradienten på `<html>`, satt till gradientens exakta sRGB-mittpunkt
(`color-mix(in srgb, …)` — inte agentens `oklab`, eftersom en
`linear-gradient` utan angiven färgrymd interpolerar i sRGB). Avvikelse mot
kanterna: 7/255 = 2,7 % i en enda kanal. **Rännstensregeln är orörd.**

### Fokusring i auth-formulär — prövas, ej beslutat

Marcus fråga om fokusring vid musklick ledde till ett fynd om appen:
**React Aria äger fokusbeteendet, inte `base.css`.** Regeln
`[data-rac]:focus-visible:not([data-focus-visible]) { outline: none }`
(0,3,0) är den som släcker ringen vid musklick i hela appen.

Enheten blev **formuläret, inte fältet**: `autocomplete` duger inte som
markör, eftersom `email` används även i `AddRegistrationModal` och
`ManuellAnmalanForm` för deltagares adress. Byggt som
`.mm-auth-formular input[data-rac]:focus` (0,3,1).

Två försök misslyckades först, och det andra var värt lärdomen: en
Tailwind-variant slog aldrig igenom, eftersom `base.css` är **olagrad** och
slår alla Tailwind-lager. Filens egen kommentar sade redan det.

### Mina fel i passet, bokförda

1. **Tre gånger fel om rännstenen** — se ovan. Marcus fångade varje gång.
2. **Fokusringens färg** rapporterades som fel (`rgb(36,36,36)` mot
   tokenets `#1b4965`). Marcus verifierade i riktig webbläsare att den är
   korrekt blå. Headless och Chrome rapporterar `outlineColor` olika.
3. **`!important`-räkningen** gav 10 mot 9 och såg ut som om jag tillfört
   en — grep:en räknade ordet i en KOMMENTAR. Korrekt mätning: 5 mot 5.
   Samma klass som `T110` A.
4. **Cwd-drift till huvudkatalogen** upprepade gånger. Fångat varje gång
   före skrivning, men formen är skör; absoluta sökvägar är den enda
   pålitliga vägen i ett träd med worktrees.
5. **Trodde `#675`/`#676` hade landat** för att `main` gick fram två
   commits — de två var `#673`/`#674`. Slutsats ur fel observation.

### Två lokala verifieringar som INTE går att lita på i en worktree

Mätt i detta pass, värt att minnas:

- **`biome` i en git-worktree.** `biome.json` har `vcs.enabled: true`, och
  `.git` är en FIL i en worktree — ignore-hanteringen bryts och filer i
  `docs/backfill/` och `tests/api/` fälls lokalt men inte på `main`.
  Dessutom saknade worktreen `@biomejs/biome` i `node_modules`, så `npx`
  hämtade 2.5.6 medan projektet kör 2.5.5.
- **`tsc` i en NY worktree.** `src/routeTree.gen.ts` är gitignored och
  genereras vid dev/build.

CI är den auktoritativa grinden för båda.

### Landat

`#678` växlar-kontraktet (stående komponent + ADR-074 Amendering 6 +
URL-STATE-SPEC) · `#677` rännstens-researchen · `#676`
branschmönster-researchen · `#675` `T110`-instansen. Prototypkoden och
`base.css` landar separat — se § Öppet nedan.

Facit-bilderna: `tasks/sessions/bilagor/s96-auth-prototyp-facit/`.

## Paushistorik — Session 96, paus 3 (pausad 2026-08-03 kväll, återupptagen 2026-08-04)

### ⚠️ LÄS DETTA FÖRST — merge-kön är blockerad

**Tre nya high-advisories fäller `audit-ci` på ORÖRT `main`.** Ingen PR kan
landa förrän strategin är vald, och per ADR-028 § Konvention punkt 3 är det
valet Marcus, inte mitt. Full bakgrund: **`T118`**.

Sårbarheterna: `brace-expansion` 4.0.0–5.0.8 (high) · `fast-uri`
3.0.0–3.1.4 (high) · `postcss` <=8.5.22 (moderate). Samtliga anger
`fix available via npm audit fix`.

Tidsfönstret är mätt: sista gröna `merge_group` 17:01:48 (`#674`), första
röda 17:18:39 (`#675`). Advisoryn publicerades under passet — den är inte
orsakad av något arbete i sessionen.

**Åtgärds-matris att välja ur** (ADR-028 beslut 1 väger):

1. **Pin exakt + `overrides`** för transitiva — ADR-028:s förstahandsform.
2. **`npm audit fix`** utan `--force` — semver-säkert men rör lock-filen brett.
3. **Invänta dependabot** `#632`–`#635`, som redan står öppna.
4. **Höj `audit-ci`-tröskeln tillfälligt** — snabbast, sämst.

### TILLSTÅND

- `main` på **`52898a9c`**, rent träd i huvudkatalogen.
- **Sex PR:er armerade och köande.** De flödar in av sig själva så snart
  grinden är grön — inget hänger på handpåläggning:

| PR | Innehåll |
|---|---|
| `#675` | `T110`-instansen (klass C på ett skill-kontrakt) |
| `#676` | Research: aktiveringssidors branschmönster |
| `#677` | Research: scrollbar-gutter-fonden |
| `#678` | Växlar-kontraktet: `PrototypeSwitcher` vy-axel + ADR-074 Am. 6 |
| `#679` | Research: fokusring vid musklick |
| `#680` | Del 9 + `T117` + `T118` + detta paus-block |

- **Prototypkoden och `base.css` är INTE pushade.** De ligger som **sjutton
  lokala commits** i `.claude/worktrees/s96-work-batch`, gren
  `proto/s96-konvergens-varv2` (HEAD `709f7e87` + merge `eb9ceb75`).
  Kadensen från `T116` höll hela vägen: inget pushades under iterationen.
- **Dev-servern kör på 5174** ur den worktreen.
- Fyra dependabot-PR:er (`#632`–`#635`) står öppna och oarmerade.

### CARRY / ÖPPNA TRÅDAR

- **Facit är INTE formellt låst.** Marcus sista ord: *"därefter låser vi
  dessa sidor som facit"* — det skulle ske efter fokusring-researchen, som
  landade under hans frånvaro. Låsningen är alltså **hans nästa handling**,
  inte utförd.
- **`T118`** — säkerhetsstrategin (blockerar allt).
- **`T117`** — vem äger appens fokusbeteende. Research svarade JA på
  musklick-frågan och **bredare än auth**: rekommendationen gäller varje
  textfält på `<Input>`-primitiven. Den breda ändringen är INTE gjord.
- **`base.css` bär tre ändringar** från passet (kamouflage,
  `body`-transparens, auth-fokusring) och ska brytas ut i **egen PR** — den
  är produktionskod som nu ligger i en prototyp-gren.
- **Semantiska tokens** behövs innan skarp implementation; prototypen
  använder primitiva (`--p-gold-3` m.fl.).
- `public/miranon-media-ordmarke.svg` ligger kvar **oanvänd** — Marcus:
  *"låt logotypen ligga bara, det tar vi upp långt senare"*.

### NUMRERING ATT RE-VERIFIERA VID RESUME

Disk-verifierat 2026-08-03 på `52898a9c`: nästa ADR **095** · nästa lesson
**L444** (+2 nummerlösa fragment) · nästa tråd **T119** (T117/T118 mintade i
`#680`, ännu ej landade — **kolla om PR:en gått in innan du mintar**) ·
nästa kort **task-133** · nästa fälla **f47** (hypotes).

### NÄSTA KONKRETA STEG

1. **Marcus väljer säkerhetsstrategi** ur matrisen ovan (`T118`). Utan det
   landar ingenting.
2. Kön töms av sig själv — sex PR:er är redan armerade.
3. **Marcus låser facit** för login + inbjudan.
4. Prototypkoden landar i två PR:er: `base.css` för sig (produktionskod),
   prototyp + bilagor för sig (kastbart).
5. Byggkrav ut till `TASK-127.3` / `TASK-127.6` ur facit-bilagan.

## Del 10 — Tredje resumen: T118 löst forskningsgrundat, kön tömd, beslutsbordet exekverat

> Skriven vid resume-flippens landning, 2026-08-04. Flippen kunde inte göras
> vid själva återupptagningen: pausens egen landning (`#680`) satt fast i den
> kö `T118` blockerade, så doket på `main` sade `active` medan det sanna
> pausläget låg olandat. Divergensen flaggades öppet i resume-rapporten;
> tillstånds-återställningen görs FÖRST nu, efter `#680`.

### T118 — vägen ur

- **Research före beslut** (Marcus krav: *"skeppar en agent som
  falsifierar/verifierar din rekommendation"*): passet (`#682`) VERIFIERADE
  overrides-vägen och **falsifierade halva rekommendationen** — full
  lockfile-regenerering är ADR-028:s malware-regel, fel klass här; sex
  tidigare advisory-incidenter i git-historiken löstes alla med riktad bump.
  Mätning i passet: `npm audit fix` rör ALDRIG befintliga overrides-fält —
  alternativ B hade lämnat `brace-expansion` tyst olöst.
- **Fixen `#684`** (`876ec731` → main `c227593f`): overrides-bump ×3
  (brace-expansion 5.0.9 · fast-uri 3.1.5 · postcss 8.5.25), riktad install,
  driftverifierad lockfil, audit-ci Passed, typecheck/biome/build gröna,
  test:api 450/450. ADR-028-avvikelsen öppet bokförd; amenderingen = `TASK-133`.
- **Kön tömd:** åtta stående PR:er uppdaterade/omarmerade (`#675` hade
  konsumerad armering — fjärde läget). `#680` fälldes av Vale (gemena
  "dependabot", enordsrättelse `ce32beb0`). `#677` stängd med motivering:
  prototyp-barlast från gren-basen blev DIRTY när `#678` landade; unika
  innehållet (rännstens-researchen) utbrutet som `#688`.

### Beslutsbordet — Marcus samlade GO (2026-08-04)

1. T118 väg A i uppmätt form — utförd (ovan).
2. **FACIT LÅST** för login + inbjudan (bilagor `72d169cc`).
3. `T117`: ring på varje textfält + `[data-rac]`-släckaren smalnas —
   `TASK-134`; auth-undantaget rivs, ärvs inte.
4. `TASK-129` väg A — utförd av agent (`#687`): ADR-091-rivningen
   rubrikbaserad, domänfixen utbruten som `TASK-136`. Kortets Done-flipp
   väntar på grön CI (tvåstegs-stängningen).
5. Installations-GO: Resend-plugin med MEKANISKT mail-lås byggt och bevisat
   FÖRE aktivering (Rogers krav — deny-regler + hook + nycklar utom räckhåll,
   aldrig prosa) · gddy-CLI + skill · Vercel MCP (OAuth).
6. Mekaniserings-GO: process-utredningens åtgärdsprogram — tråd mintas
   som `T119`.

### Övrigt landat i passet

- **Larmhygien med instansierat verifikat:** `#616`/`#619`/`#636` stängda
  (S93:s K6-fix + nattens Nightly körde staging-sviten GRÖN på main);
  `#656` stängd med öppen motivering (rotorsak ej fastställd,
  återöppningsvillkor). T110-A-fällan UNDVEKS: de mellanliggande "gröna"
  post-merge-körningarna hade aldrig instansierat staging-jobbet.
- **`TASK-132` DoD #3** bockad mot verifikat (`#685`) — nightly-grindens
  fynd, bokföringsmiss från första resumen.
- **`TASK-135` mintad — svep-defekten:** `#684`:s landning skrevs ALDRIG som
  avancemang-rad av heartbeat-svepet (rå-logg bevisar; usage-blocket lovar
  motsatsen). Marcus fångade stagnationen, inte vakten — T112-klassens
  instans i själva motmedlet.
- **Kortstängningar** `126.4`/`127.4`/`127.5` → Done (`#686`, agent;
  DoD #3 bockad mot lästa check-utfall, aldrig antaget).
- **Process-utredningen körd:** intern inventering (40+ trådar, tio
  fel-kluster; entydigt mönster — mekanism håller, prosa bryts) +
  branschresearch (`#683`): tre-lagers-doktrinen (deterministisk
  exekveringspunkt · sannings-reconciliation · öppet deklarerad gräns med
  kontrakt + extern verifiering). Anthropic förstapart: *"hooks are
  deterministic … CLAUDE.md instructions are advisory."*

### Kvar vid denna landning

Delegationsvåg `TASK-133`/`134`/`135` (skickas vid denna flipps armering) ·
`TASK-129`-stängningen efter `#687`-grönt · prototyplandningen (`base.css`
och bilagor; deltat re-deriveras mot main efter `#678`) · `T119`-tråden ·
installationsfasen med Marcus · `#681` stängs efter grön natt.

### Del 10-amendering — slutbuntens landningar och passets egna fel

> Skriven av slutbuntens agent (2026-08-04), efter Del 10:s ursprungliga
> skrivning. Registrerar två av passets egna processfel plus buntens
> faktiska landningar, per uppdrag.

**(a) Två L440-instanser hos orkestreraren, samma pass.** Biome-
förpush-kollen för `#690` kördes genom `| tail` som åt exitkoden →
formatfelet nådde CI och fällde PR:en; samma pipe-klass upprepades i en
diagnostik-körning strax före. Detta är fjärde och femte instansen av
L440-klassen — `lessons.md` L440 § Empiri bär de två första (S91),
L441 bär den tredje (S94). Bokförs som färsk empiri i `T119`-trådens
arbetslista (`tasks/threads/README.md`, `T119`-raden, redan skriven vid
trådens registrering: *"FÄRSK empiri samma dag tråden mintas:
L440-klassen slog till TVÅ gånger till hos orkestreraren"*). Ingen ny
lesson mintas — L440/L441 äger klassen redan.

**(b) `TASK-135`:s uppdragspremiss falsifierades av mottagande agenten.**
Uppdragets hypotes — *"kod och dokumentation motsäger varandra: antingen
respekterar avancemang-raden `--quiet` felaktigt, eller skrivs den inte
alls i loop-läget"* — höll INTE vid empirisk prövning: koden skrev
avancemang-raden korrekt i varje konstruerat scenario. Den faktiska luckan
var en tidigare oupptäckt kallstarts-observabilitetsbrist (ett tomt
`HEARTBEAT_STATE_DIR`/inget känt föregående SHA gick via en tystad
`say()`-gren, omöjlig att skilja från en genuin tyst sopning i en
`--quiet` rå-logg). `ADR-086`-mönstret verkade denna gång åt andra
hållet: mottagaren prövade och FÄLLDE orkestrerarens egen hypotes, i
stället för att bygga vidare på den. Incidentens exakta ursprungliga
mekanism kvarstår som hypotes (den levande processen som producerade
rå-loggen låg utanför agentens worktree).

**(c) Slutbuntens landningar.** `T119` (tråden, mekaniserings-programmet)
registrerad via PR `#695` (merge `c51c1332`) · mail-låset `TASK-137`
landat via PR `#697` (merge `135a659f`) — 26/26 tvåsidigt bevisat
(`scripts/test-deny-resend-send.sh`), fail-closed · fokusringen `TASK-134`
landad via PR `#696` (merge `93d4992d`) — verifierad i riktig browser
(chrome-devtools MCP, headed Chrome), TextArea-uppföljning flaggad i
kortets notes som ej scope-utökad · `TASK-133` (ADR-028-amenderingen)
landad via PR `#693` (merge `9f38d226`). Fyra kort (`TASK-133`/`134`/
`135`/`137`) stängda `Done` i denna bunt, tvåstegs-stängningens andra
steg — `gh pr checks` läst faktiskt per PR före varje DoD #3-bock.

**(d) NÄSTA-läget inför paus.** Installationskvarten (Marcus och
orkestreraren: installs, OAuth/PAT, mail-låsets skarpbevis och
post-installations-prefix-reverifiering) → session-paus S96 → S97
reserveras för mekaniserings-programmet (`T119`) → därefter S96-resume
(Grind 0 DNS) och S93 parallellt.

## Paushistorik — Session 96, paus 4 (pausad 2026-08-04, återupptagen 2026-08-05)

### TILLSTÅND

- `main` på **`4bbd1f98`** (efter `#698`, slutbunten), rent träd. Sessionen
  körde detta pass i **huvudkatalogen** (S93 pausad, katalogen fri —
  kvitterat i resume-rapporten 2026-08-04). CI grön per jobb på HEAD
  (CI · Post-merge · Push on main, samtliga success).
- **Kön TOM på arbets-PR:er.** Endast dependabot `#632`–`#635` öppna,
  oarmerade (Marcus stående beslut: tas efter inbjudan).
- **Installationer UTFÖRDA** (2026-08-04, förstaparts-verifierade
  kommandon): `resend@claude-plugins-official` (user-scope; hostad MCP
  `mcp.resend.com` — lager 0, ingen nyckel på disk) · gddy CLI 0.2.1
  (`~/.local/bin/gddy`) + skillen `gddy@godaddy` · Vercel MCP
  (`mcp.vercel.com`). **Båda MCP:erna står "Needs authentication"** —
  Marcus OAuth/PAT görs i ny session (S97:s start).
- **Mail-låset AKTIVT på main** (`TASK-137`, `#697`): deny ×8 +
  fail-closed PreToolUse-hook + 26/26 tvåsidigt bevis. Prefix-hypotesen
  `mcp__plugin_resend_resend__*` STÄRKT av MCP-registrets servernamn
  `plugin:resend:resend`; slutverifiering av enumererade verktygsnamn
  görs EFTER auth (S97 punkt 1).
- **Worktree-städning utförd vid pausen:** 12 agent-träd borttagna
  (fem grindar, inga scope-brott), 173 → 152 lokala grenar. 15 träd
  kvar, samtliga utanför harness-scope (sessions-/systerträd).
- Larmläge: `#616`/`#619`/`#636`/`#656`/`#681` stängda med verifikat;
  `#469`/`#464` kvarstår medvetet (egen klass resp. stående ärende).

### CARRY / ÖPPNA TRÅDAR

- **`T119` — RESERVERAT SCOPE FÖR S97** (Marcus-beslut 2026-08-04):
  mekaniserings-programmet. Först i S97: MCP-auth ×2, `gddy auth login`,
  mail-låsets SKARPBEVIS (sändförsök nekas i laddad session) och
  prefix-reverifiering. Sedan hookdesignerna (a)–(d) ur T119-raden till
  Marcus-granskning före bygge. Färsk L440-empiri (fyra–fem instanser,
  Del 10-amendering — plus en TREDJE påhittat-SHA-instans vid själva
  paus-landningen, självfångad) hör till arbetslistan.
- **Efter S97, vid S96-resume:** Grind 0 DNS-momenten med autentiserade
  verktyg (T46-kartans 7-punktslista: CNAME · Resend-domänens tre poster
  · DMARC) · byggkrav till `TASK-127.3`/`127.6` ur facit-bilagan ·
  `TASK-136` + `TASK-138` plockbara · `TASK-134`:s TextArea-uppföljning
  (kortets notes) · dependabot-kvartetten efter inbjudan.
- **`T117`-tråden:** breda ändringen är UTFÖRD (`TASK-134`, `#696`) —
  trådens öppna fråga besvarad; radens stängning är liten bokföring vid
  nästa trådsvep.
- 2 lesson-fragment i `tasks/lessons.d/` (nummerlösa) · hub-lyftet
  `L433`–`L443` vid hub-sync-moment (ej S96-scope).
- S93 (appen) pausad parallell — återupptas efter S97, parallellt med S96.

### MARCUS-SEKVENS (beslutad väg framåt, kvitterad 2026-08-04)

1. **S97 session-start i nytt fönster** — mekaniserings-programmet
   (`T119`). Startmoment: `/mcp`-auth (Vercel + Resend) ·
   `! gddy auth login` · mail-låsets skarpbevis.
2. Därefter **S96-resume** (Grind 0 + T95-fortsättning) och
   **S93-resume** (appen) parallellt — ADR-090-hooken ur S97 står då
   klar FÖRE det parallella läget som historiskt producerat brotten.

### NUMRERING ATT RE-VERIFIERA VID RESUME

Disk-verifierat 2026-08-04 på `4bbd1f98`: nästa session **S97**
(reserverad, mekaniseringen) · nästa ADR **095** (sista `ADR-094`) ·
nästa lesson **L444** (+2 nummerlösa fragment) · nästa tråd **T120**
(sista `T119`) · nästa kort **task-139** (sista `138`) · nästa fälla
**f47** (hypotes). **S97 kör före resumen och förbrukar nummer** —
re-derivera mot disk före varje minting.

### NÄSTA KONKRETA STEG

Efter S97: återuppta med `session-resume` (S96 behålls). Första handling:
svep landningsläget, verifiera S97:s utfall mot T119-raden, och ta
Grind 0:s DNS-moment med de då autentiserade verktygen.

## Del 11 — Fjärde resumen: Grind 0 från två till fem, och appen fick en adress

Marcus pekade ut S96 trots att fyra dok stod pausade och S98 hade pausats samma
dag med okonsumerad HANDOFF. Vägvals-grinden hölls: fyndet rapporterades, valet
var hans.

### Resumens LÄS-fas fångade en genomgående stale numrering

Handoffen skrevs "efter S97", men **både S97 och S98** hade körts sedan pausen.
Samtliga axlar utom fällan var förbrukade: ADR 095→096 · lesson L444→L470 ·
tråd T120→T124 · kort task-139→task-143 · main `4bbd1f98`→`53ad8a4b`. Disk var
facit; handoffens paus-tida värden var hypotes, precis som resume-skillen
föreskriver.

En annan fångst i samma fas: `grep -l "lifecycle: paused"` ger falska träffar,
eftersom historiska dok NÄMNER strängen i prosa. Frontmatter måste läsas som
frontmatter — fyra pausade dok, inte tjugotvå.

### Grind 0: från två utförda till fem

**Punkt 4 (`send.miranon.dev`) stod `failed` sedan 2026-08-03 — checklistan
hade gissat postnamnen.** Resend lägger bounce-hanteringen på en
`send`-subdomän AV den domän man verifierar, alltså `send.send`, inte `send`.
De `send`-poster som redan låg i zonen tillhörde root-domänen. Två av tre rader
var fel. Rättade, domänen `verified`, och lärdomen nedskriven i T46: hämta
postnamnen ur `get-domain`-responsen i stället för att härleda dem.

**Punkt 1 (deployen) var aldrig blockerad — jag hade fel.** Bedömningen
"Vercel-MCP:n är oautentiserad" stannade vid MCP-ytan utan att pröva att Vercel
har en CLI. Marcus pressade på frågan (*"varför bygger vi inte deploy då? Vad
stoppar oss?"*) och den föll på första kontrollen. `vercel.json` byggd mot
ADR-091, preview verifierad, och efter Marcus GitHub-koppling deployar main
automatiskt på 13 sekunder.

**Punkt 2 (domänen) togs i samma svep.** `admin.miranon.dev` är live med
Let's Encrypt-certifikat. Vercels egen inspect erbjöd två vägar, och alternativ
b — byt nameservers till Vercel — hade flyttat hela zonen och rivit MX, SPF,
DKIM och DMARC som satts upp samma förmiddag. A-posten valdes; alla tio
mail-poster verifierat orörda efteråt.

### `config push` är deklarativ — säkerhetsnoten var mer berättigad än den trodde

T46 punkt 6 bar en not som sa att nollställnings-risken var *"INTE bekräftat mot
en skarp push (ingen sådan är körd)"*. Pushen kördes, med före- och efterbild ur
Management API (242 fält) runt varje körning.

**22 fält ändrades. Sex var oavsiktliga regressioner** — och bara två av dem
hade förutsetts:

| Fält | Före | Efter |
|---|---|---|
| `mailer_autoconfirm` | `False` | `True` — e-postbekräftelse AV |
| `mfa_totp_enroll_enabled` | `True` | `False` — **TOTP-registrering AV** |
| `mfa_totp_verify_enabled` | `True` | `False` — **TOTP-verifiering AV** |
| `mailer_otp_length` | `8` | `6` |
| `smtp_max_frequency` | `60` | `1` |
| `site_url` | `localhost:3000` | `127.0.0.1:3000` |

MFA-avstängningen stod inte ens i notens egen exempellista. Hade ordningen varit
prod-först — och prod VAR det länkade projektet när sessionen började — hade
tvåfaktor slagits av tyst i produktion.

Samtliga sex är nu låsta i `config.toml` med motivering per fält. Tre operativa
slutsatser bokförda i T46: förebild ur API:t (maskinell diff, inte
ögonjämförelse) · **läs CLI:ts diff, den är enda förhandsgranskningen som
finns** · riktad `PATCH` för enstaka fält i stället för full push.

Rättelse i efterhand: `custom_oauth_max_providers` 3→32767 tillskrevs först
`config push`. Fel — samma förändring inträffade vid en riktad PATCH som inte
nämnde fältet. Det är API:t som normaliserar det vid varje skrivning. Sex, inte
sju, berodde på pushen.

### Site URL — en tyst blockerare ingen punkt i paketet fångade

Båda miljöerna hade `site_url = http://localhost:3000`, satt när projekten
skapades (prod 2026-03-30, staging 2026-06-13) och aldrig ändrat. Adressen
används i ALLA auth-mail utan explicit redirect — **varje inbjudningslänk hade
pekat på mottagarens egen dator.** Hela T95:s inbjudningsväg vilade på en
utvecklingsstandard. Rättad i båda miljöerna, låst i `config.toml`.

### Falsifierad premiss: DMARC

T46 punkt 5 motiverade ett hopp över uppvärmningsfasen med att *"inget legitimt
root-utskick existerar"*. Resend-loggen visar att `invite@miranon.dev` skickade
skarpt broadcast till Roger, Lotta och fler **2026-07-10** — en månad före
påståendet. `p=reject` sattes därför inte; `rua` pekar nu på Marcus och policyn
flippas på data. Zonen bar dessutom GoDaddys automat-DMARC med rapporterna till
`onsecureserver.net` — ingen hade sett en enda.

### Fyra skivor byggda, och ett fel som var mitt

`TASK-126.2` och `127.2` stängdes som ren bokföring (arbetet var levererat
sedan dagar). `126.3`, `127.3`, `127.6` och `127.7` byggdes av agenter.

**Två agenter fångade oberoende av varandra ett fel i mina uppdrag:** jag
påstod att `126.2`/`127.2` var `Done`, men stängningarna låg OCOMMITTADE i
huvudkatalogen när deras worktrees grenades ur main. Kortet var stängt i min
katalog och öppet i deras. Båda byggde mot verifierat tillstånd i stället för
mot mitt påstående — ADR-086 i praktiken. Lärdomen är operativ och gäller
bredare än kortnummer-regeln den härmar: **en kortändring som inte är committad
existerar inte för en agent-worktree.**

`127.8` (passkey) parkerade i stället för att leverera — åtta byggda filer,
noll committade. Egen fragment-post; Marcus har bordlagt mekanismfrågan.

### CodeQL fällde en PR, och det var värt det

`js/incomplete-url-substring-sanitization`, high severity, på
`url.includes('pwnedpasswords.com')` i en testfil. I sak en falsk positiv —
koden observerar, den grindar inte. Formen rättades ändå: gamla mönstret gav
**tre falska träffar av fem** testade URL:er (`?x=pwnedpasswords.com`,
`pwnedpasswords.com.elak.example`, path-prefixet `/auth/v1/userdata`), nya noll.
`src/lib/env-coherence.ts` bär ett ytligt likartat mönster men rördes inte: den
är fail-closed, och CodeQL flaggade den aldrig.

### Två tillståndsytor som ljög, båda rivna

`config.toml` bar *"INTE bekräftat mot en skarp push"* i två dygn efter att
pushen körts — och filen strax nedanför dokumenterade motsatsen. T46-kartan
påstod att ingen frontend-deploy fanns, att DNS-posterna hette något de inte
gjorde, och att push-risken var obekräftad. Båda ersatta med utfall inline,
inte med planen som den skrevs.

## Paushistorik — Session 96, paus 5 (pausad 2026-08-05, återupptagen 2026-08-05)

### TILLSTÅND

- `main` på **`9ba0873d`**, arbetsträd **rent**. Sessionen körde detta pass i
  **huvudkatalogen** (ägarlappen var fri vid resumen, kvitterat i
  resume-rapporten). Post-merge grön på HEAD; `CI` + `Push on main` var
  `in_progress` vid paus-skrivningen — **verifiera dem först vid resume**.
- **15 PR:er landade** (`#775`–`#790`). Kön TOM på arbets-PR:er; endast
  dependabot `#635` (web-vitals 5.3.0→6.0.0) öppen och oarmerad per Marcus
  stående beslut.
- **APPEN ÄR LIVE:** `https://admin.miranon.dev` — Let's Encrypt (giltigt t.o.m.
  2026-11-03), sju säkerhetsheaders verifierade skarpt, SPA-rewrite grön på tre
  djupa routes. Auto-deploy från `main` på ~13 s sedan Marcus kopplade
  GitHub-integrationen. `Vercel` + `Vercel Preview Comments` är nu CI-checkar på
  varje PR — **en ny required-ish yta ingen designat medvetet; värd en titt.**
- **Worktree-städning utförd vid pausen:** 5 agent-träd borttagna (samtliga
  landade + rena), 9 grenar frigjorda, 233 → 224 lokala grenar. 18 träd kvar,
  samtliga utanför harness-scope. Ingen kandidat pekade utanför scopet —
  STOPPA-grinden ej utlöst.

### GRIND 0 — FEM AV SJU

| # | Punkt | Läge |
|---|---|---|
| 1 | Vercel-projekt + config | ✅ `vercel.json` mot ADR-091; GitHub-integration kopplad av Marcus |
| 2 | CNAME `admin.miranon.dev` | ✅ A-post `76.76.21.21`, verifierad hos Vercel |
| 3 | **CORS-utökning** | ❌ **NÄSTA RESUME — appen är live men DATALÖS tills den är gjord** |
| 4 | `send.miranon.dev` | ✅ `verified` i Resend, alla tre poster gröna |
| 5 | DMARC | 🟡 `rua` → `marcus@h5gruppen.se`, `p=quarantine`. `p=reject` flippas på rapportdata (~2 v) |
| 6 | Supabase custom SMTP | 🟡 **staging klar, PROD ÅTERSTÅR** |
| 7 | OTP 24 h + redirects | 🟡 **staging klar, PROD ÅTERSTÅR** |

### CARRY / ÖPPNA TRÅDAR

- **MILJÖKONFIGURATION, samlad post för nästa resume** (Marcus order
  2026-08-05: *"I nästa resume gör vi allt som återstår inklusive CORS och
  HIBP"*):
  1. **CORS** — `CORS_ALLOWED_ORIGINS` + `admin.miranon.dev` i staging **och**
     prod. Blockerar allt användbart i appen.
  2. **HIBP + lösenordsgolv** — `password_hibp_enabled` är `false` i BÅDA
     miljöerna (mätt), och `password_min_length` är `6` medan UI:t kräver 8.
     Marcus beslut: kör BÅDA lagren (server-side kan ej kringgås, klient-side
     ger bättre UX). Riktad `PATCH`, inte `config push`.
  3. **`supabase config push`** — `additional_redirect_urls` bär
     `/nytt-losenord` sedan `TASK-127.7`, men värdet är INTE skarpt i någon
     miljö. Återställningslänken fungerar därför inte ännu.
  4. **Prod-halvan av punkt 6+7** — kräver den mätta ordningen: förebild ur
     Management API, LÄS CLI:ts diff, riktad PATCH för enstaka fält.
- **`TASK-127.9`** (rundturs-e2e) är **plockbar nu** — deps `127.3`/`127.5`/
  `127.6` samtliga Done. Hölls medvetet tillbaka under passet eftersom
  `127.7`/`127.8` ändrade just de vyer den testar.
- **`TASK-126.3`** — koden landad och CI-grön, men kortet står `To Do` på
  **Marcus Gunilla-grind** (DoD #5): install-ytans instruktioner ska bedömas
  pedagogiskt. Agenten lämnade den öppen med avsikt, enligt uppdraget.
- **`TASK-143`** (namn + inbjudare i Användarinbjudan) — mintad på Marcus
  beslut *"Vi ska DEFINITVT ha namn där vid inbjudan!"*. **Bör tas FÖRE
  `TASK-127.10`**, annars granskar Marcus en inbjudan som medvetet saknar det
  han bett om.
- **QA-korten `126.5` + `127.10`** — `ready-for-human`, Marcus egna. Avblockeras
  av CORS (utan den kan ingen logga in).
- **`GoDaddy-parkeringssidan`** — `A @` och `CNAME www` kan INTE tas bort via
  API (`HTTP 409 IMMUTABLE_RECORD`, låsta av `dpsAws`). Marcus hittade inte
  var produkten kopplas loss; **medvetet släppt**, inte glömt.
- **`custom_oauth_max_providers` 3 → 32767** — inträffar vid VARJE skrivning mot
  Management API oavsett väg. Bedömd ofarlig (gräns, ej aktivering; noll custom
  providers finns) men **inte åtgärdad**. Känd kant.
- **Öppen gräns i `127.8`:** `NotAllowedError`-grenen i `tolkaPasskeyFel` är
  kodgranskad men aldrig browser-bevisad. Supabases `passkey_enabled` är
  dessutom `false` på staging — funktionen degraderar tyst tills
  plattformsflaggan slås på.
- **1 lesson-fragment** i `tasks/lessons.d/` (nummerlöst):
  agent-parkeringen överlevde DUBBEL instruktion. Ren empiri på Marcus order —
  mekanismfrågan är bordlagd, tas upp om klassen återkommer.
- Hub-lyftet `L433`–`L469` vid hub-sync-moment (ej S96-scope). S93 (appen)
  pausad parallell.

### NUMRERING ATT RE-VERIFIERA VID RESUME

Disk-verifierat 2026-08-05 på `9ba0873d`: nästa ADR **096** (sista `ADR-095`) ·
nästa lesson **L470** (sista `L469`, +1 nummerlöst fragment) · nästa tråd
**T124** (sista `T123`) · nästa kort **task-144** (sista `143`) · nästa fälla
**f47** (sista 46). **S92, S93 och S98 står alla `paused` och kan förbruka
nummer före resumen** — re-derivera mot disk före varje minting.

### NÄSTA KONKRETA STEG

Återuppta med `session-resume` (S96 behålls). Ordning:

1. **Svep landningsläget** — verifiera `CI` + `Push on main` på `9ba0873d`
   (de var `in_progress` vid paus).
2. **CORS** — den enda posten som blockerar att appen blir användbar.
3. **HIBP + `password_min_length` 6→8**, riktad PATCH i båda miljöerna.
4. **`supabase config push`** mot staging, sedan prod enligt den mätta
   ordningen.
5. **`TASK-127.9`** ut på agent (plockbar).
6. **`TASK-143`** före QA-korten.

## Del 12 — Femte resumen: miljöpaketet betalt, och secreten som inte gick att läsa

Marcus pekade ut S96 av fyra pausade dok. Numreringen var för en gångs skull
**oförändrad** på samtliga axlar — pausen var drygt en timme gammal och ingen
mellansession hade hunnit emellan. Handoffens punkt 1 var redan betald när den
lästes: `CI`, `Post-merge` och `CodeQL` stod gröna på både `9ba0873d` och
paus-commitens egen merge `f09e4183`.

### Secreten gick inte att läsa — och en skrivning hade raderat det okända

`supabase secrets set` är **destruktiv mot hela värdet**: listan är en enda
komma-separerad sträng. För att lägga till en origin måste man alltså känna
samtliga befintliga. Men värdet är write-only — CLI:t visar bara en digest, och
Management API:ts `/secrets`-endpoint returnerar **samma digest**, inte värdet.

Vår egen bokföring räckte inte heller. Den enda posten som fanns
(`2026-05-04`, security-hardening) angav
`https://admin.miranon.se,http://localhost:5173,http://localhost:4173` som
"staging-secret" — men den strängen visade sig hasha till **PRODUKTIONENS**
digest. Förklaringen är kronologisk: 2026-05-04 fanns bara ETT projekt (prod,
skapat 2026-03-30); staging föddes först 2026-06-13. Dokumentet beskrev alltså
korrekt det projekt som i dag är prod. Bokföringen var inte fel — den var
föråldrad på ett sätt som läste som fel.

**Vägen fram blev att bevisa värdet i stället för att gissa det.** Digesten är
rå SHA256 av strängen (bevisat av att prod-kandidaten matchade exakt), vilket
gör varje hypotes verifierbar utan en enda skrivning. Kombinerat med
preflight-mätning per origin — `OPTIONS` mot en Edge Function ger 200 för
allowlistad origin och 403 för icke-allowlistad, helt utan autentisering —
gick listan att rekonstruera:

| Steg | Fynd |
|---|---|
| Preflight-svep, kända kandidater | `5173` + `4173` tillåtna; `.se`, `.dev`, 3000, 5180, 5299 nekade |
| 504 hash-varianter av de två | ingen träff — minst en post saknades |
| Bredare portsvep | `5174` → 200, sedan `5175` → 200 |
| Hash av fyra origins, 384 permutationer | **exakt en träff** |

Stagings verkliga värde var
`http://localhost:5173,http://localhost:4173,http://localhost:5174,http://localhost:5175`
— tre Vite-portar, eftersom Vite trappar upp porten när 5173 är upptagen.
**Ingen bokföring nämnde 5174 eller 5175.** Hade listan skrivits efter
dokumentationen hade två fungerande utvecklingsportar tystnat, och felet hade
visat sig först när någon körde två dev-servrar samtidigt.

Båda miljöerna sattes därefter **additivt**, och digesten verifierades mot en
lokalt beräknad SHA256 av det avsedda värdet före något funktionstest.
`admin.miranon.se` lämnades kvar i prod trots att `TASK-136` bekräftat att
domänen är stale — att riva den är en säkerhetsändring, inte en del av ordern.

### CORS-vägen bevisad hela vägen, inte bara i preflight

Ett `GET` mot prod utan token svarar `access-control-allow-origin: *`. Den
headern kommer från Supabases gateway, som avvisar på JWT-nivå **innan vår kod
körs** — den bevisar ingenting om vår allowlist. Med anon-nyckeln passerar
anropet gateway och når `corsHeadersFor()`, som svarar
`access-control-allow-origin: https://admin.miranon.dev`. Statusen 401 är då vår
egen auth-kontroll, korrekt för en icke-admin. Först det är ett bevis.

### `config push` avstyrdes av sin egen diff

Punkt 3 stod som `supabase config push` i handoffen. CLI:t varnade direkt att
`AUTH_SMTP_PASS` är unset — och stagings custom SMTP (punkt 6) var redan
konfigurerad. En push hade kunnat riva den.

Diffen lästes i stället, med `n` på prompten. Den visade **exakt en rad**:
`additional_redirect_urls`. Posten togs därför som riktad `PATCH` av
`uri_allow_list`, vilket gav 1 ändrat fält av 242 och lämnade SMTP-ytan
oåtkomlig. Det följer den mätta lärdomen från Del 11 bokstavligt: riktad PATCH
framför full push.

**Prod var värre än handoffen trodde.** Den sa att `/nytt-losenord` saknades i
båda miljöerna. Prods `uri_allow_list` var i själva verket **helt tom** — även
`/valkommen` saknades. Inbjudningsflödet mot `admin.miranon.dev`, som pekar på
prod, hade alltså ignorerat sin `redirectTo` tyst. Båda posterna satta nu.

### Lösenordsgolvet var svagare på servern än i UI:t

Båda miljöerna stod på CLI-defaulten `password_min_length = 6`, medan
`src/routes/nytt-losenord.tsx` rad 177 kräver 8 med ASVS 6.2.1 som referens.
Klientkontroller kan kringgås, så det verkliga golvet var 6. Höjt till 8 och
`password_hibp_enabled` satt `true` i båda — två fält av 242, maskinellt
diffade före och efter.

**`minimum_password_length` är låst i `config.toml`. HIBP kan inte låsas.**
CLI 2.75.0:s scaffold saknar fältet helt (verifierat med `supabase init` i en
tom katalog: noll träffar på "hibp"), så det kan varken versionshanteras eller
skyddas mot en framtida push. Bokfört som känd kant i filen i stället för
lappat.

### Handoffens `custom_oauth_max_providers`-påstående föll

Del 11 bokförde att fältet normaliseras 3→32767 "vid VARJE skrivning mot
Management API oavsett väg". Fyra riktade PATCH:ar kördes detta pass — samtliga
gav maskinell diff på **exakt de avsedda fälten**, och fältet rördes inte en
enda gång. Påståendet gäller alltså inte generellt; det som mättes i Del 11 var
smalare än slutsatsen som drogs.

### Prod-halvan av 6+7, och en tredje dimension ingen punkt beskrev

Prod saknade SMTP **helt** (samtliga sex fält `None`) och stod på
`mailer_otp_exp = 3600` mot stagings `86400`. Sju fält satta, maskinellt
diffade.

Vid jämförelsen framträdde något punktlistan inte nämner: **prods samtliga
mail-mallar och ämnesrader är Supabase-defaults på engelska** — `"You have been
invited"`, `"Reset Your Password"` — medan staging bär de svenska som byggdes i
`TASK-127.x` (`"Ditt konto hos Miranon Media"`, `"Återställ ditt lösenord —
Miranon Media"`). En inbjudan skickad från prod i dag kommer på engelska med
Supabase-standardtext. Det är utanför punkt 6+7 som de var formulerade och
lämnas som STOPPA-fråga till Marcus, inte avgjort ensidigt.

## Del 13 — Agentvågen: två fångster i mitt eget arbete, och en flagga som levde i sju filer

Marcus order efter miljöpaketet: *"Ut med så mycket jobb som är möjligt att få
ut i läget vi står i."* Sju bygg-agenter gick ut. Två pushbacks — en från
Marcus, en från en agent — rättade kursen, och båda träffade fel jag gjort.

### Marcus fångst: korten var inte aktualitets-prövade

*"Har vi ens kollat om alla kort du satte agenter på fortfarande är
'aktuella'?"* Nej. Jag hade läst kortens beskrivningar men inte prövat dem mot
disk. I stället skrev jag in en verifierings-instruktion i varje uppdrag — vilket
är att flytta jobbet till agenten och betala full agent-kostnad för att upptäcka
att arbetet redan är gjort.

Mätningen som följde tog två minuter och fann **två fel av sex kort**:

| Kort | Utfall |
|---|---|
| `TASK-116` | Fixen landade i **S97** (`39e55a58`), tre dagar efter kortet skapades |
| `TASK-128` | Redan byggd — `isInMergeQueue` finns 9× i skriptet sedan `2d6bad0e` |

Två `grep` hade räckt. Att jag gav `TASK-128`-agenten exakt den kontrollen — och
inte de andra fem — visar att jag såg risken men bara tillämpade den på ett kort.

Marcus styrde samtidigt tillbaka scopet till S96. Fyra agenter stoppades
(`116`, `123`, `124`, `138`), deras träd togs bort och grenarna raderades.
Mätningen bokfördes i `TASK-116` och `TASK-138` så nästa plockning inte ärver
den falska premissen — `138`:s radhänvisning visade sig dessutom peka fel OCH
underskatta: texten sitter på rad 205 **och** 346, inte 122.

### `TASK-128`-agenten gjorde det rätta: byggde ingenting

Den verifierade att fixen låg på `main` sedan 2026-08-03, byggde ingen kod, och
levererade i stället ett oberoende tvåsidigt regressionsbevis: tog bort
`inqueue == "false"` ur kandidat-villkoret → `T9b` föll (exit 4) → återställde →
36/36 grönt. Det är starkare än att bygga om något som redan fanns.

### Flaggan som levde i sju aktiva instruktioner

Samma agent flaggade att `scripts/heartbeat-svep.sh` föreslår
`gh pr merge <nr> --auto --merge` i sin ALARM-text — en form `CLAUDE.md`
förklarat upphävd sedan 2026-08-04. Fyndet var **större än agenten såg**: sju
aktiva bärare, inte två. Den viktigaste var
`.claude/agents/bygg-agent.md` — **agent-definitionen själv**.

Det förklarar något jag gjort hela dagen utan att förstå varför: jag skrev
"INGEN strategiflagga" för hand i vartenda agent-uppdrag. Jag kompenserade för
en trasig definition i stället för att laga den.

Min egen första sökning efter bärarna missade en, eftersom jag klippte den med
`head -8` — samma klass av fel som pipe-hooken vaktar mot, begången av den som
just läst hookens varning.

**En mätning avvek från `CLAUDE.md`.** Raden sade att formen ger `exit 1`. Mot en
REDAN ARMERAD PR (`#796`) ger den samma avvisningstext men **exit 0**, med
armeringen orörd. S97:s `exit 1` mättes vid armering av en oarmerad PR. Båda står
nu i filen, med slutsatsen: läs texten, inte bara exitkoden — ett skript som
kollar `$?` ser formen som lyckad i det ena fallet och misslyckad i det andra,
fast den är fel i båda.

`ADR-076` rad 123 bär formen i presens. Den lämnades orörd: att redigera en ADR i
förbifarten är fel väg, den hör till en amendering.

### `TASK-143`-agentens fångst: en lucka jag själv skapade

Marcus valde väg 2 för mail-mallarna — *"invite-mallen följer med `TASK-143`,
som ändå rör `supabase/templates/invite.html`"* — och jag skrev in det i
`TASK-144`:s avgränsning. Samtidigt instruerade jag `TASK-143`:s agent: *"försök
inte laga prod-mallarna."*

De två går inte ihop, och agenten såg det. Den sökte igenom samtliga
`tasks/sessions/*.md` efter belägg för citatet, hittade inget, behandlade det som
HYPOTES och byggde inte vidare på det.

Den hade rätt på båda punkterna. Citatet saknades i filartefakterna eftersom
Marcus beslut fattades i chatten **efter** att Del 12 skrevs — exakt den
efemära-trail-fälla konstitutionen varnar för. Och luckan var verklig, mätt efter
leveransen: `invite.html` är uppdaterad i git, medan prods
`mailer_subjects_invite` fortfarande står `'You have been invited'`. Ingen ägde
mallens väg till miljöerna.

Rättat: `TASK-143` äger mallens INNEHÅLL, `TASK-144` äger dess DEPLOYMENT —
inklusive invite. Dependencyn står kvar, men skälet är nu "annars deployas en
mall som inte bär namnet än", inte "annars skrivs den över två gånger".

**Två agenter har nu fångat fel i mina uppdrag under samma session** (Del 11 hade
de första två). Mönstret är stabilt nog att vara en egenskap hos arbetsformen,
inte otur: ADR-086:s premiss-prövning fångar orkestrerarens fel, inte bara
kortens.

### `TASK-143` levererade med ett spoof-bevis

Inbjudarens identitet härleds server-side ur den verifierade JWT:n. Agenten
bevisade det skarpt mot staging: ett anrop med `inviter_name`/`invited_by`
spoofade i request-body gav lagrat `inviter_name` = den ÄKTA anropande adminen.
Spoofen hade noll effekt. Testraden raderades och frånvaron verifierades.
451/451 API-tester gröna, och det nya deny-testet differentialbevisat (avstängd
validering → rött → återställd → grönt).

## Del 14 — Miljöpaketets svans, och frågan som rev min egen rekommendation

Passet efter Del 13 landade nio PR:er till (`#799`–`#811`). Två skivor
levererades, tre kort stängdes, en statusyta rättades — och en enda fråga från
Marcus vände ett arkitekturbeslut jag redan lagt fram.

### `TASK-144` rättade kortet jag själv skrev

Mall-synken levererades, men bygget föll inte ut som kortet beskrev. Jag hade
skrivit "~20 fält skiljer". Mätningen gav **28** — och den kvalitativa
rättelsen vägde tyngre: av 13 malltyper är endast **invite och recovery**
genuint svenska på staging. De övriga elva är engelska på BÅDA miljöerna;
skillnaden är bara vilken default-ögonblicksbild som frusit vid
projektskapandet. Agenten synkade dem därför inte — det hade bytt en engelsk
formulering mot en annan och låst fast dem utan värde för mottagaren.

Ett fynd i samma svep var mer oroande än kortets egen uppgift: **stagings LIVE
invite-mall låg kvar i före-`TASK-143`-läget**, utan `display_name` och
`inviter_name`. `TASK-127.9`:s rundtur hade alltså kunnat gå grön mot en mall
som saknade precis det `TASK-143` just byggt.

AC #3 besvarades med ett bevis snarare än en bedömning: mallarna ÄR redan
låsta via `[auth.email.template.*]`, verifierat med en `config push` som
svarade *"Remote Auth config is up to date"* plus en 242-fälts omkontroll.

### `TASK-127.9` byggde ingenting, och det var leveransen

Agenten hittade att `generateLink` och `deleteUser` båda kräver `service_role`,
otillgängligt i CI, och att stängningen av den luckan kräver antingen en ny
CI-hemlighet eller en ny Edge Function — båda utanför dess yta. Den stannade,
bevisade i stället hela kedjan **live mot skarp staging** i nio steg (inbjudan
→ generate_link → verify 303 → lösenord → inloggning → autentiserad vy med
riktig data → radering → 404-bekräftelse), och lämnade kortet `To Do` med noll
AC avbockade.

Den fångade också ett faktafel i mitt uppdrag: jag skrev att `TASK-143` landade
som `#800` med merge-commit `d467a3d4`. Fel — `#800`:s är `53d32731`;
`d467a3d4` tillhör `#802`. Agenten verifierade koden direkt i stället för att
lita på hänvisningen.

### Statusytan T46 låg efter verkligheten

Kartan sade att CORS-punkten var oöppnad och att SMTP/OTP bara var gjorda mot
staging — allt utfört mot båda miljöerna i detta pass. Punkt 2 saknade sitt
utfall helt. Rättat: **Grind 0 står 6 av 7**, och punkt 5 (DMARC `p=reject`)
väntar på rapportdata, inte på arbete.

Samma klass som fragmentet `bokforing-kan-bli-falsk-utan-att-nagon-andrar-den`
beskriver, skrivet samma dag. Att skriva lärdomen skyddade uppenbarligen inte
mot att begå den.

### Ett eget misstag i landnings-hygienen

Första stängnings-PR:en för `TASK-144` (`#808`) byggdes på en `main` från FÖRE
agentens egen kortlanding och blev konfliktad. Orsaken: `git merge --ff-only`
avbröts med felkod medan `git checkout -b` i samma kedja gick igenom — så
grenen skapades från ett stale träd utan att något stoppade det. Stängd och
omgjord som `#809`; inget innehåll gick förlorat.

### Frågan som rev rekommendationen

`TASK-127.9`:s blockering lämnade ett vägval. Jag lade fram två alternativ och
rekommenderade **A** — en snäv, staging-only Edge Function — med repots
`test-auth`-precedent som motivering.

Marcus svarade inte med ett val utan med en fråga:

> *"Om alternativ A är hur branschledare gör så väljer jag det, är de det?"*

Jag hade inte belagt det. Två snabbsökningar senare svarade jag med emfas att
**C** (lokal Supabase-stack + Mailpit) var branschmönstret, och att `ADR-063`
redan sagt det.

**Det var också fel.** Det ordentliga research-passet som följde visar att
**A ÄR branschmönstret**. Fyra varianter i läsbar mergad kod — Ghost (token ur
modellagret), cal.com × 2 (DB-query respektive API-svarskropp), twenty
(clipboard via en produktfunktion) — kortar alla mail-hoppet och kör resten av
UI-kedjan skarpt. **Inget av 6+ granskade projekt läser en riktig mailbox i
CI.** Cal.com har Mailhog, men gated, med fast 5-sekundersväntan, och bara för
att verifiera ämnesraden.

Felet i mitt mellansvar var en generalisering: Supabases råd att köra
`supabase start` i CI besvarar frågan *"hur testar man Supabase-appar"*, inte
*"hur konsumerar man en mail-länk i ett auth-flöde"*. Jag flyttade ett svar
mellan två frågor utan att märka att de var olika — samma felform som
`CLAUDE.md`:s worktree-rad bokför om sig själv.

Spänningen jag inte kunde lösa löstes av passet: `supabase-community/e2e`
testar Supabases EGEN dashboard mot hostade projekt, inte app-bruk av Auth.
Produkt-testning, inte app-testning.

### Passets mest värdefulla fynd stod inte i frågan

`TASK-127.9`:s "lucka 2" — CI:s e2e-origin matchar inte
`additional_redirect_urls` — är **inte en ny öppen fråga**.
`tests/e2e/auth-flow.staging.test.ts` rad 24–31 bär ett daterat Marcus-beslut
från **Session 5**: frågan väntar på Fas 7:s Vercel-preview-pipeline. Tolv
sessioner innan `TASK-127.9` stötte på samma problem för invite-flödet.

Det ändrar bokföringen: full grön AC #1 är **Fas 7-beroende**, inte enbart
blockerad av service-role. Väg A bör byggas ändå — den löser den del som inte
är Fas-7-bunden och kopplas in samma dag pipen landar.

### Mönstret för dagen

Fyra fel i mitt eget arbete fångades externt under passet: korten som inte
aktualitets-prövades (Marcus), `TASK-144`-luckan (agent), merge-commit-
hänvisningen (agent), och rekommendationen utan belägg (Marcus fråga). Noll
fångades av mig själv. Fångst-raterna i konstitutionen — självgranskning ~9 %,
extern fångst dominerar — höll med obehaglig precision.

## PAUSLÄGE — Session 96 pausad (2026-08-05, sjätte pausen)

### TILLSTÅND

- `main` på **`6ee089bf`**, arbetsträd **rent**. Passet kördes i
  **huvudkatalogen** (ägarlappen fri vid resumen).
- **15 PR:er landade** (`#792`–`#813`, utom S93:s `#807`/`#812`). Kön TOM på
  arbets-PR:er; endast dependabot `#635` öppen och oarmerad per stående beslut.
- Heartbeat-monitorn **stoppad** (`TaskStop`) före paus-skrivningen — se
  § Kända kanter nedan.
- **S93 kör parallellt och är LEVANDE.** Den återupptogs under passets slut
  (`#812`, mergad). Dess aktör rättade själv en lint-fällning i `tasks/todo.md`
  som blockerade båda våra poster i ~25 min.

### VAD SOM LEVERERADES

- **Miljöpaketet komplett, båda miljöerna:** CORS · HIBP + lösenordsgolv 6→8 ·
  `uri_allow_list` · prod-halvan av SMTP + OTP. Varje skrivning riktad PATCH med
  maskinell 242-fälts före/efter-diff — noll oavsiktliga ändringar.
- **`TASK-143` Done** (namn + inbjudare i Användarinbjudan, spoof-immuniteten
  live-bevisad mot staging) · **`TASK-144` Done** (svenska mail-mallar i prod) ·
  **`TASK-128` Done** (bokförings-rest, fixen låg redan på `main`).
- **`TASK-136`** landad · **`TASK-116`** delavstämd (3 av 4 AC).
- **`T46` Grind 0: 6 av 7.** Punkt 5 (DMARC `p=reject`) väntar på rapportdata,
  inte på arbete.
- **Research-passet landat** (`#811`) — se NÄSTA KONKRETA STEG punkt 1.

### NUMRERING ATT RE-VERIFIERA VID RESUME

Disk-verifierat 2026-08-05 på `6ee089bf`: nästa ADR **096** (sista `ADR-095`) ·
nästa lesson **L470** (sista `L469`, **+6 nummerlösa fragment** i
`tasks/lessons.d/`) · nästa tråd **T124** (sista `T123`) · nästa kort
**task-145** (sista `144`) · nästa fälla **f47**. **S92, S93 och S98 står
`paused` — S93 är dessutom AKTIV och kan förbruka nummer före resumen.**
Re-derivera mot disk före varje minting.

### CARRY / ÖPPNA TRÅDAR

- **`TASK-127.9`** är kvar `To Do`, noll AC bockade. Blockeringsfyndet är
  landat (`#806`) och research-passet ger vägen — men se punkt 1 nedan, den
  kräver Marcus beslut först.
- **`TASK-116` AC #3** är hela kortets rest: en `simulate_missing`-dispatch mot
  nattvakten efter `39e55a58`, med run-länk bokförd. AC #1/#2/#4 är avbockade
  mot S97-fixen.
- **`TASK-138`** plockbar med rättade radnummer (205 **och** 346, inte 122).
- **`TASK-126.3` · `126.5` · `127.10`** — Marcus egna grindar. `127.10` är
  avblockerad nu när CORS är på plats och prod-mailen är svenska.
- **`ADR-076` rad 123** bär `--merge`-formen i presens. Medvetet ej rörd — en
  ADR ändras via amendering, inte i förbifarten.
- **`TASK-129`** (ADR-091:s CSP-rivning + stale admin-domän) plockbar, ej
  påbörjad. Marcus nämnde "129.9" i en order; det tolkades som `127.9`, men
  `TASK-129` finns och är obehandlad.
- **6 lesson-fragment** väntar konsolidering. Ett (`regeln-maste-bo-pa-ytan-
  som-levererar-signalen`) är en agents, inte mitt.
- Hub-lyftet `L433`–`L469` vid hub-sync-moment (ej S96-scope).

### KÄNDA KANTER, BOKFÖRDA

- **Heartbeat-svepet larmade nio gånger på samma parkerade PR.** Loopen kostar
  noll, men varje LARM kostar en modell-tur. Det är instansen av den kända
  egenskapen i `CLAUDE.md` (§ svep): mekanismen saknar kontexten "detta är
  parkerat med avsikt". `.heartbeat-svep-policy.conf` har en undantagslista —
  den användes inte, eftersom posten var en annan sessions.
- **`git merge --ff-only` kan avbrytas medan `git checkout -b` i samma kedja går
  igenom**, vilket ger en gren från ett stale träd utan att något stoppar det.
  Kostade en konfliktad PR (`#808`, stängd och omgjord som `#809`).
- **CI kan inte se miljötillstånd.** `TASK-144`:s skrivningar verifierades mot
  Management API efter landning, inte av CI — staging-sviten stod `SKIPPED`.

### NÄSTA KONKRETA STEG

Återuppta med `session-resume` (S96 behålls). Ordning:

1. **MARCUS BESLUT — research-passets rekommendation** (`docs/research/
   auth-invite-e2e-service-role-branschprecedent-2026-08-05.md`). Svaret på
   *"är väg A hur branschledare gör?"* är **JA** — fyra varianter i läsbar
   mergad kod (Ghost, cal.com ×2, twenty) kortar alla mail-hoppet via
   privilegierad backend-läsning och kör resten av UI-kedjan skarpt; inget av
   6+ granskade projekt läser en riktig mailbox i CI. Rekommendationen: bygg
   `test-invite-completion` som staging-only EF bakom `ADMIN_EMAILS`,
   utelämnad ur `.prod-functions-allowlist.conf` (`test-auth`-precedenten),
   noll nya CI-hemligheter. **Bokför samtidigt passets §8-fynd:** full grön
   AC #1 är **Fas 7-beroende** (Vercel-preview-pipeline, Marcus-beslut från
   Session 5 i `tests/e2e/auth-flow.staging.test.ts` rad 24–31), inte enbart
   service-role-blockerad.
2. **`TASK-127.9`** enligt beslutet i punkt 1.
3. **`TASK-116` AC #3** — en dispatch, sedan stängning.
4. **`TASK-129`** eller **`TASK-138`**, båda plockbara.
5. **QA-korten** när Marcus vill: `126.3` (Gunilla-grind), `126.5`, `127.10`.
