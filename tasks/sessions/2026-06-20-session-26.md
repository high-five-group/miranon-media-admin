---
owner: marcus803
updated: 2026-06-21
review_by: 2026-09-20
status: stable
lifecycle: active
---

# Session 26 — Fas 6c (Registrations + Väntelista)

> Doket fött vid sessionsstart per ADR-043 + L67/L68 (levande artefakter har
> landnings-kadens, inte avsluts-kadens). Fött via session-start-skillens
> create-session-doc-gren (plugin marcus-system,
> `references/create-session-doc.md`). `lifecycle: active` satt explicit vid
> födelse (skill-ägt, ADR-052 beslut 3 + create-session-doc steg 8). Del 2+ fylls
> vid respektive landnings-ögonblick (committad + pushad + CI-grön), inte vid
> sessionsavslut.

## Del 1 — Syfte och scope

### NULÄGE / scope

Session 26 bär **Fas 6c — Registrations + Väntelista** (byggplan §4 Fas 6-tabellen).
Föregående session (25) stängde Inc 4 (kall /arch-audit mot Fas 6a, ren) + Fas 6b
(Events-domän: get-event + get-attendance, väg D mot T15) och lämnade 6c-stubbarna
på plats. 6c bygger ut anmälnings-domänen:

**(a) Vyer.** `/event/$eventId/anmalda` (Anmälda per event) + `/mer/vantelista`
(Väntelista). Vyerna renderar mot live-data per DoD 11/10/10.

**(b) `create-registration` Edge Function (write, idempotent).** Deployas med
idempotency-nyckel-strategi per **ADR-014** (client-genererad UUID via
`useCreateRegistration`-mutation, INVARIANT-check 400 vid saknad nyckel, lagring av
nyckel med TTL). Adresserar dubblettbuggen i `data-model.md §F.4`. Idempotency-
lagringens väg (Airtable-tabell skapad i 6c vs Fas B) avgörs av Del C-utredningen
nedan (Marcus-kvittens krävs — STOPPA-grind C1).

**(c) `get-waitlist` Edge Function (read).** Läser den egna `Väntelista`-tabellen
(`tbl2VxMx7JMkIxD4Q`), filtrerad per event + aktiv (ej "Flyttad till anmälan").

**(d) `get-registrations`-fix (T15).** Den deployade get-registrations bär den
latenta länk-ID-filter-klassbuggen (T15): `buildLinkedRecordFilter('Event', eventId)`
matchar länkens primär-display, ej record-ID → matchar ALDRIG mot skarp länk-data.
Smäller i 6c:s "Anmälda per event". Fixas per fix-mall — se STOPPA-grind B1.

Per-operations-kontrakt (ärvt): varje EF-deploy följer en namngiven UI-konsument
(M4); varje write-operation registreras i `field-allowlists.ts` med 1 deny + 1
allow-test; vy-Playwright-baseline + axe-core 0 violations. 6c ärver Fas 6a/6b:s
mönster: single-record-EF + 404-mall, `_shared/coerce.ts`, list-paginering via
`fetchAirtablePage`, cursor-conformance-test, **conformance mot SKARP datakälla**
(L154 — syntax-grön ≠ semantik-korrekt), record-ID-batch-mall (väg D) från
get-attendance/get-person, mutation-mall (ADR-016). Arv: ADR-016/026/049/050/055 +
T14 + T15.

### EJ I SCOPE

- **Fas 6d (Hem)** + **Fas 6e (Mer)** — separata sub-faser, egna sessioner.
- **Realtime / polling** (`usePollingQuery`, 60s + pull-to-refresh) — hör till 6d
  (ADR-017), inte 6c.
- `sendEmail` / direct-Resend (ADR-015) — 6e.
- Inget ADR skrivs i förskott; ADR-059 hålls reserverat. Nytt ADR endast om något
  arkitektoniskt dyker upp (STOPPA-OCH-FRÅGA).

### NUMRERING (disk-verifierat vid födelse, ej index-antaget)

- Session: senaste landad = 25 (live). Detta = **Session 26**. (DISK-VERIFIERAT)
- ADR: 58 filer på disk, högsta = ADR-058. Nästa ledig = **ADR-059** (reserverad,
  ej skriven). (DISK-VERIFIERAT)
- Lesson: senaste på disk = L154. Nästa ledig = **L155**. (DISK-VERIFIERAT)

### INGÅNGSTILLSTÅND (snapshot vid födelse, disk- + live-verifierat A–C)

**Repo (A1):** HEAD `49f4e5e`, branch `main`, arbetsträd rent, i sync med
`origin/main`. Session 25 stängd på disk (`lifecycle: closed`).

**6c-stubbar (A3):** Port-paritet **15 == 15 == 15** (iface
`DataSourceAdapter.ts` == `AirtableAdapter.ts` == `SupabaseAdapter.ts`).

- `AirtableAdapter.createRegistration` ([AirtableAdapter.ts:132](../../src/data/adapters/AirtableAdapter.ts#L132)) → `throw new Error('Not deployed yet — see Fas 6c')`. STUBBAD ✓
- `AirtableAdapter.fetchWaitlist` ([AirtableAdapter.ts:172](../../src/data/adapters/AirtableAdapter.ts#L172)) → samma stub. STUBBAD ✓
- `SupabaseAdapter.createRegistration` / `fetchWaitlist` ([SupabaseAdapter.ts:58,70](../../src/data/adapters/SupabaseAdapter.ts#L58)) → `NOT_IMPLEMENTED` (ADR-057 port-paritet). STUBBAD ✓

**EF:er på disk (A4):**

- `get-registrations/` finns + deployad. Filtrerar event via
  `buildLinkedRecordFilter('Event', eventId)` ([get-registrations/index.ts:67](../../supabase/functions/get-registrations/index.ts#L67)) → bär latent T15-klassbugg.
- `create-registration/` och `get-waitlist/` finns **INTE** på disk — skapas i 6c.

**B1 — länk-ID-filter-klassbuggen (T15), fix-väg verifierad:**
`Eventplanering` bär ett symmetriskt länkfält **`Anmälningar (länkat fält)` =
`fldUAjTutSM0fziMT`** (multipleRecordLinks → Anmälningar; read-only spegel av
`Anmälningar.Event` = `fldi3enUaMdbuGSlm`, data-model §Kända fällor #3). Exakt
analogt med `Närvaro (records)` (`fldwyD1cheb759YN1`, event→Deltaganden) som
get-attendance väg D använde. → **Väg D (record-ID-batch från event-hållet) är
farbar för get-registrations-fixen.** Designas EJ här (STOPPA-grind, Chat/Marcus-
kvittens). Sido-watch: `Väntelista.Event` (`fldC01Nf3lVWrOgdw`) är också länkfält →
samma T15-klass gäller get-waitlist:s event-filter.

**C1 — idempotens-lagring (ADR-014):** **Ingen `Idempotency Keys`-tabell existerar
i vare sig prod (`app8uGPrVCVOm6LfD`) eller staging (`apphjj8Q7lkXCMsL4`).** Måste
skapas före/vid create-registration-deploy. Egen `Väntelista`-tabell
(`tbl2VxMx7JMkIxD4Q`) finns (för get-waitlist). Rekommendation läggs fram vid
STOPPA-grind C1 — ingen tabell skapad, inget Airtable-beslut committat.

## Del 2 — ADR-014-falsifiering → Alt Y → ADR-059 (Proposed)

> Landnings-post (committad + pushad + CI-grön). Beslutskedjan dokumenterad så
> trailen är härledbar.

**Beslutskedja (research → revisit VACKLAR → Alt Y).**

1. **Falsifiering (Chat web-research).** ADR-014:s race-skydd vilade på en Airtable
   unique-constraint på idempotency-nyckel-fältet. Branschpraxis + Airtable-dok:
   Airtable kan **inte** påtvinga unik-constraint på ett *skrivbart* fält (endast
   auto-record-ID är unikt, ej skrivbart). Idempotens-**kravet** giltigt;
   lagrings-**mekanismen** strukturellt oförmögen att ge race-säkerheten ADR-014:s
   egen DoD (scenario 3) kräver.

2. **Revisit-pass (Code LÄS→RAPPORTERA, föregående pass) → FUNDAMENT VACKLAR för
   Postgres-reservation nu.** Disk/live-verifierat: noll migrations/DDL-workflow på
   disk (bara `config.toml` + `functions/`; noll `.sql`; `todo.md`: *"Migrations ej
   tillämpligt (L115)"*); `@supabase/supabase-js` finns men ENBART för auth
   (`_shared/auth.ts` + `create-admin-user`); `.from(`/`.rpc(` över EF-lagret = tomt
   → service-role rör aldrig data; ADR-057 parkerar Postgres-DATA till Fas E.
   Postgres-reservation nu = ny arkitektur-yta + ny distribuerad-transaktions-felmod.

3. **Beslut (Chat seniorval — Alt Y).** Defer äkta server-side-idempotens till Fas E
   (Postgres-datahem, UNIQUE-reservation naturlig där). Interimt: klient-skydd
   (TanStack `mutationKey`-dedup + disabled submit under `isPending`). Klient-UUIDv7-
   nyckel **bevaras i request-kontraktet** + loggas nu → Fas E-aktivering additiv.
   ADR-014:s avvisning av "Alt 4 — defer till Fas E" ärvs **inte**: dess premiss
   (Airtable kan ge race-säkerhet nu) är falsifierad. Smalt multi-session-race-fönster
   accepterat öppet (single-admin-golv); Alt X (Airtable check-write + kompenserande
   dedup) noterat som fallback.

**Artefakt:** [ADR-059](../../docs/decisions/ADR-059-idempotens-lagring-defer-fas-e.md)
skriven, **Status: Proposed** (Marcus kvitterar efter läsning — ej självaccepterad).
ADR-014 markerad med partiell-supersession-not + inline-errata (Status kvar Accepted,
historisk text bevarad, per ADR-016↔ADR-049-precedensen). README-katalog uppdaterad
(ny ADR-059-rad + superseder-not på ADR-014-raden).

**6c-konsekvens:** create-registration byggs **utan** nyckel-lagring; ingen Airtable
Idempotency Keys-tabell skapas (B/C1-vägvalet från orienterings-passet därmed
avgjort: ingen tabell). C1-STOPPA-grinden från Del 1 är **stängd** av detta beslut.

## Del 3 — airtable-constraints.md skapad (Pass 1 skörd → Pass 2 dokument)

> Landnings-post (committad + pushad + CI-grön). `lifecycle` kvar `active`.

**Pass 1 (forensisk skörd, LÄS→RAPPORTERA).** Uttömmande inventering av alla dokumenterade
Airtable-begränsningar tvärs repot (ADR:er, sessionsdok inkl. arkiv, lessons.md, data-model.md,
threads, EF-källkod, CLAUDE.md-guards). Resultat: **45 distinkta, fil:rad-belagda + klass-flaggade
poster** efter merge av ~110 råa omnämnanden — 25 PLATTFORM, 15 DATA-INSTANS, 5 OKLAR. Fem kända
begränsningar (unique-constraint, länkfält-filter/T15, NaN-objekt/L152, formelfält-skrivbarhet,
MCP-blindhet) korsade mot skörden → noll luckor.

**Pass 2 (författande).** [`docs/reference/airtable-constraints.md`](../../docs/reference/airtable-constraints.md)
skapad — auktoritativ, levande katalog över Airtables **strukturella** begränsningar; tillika
migrations-kravspec för Supabase-adaptern (Fas E, ADR-057). 25 plattform-poster grupperade tematiskt
(Integritet · Frågekraft & paginering · Typning & coercion · Automationer & timing · Schema &
operationellt), var och en med 4-delad anatomi (Begränsning / Kostnad+källor / v1-kompensation / Fas
E-krav). 15 data-instans-fällor sammanfattade med pekare till data-model §Kända fällor (ej duplicerade).
**Allvarlighets-axel:** tre ⚠️ tyst-korruption-poster (P2 inga transaktioner, P16 automation-run-status,
send-email/§Kända fällor 29) lyfta som starkaste Fas E-argument. Klassningsbeslut applicerade: O1 + O3
utelämnade (Fas E-readiness resp. overifierad inferens), O2 → underrad P25, O4 → data-instans med
`[HYPOTES — EJ VERIFIERAD]` bevarad, O5 → P19. Integration: pekare i data-model §Kända fällor-ingress +
rot-README docs-länklista.

## Del 4 — register-synk-wiring (hub + spoke) + constraints-doket lyft till governing

> Landnings-post (committad + pushad + CI-grön). `lifecycle` kvar `active` (vi pausar strax, avslutar ej).

**Föregångare:** LÄS-pass (fil:rad-belagt) kartlade session-end-ytan. STOPPA-fynd: (1) ingen hub-skill
läser en spoke-deklarerad lista — vi inför INTE det mönstret; (2) session-end är delad i två halvor
(Code-verifiering i `plugins/`, Chat-orkestrering i `claude-app-skills/`) med olika idiom. Trådar
(ADR-053) saknades helt i session-end — genuin lucka, ej kollision.

**Hub (`~/Repon/marcus-system`).** Lade ett tråd-synk-steg DIREKT-NAMNGivet bredvid lessons-steget i
BÅDA halvorna, var i sitt eget idiom — universella artefakter namngivna direkt (samma mönster hub redan
använder), inget spoke-deklarations-dataflöde:

- Code-halva (`plugins/.../session-end/SKILL.md`): nytt Do-confirm-item 7 ("Trådar synkade?") i
  verifierings-idiom; items 7–10 renumrerade 8–11 rent. Lessons-item 6 + lessons-hub-sync-ägarskapet ORÖRT.
- Chat-halva (`claude-app-skills/session-end/SKILL.md`): nytt handlings-steg 6 ("Tråd-synk:") i
  handlings-idiom; gamla steg 6 → 7. Lessons-steg 5 ORÖRT.
- **Källa-vs-installerad-gap (STOPPA, ej åtgärdat):** källediten propagerar INTE automatiskt till den
  installerade pluginen (`marcus-system@marcus-hub` v1.4.0, cache-snapshot). Aktivering kräver
  version-bump + ominstallation — distributions-beslut, inväntar Marcus-kvittens.

**Spoke (`~/Repon/miranon-media-admin`).** Två koordinerade ändringar:

- `CONTRIBUTING.md` per-session-DoD: två nya rader — tråd-synk till `tasks/threads/` (ADR-053) +
  villkorad rad för ny strukturell Airtable-vägg → `airtable-constraints.md` + ändringslogg.
- `airtable-constraints.md` lyft till governing (tre-fil-lås i samma commit): `.frontmatter-policy.conf`
  (intill data-model.md), `test-check-frontmatter.sh` `write_all_valid()` (10:e fixtur) + T1/T12-
  assertioner "9"→"10". Lokalt: test-suite 14/14, real-config-check "alla 10 styrande docs passerar",
  constraints-dokets 5 frontmatter-checkar gröna.
