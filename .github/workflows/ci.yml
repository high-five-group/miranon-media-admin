---
name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  # MERGE QUEUE (A7:3 / TASK-70.1). GitHub är kategorisk: "You MUST use the
  # `merge_group` event to trigger your GitHub Actions workflow when a pull
  # request is added to a merge queue" — utan den startar de required checkarna
  # ALDRIG på kö-ytan, och mergen failar på en check som aldrig rapporteras.
  #
  # `checks_requested` är i skrivande stund den ENDA aktivitetstypen eventet
  # har. Den skrivs ändå ut: en tom `merge_group:` är ekvivalent i dag men
  # tyst om vad den lyssnar på, och en framtida andra typ skulle då tas in
  # utan beslut.
  #
  # Check-NAMNET måste vara identiskt på båda ytorna, annars matchar rulesetets
  # required check inte kö-körningen. Vi rapporterar `CI Passed or Skipped`
  # från samma aggregator-jobb oavsett event — se jobbet längst ned.
  merge_group:
    types: [checks_requested]
    branches: [main]

# Security hardening: least-privilege default per ADR-029 §4.
# GITHUB_TOKEN får endast contents:read via explicit grant i jobs.
permissions: {}

# Auto-cancel in-progress runs på samma branch/PR per ADR-029 §4.
#
# MERGE QUEUE-SKYDDET (TASK-70.1) — cancel-in-progress är AVSTÄNGT för
# `merge_group`, och det är inte en detalj. Att avbryta en kö-körning gör att
# dess required check aldrig rapporteras, PR:en faller ur kön, och i värsta
# fall fälls hela kön. Det är en dokumenterad merge queue-fälla
# (community-discussion #137976), inte en teoretisk risk.
#
# Nyckeln bär `merge_group.head_sha` EXPLICIT. Utan den raden hade uttrycket
# fallit till `github.sha`, som råkar vara unik per merge group — alltså rätt
# beteende av en slump, buret av att `event.number` är tom på kö-ytan. Ett
# skydd ingen skrivit medvetet är ett skydd nästa refaktor tar bort utan att
# veta om det.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.event.merge_group.head_sha || github.sha }}
  cancel-in-progress: ${{ github.event_name != 'merge_group' }}

jobs:
  # Job 1: Detect changed files — sets should_skip_tests + docs_changed
  # outputs for downstream conditional jobs. Per ADR-029 §1a.
  changed:
    name: Detect changed files
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
      # actions: read — dedup-steget (task-36.4) frågar körnings-API:t
      # (gh run list) om PR-headen redan har en grön CI-run. Minsta grant
      # utöver contents:read.
      actions: read
    outputs:
      should_skip_tests: ${{ steps.changed-files.outputs.only_changed == 'true' }}
      docs_changed: ${{ steps.changed-docs.outputs.any_changed == 'true' }}
      # ═══ UTAN KONSUMENT SEDAN A7:5 (TASK-70.3) — LÄS DE TVÅ NÄSTA SOM HISTORIK
      # De beskriver vad klasserna GJORDE när staging låg i PR-grinden. Sedan
      # `suite`-jobbet skickar `run_staging: false` villkorslöst styr ingen av
      # dem längre någonting; de beräknas, exponeras och läses av noll jobb.
      # Varför de ändå står kvar — och vem beslutet tillhör — står vid
      # `with: run_staging:` i `suite`-jobbet nedan. ═══
      #
      # D1-klassen (task-36.3 / ADR-077): true = ALLA ändrade filer är ren UI-yta
      # (stilmallar/CSS/publika statiska filer) OCH ingen exkluderad fil (samma
      # exkludering som docs-klassen). Släckte staging, körde a11y/pure/build.
      ui_low_risk: ${{ steps.changed-ui.outputs.only_changed == 'true' }}
      # Acceptance-klassen (task-59.7 / ADR-080): true = ALLA ändrade filer bor
      # i tests/acceptance/**. Släckte staging + purge (ingen mutex) medan
      # acceptance/a11y/pure/build kördes. Samma LEVER som ui_low_risk, egen
      # klassning — se steget för varför levern delades.
      acceptance_local: ${{ steps.changed-acceptance.outputs.only_changed == 'true' }}
      # Merge-dedup (task-36.4 / ADR-077): true = main-push vars träd redan
      # bevisats grönt i PR:en → tunga jobb hoppas. Fail-closed (se steget).
      dedup_hit: ${{ steps.dedup.outputs.dedup_hit == 'true' }}
      # ACCEPTANCE-URVALET (TASK-75). Blankstegsseparerad lista med de
      # spec-filer `Acceptance (hermetisk)` ska köra i stället för hela klassen,
      # eller TOM STRÄNG för full klass. Tomt är defaulten i varje osäkert läge
      # — se `acceptance-urval`-steget nedan och scripts/acceptance-urval.sh.
      acceptance_urval: ${{ steps.acceptance-urval.outputs.acceptance_urval }}
    steps:
      # ═══ SHA-PINNADE FIRST-PARTY ACTIONS (TASK-312, 2026-08-24) ═══
      #
      # actions/checkout, actions/setup-node, actions/cache och
      # actions/upload-artifact bar tidigare TVÅ av tre samexisterande
      # pinnings-discipliner i detta repo (exakt tagg: setup-node@v7.0.0,
      # cache@v6.1.0; flytande major: checkout@v7, upload-artifact@v7) —
      # tredje disciplinen är SHA-pin (tj-actions/changed-files,
      # lycheeverse/lychee-action, redan i detta repo). Flytande major är
      # strukturellt samma svaghet som ett opinnat npx-anrop: en tagg
      # leverantören kan flytta. ADR-029 § Beslut kräver redan "SHA-pin
      # obligatoriskt på alla non-GitHub-officiella Actions" — detta kort
      # utökar den till husets FÖRSTA-parts Actions också, så alla fyra
      # konvergerar till EN disciplin (SHA-pin), husets starkaste form.
      #
      # VERSIONSVAL: INGEN version bumpades — varje SHA nedan pekar på
      # EXAKT samma commit den flytande/exakta taggen redan pekade på
      # (checkout v7 → v7.0.1, upload-artifact v7 → v7.0.1, setup-node
      # v7.0.0, cache v6.1.0 — verifierade IDENTISKA via `gh api
      # repos/<org>/<repo>/git/refs/tags/<tag>` OCH `git ls-remote`, två
      # oberoende vägar, 2026-08-24). Detta kort ändrar PINNINGSFORMEN,
      # inte BETEENDET.
      #
      # ADVISORY-STATUS FÖRE BUMP-VAL (K18-disciplinen, ADR-029 §
      # Konsekvenser): `gh api "advisories?affects=<action>"` för samtliga
      # fyra gav TOM TRÄFFMÄNGD ([]) 2026-08-24 — inga kända sårbarheter,
      # ingen bump-anledning. Se docs/decisions/ADR-029-ci-architektur-
      # changed-files-pattern.md § Konsekvenser för metodologin (samma som
      # redan etablerad för tj-actions/changed-files, K1.C).
      #
      # UPPGRADERING: bumpa taggen i `gh api`-uppslaget ovan, kör om båda
      # verifieringarna, uppdatera SHA + kommentarens versionsnummer på
      # VARJE anropsställe (45 st, samtliga sju workflow-filer — inga
      # centrala variabler i GitHub Actions `uses:`-syntax, samma
      # begränsning som tj-actions/lychee-action redan lever med).
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          # fetch-depth: 0 = FULL historik (ADR-054, 2026-06-17). Finit djup var
          # anti-mönstret: 1→50→100→250 brast 4 ggr när orörda governing-docs
          # passerade boundaryn (Session 22: dok-commit sköt fönstret 263→264).
          # check-frontmatter.sh Check 2 kräver filens sanna commit-datum. Per K1.D §0 utelämning #6.
          fetch-depth: 0

      - name: Get changed files
        id: changed-files
        # SHA-pin per ADR-029 §6 third-party Actions-policy.
        # tj-actions/changed-files v47.0.6 verifierad 2026-05-13 (K1.C Block 2).
        # Veckovis Actions-granskning per tasks/todo.md.
        uses: tj-actions/changed-files@9426d40962ed5378910ee2e21d5f8c6fcbf2dd96  # v47.0.6
        with:
          # quotepath: false — matcha non-ASCII-paths verbatim (upstream-
          # dokumenterad input i action.yml @ pinnad SHA). Gits default
          # core.quotepath=true oktal-escapar t.ex. backlog-kortfilers å/ä/—
          # och citerar strängen → den slutar på '"', inte '.md' →
          # **/*.md-globben missar → only_changed=false → onödig full
          # Test+Build för varje commit som rör kortfiler utan kod
          # (K1.17-klassen; TASK-15 S69, kontrastbevis run 29657524469
          # [kortfil → full] vs 29657760975 [ren ASCII-docs → skip]).
          #
          # DOCS-VERKTYGENS EGNA CONFIGFILER (S91): .lycheeignore, .vale.ini,
          # .vale/** och .markdownlint-cli2.jsonc står i listan nedan. De läses
          # ENBART av docs-jobbets grindar (lychee, markdownlint, Vale) och kan
          # per konstruktion inte påverka testsviten — verifierat med grep över
          # hela trädet 2026-07-26; enda träffarna utanför denna fil är
          # kommentarer plus scripts/test-vale-regression.sh, som självt kör i
          # docs-jobbet. Samma rationale som tests/vale-regression/** +
          # scripts/test-vale-regression.sh, vilka redan bar klassningen —
          # dessa fyra var luckan i den.
          # EMPIRI, två oberoende fall där full svit kördes i onödan:
          #   run 30201519323 — .lycheeignore + 1 .md                        → 14,3 min
          #   run 30163753632 — .vale.ini + .markdownlint-cli2.jsonc + 3 .md →  9,8 min
          # PARAD ÄNDRING (obligatorisk): varje docs-config-post här måste ALLTID
          # finnas även i changed-docs-stegets lista nedan. Utan paret blir en
          # ensam-ändring av filen BÅDE testsvit-skippad OCH docs-skippad, alltså
          # tyst ovaliderad (fail-open, L322-klassen). De två listorna härleds
          # oberoende och ingen grind håller dem synkade — paret hålls för hand.
          #
          # NOT OM FORMEN: kommentarer måste stå HÄR, aldrig inne i "files: |".
          # Det blocket är en YAML literal block scalar — en "#"-rad där blir en
          # literal glob-post, inte en kommentar. Felet gjordes och fångades
          # under bygget av just denna ändring (S91), därav noten.
          #
          # AGENT- OCH HARNESS-KONFIG (S91, TASK-71): .claude/**-raderna nedan.
          # Katalogen kan per konstruktion inte påverka en testkörning — CI kör
          # aldrig Claude Code. Grep över src, tests, scripts, .github och
          # supabase 2026-07-28 gav tre träffar, alla ofarliga:
          # scripts/agent-spawn-log.sh och scripts/agent-spawn-metrics.mjs läser
          # katalogen via CLAUDE_PROJECT_DIR (körs bara av Claude Code självt),
          # och scripts/test-agent-spawn-log.sh — den enda av de tre som CI kör —
          # pekar CLAUDE_PROJECT_DIR på en /tmp-sandlåda och rör alltså aldrig
          # repots .claude/.
          #
          # VARFÖR RADERNA BEHÖVS TROTS **/*.md OVAN: micromatch matchar inte
          # dot-yta utan dot: true. Mätt mot node_modules/micromatch 4.0.8
          # 2026-07-28: '**/*.md' ger NOLL träff på .claude/agents/bygg-skiva.md,
          # '.claude/**' full träff. Samma skäl som .github/PULL_REQUEST_TEMPLATE.md
          # och .github/ISSUE_TEMPLATE/** står explicit trots **/*.md — vore
          # dot-matchning påslagen vore de raderna döda.
          #
          # DÄRFÖR ÄR POSTERNA TVÅ. Samma dot-regel biter en gång till INNE i
          # katalogen: '.claude/**' matchar INTE .claude/.markdownlint.jsonc,
          # eftersom filnamnet börjar med punkt (mätt, samma körning). Med bara
          # den ena raden hade den nya markdownlint-configen legat utanför båda
          # listorna — alltså exakt det fail-open kortet finns för att stänga,
          # återinfört av sin egen fix. '.claude/**/.*' täcker dot-filer på alla
          # nivåer under katalogen och är verifierad mot en nästlad probe-sökväg.
          #
          # .claude/settings.json INGÅR MEDVETET (TASK-71 AC#5). Den är JSON och
          # kan inte grindas av markdownlint, Vale eller lychee — men den är inte
          # ovaliderad: `npx @biomejs/biome check .` i lint-jobbet, som kör ALLTID
          # (inget if-, inget needs-villkor), traverserar .claude/ och fäller på
          # trasig eller ofärdig JSON. Verifierat med probe-fil 2026-07-28: en
          # felformaterad .claude/-JSON rapporteras av `biome check .`. Filens
          # grind byter alltså jobb, den försvinner inte. Empirin bakom valet:
          # ab52cd5, 7b60dc1, b48ece0 och 8b98e95 rörde ENBART den filen och drog
          # var och en full svit bakom staging-mutexen.
          quotepath: false
          # ═══ PARITETS-MARKÖRER (TASK-85) ═══
          # Raderna `# paritet:start …` / `# paritet:slut …` avgränsar en LISTA
          # som scripts/check-listparitet.sh läser och jämför mot sin motpart.
          # Vilka par som finns, och varför, står i .listparitet-policy.conf.
          #
          # MARKÖRERNA MÅSTE STÅ UTANFÖR `files: |`, aldrig inne i blocket. Det
          # är samma regel som "NOT OM FORMEN" ovan slår fast: en `#`-rad inne i
          # en literal block scalar blir en literal glob-post, inte en kommentar.
          # Verifierat efter placeringen med js-yaml, yamllint och actionlint —
          # blockets värde är oförändrat med markörerna på plats.
          #
          # DENNA LISTA BÄR TVÅ PAR PÅ EN GÅNG: `!`-raderna jämförs mot D1:s och
          # acceptance-klassens (kommentarerna i de stegen säger IDENTISKA), och
          # de positiva raderna mot changed-docs-stegets (PARAD ÄNDRING ovan).
          # paritet:start klassning-d0
          files: |
            **/*.md
            LICENSE
            docs/**
            tasks/**
            tests/vale-regression/**
            scripts/test-vale-regression.sh
            .github/ISSUE_TEMPLATE/**
            .github/PULL_REQUEST_TEMPLATE.md
            .github/CODEOWNERS
            .vscode/extensions.json
            .editorconfig
            .lycheeignore
            .vale.ini
            .vale/**
            .markdownlint-cli2.jsonc
            .claude/**
            .claude/**/.*
            !.github/workflows/**
            !.github/dependabot.yml
            !package.json
            !package-lock.json
            !audit-ci.jsonc
            !tsconfig*.json
            !biome.json
            !vite.config.ts
            !playwright.config.ts
            !tsr.config.json
            !.nvmrc
            !.gitignore
          # paritet:slut klassning-d0

      - name: Get changed docs files
        id: changed-docs
        # Separat docs-härledning (väg ii-a, Session 11): docs_changed ska bli true
        # även vid .lycheeignore-/ci.yml-ändring (lychee-config/ignore re-validerar
        # docs-link-check — stänger skip-gapet). Hålls ISÄR från should_skip_tests-
        # steget så det medvetna !.github/workflows/**-valet bevaras (en ci.yml-only-
        # commit kör fortfarande Test+Build via det orörda only_changed-filtret).
        #
        # S91: listan utvidgad så den TÄCKER should_skip_tests-stegets docs-poster.
        # Två klasser lades till, av två olika skäl:
        #   (a) .vale.ini / .vale/** / .markdownlint-cli2.jsonc — nya i BÅDA listorna.
        #       Paret är obligatoriskt: utan raden här hade skip-tests-tillägget gjort
        #       en ensam-ändring av dem tyst ovaliderad (svit skippad + docs skippad).
        #   (b) tests/vale-regression/** + scripts/test-vale-regression.sh — stod
        #       REDAN i skip-tests-listan men saknades här, alltså ett befintligt
        #       fail-open: en ensam-ändring skippade testsviten OCH docs-jobbet, och
        #       docs-jobbet är det enda som kör Vale-regressionssviten. Shellcheck i
        #       det alltid-på lint-jobbet gav syntaxtäckning men ingen funktionell.
        # Invarianten framåt: docs-verktygs-config hör hemma i BÅDA listorna.
        #
        # S91 forts. (TASK-71): .claude/** tillagt i BÅDA listorna i SAMMA
        # ändring — den parade formen, inte en rad i taget. Docs-grindarnas
        # scope utvidgades samtidigt så posten faktiskt validerar något:
        # .claude/**/*.md lintas nu av markdownlint (.markdownlint-cli2.jsonc
        # globs + .claude/.markdownlint.jsonc), av Vale (npm run lint:prose) och
        # av lychee (arg-blocket i docs-jobbet nedan). Utan raden här hade
        # skip-tests-tillägget gjort en agentfil BÅDE svit-skippad och
        # docs-skippad — tyst ovaliderad, L322-klassen.
        #
        # POSTERNA ÄR .claude/** + .claude/**/.*, INTE .claude/**/*.md, och det
        # är avsiktligt: listorna ska vara IDENTISKA post för post. En snävare
        # post här hade lämnat .claude/settings.json ensam i skip-tests-listan
        # och därmed återinfört precis den asymmetri paret finns för att
        # förhindra. Att settings.json grindas av Biome i det alltid-på
        # lint-jobbet (se skip-tests-steget) ändrar inte formen på invarianten.
        # Varför dot-raden är en egen post: se samma steg — '.claude/**' missar
        # dot-filer, och .claude/.markdownlint.jsonc är en av dem.
        uses: tj-actions/changed-files@9426d40962ed5378910ee2e21d5f8c6fcbf2dd96  # v47.0.6
        with:
          # quotepath: false — samma invariant som changed-files-steget
          # (TASK-15 S69): utan den ger en UTF-8-namngiven .md-ändring
          # docs_changed=false → Docs link check TYST SKIPPAD på en äkta
          # docs-ändring (allvarligare granne till only_changed-symptomet).
          quotepath: false
          # paritet:start klassning-docs (se markör-noten i changed-files-steget)
          files: |
            **/*.md
            .lycheeignore
            .vale.ini
            .vale/**
            .markdownlint-cli2.jsonc
            tests/vale-regression/**
            scripts/test-vale-regression.sh
            .github/workflows/ci.yml
            .claude/**
            .claude/**/.*
          # paritet:slut klassning-docs

      - name: Get changed UI files (D1-klassning)
        id: changed-ui
        # D1-klassen (task-36.3): en ALLOWLIST, aldrig blocklist. only_changed
        # blir true ENDAST när varenda ändrad fil matchar en UI-positiv OCH ingen
        # matchar en exkludering. Exkluderingarna är IDENTISKA med
        # should_skip_tests-stegets (AC#2): CI-workflow, paketmanifest, låsfil och
        # byggkonfig kan ALDRIG bli D1 — även om de ligger i samma commit som en
        # CSS-fil faller hela commiten till full svit (allowlist-golvet, AC#4).
        # quotepath: false — samma invariant som de andra stegen (TASK-15).
        # Bevisas med kontrastbevis-tripel, inte resonemang (AC#6).
        uses: tj-actions/changed-files@9426d40962ed5378910ee2e21d5f8c6fcbf2dd96  # v47.0.6
        with:
          quotepath: false
          # paritet:start klassning-d1 (se markör-noten i changed-files-steget)
          files: |
            **/*.css
            src/styles/**
            public/**
            !.github/workflows/**
            !.github/dependabot.yml
            !package.json
            !package-lock.json
            !audit-ci.jsonc
            !tsconfig*.json
            !biome.json
            !vite.config.ts
            !playwright.config.ts
            !tsr.config.json
            !.nvmrc
            !.gitignore
          # paritet:slut klassning-d1

      - name: Get changed acceptance-class files (klass-lokal ändring)
        id: changed-acceptance
        # ACCEPTANCE-KLASSEN I RISK-KLASSNINGEN (task-59.7, AC#3 / ADR-080).
        # Klassen fick eget mutexfritt jobb i task-59.3 men ingen plats i
        # klassnings-mekaniken: en ändring som BARA rör tests/acceptance/**
        # drog fortfarande med sig hela sviten, inklusive staging bakom den
        # globala mutexen. Filerna som klassas här kan per konstruktion inte
        # påverka staging — acceptance-projektet har egen testDir, körs mot
        # fixturvärlden, och ingen fil under tests/e2e, tests/api eller
        # tests/a11y importerar från katalogen (verifierat med grep 2026-07-28).
        #
        # SAMMA LEVER SOM D1, EGEN KLASSNING. Utfallet wirades till suite-jobbets
        # run_staging-input, exakt som ui_low_risk — inte till en ny input.
        # Skälet var att den dyra resursen är EN: staging-mutexen. En andra input
        # hade gett två sätt att uttrycka samma sak och därmed en ny
        # divergens-yta i precis den mekanik ADR-077 byggde för att slippa den.
        # SEDAN A7:5 (TASK-70.3) HAR LEVERN INGEN KONSUMENT — se outputs-blocket
        # ovan och `with: run_staging:` i suite-jobbet.
        #
        # INGET NYTT D-NUMMER MINTAS HÄR. ADR-077 diskuterar D2-slotten
        # explicit och lämnar den öppen för en framtida testgrafs-design;
        # klassen tar därför ett beskrivande namn i stället för att beslagta ett
        # nummer som är reserverat för en annan idé.
        #
        # ALLOWLIST, ALDRIG BLOCKLIST (ADR-077 §1). only_changed blir true
        # ENDAST när VARENDA ändrad fil ligger under tests/acceptance/**. En
        # commit som rör både klassen och något annat faller till full svit.
        #
        # DEN FARLIGA FRÅGAN, VERIFIERAD I KÄLLAN OCH INTE ANTAGEN: blir
        # only_changed vakuöst `true` när INGEN fil matchar allowlisten? Det
        # hade släckt staging för varenda PR som inte rör klassen — ett
        # katastrofalt fail-open. Svaret är NEJ, läst i actionens källa vid
        # EXAKT den pinnade SHA:n ovan (src/changedFilesOutput.ts):
        #   onlyChanged = otherChangedFiles.length === 0
        #              && allChangedFiles.paths.length > 0
        #              && filePatterns.length > 0
        # Andra ledet kräver minst EN matchande fil. Noll matchningar ⇒ false ⇒
        # full svit. Kontrollera om vid varje SHA-bump av actionen.
        # Exkluderingarna nedan är IDENTISKA med D0:s och D1:s. De är i dag
        # REDUNDANTA — ingen av dem kan matcha under tests/acceptance/** — och
        # står här som klassrymdens invariant, inte som bärande logik: skulle
        # allowlisten någon gång vidgas är golvet redan på plats. Läs dem inte
        # som att de gör något i nuvarande form.
        #
        # quotepath: false — samma invariant som de andra stegen (TASK-15).
        uses: tj-actions/changed-files@9426d40962ed5378910ee2e21d5f8c6fcbf2dd96  # v47.0.6
        with:
          quotepath: false
          # paritet:start klassning-acceptance (se markör-noten i changed-files-steget)
          files: |
            tests/acceptance/**
            !.github/workflows/**
            !.github/dependabot.yml
            !package.json
            !package-lock.json
            !audit-ci.jsonc
            !tsconfig*.json
            !biome.json
            !vite.config.ts
            !playwright.config.ts
            !tsr.config.json
            !.nvmrc
            !.gitignore
          # paritet:slut klassning-acceptance

      - name: Acceptance-urval — kör den delmängd diffen rör
        id: acceptance-urval
        # TASK-75 / ADR-077 §§ 1+3. Efter A7:5 (TASK-70.3) är `Acceptance
        # (hermetisk)` ENSAM bärare av PR-grindens kritiska väg — mätt till
        # 422/433/422 s över tre fulla körningar 2026-07-28, alltså cirka 50 s
        # LÅNGSAMMARE än den staging-svit som flyttades bort. Klassen kör alla
        # 18 spec-filer oavsett vad diffen rör; detta steg väljer delmängden.
        #
        # STEGET ÄGER NOLL GLOBAR, OCH DET ÄR HELA FORMEN. Det läser
        # `changed-files`-stegets EGEN `other_changed_files` — per actionens
        # källa vid den pinnade SHA:n exakt "de ändrade filer som inte matchade
        # D0-listan", inklusive dem som slogs ut av `!`-exkluderingarna. D0:s
        # lista förblir därmed ENDA hemvist för docs-klassningen. En TREDJE
        # hand-hållen kopia (filen bär redan två, med uttrycklig varning om att
        # paret hålls för hand) vore precis den divergens-skuld ADR-077
        # § Beslut 1 avvisar när den slår fast att klassningen "förblir
        # deklarativ i changed-files-steget".
        #
        # VARFÖR D0-LISTAN DUGER SOM NEUTRAL GRUND: den är redan betrodd för ett
        # STARKARE påstående — att hoppa HELA testsviten. Att här bara låta dess
        # filer vara neutralt sällskap åt en spec-ändring är strikt svagare än
        # vad repot redan gör. Och det är inte akademiskt: repots arbetsregel
        # lägger kortändringen i SAMMA commit som koden, så varje verklig
        # spec-PR bär en `backlog/**`-fil. Mätt på de två faktiska spec-PR:erna
        # i historiken — `#377` (`990add4a2`) och `#368` (`d7b9558b2`) — bestod
        # bådas diff av exakt ett kort plus 2 respektive 3 spec-filer. Utan
        # docs-klassen som neutralt sällskap hade urvalet aldrig kunnat falla ut
        # en enda gång, och mekaniken varit en tyst nolla.
        #
        # ALLOWLIST, ALDRIG BLOCKLIST: skriptet ger urval ENDAST när VARENDA
        # post är en acceptance-spec-fil som finns på disk. En källfil, den
        # delade sömmen tests/acceptance/acceptance-bas.ts, en workflow eller
        # en okänd filtyp ⇒ tomt ⇒ full klass. Okänt givet faller till full svit.
        #
        # SKYDDSNÄTET: post-merge.yml skickar ingen input och kör därmed full
        # klass på varje mergat träd (ADR-077 § Kontext, Google-modellen).
        env:
          # Karantän i env, aldrig interpolerat in i skalet: `other_changed_files`
          # är angriparstyrbar via filnamn i en PR (ADR-029 § script injection).
          OVRIGA_ANDRADE: ${{ steps.changed-files.outputs.other_changed_files }}
        run: |
          set -euo pipefail
          urval=$(bash scripts/acceptance-urval.sh "${OVRIGA_ANDRADE}")
          echo "acceptance_urval=${urval}" >> "${GITHUB_OUTPUT}"

      - name: Merge-dedup — hoppa tungt om trädet redan bevisats grönt
        id: dedup
        # task-36.4 / ADR-077. Innehållsadresserad dedup UTAN ny lagringsyta:
        # på en main-push (merge-commit) läses andra föräldern (den mergade
        # PR-headen), och de tunga jobben hoppas ENDAST om merge-commitens träd
        # är identiskt med PR-headens träd OCH den SHA:n redan har en grön
        # CI-run. Sundheten vilar på merge-grindens strict up-to-date-krav
        # (ADR-076): en up-to-date-branch ger merge-commit vars träd == PR-head.
        #
        # ASYMMETRIN ÄR MEDVETEN: nyckeln ÄR trädet självt, så dedupen kan bara
        # orsaka onödig körning, aldrig släppa förbi otestat. FAIL-CLOSED på
        # VARJE avvikelse — ingen andra förälder, träd-avvikelse, API-fel eller
        # icke-grön run ⇒ dedup_hit=false ⇒ full svit. Full SHA mot API:t (L314;
        # förkortad SHA ger noll träffar). Bor i changed-jobbet som redan har
        # fetch-depth: 0 → fetch-depth-invarianten (ADR-039/054) ORÖRD.
        # Logiken unit-testad lokalt mot 6 grenar (1 hit + 5 fail-closed) före
        # bygget. Cache-formen är FALSIFIERAD och byggs aldrig (L325).
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          set -euo pipefail
          dedup_hit=false
          if [ "${EVENT_NAME}" = "push" ]; then
            if head2=$(git rev-parse --verify HEAD^2 2>/dev/null); then
              tree_head=$(git rev-parse 'HEAD^{tree}')
              tree_head2=$(git rev-parse "${head2}^{tree}")
              if [ "${tree_head}" = "${tree_head2}" ]; then
                conc=$(gh run list --commit "${head2}" --workflow ci.yml \
                  --json conclusion,status \
                  --jq '[.[] | select(.status == "completed")][0].conclusion // ""' \
                  2>/dev/null || echo "APIERR")
                if [ "${conc}" = "success" ]; then
                  dedup_hit=true
                  echo "✅ Dedup-TRÄFF: träd == ${head2:0:12}^{tree} och den SHA:n har grön CI-run → tunga jobb hoppas."
                else
                  echo "Dedup-miss: PR-head-run conclusion='${conc}' ej success → full svit (fail-closed)."
                fi
              else
                echo "Dedup-miss: träd-avvikelse (${tree_head:0:12} != ${tree_head2:0:12}) → full svit (fail-closed)."
              fi
            else
              echo "Dedup-miss: ingen andra förälder (ej merge-commit) → full svit (fail-closed)."
            fi
          else
            echo "Dedup ej tillämplig (event=${EVENT_NAME}, ej push) → full svit."
          fi
          echo "dedup_hit=${dedup_hit}" >> "${GITHUB_OUTPUT}"

  # Job 2: Always-on lint + audit + typecheck.
  # Kör på ALLA commits oavsett doc-only-detection. Per ADR-029 §1b + K17.
  lint:
    name: Lint + Audit + TypeCheck
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          # fetch-depth: 0 = FULL historik (ADR-054, 2026-06-17) för
          # scripts/check-frontmatter.sh Check 2 (git log -1 --format=%cs -- <fil>).
          # Finit djup var anti-mönstret (1→50→100→250 brast 4 ggr när orörda
          # governing-docs passerade boundaryn). Konsistent med changed-jobbet;
          # invarianten (ADR-039) hålls enhetlig över alla 5 bärare.
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Audit dependencies (audit-ci with allowlist)
        run: npx audit-ci --config audit-ci.jsonc
        # K17 supply-chain-skydd: kör på varje commit (kod ELLER doc-only).
        # Allowlist tom efter K0åh (2026-05-13). ADR-028 reverse-flow aktiv.

      - name: Biome check
        run: npx @biomejs/biome check .

      - name: TypeScript check
        run: npm run typecheck

      - name: TypeScript check (tests)
        run: npm run typecheck:tests

      - name: Check workflow files (actionlint)
        # Release-pinnad install + egen SHA256-verifiering — shellcheck-
        # stegets mönster (S77 våg 1; processgranskningen 2026-07-23 § 8).
        # Ersätter ADR-029 utelämning #3:s download-script-form: skriptet
        # hämtades från MUTERBAR main och exekverade före all verifiering —
        # dess egen checksumme-koll skyddar inte mot komprometterad källa.
        # SHA256 downstream-beräknad mot fast release-asset 2026-07-23
        # (releases är immutabla); re-verifiering: ladda samma URL +
        # sha256sum lokalt. Erratum-not i ADR-029.
        # -ignore (smal, exakt feltext): actionlint 1.7.12:s schema släpar
        # efter Actions-featuren `queue:` i concurrency (plattforms-release
        # 2026-05-07; runtimen accepterar nyckeln — Test+Build grön i run
        # 29200533939 med den aktiv). LIFT: ta bort flaggan när actionlint
        # stödjer nyckeln (regexen blir då vestigial, matchar inget).
        #
        # TILLGÄNGLIGHET (TASK-83, 2026-07-29) — curl-flaggorna bär samma
        # rationale som `Install shellcheck (pinned v0.11.0)` nedan, där den är
        # skriven i sin helhet: --retry ensam täcker inte connect-fel, och
        # --retry-all-errors (curl 7.71.0+) är flaggan som gör det.
        # sha256sum-verifieringen nedan är ORÖRD.
        run: |
          AL_VERSION="1.7.12"
          AL_SHA256="8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8"
          AL_URL="https://github.com/rhysd/actionlint/releases/download/v${AL_VERSION}/actionlint_${AL_VERSION}_linux_amd64.tar.gz"
          curl -fsSL --retry 5 --retry-all-errors --retry-max-time 60 "${AL_URL}" -o /tmp/actionlint.tar.gz
          echo "${AL_SHA256}  /tmp/actionlint.tar.gz" | sha256sum -c
          tar -xzf /tmp/actionlint.tar.gz -C /tmp actionlint
          /tmp/actionlint -color -ignore 'unexpected key "queue" for "concurrency" section'

      - name: Install yamllint (pinned 1.38.0)
        # TASK-312 (2026-08-24): repots ENDA CI-verktyg utan version-pin
        # fram till detta kort — ett flytande `pip install yamllint` kunde
        # tyst hämta en ny major med andra default-regler.
        #
        # Exakt version-pin (PyPI-releaser är immutabla per version, samma
        # "byte-determinism utan behov av hash" som scripts/lib/
        # supabase-cli.sh § VARFÖR VERSIONEN INTE PINNAS I package.json
        # resonerar för npx-installerade paket). SHA256-hash-verifiering
        # (full parity med shellcheck/actionlint/vale-mönstret nedan)
        # MEDVETET AVSTÅDD: pip:s `--require-hashes`-läge kräver att VARJE
        # transitiv dependency (yamllint drar in `pathspec`) OCKSÅ pinnas
        # exakt + hash-verifieras — empiriskt bekräftat 2026-08-24
        # (`pip install --require-hashes` avvisar med "In --require-hashes
        # mode, all requirements must have their versions pinned with =="
        # så fort en icke-pinnad transitiv dependency finns i trädet). Det
        # är en betydligt skörare underhållslast än en enda fristående
        # static binär utan dependencies (shellcheck/actionlint/vale) —
        # varje framtida yamllint-release som ändrar/lägger till en
        # dependency bryter CI på ett sätt som inte har med YAML-innehållet
        # att göra. Version-pinningen ensam stänger den faktiska risken
        # kortet flaggar (flytande install, ingen determinism) utan den
        # kostnaden.
        #
        # SHA256 för denna version, bokförd som referens (INTE verifierad
        # av detta steg, se ovan för varför): verifierad två oberoende
        # vägar 2026-08-24 (PyPI JSON-API + lokal `pip download` +
        # `shasum -a 256`, identiska) —
        # yamllint-1.38.0-py3-none-any.whl =
        # fc394a5b3be980a4062607b8fdddc0843f4fa394152b6da21722f5d59013c220
        #
        # Python pre-installed på ubuntu-latest; pip-install ~3-5s.
        run: pip install --quiet "yamllint==1.38.0"

      - name: Check YAML syntax (yamllint)
        # Per ADR-030 § Del 1.4: yamllint kompletterar actionlint (yamllint =
        # generic YAML syntax/style; actionlint = GitHub Actions semantics).
        # Config: .yamllint.yml. Scope: .github/ (workflow + dependabot).
        # Ingen pre-commit-hook — CI-only-grindvakt per Marcus' direktiv K2.
        #
        # SEPARERAD FRÅN BOOTSTRAP-STEGET OVAN (TASK-312, 2026-08-24):
        # tidigare konflaterade EN run:-rad `pip install` (opinnat) +
        # `yamllint .github/` — vilket tvingade scripts/verify-ci-parity.mjs
        # till en särskild "toolPresenceLastLine"-hantering för att kunna
        # köra grinden lokalt utan att fällas av steg 1 (se
        # .ci-parity-policy.json § specialStepsRationale, och
        # verify-ci-parity.mjs:s hanteraToolPresenceLastLine() — kvar som
        # generell mekanism, men ingen policy-post namnger den längre).
        # Separationen låter DETTA steg köras verbatim lokalt (yamllint
        # antas redan finnas på PATH, ansvaret för det ligger nu helt på
        # bootstrap-steget ovan).
        run: yamllint .github/

      # ═══ PARITETS-MARKÖRER (TASK-109) ═══
      # Regionen från start-markören på nästa rad till dess slut-markör (efter
      # listparitets-steget) är B-SIDAN i
      # paret `docs-grindar` (.listparitet-policy.conf): lint-jobbets
      # `bash scripts/check-*.sh`-körningar hålls mängd-lika med
      # scripts/check-docs.sh:s run_gate-lista (post 5–13). Driftar de isär —
      # en grind CI kör men listan saknar, eller en listan har men CI inte
      # kör — fäller scripts/check-listparitet.sh. Det var TASK-98:s och
      # TASK-106:s felklass, funnen två gånger av manuell grep, aldrig av en
      # grind. REGIONEN LÄSES SOM DATA: en kommentar här inne får aldrig bära
      # frasen "bash scripts/check-<namn>.sh" (se noten i listparitets-steget).
      # paritet:start docs-grindar-ci
      - name: Check public docs for unchecked items
        # Per ADR-030 § Del 1 (position #5): scripted-checklist-check fångar
        # oavslutade `- [ ]` i publika docs (README/CHANGELOG/SECURITY/
        # CONTRIBUTING/byggplan/BYGGPLAN-LÄTTLÄST-v3). CONTRIBUTING.md
        # `## Definition of Done`-sektioner exkluderade via per-sektion-
        # medveten awk-pattern. Snabb bash (<1s overhead). Ingen pre-commit-
        # hook (CI-only). Empirisk 4-test-suite verifierad vid K5-
        # implementation 2026-05-14; utvidgad till 5-test-suite vid K7.5
        # config-driven-refactor 2026-05-15. Config: .checklist-policy.conf.
        run: bash scripts/check-public-checklists.sh

      - name: Validate frontmatter on governing docs
        # Per ADR-030 § Del 3: 5-check-validator för YAML-frontmatter på
        # 17 styrande docs (TASK-161.4, 2026-08-08 — räknat om mot
        # .frontmatter-policy.conf FRONTMATTER_GOVERNING_DOCS; stod som "14"
        # (TASK-161.2) innan CONTRIBUTING.md/README.md/DESIGN-SYSTEM-SPEC.md
        # lades till listan) (existens, updated-match, review_by > today,
        # status-enum, owner-enum). Config: .frontmatter-policy.conf.
        # Empirisk 9-test-suite verifierad K7.B 2026-05-14.
        # Hub-spoke-portabel design per K7 Lesson #6 (UNIVERSAL).
        run: bash scripts/check-frontmatter.sh

      - name: Validate lifecycle field on session docs (ADR-052 gate)
        # ADR-052 beslut 4: dedikerad lätt grind (enum + konsistens fält↔kropp),
        # skild från frontmatter-governing-regimen (immutabilitets-skäl, ADR-023).
        run: bash scripts/check-lifecycle.sh

      - name: Validate thread index integrity (TASK-108 gate)
        # Komplement till check-lifecycle.sh, som loopar över trådFILER och tyst
        # hoppar varje fil utan lifecycle:-fält — mätt täckning 13 av 109 trådar
        # (11,9 %) när grinden byggdes. Denna grind validerar INDEXET självt
        # (tasks/threads/README.md): radform + kolumnantal, enum-giltigt
        # tillstånd i RÄTT kolumn, numreringens integritet (dubblett/lucka/
        # omkastning) och index↔fil åt båda håll. Ingen invariant överlappar
        # lifecycle-grinden — två grindar med rätt att säga emot varandra är
        # värre än en (skriptets § MEDVETET UTANFÖR SCOPE).
        #
        # EGET STEG HÄR ÄR NÖDVÄNDIGT, INTE EN DUBBLERING: TASK-108:s
        # wiring-beskrivning antog att grinden "följer med automatiskt via
        # check-docs.sh", men check-docs.sh körs INTE av CI (verifierat
        # 2026-07-31: noll run-träffar i något workflow — den är den LOKALA
        # samlaren som speglar detta jobb). Utan detta steg hade check-docs.sh
        # räknat upp grinden under rubriken "ci.yml lint-jobbet" medan CI körde
        # den noll gånger — exakt ADR-083-felklassen som TASK-98 lagade för
        # check-permissions-claims.sh.
        #
        # Ren textläsning (policy-driven, .thread-index-policy.conf — som sedan
        # denna ändring står i shellcheck-scopet nedan). Inget nätverk, inga
        # secrets. KOSTNADEN ÄR INTE FÖRSUMBAR: 41,0 respektive 43,4 s LOKALT
        # mätt (två körningar, macOS 2026-08-01, 98 % CPU — beräkningsbunden
        # bash-regex över registrets långa rader, inte maskinlast). CI-tiden är
        # INTE mätt av mig; jobbet bar 83–103 s mot taket 300 s före detta steg
        # (tre gröna main-körningar 2026-07-31), så marginalen bär steget även
        # om CI-talet blir högre än det lokala. Läs av den första skarpa
        # körningen; krymper marginalen är grindens regex (inv 3-länkmatchningen
        # mot långa rader) första optimeringskandidat — inte avwiring.
        run: bash scripts/check-thread-index.sh

      - name: Check ADR count consistency (ADR-039 gate)
        # Per ADR-039 § kadens-principen: deterministisk konsistens-check vid
        # VARJE push (ej bara fas-avslut). ADR-fil-antal == README:s kanoniska
        # levande räkning, nyckar mot samma "[0-9]+ arkitekturbeslut"-token som
        # phase-end-verify.sh head -1:ar. Stänger K0a-driften (README 28 vs 38).
        run: bash scripts/check-adr-count.sh

      - name: Check lesson numbering (ADR-081 gate)
        # Per ADR-081: nummer tilldelas VID LANDNING, inte vid skrivning.
        # Två invarianter — ingen duplicerad `### L<nnn>` i lessons.md, och
        # inget fragment i tasks/lessons.d/ som bär ett nummer. Empirin är
        # S91 2026-07-26: två parallella agenter mintade båda L354+L355,
        # kollisionen löstes för hand. Frånvarande fragment-katalog är GRÖNT
        # (vägen är tillgänglig, ej obligatorisk). Config: .lesson-policy.conf.
        run: bash scripts/check-lesson-numbers.sh

      - name: Check permissions claims (ADR-083 gate)
        # Per ADR-083: en styrande fil får inte påstå en permissions-mekanism
        # som inte finns. Nämner CLAUDE.md / CONTRIBUTING.md / agentdefinitionerna
        # `permissions.deny` eller `permissions.ask` tillsammans med en
        # påstående-markör, ska nyckeln finnas och vara icke-tom i en
        # settings-fil. Config: .permissions-claims-policy.conf.
        #
        # WIRAD FÖRST I TASK-98, och frånvaron dessförinnan är hela poängen.
        # ADR-083 mintades 2026-07-30 för prosa som påstår sig vara mekanism.
        # Grinden byggdes samma dag, kopplades in i `npm run check:docs` och
        # fick sin self-test körd här — men grinden SJÄLV wirades aldrig.
        # check-docs.sh:34-46 räknade ändå upp den under rubriken "ci.yml
        # lint-jobbet (kör alltid, även på kod-PR:er)". Mätt 2026-07-30 med
        # `grep -c 'bash scripts/<grind>.sh' ci.yml`: de fem syskongrindarna 1
        # var, denna 0. Alltså prosa som påstod en mekanism som inte fanns —
        # ADR-083:s egen felklass, i ADR-083:s egen grind. Funnen av TASK-85:s
        # agent, som klassade den som ett sjätte listparitets-par med faktisk
        # drift och eskalerade i stället för att bygga (CI-verkan = scope-beslut).
        #
        # HÄR OCH INTE I DOCS-JOBBET, och skälet är smalare än det först ser ut.
        # Lint-jobbet har varken `if:` eller `needs:` och kör alltså alltid;
        # docs-jobbet är villkorat på `docs_changed`.
        #
        # Det NAIVA argumentet håller inte, och står här för att ingen ska bygga
        # om det: de filer grinden LÄSER — de fyra styrande .md-filerna och
        # `.claude/settings.json` — ligger under `**/*.md` respektive `.claude/**`
        # i `klassning-docs`. En ändring av dem sätter alltså `docs_changed`, och
        # docs-jobbet hade kört i just de fallen.
        #
        # Det argument som FAKTISKT bär är grindens EGNA in-filer. Varken
        # `.permissions-claims-policy.conf` (GOVERNING_FILES, CLAIM_MARKERS,
        # CLAIM_WINDOW) eller `scripts/check-permissions-claims.sh` står i
        # `klassning-docs`-listan ovan. En PR som lägger till en styrande fil som
        # redan bär ett falskt påstående, stryker en markör, eller försvagar
        # skriptet, hade med grinden i docs-jobbet landat UTAN att grinden kördes.
        # Verifierat 2026-07-31: `grep -n 'permissions-claims' ci.yml` ger noll
        # träffar i klassnings-blocken.
        #
        # Andra halvan är kopplingens SKÖRHET. Att grinden körs på sin lästa yta
        # skulle bero på att `klassning-docs` råkar innehålla `.claude/**` — en
        # koppling ingen fil deklarerar och ingen grind vaktar. Att listan är
        # levande är mätbart: paret `klassning-docs-config` bär nio deklarerade
        # asymmetrier i .listparitet-policy.conf (åtta på D0-sidan, en på
        # changed-docs-sidan). Driftar `.claude/**` bort ur den upphör grinden
        # tyst att köra på sin egen lästa yta. Ett alltid-på jobb har ingen sådan
        # beroendekedja.
        #
        # FÖRKASTAT ALTERNATIV — rätta prosan i check-docs.sh i stället, alltså
        # flytta posten ur lint-rubriken och skriva att grinden bara körs lokalt.
        # Det hade gjort texten sann och lämnat hålet kvar. ADR-083 dömer den
        # utvägen i sin egen alternativ-sektion, om hub-repot: "En grind som
        # ingen kör är inte en grind, utan just den sortens artefakt denna ADR
        # handlar om." Samma mening gäller spoke-sidan. Prosa-rättning hade
        # dessutom sänkt skyddet till noll för `CLAUDE.md` och `CONTRIBUTING.md`
        # på varje PR där ingen råkar köra `npm run check:docs` för hand.
        #
        # Ren textläsning plus `jq` mot två settings-filer. Inget nätverk, inga
        # secrets. 0,066–0,079 s LOKALT mätt över tre körningar (macOS,
        # 2026-07-31, loadavg 3,6–3,9). CI-TIDEN ÄR INTE MÄTT AV MIG och kan
        # avvika åt båda håll.
        run: bash scripts/check-permissions-claims.sh

      - name: Check fetch-depth invariant (ADR-039 gate)
        # Per ADR-039 + L50: de 6 LEVANDE fetch-depth-bärarna (ci.yml ×4 jobb +
        # .frontmatter-policy.conf + check-frontmatter.sh-default) hålls
        # ömsesidigt identiska; ADR-029/030 (frusen text) bär erratum-not.
        # Frusen ADR-text/arkiv/kommentarer ignoreras (läser bara namngivna
        # bärar-platser). L50:s obyggda test-hälft.
        run: bash scripts/check-fetch-depth-invariant.sh

      - name: Check list parity (TASK-85 gate)
        # Repot bär flera invarianter som står på TVÅ ställen och som filerna
        # SJÄLVA säger ska hållas i synk: "PARAD ÄNDRING (obligatorisk)",
        # "Exkluderingarna är IDENTISKA med should_skip_tests-stegets", "speglar
        # det blocket i sak … får inte glida isär". ci.yml skrev till och med ut
        # att mekaniken saknades: "ingen grind håller dem synkade — paret hålls
        # för hand". Fem par vaktas nu; vilka, och varför just de, står härlett i
        # .listparitet-policy.conf.
        #
        # HÄR OCH INTE I DOCS-JOBBET, trots att ett av paren läser CONTRIBUTING.md:
        # tre av fem par bor i DENNA fil, och docs-jobbet är villkorat på
        # docs_changed. En ci.yml-only-ändring hade då kunnat drifta klassnings-
        # listorna utan att grinden kördes. Lint-jobbet kör alltid.
        #
        # Ren textläsning, inget nätverk. 1,15–1,28 s väggtid över tre körningar,
        # LOKALT mätt (macOS, 2026-07-30, loadavg 3,97–4,13, `time` på
        # skriptet). CI-TIDEN ÄR INTE MÄTT AV MIG
        # och kan avvika åt båda håll — en lokal mätning projicerad till CI är
        # ingen mätning.
        #
        # ORDALYDELSEN OVAN ÄR MEDVETET INDIREKT (TASK-109): raden skrev
        # tidigare ut kommandot med bakåtfnuttar, och den frasen matchade
        # paret `docs-grindar`-uttrycket `bash scripts/check-[a-z0-9-]+\.sh`.
        # Detta är regionens ENDA kommentar som matchade — mätt 2026-08-01:
        # med körnings-raden nedan borttagen gav hela-filen-extraktionen ändå
        # posten ur just denna kommentar, dvs paret hade rapporterat synk på en
        # avwirad grind (TASK-98:s felklass). Skriv ALDRIG frasen
        # "bash scripts/check-<namn>.sh" i en kommentar mellan
        # paritet-markörerna — grinden läser regionen som data.
        run: bash scripts/check-listparitet.sh

      - name: Validate locked facit is addressable (ADR-102 gate)
        # Per ADR-102 B1/B3: den låsta prototypen ÄR facit, och prototyp-kod
        # rivs aldrig före Marcus godkännande. Grinden läser facit-manifesten
        # under tasks/sessions/bilagor/ och fäller på (a) facit-bilder utan
        # manifest, (b) manifest som inte stämmer med disken — odeklarerad
        # bild, saknad bild, yta utan "bilder"-nyckel — och (c) rivning av
        # prototyp-markörerna medan "godkand" är null.
        #
        # ROTORSAKEN, mätt: TASK-145.3:s utförare rapporterade "bilderna finns
        # inte i repot och fanns inte i uppdraget" och byggde vidare mot en
        # annan baslinje — dagen EFTER att bilderna landat. Ett misslyckat
        # uppslag lästes som frånvaro. Grinden gör det uppslaget omöjligt att
        # missa: facit har en deklarerad adress, eller så är trädet rött.
        #
        # Grinden avgör INTE om ytan SER UT som facit — den jämförelsen är
        # Marcus öga. Config: .facit-policy.conf.
        run: bash scripts/check-facit.sh
      # paritet:slut docs-grindar-ci

      - name: Check mall parity (TASK-309.4 gate, ADR-125 § Beslut 4)
        # De genererade TS-strängmodulerna i
        # supabase/functions/_shared/mallar/ (html/css/typsnitt/bilder) MÅSTE
        # vara byte-identiska (efter avkodning) mot sin källa i
        # docs/mallar/bilagor/ och public/fonts/bilagor/ — annars renderar
        # prod-EF:en (`_shared/mall-render.ts`) en ANNAN mall än den Marcus
        # granskat. TASK-309.1s minimaltest bevisade skarpt att
        # `static_files`-bundling FALLERAR i denna miljö (ingen Docker,
        # API-bundlingsvägen), vilket är VARFÖR en committad kopia behövs
        # över huvud taget i stället för att läsa `docs/`-filerna direkt vid
        # deploy — se ADR-125 § Updates 2026-08-23.
        #
        # EN generator, TVÅ lägen (scripts/synka-bilagemallar.mjs, sync
        # ELLER --check) — check-mallparitet.sh är en tunn wrapper, ingen
        # fjärde handhållen kopia av jämförelselogiken (samma
        # --kontrollera/--deploya-disciplin som scripts/fas4-prod-deploy.sh).
        # Ren fil-läsning, inget nätverk.
        run: bash scripts/check-mallparitet.sh

      - name: Check for long dashes in UI strings (TASK-172 gate, scope A)
        # Marcus-beslut 2026-08-09 (task-172 Description): "Ta bort alla
        # långa bindestreck överallt, jag gillar de korta bindestrecken (-)".
        # Scope A = användar-synliga strängar i src/ (det Lotta och Marcus
        # ser i appen) — kodkommentarer och dokumentation ingår INTE.
        #
        # HÄR OCH INTE I DOCS-JOBBET: grinden läser TypeScript/TSX-källkod
        # under src/, inte markdown. Docs-jobbet är villkorat på
        # docs_changed; en ren kod-PR hade aldrig kört grinden där. Lint-
        # jobbet kör alltid.
        #
        # AST-baserad (@babel/parser + @babel/traverse, redan en transitiv
        # dependency av @tanstack/router-cli — se skriptets eget huvud för
        # fullständig motivering), inte regex: kommentarer besöks aldrig av
        # traverse() och kan strukturellt inte fällas — endast StringLiteral/
        # JSXText/TemplateElement. Config-drivna undantag (tom-markören +
        # 11 referens-/baseline-låsta REST-poster, se .langa-streck-policy.json
        # _readme) — värden i config, logik i skriptet, per CLAUDE.md-regeln
        # om config-drivna grindvakter (Lesson #6, UNIVERSAL).
        #
        # INGEN NÄTVERKSTRAFIK, inget nytt npm-paket. ~1 s lokalt mätt (macOS,
        # 2026-08-09, 175 filer med streck-tecken av 194 totalt under src/).
        run: node scripts/check-langa-streck.mjs

      - name: "Check for mailto: paths in send flows (TASK-147.8 gate, AC #1)"
        # TASK-147 (och dess skivor) river stämplingslögnen: ett utskick är
        # bara "skickat" om SERVERN skickade det (useSendActionEmail/
        # useSendActionTestEmail), aldrig när ett klick bara öppnade
        # mail-klientens eget fönster. Denna grind gör den rivningen
        # mekaniskt fälld vid återinförsel i stället för att vila på
        # ögonkontroll — AC #1 säger det uttryckligen.
        #
        # HÄR OCH INTE I DOCS-JOBBET: samma skäl som em-dash-grinden ovan —
        # grinden läser TypeScript/TSX-källkod under src/, en ren kod-PR
        # hade aldrig kört den i docs-jobbet. Lint-jobbet kör alltid.
        #
        # AST-baserad (samma @babel/parser + @babel/traverse-motor som
        # check-langa-streck.mjs, se scripts/check-mailto.mjs eget huvud):
        # kommentarer besöks aldrig av traverse() och kan strukturellt inte
        # fällas — endast StringLiteral/JSXText/TemplateElement. EN
        # config-driven fil-scopad undantags-post (.mailto-policy.json):
        # PersonDetailPrototyp.tsx (S90 throwaway-prototyp, 1:1-
        # kontaktlänk — inte ett utskicksflöde) — värden i config, logik i
        # skriptet, per CLAUDE.md-regeln om config-drivna grindvakter
        # (Lesson #6, UNIVERSAL).
        #
        # INGEN NÄTVERKSTRAFIK, inget nytt npm-paket. 0,68 s LOKALT mätt
        # (macOS, 2026-08-10, `time node scripts/check-mailto.mjs`, 1 fil med
        # "mailto:"-substräng av ~194 totalt under src/) — CI-tiden är INTE
        # mätt av mig och kan avvika åt båda håll.
        run: node scripts/check-mailto.mjs

      - name: Test consistency gates (truth-table self-verification)
        # Per ADR-039 § lesson→grind: en grind är inte en grind förrän dess
        # fyrning FORTLÖPANDE verifieras (L43). Kontrast-testet bevisade dem
        # vid bygg-tid; dessa suiter bevisar att de fortsätter fånga drift
        # efter framtida edits. Kör varje push (snabba; changed-files-villkor
        # icke-trivialt för scripts/-domänen). Form-val: Session 8 K0b.
        run: |
          bash scripts/test-check-adr-count.sh
          bash scripts/test-check-fetch-depth-invariant.sh

      - name: Test gatekeeper script suites (frontmatter / checklists / hook / ci-wait)
        # Per K6-auditen (Session 17): 3 av 6 grindvakts-testsviter körs
        # tidigare endast manuellt — samma kadens-missmatch-felklass som
        # ADR-039 stänger. Kör varje push; snabba (~31 s summa, Grind B-mätt);
        # sandboxade i egna /tmp-kataloger med trap-cleanup (rör aldrig
        # repots .git — test-pre-commit-hook fixture-git-init:ar i /tmp).
        # Changed-files-villkor för scripts-domänen avfärdat som icke-
        # trivialt — samma motivering som consistency-gates-steget ovan.
        # STOPPA-beslut B (Session 17 K7): eget steg med ärligt namn i
        # stället för att utöka ADR-039-stegets — stegnamn ska inte ljuga
        # om sitt innehåll.
        #
        # test-agent-spawn-log.sh (S91/A7:2) hör hit under stegets hook-hälft:
        # den prövar Claude Code-hooken som skriver spawn-mätningens logg. Den
        # är en MÄTARE, inte en grind — men samma kadens-argument gäller, och
        # en testsvit som inte körs fortlöpande ruttnar. ~0,3 s.
        #
        # test-ci-wait.sh (TASK-72): sviten fanns sedan S87 men kördes ALDRIG av
        # CI — exakt den kadens-missmatch raden ovan beskriver, på repots sista
        # kontroll före armering. Utan detta steg kan urvalet-av-körning-defekten
        # återuppstå tyst, vilket är vad TASK-72 AC 5 kräver stängt. Stubbad,
        # ingen nätverkstrafik. ≈13 s LOKALT mätt (macOS, 2026-07-28) — CI-tiden
        # är inte mätt av mig och kan avvika.
        #
        # test-staging-semaphore.sh (TASK-77) hör hit av EXAKT samma skäl som
        # raden ovan bokför: sviten vaktar preflighten som hindrar lokala
        # staging-körningar från att kollidera med CI, och den vakten sitter i
        # Playwrights setup-projekt — en yta ingen annan grind rör. Utan detta
        # steg kan namn-matchningen mot jobbnamnen drifta tyst, och utfallet av
        # en tyst drift är att mekanismen slutar fälla utan att någon ser det.
        # Stubbad, ingen nätverkstrafik. 7,6 s LOKALT mätt (macOS, 2026-07-29,
        # `time bash scripts/test-staging-semaphore.sh`) — CI-tiden är INTE mätt
        # av mig och kan avvika åt båda håll.
        #
        # test-check-thread-index.sh (TASK-108) hör hit av samma kadens-skäl.
        # 24 fall (var 17 t.o.m. TASK-140): 19 planterar ett känt fel per
        # invariant (T4 är falskpositiv-regressionen — ordet "paused" i prosan
        # får aldrig räknas som radens tillstånd; T11 är täcknings-
        # regressionen — trådfil UTAN lifecycle:-fält, exakt klassen
        # check-lifecycle.sh tyst hoppar) och 5 prövar grönt på korrekt
        # indata. T18–T24 (TASK-141) prövar Inv 6 (barn-manifest, ADR-095
        # beslut 4): giltig barn-tråd/-kort släpps, saknad barn-tråd/-kort/
        # förälder och okänd ID-form fälls, saknat manifest är grönt (opt-in).
        # Sandboxad i mktemp, ingen nätverkstrafik. 3,7 s LOKALT mätt (macOS,
        # 2026-08-05, `time bash scripts/test-check-thread-index.sh`) —
        # fixturerna är små, det är registrets verkliga rader som är dyra
        # (se grind-steget ovan, inte sandbox-sviten). CI-tiden är inte mätt
        # av mig och kan avvika åt båda håll.
        #
        # test-check-listparitet.sh (TASK-85) hör hit av samma kadens-skäl.
        # Sviten prövar listparitets-grinden i BÅDA riktningar (post borttagen ur
        # den ena listan, post tillagd i den andra) plus elva fail-closed-grenar.
        # Den viktigaste av dem är NOLL EXTRAHERADE POSTER: två tomma mängder är
        # lika, alltså grönt, alltså en grind som rapporterar paritet på ett par
        # den inte längre läser. Det felläget inträffade skarpt under bygget.
        # Sandboxad i egen mktemp-katalog med trap-cleanup, ingen nätverkstrafik.
        #
        # test-check-backlog-closure.sh (TASK-102, wirad T107) hör hit av samma
        # kadens-skäl — grinden själv kör i NATTEN (nightly.yml, jobbet
        # backlog-closure: en inaktuellt-tillstånd-kontroll hör inte i
        # PR-grinden, se grindens huvud § VARFÖR NATTEN), men dess testsvit ska
        # fälla FÖRE landning när någon försvagar grindens logik. Sviten kör mot
        # STUBBAT backlog-CLI (fixtur-data i mktemp-sandlåda), aldrig mot repots
        # kort — ingen nätverkstrafik, inget beroende av node_modules/.bin.
        # 50 fall, varav T49/T50 vaktar att npx-namnkollisionen (fel paket
        # `backlog` i registret) inte återinförs som default eller åtgärds-tips.
        # 24,7 s LOKALT mätt (macOS, 2026-07-31, loadavg 5,1) — CI-tiden är INTE
        # mätt av mig och kan avvika åt båda håll.
        #
        # test-stop-vakt.sh (TASK-113/ADR-087, wirad TASK-117) hör hit av samma
        # kadens-skäl. Vakten (scripts/stop-vakt.sh) själv är en Stop/
        # SubagentStop-HOOK som körs live vid varje turavslut, aldrig i CI — den
        # kan alltså inte "köras i natten" som backlog-closure-grinden ovan; det
        # ENDA som hör hemma i en pipeline är sviten som skyddar hookens LOGIK
        # mot regression innan landning. ADR-087 § Ärliga svagheter punkt 8
        # bokförde owiring som öppen uppföljning ("ci.yml var upptagen" — se
        # tasks/sessions/archive/2026-07/2026-07-26-session-91.md rad 7973-7974); TASK-117
        # stänger den. 16 fall, tvåsidigt bevis (planterat väntepåstående-fel
        # FÄLLS, korrekt avslut GRÖNT) + fail-closed-grenar + stop_hook_active-
        # genomsläpp. Sandboxad under /tmp/task-113-test-stop-vakt med
        # trap-cleanup, rör aldrig repo-rotens .stop-vakt-policy.json, ingen
        # nätverkstrafik. 1,31 s LOKALT mätt (macOS, 2026-08-01, loadavg 4,21,
        # `time bash scripts/test-stop-vakt.sh`) — CI-tiden är INTE mätt av mig
        # och kan avvika åt båda håll.
        #
        # test-check-langa-streck.mjs (TASK-172) hör hit av samma kadens-skäl:
        # grinden ovan (check-langa-streck.mjs) är inte en grind förrän dess
        # fyrning fortlöpande verifieras. 16 fall — tvåsidigt bevis (seedat
        # em-dash/en-dash/template-fel FÄLLS i tre nod-typer; ren kod, kod med
        # streck ENDAST i kommentar, tom-markör-undantag och fil-scopat
        # REST-undantag PASSERAR) plus fail-closed (saknad policy-fil) och en
        # strukturell exkludering (import-modulspecifikation). Sandboxad i
        # egen mktemp-katalog, ingen nätverkstrafik. ~0,3 s LOKALT mätt
        # (macOS, 2026-08-09) — CI-tiden är INTE mätt av mig och kan avvika.
        #
        # test-check-nattvakt-dedup.sh (TASK-180) hör hit av samma kadens-
        # skäl: prövar dedup-beslutet nightly-watchdog.yml:s "Kom natten
        # igång?"-jobb anropar (scripts/check-nattvakt-dedup.sh). 13 fall,
        # tvåsidigt bevis — SIDA 1: tom mängd, stängt UTAN kommentar och
        # stängt MED kommentar men UTANFÖR fönstret larmar fortfarande;
        # SIDA 2: öppet ärende och #1042-scenariot ordagrant (stängt MED
        # rotorsaksmotivering inom fönstret) dedupar korrekt — plus
        # fail-closed-grenar (saknad/otolkbar fake-JSON, trasig policy).
        # Riggen läser NATTVAKT_DEDUP_FAKE_JSON i stället för att fråga
        # GitHub, ingen nätverkstrafik. 0,46 s LOKALT mätt (macOS,
        # 2026-08-10, `time bash scripts/test-check-nattvakt-dedup.sh`) —
        # CI-tiden är INTE mätt av mig och kan avvika.
        #
        # test-check-sessionsdok-fonster.sh (TASK-158.4) hör hit av samma
        # kadens-skäl: grinden själv kör i NATTEN (nightly.yml, jobbet
        # sessionsdok-fonster — en inaktuellt-tillstånd-kontroll hör inte i
        # PR-grinden, se grindens huvud § HÖR I NATTEN), men dess testsvit ska
        # fälla FÖRE landning när någon försvagar grindens logik. 16 fall,
        # tvåsidigt bevis: SAMMA fixturrepo klassificeras DRIFT vid N=2 och
        # GRÖNT vid N=3 (bevisar AC #1 — fönstertalet läses ur policy-konfigen,
        # inte hårdkodat: en hårdkodad konstant hade inte kunnat flippa
        # utfallet) plus fyra fail-closed-grenar (saknat skript, saknad/tom
        # policy, otolkbar rapport, motsägelsefull rapport). Sandboxad i egen
        # mktemp-katalog med trap-cleanup, ingen nätverkstrafik. 0,66 s LOKALT
        # mätt (macOS, 2026-08-10, `time bash scripts/test-check-sessionsdok-
        # fonster.sh`) — CI-tiden är INTE mätt av mig och kan avvika.
        #
        # test-check-mailto.mjs (TASK-147.8) hör hit av samma kadens-skäl:
        # grinden ovan (check-mailto.mjs) är inte en grind förrän dess
        # fyrning fortlöpande verifieras — samma AST-motor och samma
        # fixtur-rigg som test-check-langa-streck.mjs. 13 fall — tvåsidigt
        # bevis (seedad mailto: FÄLLS i StringLiteral/TemplateElement/
        # JSXText, case-okänsligt; ren kod, mailto: ENDAST i kommentar och
        # fil-scopat undantag PASSERAR) plus fail-closed (saknad policy-fil)
        # och en strukturell exkludering (import-modulspecifikation).
        # Sandboxad i egen mktemp-katalog, ingen nätverkstrafik. 1,6 s
        # LOKALT mätt (macOS, 2026-08-10, `time node
        # scripts/test-check-mailto.mjs`) — CI-tiden är INTE mätt av mig och
        # kan avvika åt båda håll.
        #
        # test-fas4-prod-deploy.sh (TASK-272) hör hit av ett SKÄRPT skäl:
        # skriptet det prövar kör skarpa operationer mot PROD, och dess
        # viktigaste egenskap är att det INTE är en väg förbi prod-låset
        # (scripts/deny-prod-ref.sh). Den egenskapen vilar på att project-refen
        # kommer ur ARGUMENTET, aldrig ur .prod-ref-policy.conf — en enda
        # bekvämlighets-refaktor som läser refen ur policyn skulle göra låset
        # verkningslöst för varje agent, tyst. Fall 8/9 vaktar exakt den
        # invarianten i båda riktningar. Sviten rör inget nätverk: samtliga 21
        # fall (var 17 t.o.m. S108) ligger före första nätverksanropet, och de
        # skarpa vägarna är otestbara här per konstruktion (de kräver
        # prod-refen, som låset spärrar). Fall 18–21 (S108, 2026-08-24) prövar
        # STRUKTURELLT att den pinnade Supabase CLI-vägen
        # (scripts/lib/supabase-cli.sh) faktiskt är inkopplad — sourcad, guard
        # i Preflight, inga opinnade `npx supabase `-anrop kvar, ≥4
        # operationsställen på supabase_cli(). 0,26 s LOKALT mätt (macOS,
        # 2026-08-24, `time bash scripts/test-fas4-prod-deploy.sh`) — CI-tiden
        # är INTE mätt av mig.
        #
        # test-supabase-cli-policy.sh (S108, 2026-08-24) hör hit av samma
        # kadens-skäl: prövar scripts/lib/supabase-cli.sh + .supabase-cli-
        # policy.conf i BÅDA riktningar — policyfil saknas / policy utan
        # SUPABASE_CLI_VERSION / version-missmatch (T1–T3, alla FÄLLER) och
        # korrekt version (T4, PASSERAR) — plus att `supabase_cli()` faktiskt
        # anropar `npx --yes "supabase@<pinnad version>"` VERBATIM (T5–T7) och
        # att repots RIKTIGA .supabase-cli-policy.conf laddar ett äkta
        # versionsvärde (T8, sanity mot disk). 8 fall, 20 assertioner. Stubbar
        # `npx` på PATH i en mktemp-sandlåda — ingen nätverkstrafik, ingen
        # riktig Supabase-åtkomst. 0,52 s LOKALT mätt (macOS, 2026-08-24,
        # `time bash scripts/test-supabase-cli-policy.sh`) — CI-tiden är INTE
        # mätt av mig.
        #
        # test-visual-baselines-scope.sh (TASK-298) hör hit av samma
        # kadens-skäl som syskonen: den prövar den fail-closed grind som
        # avgör vilken delmängd en baseline-dispatch får köra, och en grind
        # är inte en grind förrän dess fyrning fortlöpande verifieras.
        # Sviten bär 25 fall — elva SIDA 1 (skräp-input i sju former,
        # ledande bindestreck, överlängd, noll matchande specar, och den
        # tysta varianten: exit 0 men noll spec-rader), åtta SIDA 2 (tom
        # input ger full svit, giltigt filter ger riktad, alternation,
        # ankare, sökvägsfragment) och sex INVARIANTER i
        # visual-baselines.yml. Invarianterna är skälet steget måste ligga
        # här och inte enbart hos workflowen: de vaktar att default-vägen
        # förblir byte-identisk, att den råa inputen ALDRIG interpoleras in
        # i ett run-block (GitHubs script-injection-väg) och att
        # GITHUB_TOKEN-formen inte byts mot en PAT — tre egenskaper som en
        # bekvämlighets-refaktor annars river tyst. Riggen shimmar `npm` på
        # PATH och rör därför varken nätverk eller webbläsare; ingen
        # test-only-gren finns i grinden själv. Båda mutationerna prövade
        # skarpt 2026-08-22 (default-kommandot filtrerat → fall 20 rött;
        # råa inputen interpolerad → fall 23 rött). 0,4 s LOKALT mätt
        # (macOS, 2026-08-22) — CI-tiden är INTE mätt av mig.
        #
        # test-check-mallparitet.sh (TASK-309.4) hör hit av samma kadens-skäl:
        # grinden ovan (check-mallparitet.sh, egentligen synka-bilagemallar.mjs
        # --check) är inte en grind förrän dess fyrning fortlöpande
        # verifieras. Sex fall, sandboxade i mktemp via
        # MALLPARITET_*_DIR-miljövariabler (synka-bilagemallar.mjs § filhuvud
        # — rör ALDRIG repots riktiga docs/mallar/bilagor/): sync skapar
        # html/css/font/bild-moduler + auto-upptäckt av en `<img>`-referens,
        # RIKTNING 1 (manipulerad genererad fil fälls), RIKTNING 2
        # (källdrift utan omsynk fälls), FÖRÄLDRALÖS genererad fil (borttagen
        # källa fälls), omsynk läker paritet-brottet, och byte-fidelity
        # (avkodat innehåll === källan, exakt). Ingen nätverkstrafik.
        #
        # test-jq-guard.sh (TASK-312, 2026-08-24) hör hit av samma
        # kadens-skäl som test-supabase-cli-policy.sh ovan: prövar
        # scripts/lib/jq-guard.sh + .jq-version-policy.conf i BÅDA
        # riktningar — jq saknas / policyfil saknas / policy utan
        # JQ_MIN_VERSION / version under floor (T1–T4, alla FÄLLER) och
        # version på/över floor inklusive numerisk (ej lexikografisk)
        # jämförelse och ojämnt antal versionskomponenter (T5–T8,
        # PASSERAR) — plus att repots RIKTIGA .jq-version-policy.conf
        # laddar ett äkta värde (T9), att den FAKTISKA lokala jq-binären
        # klarar den (T10), och att default-policyvägen är LIB-relativ
        # snarare än cwd-relativ (T11 — skiljer sig medvetet från
        # supabase-cli.sh:s cwd-relativa default, se jq-guard.sh §
        # POLICYFILENS SÖKVÄG). 11 fall, 18 assertioner. Stubbar `jq` på
        # PATH i en mktemp-sandlåda för T1/T4–T8/T11 — ingen
        # nätverkstrafik. 0,1 s LOKALT mätt (macOS, 2026-08-24, `time bash
        # scripts/test-jq-guard.sh`) — CI-tiden är INTE mätt av mig.
        #
        # test-gh-guard.sh (TASK-312, 2026-08-24) hör hit av IDENTISKT
        # skäl och form som test-jq-guard.sh ovan, applicerad på
        # scripts/lib/gh-guard.sh + .gh-version-policy.conf i stället för
        # jq. 11 fall, 18 assertioner. Stubbar `gh` på PATH i en
        # mktemp-sandlåda — ingen nätverkstrafik. 0,1 s LOKALT mätt (macOS,
        # 2026-08-24) — CI-tiden är INTE mätt av mig.
        #
        # TASK-185 (2026-08-26): sjutton sviter nedan var ALDRIG inwirade i
        # NÅGOT jobb — samma kadens-missmatch-felklass som resten av detta
        # steg (ADR-039 § lesson→grind, L43), avtäckt av ett fullständigt
        # svep av scripts/test-*.sh|.mjs mot samtliga .github/workflows/*.yml
        # (grep på "(bash|node) scripts/test-"), inte kortets egen — då tre
        # år gamla och ofullständiga — lista. Sex ANDRA o-wirade sviter
        # (test-check-claims-tackning.sh, test-check-merge-tree.sh,
        # test-backfill-bilagor-dokumentklass.mjs,
        # test-create-{bilagor-table,eventinnehall-modell,kvitton-table}.mjs)
        # rördes INTE: var och en bär sin egen "INTE WIRAD/AVGRÄNSAD BORT
        # (medvetet)"-kommentar i eget filhuvud (TASK-82/-139-precedent) —
        # att skriva över ett tidigare medvetet scope-beslut hör inte hemma
        # i detta kort, se PR-beskrivning/slutrapport för fullständig lista.
        #
        # test-arkivera-sessionsdok.sh (TASK-158.2, kortets eget fynd) och
        # test-check-obesvarade-larm.sh (T119 (b), kortets eget fynd) hör hit
        # av EXAKT samma skäl som test-check-backlog-closure.sh/
        # test-check-sessionsdok-fonster.sh ovan: grinden själv
        # (arkivera-sessionsdok.sh resp. check-obesvarade-larm.sh) kör i
        # NATTEN (nightly.yml, torrkörning resp. skarpt), men dess testsvit
        # ska fälla FÖRE landning när någon försvagar grindens logik.
        # Sandboxade i mktemp/äkta engångsrepo, ingen nätverkstrafik (fejkad
        # JSON via env-override i obesvarade-larm-fallet). 5,1 s resp. 2,6 s
        # LOKALT mätt (macOS, 2026-08-26).
        #
        # test-check-pausade-sessioner.sh (TASK-185 Implementation Notes,
        # tredje instansen — samma nattvakts-familj, check-pausade-
        # sessioner.sh) hör hit av identiskt skäl. 19 fall. 31,5 s LOKALT
        # mätt (macOS, 2026-08-26, loadavg förhöjd av parallell fleet-drift
        # i detta repo) — CI-tiden är INTE mätt av mig.
        #
        # test-deny-arbetsform-push.sh, test-deny-facit-godkand-skrivning.sh,
        # test-deny-frammande-huvudkatalog.sh, test-deny-grind-genom-pipe.sh,
        # test-deny-hemlighet-utskrift.sh, test-deny-precompact.sh,
        # test-deny-prod-ref.sh, test-deny-resend-send.sh och
        # test-deny-subagent-vantan.sh hör hit av EXAKT det skäl
        # test-stop-vakt.sh ovan redan etablerar: var och en prövar en
        # PreToolUse-HOOK som körs LIVE i en Claude Code-session, aldrig i
        # CI — det enda som hör hemma i en pipeline är sviten som skyddar
        # hookens LOGIK mot regression innan landning. Samtliga nio är
        # tvåsidiga (D/A/F/E-formen: fäller planterat, släpper legitimt,
        # fail-closed på trasig indata, exakt exit-kod 2), hermetiska
        # (fixturer i mktemp/TEST_DIR, PATH_NO_<verktyg>-stubbning för
        # fail-closed-fallen, inga hemliga värden i någon fixtur — se
        # respektive skripts eget filhuvud) och saknade ALLA CI-täckning
        # innan detta kort trots att flera skyddar säkerhetskritisk mekanik
        # (deny-prod-ref.sh: prod-låset: se CLAUDE.md § "Project-refen anges
        # som ARGUMENT"). test-deny-frammande-huvudkatalog.sh bygger ett äkta
        # git-repo + worktree men är uttryckligen CI-portabel (eget filhuvud
        # § "CI-PORTABILITET, MEDVETET" — processmönster-override styr bort
        # den lokala VS Code-terminal-flakigheten). 3,9–35,3 s LOKALT mätt
        # per svit (macOS, 2026-08-26, samma förhöjda loadavg som ovan) — CI-
        # tiden är INTE mätt av mig.
        #
        # test-heartbeat-svep.sh hör hit av samma hook/vakt-skäl som
        # test-stop-vakt.sh: scripts/heartbeat-svep.sh är orkestrerarens
        # persistenta landnings-svep (CLAUDE.md § Landning), körs aldrig som
        # ett CI-jobb, men dess LOGIK (fyra bitar: RÖTT/DIRTY/armerings-
        # kandidat, level-triggered per L443) måste skyddas mot regression
        # före landning. Räkningen i skriptets svit-slutrad är den
        # AUKTORITATIVA totalen (TASK-106-disciplinen: aldrig handräknad
        # hit). Hermetisk, fejkade GraphQL-svar. 17,0 s LOKALT mätt (macOS,
        # 2026-08-26).
        #
        # test-post-compact-igenkanning.sh (TASK-160.4, ADR-101 § Beslut 5)
        # hör hit av samma skäl som test-deny-precompact.sh (syskonhooken):
        # PostCompact-hookens omorienterings-injektion är LOGIK som måste
        # bevisas innan landning, oavsett att hooken själv bara körs live.
        # Hermetisk, äkta git-repo som TEST_DIR. 2,9 s LOKALT mätt (macOS,
        # 2026-08-26).
        #
        # test-stada-grenar.sh (TASK-152 AC #3) hör hit av samma kadens-skäl
        # som test-arkivera-sessionsdok.sh: tvåsidigt bevis (mergad+ingen
        # worktree raderas, ej-mergad/worktree-bunden/skyddslistad skonas) i
        # ett engångsrepo under mktemp, ingen nätverkstrafik. 7,2 s LOKALT
        # mätt (macOS, 2026-08-26).
        #
        # test-validera-review-utlatande.mjs (TASK-173.1, ADR-105) hör hit av
        # samma ADR-039-skäl som test-check-manifest-fields.mjs
        # (ci-suite.yml): en grind är inte en grind förrän dess fyrning
        # bevisas, och review-utlåtande-schemat (scripts/lib/review-
        # utlatande.mjs) är review-grindens (ADR-105) kontraktsyta.
        # Rena funktioner + ett CLI-lager (spawnSync) som bevisar att
        # exit-koderna faktiskt matchar. Hermetisk. 5,5 s LOKALT mätt (macOS,
        # 2026-08-26).
        #
        # test-review-policy.mjs (TASK-173.2, base-drift-fynd 2026-08-26 —
        # main flyttade sig under TASK-185:s eget bygge, PR #1993/#1995
        # mellanlandade) hör hit av samma ADR-039-skäl som syskonet
        # test-validera-review-utlatande.mjs ovan: policy-ytan (.review-
        # policy.json + scripts/lib/review-policy.mjs +
        # scripts/hamta-review-policy.mjs) är review-grindens ANDRA
        # kontraktsyta (ADR-105) och var lika oskyddad. AC #1 (regler läses
        # ENDAST ur `main`, aldrig ur en gren eller ett ocommittat arbetsträd)
        # bevisas i ett äkta temporärt git-repo med tre OLIKA policy-versioner
        # samtidigt (main/gren/arbetsträd) plus ett KONTRAST-test som läser
        # grenens ref och FÅR angriparens innehåll — utan den kontrasten vore
        # huvudtestet vakuöst. 44 fall. Orkestrerar-beslut (Marcus-mandat,
        # samma svep som denna rad): båda review-policy-sviterna är
        # enhetstester för skript, samma klass som de 15 övriga
        # gatekeeper-sviterna — INTE samma sak som TASK-173.4:s CI-backstopp
        # (den mekaniska spärren mot en PR utan giltigt utlåtande; byggd
        # 2026-08-28 som jobbet `review-backstopp` nedan), se CLAUDE.md
        # § Review-grinden. Hermetisk (temp-repo, node_modules-symlänk, ingen
        # nätverkstrafik). 2026-08-26 (macOS).
        #
        # test-review-risk-sektion.mjs (TASK-173.3, ÄNNU en base-drift-fynd
        # 2026-08-26 — PR #1993 landade UNDER TASK-185:s eget bygge, andra
        # gången main flyttade sig mitt i detta kort) hör hit av samma
        # ADR-039-skäl som sina två syskon ovan: risk-rendreraren
        # (scripts/lib/review-risk-sektion.mjs + scripts/uppdatera-review-
        # sektion.mjs) är review-grindens TREDJE kontraktsyta (ADR-105) och
        # var lika oskyddad. Rena funktioner (sektion A–D) + ett CLI-lager
        # mot en FAKE `gh`-binär på PATH (sektion E–F, `gh pr view`/`gh pr
        # edit --body-file -`) som bevisar KONTRAST (F2: fake gh pr view
        # fallerar → CLI exit 3, ingen pr edit anropas — annars vore F1
        # vakuöst). 47 fall. Samma Marcus-mandat som syskonen: enhetstest för
        # skript, samma klass som de övriga gatekeeper-sviterna — INTE
        # TASK-173.4:s CI-backstopp (jobbet `review-backstopp` nedan, byggt
        # 2026-08-28). Hermetisk, ingen riktig gh-/nätverkstrafik.
        # 2026-08-26 (macOS).
        #
        # test-review-loop.mjs (TASK-173.5, ADR-105 beslut 4) hör hit av samma
        # ADR-039-skäl som sina tre syskon ovan: rundtaks-loopen
        # (.review-loop-policy.json + scripts/lib/review-loop.mjs +
        # scripts/review-loop-beslut.mjs) är review-grindens FJÄRDE
        # kontraktsyta och skulle annars vara lika oskyddad som de var före
        # TASK-185. Rena funktioner (sektion A–D: config-validering,
        # rundtrösklar, prioritetsordning, eskaleringsrendering) + ett
        # CLI-lager (sektion E, exit 0/10/20/1/2/64) + ett trusted-ref-lager
        # (sektion F) som bygger ÄKTA engångs-git-repon där origin/main,
        # PR-grenen och arbetsträdet bär TRE OLIKA policy-versioner samtidigt
        # — med KONTRAST-fallet F4 som landar mildringen i main och visar att
        # den DÅ syns, utan vilket F2/F3 vore vakuösa. Samma temp-repo-
        # disciplin som test-review-policy.mjs. 103 fall.
        #
        # Sviten är tvåsidigt bevisad med mutationstest (bygg-agentens
        # slutrapport, 2026-08-26): takkontrollen bortkopplad fällde 10 fall,
        # rundtröskeln bortkopplad fällde 8 — inklusive AC #1- och AC
        # #4-kontrastparen.
        #
        # Orkestrerar-beslut (Marcus-mandat, samma som för de tre syskonen):
        # en enhetstestsvit för ett skript är en gatekeeper-svit, samma klass
        # som de övriga ~15 — INTE samma sak som TASK-173.4:s CI-backstopp
        # (den deterministiska spärren mot en PR utan giltigt utlåtande;
        # sedan 2026-08-28 jobbet `review-backstopp` nedan), se CLAUDE.md
        # § Review-grinden. Att denna svit körs i CI skyddar loop-skriptets
        # LOGIK mot regression; det gör inte i sig review-grinden mekaniskt
        # otvingbar — det gör backstopp-JOBBET. Hermetisk (temp-repon,
        # node_modules-symlänk, ingen nätverkstrafik). 9,2 s LOKALT mätt
        # (macOS, 2026-08-26, `time node scripts/test-review-loop.mjs`) —
        # CI-tiden är INTE mätt av mig och kan avvika.
        #
        # test-review-metrics.mjs (TASK-173.6, ADR-105 § Konsekvenser: "Fångst-
        # rate-instrumenteringen är en del av BYGGET, inte ett tillägg") hör hit
        # av samma ADR-039-skäl som sina FYRA review-syskon ovan:
        # instrumenteringen (scripts/lib/review-metrics.mjs +
        # scripts/review-metrics.mjs + scripts/review-metrics-kalibrering.mjs +
        # append-tillägget i scripts/review-loop-beslut.mjs) är review-grindens
        # FEMTE kontraktsyta och skulle annars vara lika oskyddad som de andra
        # var före TASK-185. Rena funktioner (sektion A–F: radbyggning,
        # JSONL-parsning fail-soft, summering, rendering, filväg-resolution) +
        # ett CLI-lager (sektion G–I) som TVÅSIDIGT bevisar append-kontraktet:
        # ett lyckat beslut loggar en rad (I1/I4), ett malformat utlåtande eller
        # policyfel loggar INGEN (I2/I3 — en trasig indata ska aldrig förorena
        # fångstrate-underlaget). 49 fall (MÄTT 2026-08-28; stod som 48 här —
        # ett fall tillkom i 173.6:s runda 2 utan att talet följde med, TASK-106:s
        # kopierings-drift i miniatyr). Samma Marcus-mandat som syskonen:
        # enhetstest för skript, samma klass som de övriga gatekeeper-sviterna.
        # Hermetisk (egen tmpdir, ingen nätverkstrafik, ingen delad state med
        # de andra review-sviterna). Mutationsprövad (bygg-agentens
        # slutrapport, 2026-08-28): append-anropet bortkopplat fällde I1/I4.
        #
        # test-review-backstopp.mjs (TASK-173.4) hör hit av samma ADR-039-skäl
        # som sina FEM syskon ovan, men med en skillnad värd att läsa: dess
        # produktionsyta (scripts/lib/review-backstopp.mjs +
        # scripts/review-backstopp.mjs) körs som ett EGET jobb i detta
        # workflow (`review-backstopp`, se längre ned) och är alltså
        # review-grindens SJÄTTE kontraktsyta OCH själva spärren. Sviten
        # skyddar dess LOGIK mot regression; jobbet är mekanismen. Sektion C
        # är kopplings-vakten mellan 173.3:s rendrerare och 173.4:s parser —
        # de två sitter i olika moduler och kan drifta isär utan att
        # någondera sviten märker det. Sektion F kör CLI:t mot en FAKE
        # `gh`-binär på PATH, med tre KONTRAST-fall (gh fallerar / gh ger
        # skräp-JSON / gh-svar utan headRefOid ⇒ exit 3, aldrig 0) — utan dem
        # vore de positiva fallen vakuösa. Sektion E bär dessutom E7, ett
        # REGRESSIONSFALL som kör CLI:t ur en katalog med mellanslag och åäö —
        # runda 2 mätte där en fail-open där entrypoint-vakten aldrig fyrade
        # (exit 0, noll byte, på en kropp UTAN sektion). 40 fall (MÄTT
        # 2026-08-28), tvåsidigt mutationsbevisat (STALE-kontrollen bortkopplad
        # fällde 3 fall, PR-nummer-kontrollen 2, ref-regexen 1, CLI:ts exitkod 3,
        # entrypoint-vakten återställd till den trasiga formen 1). Hermetisk.
        #
        # test-verify-ci-parity.mjs hör hit av samma ADR-039-skäl applicerat
        # på scripts/verify-ci-parity.mjs SJÄLVT (CLAUDE.md § verify:ci-
        # parity) — paritetsverktyget som ska fånga när ci.yml/ci-suite.yml
        # driftar från .ci-parity-policy.json hade självt noll skydd mot
        # regression i sin egen klassnings-/preflight-logik. Tre lager: rena
        # funktioner mot syntetiska fixturer, levande regression mot det
        # RIKTIGA ci.yml/ci-suite.yml (`--list`), och ett tvåsidigt CLI-bevis
        # i en sandlåda med muterade kopior av policy+ci.yml+ci-suite.yml.
        # Ingen nätverkstrafik. 18,0 s LOKALT mätt (macOS, 2026-08-26).
        #
        # test-docraptor-sjalvbarande.mjs (TASK-301) hör hit av samma
        # ADR-039-skäl: scripts/docraptor-sjalvbarande.mjs lämnade tidigare
        # en ohämtbar CSS-url() ORÖRD, exakt den referensklassen Prince
        # fäller HELA DocRaptor-jobbet på med HTTP 422 "File system access
        # is not allowed" (se skriptets eget filhuvud § inlinaCssUrls).
        # Fixen neutraliserar i stället till `local("")`, samma regel som
        # EF-lagrets `inlinaCssUrls` (supabase/functions/_shared/
        # mall-render.ts) redan bär. Fyra fall, tvåsidigt bevis (en ohämtbar
        # font-url() → local(""), INTE orörd; en hämtbar font-url() → en
        # data-URI, regressionsskydd mot att fixen neutraliserar för brett;
        # en saknad <img src> är fortfarande ett HÅRT fel — asymmetrin mot
        # fonter; neutraliseringen loggas på stderr med filnamn). Hermetisk
        # (egen mktemp-fixtur, ingen nätverkstrafik). 0,16 s LOKALT mätt
        # (macOS, 2026-08-28, `time node scripts/test-docraptor-
        # sjalvbarande.mjs`) — CI-tiden är INTE mätt av mig och kan avvika.
        #
        # test-task-338-6-prod-migration.mjs (TASK-338.6) hör hit av SAMMA
        # skäl som test-fas4-prod-deploy.sh (raden nedan, TASK-272): skriptet
        # det prövar är ett REPETERBART operativt verktyg (Marcus kör
        # --kontrollera/--utfor-schema/--utfor-rader mot prod fler gånger än
        # en, inte ett engångs-schemaskript à la create-eventinnehall-
        # modell.mjs, vars test-svit MEDVETET lämnas ovirad — se den sviten
        # § filhuvud).
        # Bevisar: bas-guarden (resolveTargetBaseId) VÄGRAR en icke-staging-
        # bas utan AIRTABLE_PROD_GODKAND_AV_MARCUS satt till EXAKT samma
        # bas-ID — mekaniken scripts/deny-prod-ref.sh INTE täcker (den
        # matchar bara Supabase-prod-refen, TASK-203; Airtable-bas-ID:n
        # nämns i .prod-ref-policy.conf endast i en kommentar). Planerings-
        # och exekverings-logiken (planSchema/runSchema för steg (i),
        # planRader/runRader för steg (iii), efter review-runda 2:s split av
        # det tidigare kombinerade --utfor-läget) är hermetisk, DI mot
        # injicerade API-stubbar (inkl. createThrowawayAndDeletes
        # fetchImpl/sleepImpl/logImpl, review-runda 3); en liten
        # barnprocess-integrationsdel bevisar de FAKTISKA exit-koderna
        # (0–3, exit 4 = post-verifieringens fail-closed-läge vid
        # schema-/rad-diskrepans efter en --utfor-*-körning, review-runda 2
        # punkt 3) men ENDAST för grenar som exitar FÖRE någon fetch — ingen
        # nätverkstrafik i sviten. 74 fall. 1,25 s LOKALT mätt (macOS,
        # 2026-08-29, `time node scripts/test-task-338-6-prod-migration.mjs`)
        # — CI-tiden är INTE mätt av mig och kan avvika.
        #
        # test-backfill-inbetalningar.mjs (TASK-346.8) hör hit av samma
        # ADR-039-skäl som syskonen ovan, med ett tillägg som är kortets egen
        # DoD #5: pengalogikens regler ska ha var sin NEGATIV kontroll som
        # fäller en trasig implementation. Sviten är därför byggd i
        # KONTRASTPAR — tydligast § B17, där samma sträng ('2.500') prövas mot
        # BÅDA parsrarna och måste ge OLIKA svar (2500 respektive null),
        # eftersom det är hela skälet till att prisfritextens tolkning finns
        # vid sidan av _shared/betalningsbelopp.ts normaliseraBelopp; och
        # § D6, som bevisar VARFÖR fack-motsägelsen listas i stället för att
        # backfillas (beloppet hade flippat båda facken i basen).
        # Sektionerna K1–K4 är KOPPLINGS-vakter mot ytor skriptet inte äger:
        # att härledningen importeras ur EF-lagret och inte kopieras, att
        # policyns betalsätt finns i migrationens check-constraint, och att
        # betalningsdatum-kolumnen ÄR nullable — faller den sista är kortets
        # "datum tomt"-krav omöjligt och skivan måste ritas om.
        # Granskningsrunda 1 lade fyra sektioner: L (länktillståndets preflight
        # mot sticky `supabase/.temp/project-ref` — L5 är kärnan: PROD i filen
        # fäller ÄVEN med korrekt --projekt-ref), M (0-kr hoppas, negativt är
        # avvikelse, båda fail-closed i sqlBelopp/byggInsertSats), N (aktiva
        # icke-Historik-inbetalningar blir avvikelse — N2 bevisar att summan
        # annars blivit 3500 av 2500) och O (spegelns konvergens: Del C
        # itererar backfill ∪ redanBackfillad, O4 vaktar att prosan inte
        # överlovar per ADR-083).
        #
        # Granskningsrunda 2 lade tre till, var och en efter ett fynd där en
        # mutation ÖVERLEVDE den dåvarande sviten — alltså tre mätta luckor,
        # inte tre påhitt: P (indexeringen bruten ur `lasInbetalningar` till
        # en ren funktion, så status-filtret blir testbart; P4/P5 bär
        # granskarens motexempel mot netto-formen: +2500 och −2500 ger netto 0
        # men ANTAL 2, och grinden prövar nu FÖREKOMST), Q (kopplingsvakter
        # som binder de rena funktionerna till anropsstället i main() —
        # en `{ ok: true }`-mutation överlevde 118/118 innan Q1 fanns, och Q2
        # låser att preflighten körs FÖRE staging-semaforen) och R (patch-
        # hoppet som betalar ned konvergensens linjära kostnad, plus att
        # länkpreflightens felmeddelanden aldrig bär en kopierbar ref).
        #
        # 137 fall (MÄTT 2026-08-31, sviten kördes och dess egen slutrad
        # lästes — aldrig avskrivet, TASK-106). Hermetisk: inga fetch-anrop,
        # ingen Airtable-token, inget supabase-CLI, ingen nätverkstrafik —
        # skriptets main() körs inte vid import (körvakten längst ned i det).
        #
        # Mutationsprövad 2026-08-31: TJUGOFYRA mutationer, 24 av 24 FÄLLDA,
        # var och en med namngivet fällande testfall. Tolv från bygget
        # (idempotens-predikatet bortkopplat, betalningsdatum satt till
        # current_date, fack-motsägelsen backfillad, tusental-regeln
        # bortkopplad, prod-ref-guarden bortkopplad, okänt avgiftspris gissat,
        # exkluderadeEventIds bortkopplad, spegelns null-hopp bortkopplat,
        # redan-backfillad-kontrollen bortkopplad, bas-guardens prod-spärr
        # bortkopplad, NUL-vakten bortkopplad, makulerade poster räknade som
        # betalda); sex från runda 1 (länktillståndets två grenar,
        # beloppsgrindens noll-gren, aktiv-inbetalnings-grinden, Del C:s
        # breddning, sqlBelopps positivt-krav); sex från runda 2 (förekomst→
        # netto ⇒ P5/P6, status-filtret ⇒ P2, preflightens wirning kortsluten
        # respektive borttagen ⇒ Q1/Q2/Q3, patch-hoppet hoppar allt ⇒ R2/R3/R4,
        # maskeringen bortkopplad ⇒ R5/R6/R7).
        #
        # 0,18 s LOKALT mätt (macOS, 2026-08-31, median av tre körningar
        # EFTER runda 2:s utökning: 172/199/179 ms) — CI-tiden är INTE mätt
        # av mig och kan avvika.
        run: |
          bash scripts/test-check-frontmatter.sh
          bash scripts/test-check-public-checklists.sh
          bash scripts/test-pre-commit-hook.sh
          bash scripts/test-check-lifecycle.sh
          bash scripts/test-check-thread-index.sh
          bash scripts/test-check-lesson-numbers.sh
          bash scripts/test-check-permissions-claims.sh
          bash scripts/test-agent-spawn-log.sh
          bash scripts/test-ci-wait.sh
          bash scripts/test-staging-semaphore.sh
          bash scripts/test-check-listparitet.sh
          bash scripts/test-check-backlog-closure.sh
          bash scripts/test-backlog-cli.sh
          bash scripts/test-stop-vakt.sh
          bash scripts/test-check-facit.sh
          node scripts/test-check-langa-streck.mjs
          bash scripts/test-check-nattvakt-dedup.sh
          bash scripts/test-check-sessionsdok-fonster.sh
          node scripts/test-check-mailto.mjs
          bash scripts/test-fas4-prod-deploy.sh
          bash scripts/test-supabase-cli-policy.sh
          bash scripts/test-jq-guard.sh
          bash scripts/test-gh-guard.sh
          bash scripts/test-visual-baselines-scope.sh
          bash scripts/test-check-mallparitet.sh
          bash scripts/test-arkivera-sessionsdok.sh
          bash scripts/test-check-obesvarade-larm.sh
          bash scripts/test-check-pausade-sessioner.sh
          bash scripts/test-deny-arbetsform-push.sh
          bash scripts/test-deny-facit-godkand-skrivning.sh
          bash scripts/test-deny-frammande-huvudkatalog.sh
          bash scripts/test-deny-grind-genom-pipe.sh
          bash scripts/test-deny-hemlighet-utskrift.sh
          bash scripts/test-deny-precompact.sh
          bash scripts/test-deny-prod-ref.sh
          bash scripts/test-deny-resend-send.sh
          bash scripts/test-deny-subagent-vantan.sh
          bash scripts/test-heartbeat-svep.sh
          bash scripts/test-post-compact-igenkanning.sh
          bash scripts/test-stada-grenar.sh
          node scripts/test-validera-review-utlatande.mjs
          node scripts/test-review-policy.mjs
          node scripts/test-review-risk-sektion.mjs
          node scripts/test-review-loop.mjs
          node scripts/test-review-metrics.mjs
          node scripts/test-review-backstopp.mjs
          node scripts/test-verify-ci-parity.mjs
          node scripts/test-task-338-6-prod-migration.mjs
          node scripts/test-docraptor-sjalvbarande.mjs
          node scripts/test-backfill-inbetalningar.mjs

      - name: Test post-merge-lagrets ärvda klassning (TASK-73)
        # Per ADR-039 § lesson→grind (L43): en grind är inte en grind förrän
        # dess fyrning FORTLÖPANDE verifieras. Sviten prövar
        # scripts/classify-post-merge.sh i BÅDA riktningar (docs-landning hoppar
        # sviten, kod-landning kör den) plus varje fail-closed-gren.
        #
        # T13 är skälet steget bor just HÄR och inte enbart i post-merge.yml:
        # klassningen ÄRVER ci.yml:s beslut för att slippa en andra kopia av
        # glob-listorna, och priset är EN koppling — att skriptets
        # CI_SUITE_JOB_NAME är detta workflows `suite`-jobbnamn. Den kopplingen
        # kan bara vaktas där ci.yml faktiskt läses. Driftar namnet utan att
        # skriptet följer med blir utfallet fail-closed men TYST: full svit på
        # varje landning, alltså exakt den defekt TASK-73 stängde, återinförd av
        # sin egen fix.
        #
        # Hermetisk (gh-stub på PATH, ingen nätverkstrafik, ~1 s). Eget steg med
        # ärligt namn per Session 17 K7-disciplinen — stegnamn ska inte ljuga om
        # sitt innehåll.
        run: bash scripts/test-classify-post-merge.sh

      - name: Test post-merge-larmets attribution (TASK-334)
        # Per ADR-039 § lesson→grind (L43), samma kadens-argument som steget
        # ovan. Sviten prövar scripts/post-merge-attribution.sh i BÅDA
        # riktningar: att larmet PÅSTÅR primär misstanke när den är sann, och
        # att det NEKAR den när sviten inte mätte landningarna däremellan.
        #
        # A11-A13 är skälet steget bor i CI och inte bara i en byggsession.
        # A11 vaktar EN sträng — post-merge.yml:s `suite`-jobbnamn, skriptets
        # enda koppling till workflowen. A12 vaktar att workflowen faktiskt
        # anropar skriptet. A13 är ADR-083-vakten: den fäller om den FALSKA
        # meningen ("… är den primära misstänkta. Revert nedan …") återinförs
        # hårdkodad i YAML, där den skrivs oavsett om den är sann.
        #
        # Utan de tre driftar fixen tyst tillbaka till det läge som mättes
        # 2026-08-28: två larm inom 13 minuter (ärenden #2043 och #2047) pekade
        # ut fel landning som primärt misstänkt, eftersom föregående körning var
        # `success` DÄRFÖR ATT den hoppade sviten på ärvd D0.
        #
        # Hermetisk (gh-stub på PATH, ingen nätverkstrafik, ~1 s). Eget steg
        # med ärligt namn per Session 17 K7-disciplinen.
        run: bash scripts/test-post-merge-attribution.sh

      - name: Test PR-grindens acceptance-urval (TASK-75)
        # Per ADR-039 § lesson→grind (L43), samma kadens-argument som stegen
        # ovan. Sviten prövar scripts/acceptance-urval.sh i BÅDA riktningar: att
        # urvalet TILLÄMPAS på en klass-lokal diff, och att det FALLER till full
        # klass på källfil, delad söm, traversal, saknad fil och workflow.
        #
        # T14 och T15 är skälet steget bor just HÄR. T14 kör klassens FAKTISKA
        # spec-filer genom mönstret — håller mönstret men matchar det inte
        # disken blir utfallet tyst full klass på varje PR, alltså en mekanik
        # som ser installerad ut och gör ingenting. T15 vaktar att ci.yml och
        # ci-suite.yml faktiskt är inkopplade, inklusive att ci-suite.yml:s
        # default är TOM STRÄNG — vore den något annat hade post-merge.yml och
        # nightly.yml ärvt ett urval, och skyddsnätet under PR-grinden upphört
        # att finnas. Båda kopplingarna kan bara vaktas där filerna faktiskt
        # läses.
        #
        # Hermetisk (riggat filträd i /tmp, ingen nätverkstrafik, ~1 s).
        run: bash scripts/test-acceptance-urval.sh

      - name: Test prod-deploy allowlist (fail-closed)
        # Bevisar fail-closed-egenskapen i scripts/deploy-prod-functions.sh
        # (ADR-050 steg 2 / Fas 7-skuld L115): test-* + nya funktioner
        # exkluderas från prod-deploy by default. Eget steg med ärligt namn
        # (Session 17 K7-disciplin: stegnamn ska inte ljuga om sitt innehåll).
        # T1–T4 kör endast --list/felvägar — INGEN faktisk deploy. T5–T7
        # (S108, 2026-08-24) kör faktiskt DEPLOY-läget (--project-ref) men mot
        # en STUBBAD `npx` på PATH — bevisar att den pinnade CLI-vägen
        # (scripts/lib/supabase-cli.sh) faktiskt nås och att guarden blockar
        # FÖRE första funktionen vid version-missmatch. Fortfarande ingen
        # nätverkstrafik och ingen riktig Supabase-åtkomst i något fall.
        run: |
          bash scripts/test-deploy-prod-functions.sh

      - name: Test purge-vaktens 404-diskriminering (TASK-82)
        # Per ADR-039 § lesson→grind (L43): en grind är inte en grind förrän dess
        # fyrning FORTLÖPANDE verifieras. Sviten var wirad i INGET jobb fram till
        # TASK-82. TASK-50 bokförde den frånvaron medvetet, två gånger — och det
        # var försvarbart så länge sviten bar beskrivande mekanism-tester. Det som
        # ändrades är inte formen utan LASTEN: TASK-76 lade sin fail-open-vakt
        # (AC #3 — "404 som beror på fel bas eller fel tabell fäller FORTFARANDE")
        # här, alltså den enda kontroll som skiljer purge-idempotensen från ett
        # tyst hål. Ett medvetet designval fattat under andra förutsättningar är
        # inte automatiskt giltigt under de nya; TASK-50:s beslut rivs öppet, inte
        # tyst.
        #
        # Extra vikt: ci.yml skickar sedan TASK-70.3 `run_staging: false`
        # villkorslöst, så purge-JOBBET (ci-suite.yml `staging-purge`) kör inte på
        # PR-ytan. Utan detta steg har purge-koden noll automatisk täckning på
        # PR-nivå — varken sitt jobb eller sin testsvit.
        #
        # HEMVIST-VAL — PR-grinden, inte natten (nightly.yml bär MÄTARE:
        # test-ci-metrics.mjs, test-flake-matserie.mjs; detta är en GRIND).
        # Sviten är hermetisk: den mockar globalThis.fetch med save/restore och
        # läser i övrigt bara .purge-staging-policy.json ur checkouten. Verifierat
        # 2026-07-29 genom körning utan STAGING_AIRTABLE_TOKEN i miljön OCH med
        # globalThis.fetch spärrad till throw — exit 0 i båda fallen, alltså inga
        # secrets och noll nätverkstrafik. Den validerar dessutom policy-filen PÅ
        # DISK, så en PR som ändrar .purge-staging-policy.json måste kunna fällas
        # av den FÖRE landning; nattlig kadens hade sett det först efteråt.
        #
        # ≈4 s LOKALT mätt (macOS, 2026-07-29) — CI-tiden är inte mätt av mig.
        run: node scripts/test-purge-staging-sentinels.mjs

      - name: Test seed-fixturens guard-funktioner (TASK-82)
        # Samma kadens-argument (L43), samma tidigare frånvaro. Sviten prövar de
        # pura guards i scripts/seed-review-fixture.mjs: bas-guarden som refuserar
        # prod-basen som expectedBaseId, länk-guarden runt de permanenta
        # rollup-fixturerna (TASK-31), och korsläsningen av granskningsfixturens
        # markörer mot den SKARPA .purge-staging-policy.json.
        #
        # HEMVIST-VAL — PR-grinden, men först efter separat kontroll. Villkoret
        # var att en svit som kräver staging-secrets INTE hör i PR-grinden.
        # Kontrollerat 2026-07-29: seed-skriptets enda process.env-läsning
        # (STAGING_AIRTABLE_TOKEN) ligger inuti main(), som körs endast som CLI —
        # testsviten importerar de pura funktionerna och rör aldrig fetch. Körd
        # utan token och med globalThis.fetch spärrad till throw: exit 0 båda
        # gångerna. Villkoret faller alltså inte ut — sviten är hermetisk och hör
        # i den billiga klassen.
        #
        # Korsläsningen mot policy-filen bär samma PR-argument som steget ovan: en
        # PR som breddar purge-mönstret kan få granskningsfixturen att städas bort
        # mitt under en pågående granskning, och det ska fällas före landning.
        #
        # ≈0,1 s LOKALT mätt (macOS, 2026-07-29) — CI-tiden är inte mätt av mig.
        run: node scripts/test-seed-review-fixture.mjs

      - name: Test facit:godkann-skriptets guard-funktioner (TASK-167)
        # Per ADR-039 § lesson→grind (L43): en testsvit utan CI-bärare bevisar
        # ingenting (TASK-82:s precedent). scripts/facit-godkann.mjs är Marcus
        # egen stämplingskanal (ADR-104 § Beslut 2) — samma klass av lokalt
        # utvecklarverktyg som seed-review-fixture.mjs ovan, och samma HEMVIST-
        # argument gäller: sviten är hermetisk (egen /tmp-sandlåda, ETT äkta
        # men tomt git-repo för att pröva `git rev-parse main` → `HEAD`-
        # fallbacken, ingen nätverkstrafik, inga secrets) och prövar de PURA
        # funktionerna (parseArgs, tillampaGodkannande, lasPolicy) plus main()
        # end-to-end mot sandlådan — aldrig det verkliga repots facit-manifest.
        #
        # TVÅSIDIGT: varje felväg (okänt pass, redan satt fält utan --ersatt,
        # tomt citat, trasig undantags-form, trasig JSON, saknad/ofullständig
        # policy) prövas i BÅDA riktningar — 29 fall.
        #
        # ≈0,3 s LOKALT mätt (macOS, 2026-08-08) — CI-tiden är inte mätt av mig
        # och kan avvika åt båda håll.
        run: node scripts/test-facit-godkann.mjs

      - name: Check staging-preflightens wiring (TASK-91 deletion-vakt)
        # Staging-preflighten (TASK-77 + TASK-84) hänger på FEM ytor: tre
        # Playwright-dependencies och två anrop i main(). Semaforens LOGIK är väl
        # täckt — scripts/test-staging-semaphore.sh har 19 fall — men WIRINGEN
        # hade noll. TASK-84:s agent rapporterade det om sig själv: "en
        # refaktorering som tappar anropsraden i main() fångas inte av någon
        # grind".
        #
        # VARFÖR EN GRIND FÖR ETT FEL SOM ALDRIG INTRÄFFAT. Huset mekaniserar
        # normalt när felet HAR inträffat, eller när det FÖRSTA felet vore
        # oacceptabelt; ingetdera gäller här. Skälet är en tredje egenskap:
        # FRÅNVARON SYNS INTE. GitLabs postmortem 2017-01-31 är det dokumenterade
        # fallet — fyra återställningsmekanismer fanns på papperet, backuperna
        # hade aldrig körts, och felrapporteringen var också trasig. Tappas en
        # anropsrad här märks det först vid en kollision, och ser då ut som ett
        # slumpmässigt staging-fel i stället för som en saknad mekanism.
        #
        # HEMVIST-VAL — PR-grinden, som de tretton andra grindarna. Vakten läser
        # Playwrights EGEN dependency-upplösning (`--list`, ingen browser-start,
        # ingen nätverkstrafik, inga secrets) plus källkod på disk. Den måste
        # kunna fälla FÖRE landning: det den vaktar är just en refaktorering som
        # råkar tappa en rad, och en sådan landar i en PR.
        #
        # ≈1,7 s LOKALT mätt (macOS 16 kärnor, 2026-07-30, loadavg ≈7). De fyra
        # `--list`-anropen körs parallellt. CI-tiden är INTE mätt av mig och kan
        # avvika åt båda håll — runnern har färre kärnor, vilket träffar just
        # parallelliteten.
        #
        # SAMMA ANROP KÖR ÄVEN I .githooks/pre-commit (T119 (d) item 4, S97,
        # villkorat på `.staging-preflight-hook-policy.conf`s trigger-ytor) —
        # DENNA rad tas INTE bort trots det. Pre-commit kan kringgås med
        # `--no-verify`; detta CI-steg är sista linjen och behåller sin egen
        # fail-closed-dom oavsett vad som hände lokalt.
        run: node scripts/check-staging-preflight-wiring.mjs

      - name: Test wiring-vaktens fyrning (TASK-91, tvåsidigt bevis)
        # Per ADR-039 § lesson→grind (L43): en grind är inte en grind förrän dess
        # fyrning FORTLÖPANDE verifieras. TASK-82 är den skarpare precedenten —
        # en guard-svit utan CI-bärare bevisar ingenting — och den gäller lika
        # mycket för en vakt vars enda uppgift är att märka en RADERING.
        #
        # Sviten bygger en sandlåda av det RIKTIGA trädet (playwright.config.ts,
        # tests/, src/, supabase/, de två Node-skripten) och skär i den: 21 fall,
        # ett par per yta plus näten. Röd-sidan bevisar att vakten fäller när
        # anropet eller dependencyn tas bort; grön-sidan (G1, G2) bevisar att den
        # INTE fäller på varje ändring — utan den halvan vore röd-sidan förenlig
        # med en vakt som alltid är röd.
        #
        # Hermetisk: /tmp-sandlåda med symlänkat node_modules, ingen
        # nätverkstrafik, inga secrets, rör aldrig repots eget träd.
        #
        # 25–42 s LOKALT mätt över fyra körningar (macOS 16 kärnor, 2026-07-30;
        # spannet är maskinlast — fem parallella agenter, loadavg ~2 → ~9 mellan
        # körningarna). CI-tiden är INTE mätt av mig. Kostnaden är medveten: den
        # bärande delen är att Playwrights faktiska dependency-upplösning körs
        # per fall i stället för att efterliknas. `lint` hade timeout-minutes: 5
        # mot ~1 min jobbtid före detta steg (TASK-82:s CI-mätning) — marginalen
        # bör läsas av på den första skarpa körningen, inte projiceras härifrån.
        run: node scripts/test-check-staging-preflight-wiring.mjs

      - name: Install shellcheck (pinned v0.11.0)
        # Per ADR-033 § Säkerhet + ADR-029 § Third-party Actions-policy.
        # ubuntu-latest har shellcheck 0.9.0-1 pre-installerat (Image
        # 20260413 per actions/runner-images manifest) — under <0.10-
        # tröskeln, saknar v0.10+ optional checks (SC2310 m.fl.).
        # Falsk-grön-risk per L9 om framtida code-edit triggar
        # v0.10+-check. CI alignas till lokal 0.11.0 via SHA-pinnad
        # download från koalaman/shellcheck GitHub releases.
        #
        # SHA-pin-rationale: koalaman publicerar inte separat .sha256sum-
        # fil per release-asset (empirisk verifikation 2026-05-16).
        # SHA256 nedan är downstream-beräknad mot fast nedladdad
        # release-asset från GitHub releases (immutability per
        # https://docs.github.com/en/repositories/releasing-projects-on-github/about-releases).
        # För re-verifikation: ladda ned samma URL + sha256sum lokalt;
        # SHA-match bevisar release-asset oförändrad. Konsekvent med
        # ADR-029 § Medvetna utelämningar #3 actionlint-precedent.
        #
        # TILLGÄNGLIGHET (TASK-83, 2026-07-29) — --retry-all-errors, inte bara
        # --retry. curl:s "transient" är EXAKT {timeout, FTP 4xx, HTTP 408, 429,
        # 500, 502, 503, 504, 522, 524}; ett TLS-connect-fel (exit 35) ingår
        # INTE, och --retry-connrefused adderar bara ECONNREFUSED. Den uppmätta
        # fällningen (PR #430, exit 35 efter 0,13 s) hade alltså överlevt både
        # --retry och --retry-connrefused — --retry-all-errors (curl 7.71.0+) är
        # den enda av dem som täcker den. `-o` är förutsättningen: curl
        # nollställer en FIL-destination mellan försöken, en shell-redirect
        # inte. `-f` gör HTTP-fel till curl-fel — annars skrivs felsidan till
        # fil och en 503 maskerar sig som checksummeavvikelse, alltså en
        # nätverksstörning förklädd till supply-chain-signal. `-S` gör felet
        # synligt trots `-s` (fällningen ovan skrev ingen utdata alls).
        # sha256sum-verifieringen nedan är ORÖRD: kortet rör tillgänglighet,
        # inte pinning.
        #
        # FREKVENSEN ÄR MÄTT, INTE GISSAD (2026-07-22..29, 1000 ci.yml-runs):
        # detta steg 1 fällning på 988 avgjorda exekveringar, actionlint-steget
        # 0 på 995. Räknat över ALLA attempts (`jobs?filter=all`) — en omkörd
        # fällning syns inte annars, och just denna kördes om till grönt.
        # Punktskattningen ~0,1 % motsvarar ungefär en fällning i veckan vid
        # takten ~145 lint-jobb/dygn, men n=1 ger ett brett intervall. Formen
        # valdes för att den kostar en flagga — inte för att takten var
        # säkerställd.
        run: |
          SC_VERSION="v0.11.0"
          SC_SHA256="8c3be12b05d5c177a04c29e3c78ce89ac86f1595681cab149b65b97c4e227198"
          SC_URL="https://github.com/koalaman/shellcheck/releases/download/${SC_VERSION}/shellcheck-${SC_VERSION}.linux.x86_64.tar.xz"
          curl -fsSL --retry 5 --retry-all-errors --retry-max-time 60 "${SC_URL}" -o /tmp/sc.tar.xz
          echo "${SC_SHA256}  /tmp/sc.tar.xz" | sha256sum -c
          tar -xJf /tmp/sc.tar.xz -C /tmp
          sudo mv "/tmp/shellcheck-${SC_VERSION}/shellcheck" /usr/local/bin/shellcheck
          shellcheck --version | grep -F "version: 0.11.0"

      - name: Validate bash scripts with shellcheck-strict
        # Per ADR-033 § Del 1: shellcheck-strict-grindvakt (0/0/0/0).
        # --severity=style aktiverar style-domän (default skip:as);
        # --enable=all aktiverar optional checks (SC2250, SC2312,
        # SC2310, SC2311 m.fl. som är default-disabled).
        # Scope per ADR-033 § Del 3: scripts/*.sh + .githooks/* +
        # scripts/lib/supabase-cli.sh (tillagd i S108-wiringen, se nedan) +
        # 20 sourced-config-filer (.checklist-policy.conf +
        # .frontmatter-policy.conf + .lesson-policy.conf, tillagd S91/ADR-081 +
        # .ci-wait-policy.conf, tillagd S91/TASK-72 +
        # .staging-semaphore-policy.conf, tillagd S91/TASK-77 +
        # .permissions-claims-policy.conf, tillagd S91/ADR-083 +
        # .backlog-closure-policy.conf, tillagd S91/TASK-90 +
        # .listparitet-policy.conf, tillagd S91/TASK-85 +
        # .thread-index-policy.conf, tillagd i TASK-108-wiringen +
        # .heartbeat-svep-policy.conf, tillagd i TASK-119-wiringen +
        # .mail-lock-policy.conf, tillagd i TASK-137-wiringen (MAIL-LÅSET) +
        # .katalogagarskap-policy.conf, tillagd i T119(a)-wiringen (S97) +
        # .sanningsavstamning-policy.conf, tillagd i T119(b)-wiringen (S97) +
        # .grind-exitkod-policy.conf, tillagd i T119(d) item 5-wiringen
        # (S97, L440-hooken) +
        # .staging-preflight-hook-policy.conf, tillagd i T119(d) item 4-
        # wiringen (S97, pre-commit-anropet av TASK-91-vakten) +
        # .claims-tackning-policy.conf + .merge-tree-mandat-policy.conf,
        # tillagda i TASK-139 (T119 (d): täcknings-passet + merge-tree-
        # grinden mekaniserade) +
        # .subagent-vantan-policy.conf, tillagd i TASK-148.2-wiringen
        # (ADR-096 § Beslut del 2, PreToolUse-spärren mot Monitor/
        # run_in_background i subagent-kontext) +
        # .stada-grenar-policy.conf, tillagd i TASK-152-wiringen
        # (scripts/stada-grenar.sh — generell städning av lokalt mergade
        # grenar) +
        # .arbetsform-push-policy.conf, tillagd i TASK-149.3-wiringen
        # (ADR-097 § Beslut del 2, PreToolUse-spärren mot git push under
        # aktiv iteration) +
        # .arkivera-sessionsdok-policy.conf, tillagd i TASK-158.2-wiringen
        # (ADR-099 rullande fönster — scripts/arkivera-sessionsdok.sh) +
        # .precompact-policy.conf, tillagd i TASK-160.2-wiringen (ADR-101
        # § Beslut 5, PreCompact-grinden mot okontrollerad kompaktering) +
        # .nattvakt-dedup-policy.conf, tillagd i TASK-180-wiringen
        # (scripts/check-nattvakt-dedup.sh — nightly-watchdog.yml:s
        # dedup-beslut, öppet ELLER stängt-med-motivering) +
        # .atkomst-diagnos-policy.conf, tillagd i TASK-202-wiringen
        # (scripts/atkomst-diagnos.sh — mekaniserad självdiagnos för
        # åtkomst och nycklar, docs/reference/atkomst-och-nycklar.md) +
        # .hemlighet-utskrift-policy.conf + .prod-ref-policy.conf, tillagda
        # i TASK-203-wiringen (scripts/deny-hemlighet-utskrift.sh +
        # scripts/deny-prod-ref.sh — mekaniska PreToolUse-lås mot kommandon
        # som skriver ut hemligheters värde resp. agent-kommandon riktade
        # mot produktions-Supabase-projektet, Marcus-order 2026-08-12) +
        # .supabase-cli-policy.conf, tillagd i S108-wiringen (pinnad Supabase
        # CLI-version, scripts/lib/supabase-cli.sh — se filens egen header
        # för S108-incidenten: den globala CLI:n 2.75.0 avbröt en prod-EF-
        # deploy efter 18 av 45 funktioner) —
        # en sourced conf utanför scopet är samma lucka som de övriga redan
        # stänger).
        # Räkningen sade "5" medan listan bar sex filer fram till TASK-90;
        # rättad i samma andetag som den sjunde lades till, räknad om till
        # åtta i TASK-85, till nio i TASK-108-wiringen, till tio i
        # TASK-119-wiringen, till elva i TASK-137-wiringen och till tolv i
        # T119(a)-wiringen, till tretton i T119(b)-wiringen, till fjorton i
        # T119(d) item 5-wiringen, till femton i T119(d) item 4-wiringen,
        # till sjutton i TASK-139, till arton i TASK-148.2, till nitton i
        # TASK-152, till tjugo i TASK-149.3, till tjugoett i TASK-158.2,
        # till tjugotvå i TASK-160.2, till tjugotre i ADR-102-wiringen,
        # till tjugofyra i TASK-180-wiringen, till tjugofem i
        # TASK-202-wiringen, till tjugosju i TASK-203-wiringen
        # (.facit-policy.conf — agenten som byggde grinden lade till conf-filen
        # men wirade den inte in hit; luckan fångades av att shellcheck-strict
        # fällde på skriptet, inte av att någon läste listan) och till
        # tjugoåtta i S108-wiringen (.supabase-cli-policy.conf — samma
        # räkning gäller BARA de sourcade .conf-filerna; scripts/lib/
        # supabase-cli.sh räknas separat ovan eftersom den är en sourcad
        # .sh-KODFIL utanför den flata scripts/*.sh-globen, inte en config)
        # och till tjugonio i TASK-312-wiringen (.jq-version-policy.conf —
        # pinnad LÄGSTA jq-version, scripts/lib/jq-guard.sh — se filens
        # egen header: ~25 jq-anropsställen i hook-/grindlagret saknade
        # HELT versionskontroll innan detta kort, bara en bar
        # `command -v jq`-presence-check; scripts/lib/jq-guard.sh räknas
        # separat nedan av samma skäl som supabase-cli.sh ovan) och till
        # trettio i SAMMA TASK-312-wiring (.gh-version-policy.conf —
        # pinnad LÄGSTA gh-version, scripts/lib/gh-guard.sh, samma
        # separat-räkning som de två andra sourcade .sh-kodfilerna).
        # Pre-aktiverings-state (post-K3.2 commit d86d846): 0/0/0/0.
        run: |
          shellcheck \
            --severity=style \
            --enable=all \
            scripts/*.sh \
            scripts/lib/supabase-cli.sh \
            scripts/lib/jq-guard.sh \
            scripts/lib/gh-guard.sh \
            .githooks/* \
            .checklist-policy.conf \
            .frontmatter-policy.conf \
            .lesson-policy.conf \
            .ci-wait-policy.conf \
            .staging-semaphore-policy.conf \
            .permissions-claims-policy.conf \
            .backlog-closure-policy.conf \
            .listparitet-policy.conf \
            .thread-index-policy.conf \
            .heartbeat-svep-policy.conf \
            .mail-lock-policy.conf \
            .katalogagarskap-policy.conf \
            .sanningsavstamning-policy.conf \
            .grind-exitkod-policy.conf \
            .staging-preflight-hook-policy.conf \
            .claims-tackning-policy.conf \
            .merge-tree-mandat-policy.conf \
            .subagent-vantan-policy.conf \
            .stada-grenar-policy.conf \
            .arbetsform-push-policy.conf \
            .arkivera-sessionsdok-policy.conf \
            .precompact-policy.conf \
            .facit-policy.conf \
            .nattvakt-dedup-policy.conf \
            .atkomst-diagnos-policy.conf \
            .hemlighet-utskrift-policy.conf \
            .prod-ref-policy.conf \
            .supabase-cli-policy.conf \
            .jq-version-policy.conf \
            .gh-version-policy.conf

  # Job 3: Tung testsvit — anropar den reusable ci-suite.yml (EN KÄLLA,
  # delad med nightly.yml OCH post-merge.yml; task-36.2 / ADR-077). Villkorat på
  # changed-detection: HELA svit-anropet skippas på doc-only-commits
  # (should_skip_tests) → aggregatorn ser 'suite: skipped' (tolererat).
  # secrets: inherit → staging-secrets når inner-jobben (bevisat via spike
  # 2026-07-23, run 30036119790). Dependabot-hanteringen bor INNE i
  # ci-suite.yml (per-jobb) — test-fast måste köra även för dependabot.
  suite:
    name: Test suite
    needs: changed
    # Skippas vid docs-only (should_skip_tests) ELLER merge-dedup-träff
    # (dedup_hit, task-36.4): main-push vars träd redan bevisats grönt kör inte
    # om tunga jobb. Paraply-checken (ci-passed, always()) blir grön ändå —
    # skipped tolereras (AC#3). Fail-closad dedup ⇒ full svit vid varje tvivel.
    if: ${{ needs.changed.outputs.should_skip_tests != 'true' && needs.changed.outputs.dedup_hit != 'true' }}
    # Permissions-taket för det anropade ci-suite.yml. Ett anropat workflow kan
    # bara BEHÅLLA eller MINSKA anroparens token-permissions, aldrig eskalera
    # (GitHub Docs: "Permissions can only be maintained or reduced—not
    # elevated—throughout the chain"). ci.yml:s topp-nivå är permissions: {}
    # (least-privilege default, ADR-029 §4) — utan detta grant ärver suite-
    # jobbet {} och ci-suite:s jobb (contents: read för checkout) blir en
    # eskalering ⇒ startup_failure. Empiriskt fångat S79 (run 30037333924);
    # spike 30036119790 var grön just för att dess reusable-jobb bar {}.
    permissions:
      contents: read
    uses: ./.github/workflows/ci-suite.yml
    # ═══ STAGING LIGGER INTE LÄNGRE I PR-GRINDEN (A7:5 / TASK-70.3) ═══
    # `run_staging: false` är VILLKORSLÖST. `Staging (API + E2E)` och
    # `Staging sentinel purge` instansieras därmed aldrig av ci.yml — varken på
    # `pull_request` eller på `push` till main.
    #
    # KONTROLLEN ÄR FLYTTAD, INTE BORTTAGEN. Båda jobben körs oförändrat på de
    # två andra anropar-ytorna, som medvetet INTE skickar inputen och därför får
    # ci-suite.yml:s default `true`:
    #   · post-merge.yml — varje kod-landning på main, direkt efter merge.
    #   · nightly.yml    — full bredd varje natt.
    # Ordningen var en invariant, inte en preferens: post-merge-lagret
    # (TASK-70.2) och den skrivna revert-vägen (TASK-70.5) landade FÖRE denna
    # rad. Utan bådadera hade raden tagit bort en kontroll i stället för väntan.
    #
    # VARFÖR — MUTEXEN, INTE SEKUNDERNA. Jobbet självt bär 375 s (run
    # 30369011230), men den kostnad som faktiskt växer är den globala
    # `staging-tests`-mutexen (ci-suite.yml, jobbet `test-staging`): den
    # serialiserar över ALLA staging-rörande körningar — PR ∥ PR, PR ∥ main-push,
    # PR ∥ natt. Mätt 2026-07-28 på två fulla körningar med IDENTISKT
    # svit-innehåll: run 30400021534 = 7,8 min (jobben startade 19 s isär och
    # kördes parallellt) mot run 30400640305 = 20,3 min (staging startade
    # 5 min 36 s EFTER att acceptance var klar — den stod i kö). Hela skillnaden
    # var kötid. Ur PR-vägen slutar kritiska vägen alltså VÄXA med antalet
    # parallella PR:er, och det är den egenskap ett flöde med flera samtidiga
    # agenter behöver mest.
    #
    # VARFÖR LITTERALT `false` OCH INTE ETT UTTRYCK. Ett villkor är en fråga om
    # NÄR staging ska köras här — och svaret är aldrig. En villkorings-mekanism
    # hade beskrivit en grind som inte längre finns, och hade dessutom behövt
    # rivas igen när merge queue (TASK-70.1) lägger `merge_group` bredvid
    # `pull_request`: två ytor som båda måste minnas att släcka samma sak.
    #
    # ÄRLIGT OM VAD SOM VINNS. Väggklockan för EN ensam kod-PR sjunker knappt:
    # `Acceptance (hermetisk)` blir ensam bärare av kritiska vägen och är mätt
    # till 404 s (`#318`), 407 s (`#323`) och 452 s (`#324`). Taket ligger därför
    # kvar kring 7 min även efter denna rad. TASK-75 är kortet som sänker det;
    # denna rad tar bort kön, inte sviten.
    #
    # FÖLJD SOM BOKFÖRS ÖPPET: `ui_low_risk` (D1, task-36.3) och
    # `acceptance_local` (task-59.7) hade `run_staging` som sin ENDA konsument.
    # De beräknas fortfarande i `changed`-jobbet men styr efter denna rad
    # ingenting. Stegen står kvar MEDVETET — de är inte glömda, och de rivs inte
    # på vägen förbi: ADR-077 § Beslut 1 äger klassrymden, och S91:s
    # arbetsflödes-granskning listar riskklassningen under "ska behållas orört".
    # A7:6 (a11y, TASK-70.4) ÅTERANVÄNDE DEM INTE — den skickar `run_a11y: false`
    # villkorslöst av samma skäl som raden ovan, så levrarna är fortfarande utan
    # konsument. Frågan om de ska avvecklas eller bevaras som klassrymdens
    # invariant är ett eget beslut och fattas inte som sidoeffekt här.
    #
    # ═══ A11Y LIGGER INTE HELLER I PR-GRINDEN (A7:6 / TASK-70.4) ═══
    # `run_a11y: false` är VILLKORSLÖST. `A11y (axe-runner)` instansieras därmed
    # aldrig av ci.yml — inte på `pull_request`, inte på `merge_group` (kö-ytan,
    # TASK-70.1) och inte på `push` till main. Samma tre ytor, samma ord: ett
    # yt-villkorat uttryck hade varit tre ställen att minnas i stället för ett.
    #
    # KONTROLLEN ÄR FLYTTAD, INTE BORTTAGEN — och den fanns redan på andra
    # sidan: post-merge.yml och nightly.yml utelämnar inputen, får defaulten
    # `true` och körde a11y även FÖRE denna rad (post-merge-körning 30402869073,
    # a11y 119 s grön). Det denna rad tar bort är alltså PR-sidans DUBBLETT av
    # ett jobb som ändå körs på det landade trädet.
    #
    # RIBBAN ÄNDRAS INTE — MÄTPUNKTEN GÖR DET. Repots kvalitetsribba säger
    # tillgänglighet 11 utan undantag (CLAUDE.md § Kvalitetsribba) och
    # 0-violations-regeln (ADR-045 § Beslut 2) står orörd. Skillnaden är NÄR den
    # mäts: efter merge i stället för före. Läser någon senare denna rad som en
    # sänkt ribba är det en felläsning — inget i toleransen, måltavlan eller
    # sviten är rört.
    #
    # ÄRLIGT OM VAD SOM VINNS — INTE VÄGGKLOCKA. A11y bär 103–104 s (run
    # 30369011230; 104 s i 30408622828) men kör PARALLELLT med `Acceptance
    # (hermetisk)`, som är kritiska vägen med 424 s i samma körning. Väggklockan
    # för en kod-PR sjunker därför inte alls av denna rad — den väntade
    # besparingen är NOLL sekunder, och det är det förväntade utfallet, inte ett
    # misslyckande. Vinsten mäts i runner-minuter: ~1,7 min per kod-PR-körning,
    # plus lika mycket per merge_group-körning av samma PR.
    #
    # OFÖRÄNDRAT AV DENNA RAD: `ci-passed` behåller sin needs-lista och sin
    # fail-closed-gren, och rulesetets enda required check heter fortfarande
    # `CI Passed or Skipped`. Aggregatorn ser numera `suite`-anropet utan sina
    # två staging-jobb och utan a11y — en ändrad jobb-mängd INUTI ett anrop som
    # fortfarande rapporterar sitt resultat uppåt.
    #
    # ═══ ACCEPTANCE-URVALET (TASK-75) ═══
    # Till skillnad från de två raderna ovan är detta INTE ett villkorslöst ord
    # utan ett UTFALL: `changed`-jobbets `acceptance_urval` är tom sträng i
    # varje läge utom ett — när varenda ändrad fil utanför D0-klassen är en
    # acceptance-spec-fil som finns på disk. Tomt = full klass, och tomt är
    # defaulten vid minsta osäkerhet (scripts/acceptance-urval.sh).
    #
    # VARFÖR EN INPUT OCH INTE EN RADERING: exakt samma skäl som `run_a11y`.
    # `post-merge.yml` och `nightly.yml` utelämnar inputen, får ci-suite.yml:s
    # default (tom) och kör därmed HELA klassen på varje mergat träd. Urvalet
    # existerar alltså bara på PR-ytan, och post-merge-lagret är det nät som gör
    # det försvarbart (ADR-077 § Kontext: en riskanpassad presubmit är
    # försvarbar ENDAST med ett post-submit-nät under sig).
    with:
      run_staging: false
      run_a11y: false
      acceptance_selection: ${{ needs.changed.outputs.acceptance_urval }}
    secrets: inherit

  # Job 4: Markdown link validation. Conditional på docs-changes.
  # Per ADR-029 §1d. Ny kvalitets-check som inte fanns innan ("höjer kvaliten").
  docs:
    name: Docs link check
    needs: changed
    if: needs.changed.outputs.docs_changed == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          # fetch-depth: 0 = FULL historik (ADR-054, 2026-06-17) för
          # scripts/check-frontmatter.sh Check 2 (git log -1 --format=%cs -- <fil>).
          # Finit djup var anti-mönstret (1→50→100→250 brast 4 ggr när orörda
          # governing-docs passerade boundaryn). Konsistent med changed-jobbet;
          # invarianten (ADR-039) hålls enhetlig över alla 5 bärare.
          fetch-depth: 0

      - name: Run lychee link check (INTERN yta — offline)
        # SHA-pin per ADR-029 §6.
        # lychee-action v2.8.0 verifierad 2026-05-13 (K1.C Block 2), Rust-baserad.
        uses: lycheeverse/lychee-action@e7477775783ea5526144ba13e8db5eec57747ce8  # v2.9.0
        with:
          # ═══ ADR-082 (2026-07-28): DENNA HALVA ÄR INTERN-ONLY ═══
          # --offline byter ut lychees schema-filter till enbart "file", så varje
          # http(s)-adress klassas Status::Excluded — inte fel, inte timeout.
          # Verifierat i källkoden (lychee-bin/src/client.rs, taggen lychee-v0.24.2),
          # inte antaget ur hjälptexten.
          #
          # VARFÖR: grinden blockerar PR:er, och 467 av scopets 903 unika länkar är
          # externa, fördelade på 125 skilda värdar. Var och en kunde ensam rödmåla
          # en PR som inte rör den. Av nio undersökta branschledande projekt låter
          # NOLL externa länkar blockera en PR. Extern yta kontrolleras i stället
          # kallt varje natt (nightly.yml → nightly-links) med egen ärende-kanal.
          # Röd docs-grind betyder från och med nu "vår text pekar fel", aldrig
          # "internet hade en dålig dag".
          #
          # CACHEN ÄR BORTTAGEN med samma ändring: en körning utan nätverk har
          # ingenting att cacha. Mätt kostnad för hela den interna kontrollen är
          # 31 ms. --cache-exclude-status '429' var dessutom redan en no-op —
          # felcachning togs bort i lychee v0.24.0 och vi kör v0.24.2 sedan
          # 2026-07-19. --header (User-Agent) föll av samma skäl: ingen begäran skickas.
          #
          # Scope efter Marcus' Gate 2 anti-genväg-review + Commit 4a scope-konsistens:
          # INKLUDERAR: aktiva docs + aktiva tasks-dok + AKTIVA sessionsdok
          #             (tasks/sessions/*.md, Session 17 K7 — stänger L91/L97-
          #             felklassen; grind A-mätt 0-skuld) + repo-root .md
          # UTELÄMNAR: tasks/sessions/archive/** (frozen ADR-023) — K1.D §0 utelämning #1
          #            docs/archive/** (frozen ADR-023-mall, normaliserad i Commit 4a 2026-05-14)
          #            docs/reference/pocock/** (vendorerat externt Pocock-material, S47 Steg 0 —
          #            samma rationale som archive/: extern korpus, ej vår prosa)
          #            backlog/** (medvetet utanför — verktygsägd yta, L226; S48 Del 2 gren C)
          #
          # .claude/**/*.md TILLAGT S91 (TASK-71): agentdefinitionerna klassas nu
          # som docs i changed-files-stegen ovan, och då måste grindarna faktiskt
          # läsa dem. --exclude-path .claude/worktrees är no-op HÄR (worktrees är
          # gitignorerade och checkas aldrig ut i CI) men står med för att
          # scripts/check-docs.sh speglar detta block i sak — den lokala
          # körningen behöver raden, och divergens mellan de två är felklassen
          # skriptet finns för att stänga.
          #
          # SPEGLINGEN ÄR SEDAN TASK-85 GRINDAD, inte längre bara utlovad:
          # markörerna nedan avgränsar blocket för scripts/check-listparitet.sh,
          # som jämför det mot check-docs.sh:s motsvarande markerade block. Alla
          # kommentarer måste stå OVANFÖR start-markören — regionen mellan
          # markörerna läses som data.
          #
          # './tasks/lessons/*.md' TILLAGT TASK-161.9 (2026-08-08,
          # ADR-085-volymformen): 'tasks/*.md' korsar inte in i underkatalogen
          # (samma gräns-fråga som lessons.d ovan) — utan raden lämnas
          # volymfilernas länkar tyst okontrollerade.
          # paritet:start lychee-ci
          args: >-
            --offline
            --no-progress
            --verbose
            --exclude-path docs/archive
            --exclude-path docs/reference/pocock
            --exclude-path .claude/worktrees
            './docs/**/*.md'
            './tasks/*.md'
            './tasks/sessions/*.md'
            './tasks/threads/*.md'
            './tasks/lessons.d/*.md'
            './tasks/lessons/*.md'
            './.claude/**/*.md'
            './*.md'
          # paritet:slut lychee-ci
          fail: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check markdown hygiene (markdownlint-cli2)
        # Per ADR-030 § Del 1.1: markdownlint-cli2 som docs-grindvakt efter
        # Lychee. Config: .markdownlint-cli2.jsonc. Scope definierad i
        # config-filens "globs"-fält (docs + tasks + repo-root .md).
        # Sessionsdok-archive + docs/archive exkluderade.
        # Ingen pre-commit-hook — CI-only-grindvakt per Marcus' direktiv K4.
        run: npx markdownlint-cli2

      - name: Install Vale
        # Per ADR-030 § Del 1.3: Vale-binary-install via curl + tar (analog
        # till actionlint per ADR-029 § Utelämning #3). vale-action är
        # underhåll-osäker (~17 mån gammal); binary minimerar Actions-
        # supply-chain-yta.
        # SHA256 tillagd S88 (T85-korrigeringspaketet punkt 3) — steget hade
        # actionlint-formens curl+tar men saknade dess checksumme-koll, alltså
        # halva mönstret. Siffran verifierad tre oberoende vägar 2026-07-25:
        # egen nedladdning + sha256sum, Vale-projektets vale_3.14.1_checksums.txt,
        # och Releases-API:ts digest-fält — identiska. Releases är immutabla;
        # re-verifiering: ladda samma URL + sha256sum lokalt.
        # NOT: errata-ai/vale är omdöpt till vale-cli/vale — URL:en nedan lever
        # på GitHub-redirect (`-L` i curl-formen nedan). Byte av org-namn görs
        # vid nästa versions-bump, inte här (URL + checksumma hör ihop).
        #
        # TILLGÄNGLIGHET (TASK-92, 2026-07-30) — curl-flaggorna bär samma
        # rationale som `Install shellcheck (pinned v0.11.0)` i lint-jobbet, där
        # den är skriven i sin helhet: `--retry` ensam täcker en UPPRÄKNAD
        # felmängd som INTE rymmer exit 35 (CURLE_SSL_CONNECT_ERROR), och
        # --retry-all-errors (curl 7.71.0+; runnern kör 8.5.0) är flaggan som
        # täcker den. `-o` är förutsättningen — curl nollställer en
        # fildestination mellan försöken, en shell-omdirigering inte.
        # Detta var mönstrets TREDJE instans; TASK-83 åtgärdade de två i
        # lint-jobbet och RAPPORTERADE denna i stället för att tyst fixa den.
        #
        # EXPONERINGEN ÄR MÄTT, INTE ANTAGEN (2026-07-30): att docs-jobbet är
        # villkorat på `docs_changed` gör den INTE lägre. Över 300 ci.yml-runs
        # (2026-07-28T19:23Z→2026-07-30T19:15Z, `jobs?filter=all` så alla
        # attempts räknas) kördes detta steg 289 gånger, mot 305 för det
        # alltid-på lint-jobbets två steg — 95 %. Villkoret tar bort en
        # tjugondel av exekveringarna, inte en storleksordning, och därför
        # bär steget samma flaggor och inte en svagare variant.
        # sha256sum-verifieringen nedan är ORÖRD: kortet rör tillgänglighet,
        # inte pinning.
        run: |
          VALE_VERSION="3.14.1"
          VALE_SHA256="ff2b49ffaa9dcd246fd5008f03ff67746d2790b75bf4d3657e2fb9530fb96db3"
          VALE_URL="https://github.com/errata-ai/vale/releases/download/v${VALE_VERSION}/vale_${VALE_VERSION}_Linux_64-bit.tar.gz"
          curl -fsSL --retry 5 --retry-all-errors --retry-max-time 60 "${VALE_URL}" -o /tmp/vale.tar.gz
          echo "${VALE_SHA256}  /tmp/vale.tar.gz" | sha256sum -c
          tar -xzf /tmp/vale.tar.gz -C /tmp
          sudo mv /tmp/vale /usr/local/bin/vale
          vale --version

      - name: Check prose with Vale
        # Per ADR-030 § Del 1.3 (post-K6.1+K6.2): Vale.Spelling AV (F.1'
        # engelsk-hunspell-mismatch), Vale.Repetition + Miranon-stilguide
        # (VueToReact + Brand + Undvik) + Vocab-genererad Vale.Terms.
        # Vale.Terms + Miranon.VueToReact deferade till Session 6.6.6 via
        # per-fil rad-1-disable (Alt F per K6.2 Steg 7).
        # Single-source: samma definition som lokalt `npm run lint:prose`
        # (package.json). Vale-binary installerad i föregående step; npm finns
        # via setup-node ovan. Session 7 K0.S2.
        run: npm run lint:prose

      - name: Validate Vale L_X.2 regression-test-suite
        # Per ADR-032 § Regression-skydd: 3-test AssertFlip-suite för
        # L_X.2-upstream-bug lift-trigger. T1 inverterad assertion
        # (PASS = bug bekräftad fortfarande); FAIL = lift-protokoll
        # aktiveras. T2 helfil-disable suppression. T3 Brand-rule
        # ej maskerad. Vale-binary redan installerad i föregående step.
        # Etablerad: Session 6.6.6 K3.6 (2026-05-20).
        run: bash scripts/test-vale-regression.sh

  # Job 5: Review-backstoppen (TASK-173.4, ADR-105 beslut 2/3) — den
  # deterministiska spärren mot att en kod-PR landar utan granskningsutlåtande.
  #
  # ═══ VARFÖR `merge_group` OCH INTE PR-YTAN ═══
  # Review-grindens sekvens är push → granskning → sektion → armering
  # (CLAUDE.md § Review-grinden). Vid PUSH saknas sektionen med NÖDVÄNDIGHET
  # — granskningen har inte hänt än. En grind på PR-ytan hade därför gjort
  # varje kod-PR röd som NORMALTILLSTÅND, vilket bryter mot CONTRIBUTING.md
  # § Rött-först: "rött i CI ska betyda EN sak: oväntad regression". På
  # kö-ytan är läget det motsatta — PR:en är armerad, alltså MÅSTE
  # granskningen ha skett, och rött betyder exakt en sak. Se skriptets
  # filhuvud för hela resonemanget och för varför en fällning här är dyr
  # (utsparkning ur kön konsumerar armeringen) och därför ska föregripas av
  # `npm run review:backstopp -- --pr <nr>` FÖRE armering.
  #
  # ═══ AC #3: INGEN EGEN KLASSNINGSLOGIK ═══
  # `needs.changed.outputs.should_skip_tests` ÄR D0-klassningen — samma
  # allowlist-glob (`paritet:start klassning-d0`) som resten av CI gatar på.
  # Backstoppen duplicerar den aldrig. Att `changed` beräknar rätt värde även
  # på kö-ytan är MÄTT, inte antaget: merge_group-körning 33138424216
  # (docs-only) skippade `Test suite` på samma output, 2026-08-28.
  #
  # ═══ AC #4: DETERMINISTISK, INGEN LLM ═══
  # Ett node-skript utan nätverk utöver ETT `gh pr view`-anrop, utan klocka
  # och utan slumpkälla. Ingen modell anropas i CI, och kan inte anropas:
  # jobbet installerar inga beroenden alls (se nedan).
  #
  # INGET `npm ci` — MED AVSIKT. scripts/review-backstopp.mjs och dess lib
  # importerar bara scripts/lib/review-risk-sektion.mjs, som självt saknar
  # importer. En backstopp ska inte kunna släckas av att det led den skyddar
  # (beroende-installationen) fallerar. Steget "Install dependencies" är
  # därför medvetet frånvarande, inte glömt.
  review-backstopp:
    name: Review-backstopp (granskningsutlåtande)
    needs: [changed]
    if: ${{ github.event_name == 'merge_group' && needs.changed.outputs.should_skip_tests != 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      # pull-requests: read — `gh pr view` läser kroppen + headRefOid på den
      # PR kö-grenen namnger. Minsta grant utöver contents:read.
      pull-requests: read
    steps:
      # INGEN `fetch-depth:`-nyckel här — MEDVETET, och raden står kvar för att
      # nästa läsare inte ska "rätta" tillbaka den. `scripts/check-fetch-depth-
      # invariant.sh` (ADR-039-grinden i lint-jobbet) kräver EXAKT TRE
      # fetch-depth-bärare i denna fil (changed/lint/docs), alla med SAMMA
      # värde. En fjärde bärare fäller grinden oavsett vilket värde den bär —
      # mätt skarpt 2026-08-28 på detta jobbs första CI-körning (`fetch-depth:
      # 1` gav "hittade 4 fetch-depth-config-rader (förväntat 3)"). Och jobbet
      # BEHÖVER ingen historik: det läser två skript ur arbetsträdet och hämtar
      # allt annat via `gh`. Checkoutens default (djup 1) är alltså både rätt
      # för jobbet och det enda som håller invarianten.
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version-file: '.nvmrc'
          # INGEN `cache: 'npm'` — jobbet kör aldrig `npm ci`, så det finns
          # inget att cacha och en cache-miss ska aldrig kunna sänka grinden.

      - name: Verifiera granskningsutlåtandet
        env:
          MG_REF: ${{ github.event.merge_group.head_ref }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: node scripts/review-backstopp.mjs --merge-group-ref "${MG_REF}"

  # Job 6: Aggregator — REQUIRED CHECK för main-rulesetet (ADR-076).
  # Per ADR-029 §1e + S77-incidenten (PR #101): jobbet KÖR ALLTID och
  # rapporterar success ELLER failure — ALDRIG skipped. Gamla formen
  # (if-villkor som skippade jobbet vid failure i needs) var FAIL-OPEN
  # som required check: GitHub räknar en skipped check som uppfylld →
  # auto-merge mergade en RÖD PR (docs-jobb failure, aggregator skipped;
  # run 30023934304 → main-rött 30024005788). Detta är dual-signal-
  # behovet som ADR-029 utelämning #5 förutsåg ("explicit failure-signal
  # överflödig utan automation som läser den") — rulesetet ÄR den
  # automationen; signalen aktiverad här. Cancelled-gotchan (Commit 1.5:
  # per-job-cancellation osynlig för cancelled()) täcks av samma
  # needs-läsning: cancelled ⇒ explicit fail.
  # Fail-grenens gate-proof: gate-proof.yml (task-36.1, levererad S78) —
  # bevis-skulden BETALD (tvåsidigt: run 30032296699 grön + self-test
  # 30032299223 röd). Kör gate-proof.yml efter varje ci.yml-ändring (T85).
  ci-passed:
    name: CI Passed or Skipped
    if: ${{ always() }}
    # review-backstopp tillagt TASK-173.4: jobbet SKIPPAS på PR-ytan
    # (`github.event_name == 'merge_group'`) och på D0-diffar — och en
    # skipped post är per fail-closed-logiken nedan "skipped-by-design",
    # alltså grön. Det är avsikten: spärren ska bita i landnings-ögonblicket,
    # inte göra varje opgranskad PR röd (se jobbets eget kommentarsblock).
    needs: [changed, lint, docs, suite, review-backstopp]
    runs-on: ubuntu-latest
    timeout-minutes: 1
    permissions: {}
    steps:
      - name: Verifiera needs-resultaten (fail-closed)
        env:
          NEEDS: ${{ toJSON(needs) }}
        run: |
          echo "${NEEDS}" | jq -r 'to_entries[] | "\(.key): \(.value.result)"'
          bad=$(echo "${NEEDS}" | jq -r '[to_entries[] | select(.value.result == "failure" or .value.result == "cancelled")] | length')
          if [ "${bad}" -ne 0 ]; then
            echo "❌ ${bad} jobb med failure/cancelled — aggregatorn FAILAR explicit (aldrig skipped; required-check-hålet stängt S77)."
            exit 1
          fi
          echo "✅ CI Passed or Skipped — alla jobb success eller skipped-by-design."
