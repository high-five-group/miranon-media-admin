name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    name: Lint + TypeCheck + Test + Build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Audit dependencies (audit-ci with allowlist)
        run: npx audit-ci --config audit-ci.jsonc
        # audit-ci ersätter `npm audit --audit-level=high` per K0åg + ADR-028.
        # Allowlist GHSA-rmmr-r34h-pfm5 är aktiv för pre-malware @tanstack/history@1.161.6.
        # Fail-fast supply-chain-gate FÖRE secrets exponeras till staging-tester.
        # Granskas veckovis per tasks/todo.md.

      - name: Biome check
        run: npx @biomejs/biome check .

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: TypeScript check (tests)
        run: npm run typecheck:tests

      - name: API tests (pure)
        run: npm run test:api:pure

      - name: API tests (staging)
        run: npm run test:api:staging
        env:
          STAGING_REQUIRED: '1'
          TEST_SUPABASE_URL: ${{ secrets.TEST_SUPABASE_URL }}
          TEST_SUPABASE_ANON_KEY: ${{ secrets.TEST_SUPABASE_ANON_KEY }}
          TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}
          TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
          TEST_ADMIN_EMAIL: ${{ secrets.TEST_ADMIN_EMAIL }}
          TEST_ADMIN_PASSWORD: ${{ secrets.TEST_ADMIN_PASSWORD }}

      - name: Build
        run: npm run build
