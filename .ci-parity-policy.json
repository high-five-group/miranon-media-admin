{
  "_readme": [
    "Config för scripts/verify-ci-parity.mjs — samma hub-spoke-portabilitet som",
    ".purge-staging-policy.json och .staging-preflight-wiring-policy.json.",
    "Skriptets LOGIK är generisk (YAML-parsning + steg-klassning + verbatim-",
    "körning); VÄRDENA här är projekt-specifika.",
    "",
    "VARFÖR DEN HÄR FILEN INTE ÄR EN GRIND-LISTA. Målet är INTE att räkna upp",
    "varenda grind CI kör (det vore en trettonde-instans av samma duplicerings-",
    "problem uppdraget bad oss undvika). Varje jobb i `derivedJobs` läses från",
    "workflow-filen VID VARJE KÖRNING och EXEKVERAS VERBATIM steg för steg — en",
    "ny rad i ett känt jobbs `run:`-block plockas upp automatiskt, ingen",
    "manifest-post krävs. Vad den HÄR filen deklarerar är bara de undantagen",
    "som INTE ska köras om (redan täckta av `npm run check:docs`, ren",
    "CI-infrastruktur som Checkout/Setup Node, eller binärer som behöver en",
    "version-koll i stället för en ny nedladdning). Kvarvarande driftrisk är",
    "alltså en NY JOBB (som denna fil inte känner till) eller GH-uttryck",
    "(`${{ ... }}`) i ett härlett steg som ingen substitution täcker — båda",
    "fäller skriptet fail-closed, se `knownJobs` / `exprSubstitutions`.",
    "",
    "Se scripts/verify-ci-parity.mjs:s laddaPolicy()/klassificeraSteg()/huvudflödet i main() för hur fälten nedan faktiskt konsumeras."
  ],
  "workflows": {
    "ci": ".github/workflows/ci.yml",
    "ciSuite": ".github/workflows/ci-suite.yml"
  },
  "knownJobs": {
    "ci": {
      "changed": "producerar klassnings-metadata (should_skip_tests/docs_changed/dedup_hit/ui_low_risk/acceptance_urval) — inget steg i jobbet validerar arbetsträdet självt. TASK-142 (2026-08-05, rättar tidigare rad här som sa 'denna policy medvetet ALLTID kör' — det var sant t.o.m. 2026-08-05 men aldrig gratis: mätt 641,0 s och 824,8 s i två fulla körningar på en ren docs-diff, för noll extra säkerhet): `should_skip_tests` KONSUMERAS numera av `diffClassification` för att härma den EXAKTA delmängd ci.yml:s `suite`-jobb självt skippar (se diffClassification-blocket nedan). `ui_low_risk`/`acceptance_local`/`docs_changed`/`dedup_hit` konsumeras INTE — verifierat i ci.yml 2026-08-05: de förra två saknar konsument på PR-ytan sedan A7:5/TASK-70.3 (endast `run_staging`, villkorslöst false), och att lägga till dem här vore att gissa en delmängd ingen gating faktiskt stödjer.",
      "lint": "DERIVERAS — se derivedJobs.ci.",
      "suite": "delegerar via `uses:` till ci-suite.yml. Har inga egna steg — de verkliga grindarna härleds direkt ur ci-suite.yml (se derivedJobs.ciSuite). Jobbets `with:`-block vaktas separat, se `suiteInputInvariants`.",
      "docs": "DERIVERAS — se derivedJobs.ci. Jobbets `if: docs_changed`-villkor ignoreras medvetet: att köra docs-grindarna på en icke-docs-PR är en säker superset, aldrig en lucka.",
      "review-backstopp": "TASK-173.4 (2026-08-28): review-grindens CI-backstopp. DERIVERAS INTE, och det är en egenskap hos jobbet — inte en lucka. Jobbet är `if: github.event_name == 'merge_group'`-gatat och prövar en LEVANDE GitHub-resurs (PR-kroppens Riskbedömnings-sektion via `gh pr view` på den PR kö-grenens namn pekar ut). Det finns ingen merge_group-ref och ingen köad PR i ett lokalt arbetsträd, så steget kan strukturellt inte köras verbatim av denna grind — att härleda det hade gett ett garanterat fel på varje lokal körning (exit 4 — brutet eller frånvarande merge_group-antagande — eller exit 3 om gh inte når PR:en), alltså brus i stället för täckning. Jobbets LOGIK är i stället CI-täckt av gatekeeper-sviten `scripts/test-review-backstopp.mjs` (40 fall MÄTT 2026-08-28, tvåsidig, körs i `lint`-jobbets 'Test gatekeeper script suites'-steg och DERIVERAS därmed hit via derivedJobs.ci.lint). Vill du pröva backstoppen lokalt mot en verklig PR: `npm run review:backstopp -- --pr <nr>`.",
      "ci-passed": "ren aggregator — läser `needs`-resultat, innehåller ingen egen grind."
    },
    "ciSuite": {
      "purge": "gated av inputs.run_staging, som ci.yml:s suite-jobb skickar VILLKORSLÖST false (mekaniskt verifierat, se suiteInputInvariants) — instansieras aldrig på PR-ytan.",
      "test-fast": "DERIVERAS — se derivedJobs.ciSuite.",
      "acceptance": "DERIVERAS — se derivedJobs.ciSuite. TASK-239 VARV 3 (2026-09-02): jobbet bär nu `strategy.matrix.shard` (villkorad på `inputs.acceptance_selection`, se ci-suite.yml:s kommentar vid jobbet) — den YAML-strukturella job-mängden är oförändrad (ETT jobb `acceptance` i workflow-filen; matrisen multiplicerar bara vid KÖRTID), så `verifieraJobbmangd` ser samma jobbnamn som förut och kräver ingen ny post här. Vad matrisen påverkar är de nya GH-uttrycken i steget `Acceptance tests (hermetiska)`s env-block — se `exprSubstitutions`.",
      "acceptance-sjalvtest": "DERIVERAS — se derivedJobs.ciSuite. TASK-239 (2026-08-17): hermetik-självtestet var ett STEG i `acceptance` och är nu ett eget parallellt jobb (motivering i ci-suite.yml vid jobbet). Paritetsytan är OFÖRÄNDRAD i sak — samma `npm run test:acceptance:sjalvtest`-steg härleds och körs verbatim som förr, bara ur ett annat jobb. Posten finns här därför att verifieraJobbmangd är fail-closed mot HELT NYA jobb (filhuvudets § PARITETS-GRINDEN punkt a), inte för att täckningen ändrats.",
      "webblasarbeteende": "DERIVERAS — se derivedJobs.ciSuite.",
      "a11y": "gated av inputs.run_a11y, som ci.yml:s suite-jobb skickar VILLKORSLÖST false (mekaniskt verifierat, se suiteInputInvariants) — instansieras aldrig på PR-ytan.",
      "test-staging": "gated av inputs.run_staging, samma skäl som purge — kräver dessutom staging-secrets som inte finns lokalt.",
      "purge-efter": "TASK-309.15 (2026-08-24): efter-körning-purgen, samma gate som purge/test-staging (inputs.run_staging, villkorslöst false på PR-ytan) och samma secret-krav (STAGING_AIRTABLE_TOKEN). Instansieras därför aldrig på PR-ytan och kan inte köras lokalt av denna grind. Posten finns här därför att verifieraJobbmangd är fail-closed mot HELT NYA jobb (filhuvudets § PARITETS-GRINDEN punkt a) — inte för att täckningen ändrats."
    }
  },
  "derivedJobs": {
    "ci": ["lint", "docs"],
    "ciSuite": ["test-fast", "acceptance", "acceptance-sjalvtest", "webblasarbeteende"]
  },
  "diffClassification": {
    "_rationale": "TASK-142 (2026-08-05). Läser D0-globen (jobbet `changed`, steget `changed-files`, `with.files`) UR ci.yml vid varje körning — aldrig en handkopierad glob-lista, se filhuvudets § DIFF-KLASSNINGEN. En diff är DOCS_ONLY endast om VARENDA ändrad fil (committat mot baseRef ∪ arbetsträd ∪ otrackat) matchar D0-globen — allowlist, aldrig blocklist, samma invariant som D0/D1 i ci.yml självt. DOCS_ONLY skippar `derivedJobs.ciSuite` (test-fast/acceptance/acceptance-sjalvtest/webblasarbeteende — det fjärde tillkom i TASK-239 när hermetik-självtestet blev eget jobb; `suite`-jobbets `if:` gatar hela det anropade workflowet, så delningen ändrar inte VILKA jobb som skippas, bara hur många namn samma skipp har) — EXAKT den delmängd ci.yml:s `suite`-jobb självt skippar när `should_skip_tests` är sant. Detta är fortsatt en superset i den riktning som skyddar: klassningen kan bara köra MER än CI (fail-closed → FULLT läge vid minsta osäkerhet), aldrig MINDRE. `gatedJobIfMustContain` vaktar STRUKTURELLT att kopplingen mellan `should_skip_tests` och `suite`-jobbets `if:`-villkor fortfarande finns (verifieraDiffKlassningskoppling, körs i preflighten). Drifar den fäller preflighten EXIT_PARITY_BROKEN, samma mönster som suiteInputInvariants — det är den ENDA situationen där diff-klassningsfel ger exit 2. Kan D0-globen inte hittas/tolkas, eller kan diffen inte beräknas (git-kommando misslyckas, ingen `origin/main` lokalt) faller klassningen i stället till FULLT läge — aldrig exit 2 för DEN sortens osäkerhet, aldrig en gissad delmängd.",
    "job": "changed",
    "stepId": "changed-files",
    "baseRef": "origin/main",
    "gatedJob": "suite",
    "gatedJobIfMustContain": "needs.changed.outputs.should_skip_tests"
  },
  "suiteInputInvariants": [
    {
      "job": "suite",
      "input": "run_staging",
      "mustBeLiteral": false,
      "why": "Om detta någonsin blir sant/villkorat måste purge + test-staging tas in i derivedJobs.ciSuite — de kräver staging-secrets som denna lokala grind inte har. Ett tyst genomsläpp här vore precis den under-täckning uppdraget finns för att stänga."
    },
    {
      "job": "suite",
      "input": "run_a11y",
      "mustBeLiteral": false,
      "why": "Samma skäl som run_staging — a11y-jobbet måste tas in i derivedJobs.ciSuite om detta slutar vara ett villkorslöst false."
    }
  ],
  "stepClassification": {
    "infra": [
      "Checkout",
      "Setup Node",
      "Install dependencies",
      "Cache Playwright browsers",
      "Ladda upp Playwright-artefakter vid rött",
      "Ladda upp Playwright-artefakter vid rött e2e"
    ],
    "infraRationale": "Steg som förbereder CI-runnerns MILJÖ (checkout, node-version, npm ci, cache-restore) eller som bara kör vid fel (artefakt-uppladdning, if: failure()). Inget av dem validerar arbetsträdet — att köra dem lokalt vore antingen meningslöst (repot är redan uppcheckat) eller farligt (npm ci mot ett symlänkat node_modules delat med huvudrepot).",
    "coveredByCheckDocs": [
      "Run lychee link check (INTERN yta — offline)",
      "Check markdown hygiene (markdownlint-cli2)",
      "Check prose with Vale",
      "Validate Vale L_X.2 regression-test-suite",
      "Check public docs for unchecked items",
      "Validate frontmatter on governing docs",
      "Validate lifecycle field on session docs (ADR-052 gate)",
      "Validate thread index integrity (TASK-108 gate)",
      "Check ADR count consistency (ADR-039 gate)",
      "Check lesson numbering (ADR-081 gate)",
      "Check permissions claims (ADR-083 gate)",
      "Check fetch-depth invariant (ADR-039 gate)",
      "Check list parity (TASK-85 gate)"
    ],
    "coveredByCheckDocsRationale": "De TRETTON grindar scripts/check-docs.sh redan kör (dess egen 'DE TRETTON'-uppräkning). Att härleda-och-köra dem HÄR ISTÄLLET vore en tredje kopia av samma anrop — check:docs körs som ett eget steg i verify-ci-parity.mjs INNAN dessa jobb läses, se scripts/verify-ci-parity.mjs."
  },
  "specialSteps": {
    "Install shellcheck (pinned v0.11.0)": {
      "handling": "versionAssert",
      "tool": "shellcheck",
      "pinRegex": "SC_VERSION=\"v?([0-9.]+)\"",
      "versionArgs": ["--version"],
      "versionOutputRegex": "version:\\s*([0-9.]+)",
      "remedyHint": "shellcheck saknas eller matchar inte CI:s pinnade version. macOS: `brew install shellcheck` eller `brew upgrade shellcheck`. CI:s exakta recept står i steget ovan (curl+sha256sum+tar) om du hellre vill spegla det manuellt."
    },
    "Install Vale": {
      "handling": "versionAssert",
      "tool": "vale",
      "pinRegex": "VALE_VERSION=\"([0-9.]+)\"",
      "versionArgs": ["--version"],
      "versionOutputRegex": "vale version\\s*([0-9.]+)",
      "remedyHint": "vale saknas eller matchar inte CI:s pinnade version. macOS: `brew install vale` eller `brew upgrade vale`."
    },
    "Check workflow files (actionlint)": {
      "handling": "actionlintHybrid",
      "pinRegex": "AL_VERSION=\"([0-9.]+)\"",
      "versionArgs": ["--version"],
      "versionOutputRegex": "^([0-9.]+)$",
      "ignoreFlagRegex": "-ignore\\s+'([^']*)'",
      "remedyHint": "actionlint saknas eller matchar inte CI:s pinnade version — faller tillbaka på att köra CI:s egen nedladdning+sha256-verifiering ur workflow-blocket (kräver nätverk, ingen sudo)."
    },
    "Install yamllint (pinned 1.38.0)": {
      "handling": "versionAssert",
      "tool": "yamllint",
      "pinRegex": "yamllint==([0-9.]+)",
      "versionArgs": ["--version"],
      "versionOutputRegex": "yamllint\\s+([0-9.]+)",
      "remedyHint": "yamllint saknas eller matchar inte CI:s pinnade version. macOS: `brew install yamllint` eller `brew upgrade yamllint` (bar `pip` finns ofta inte på Homebrew-Python; `pip3 install \"yamllint==<pin>\"` funkar också)."
    }
  },
  "specialStepsRationale": "\"Check YAML syntax (yamllint)\" konflaterade tidigare bootstrap (pip install) och grind (yamllint .github/) i EN run:-rad, opinnat — repots ENDA CI-verktyg utan version-pin (TASK-312, 2026-08-24). Löst genom att SEPARERA ci.yml:s steg: \"Install yamllint (pinned 1.38.0)\" (versionAssert, samma mönster som shellcheck/vale ovan — pip:s --require-hashes-läge kräver att HELA yamllints transitiva dependency-träd pinnas+hash-verifieras, empiriskt bekräftat 2026-08-24 och avvisat som oproportionerlig skörhet för ett enda docs-lint-verktyg, se ci.yml:s egen kommentar vid steget) och \"Check YAML syntax (yamllint)\" (nu ren `yamllint .github/`, oklassad → körs verbatim via derive-vägen, ingen special-hantering behövs längre). scripts/verify-ci-parity.mjs:s hanteraToolPresenceLastLine() är kvar som allmän mekanism för en FRAMTIDA konflaterad situation, men ingen post här namnger den handlingen längre.",
  "exprSubstitutions": {
    "${{ inputs.acceptance_selection }}": "",
    "${{ matrix.shard }}": "1",
    "${{ strategy.job-total }}": "1"
  },
  "exprSubstitutionsRationale": "GH-Actions-uttryck i acceptance-jobbets env-block. `inputs.acceptance_selection` → tom sträng ⇒ FULL acceptance-klass körs lokalt, samma default som post-merge.yml och nightly.yml — en säker superset av vad ci.yml:s PR-urval (TASK-75) hade kört. `matrix.shard`/`strategy.job-total` → \"1\"/\"1\" (TASK-239 VARV 3, 2026-09-02, sharding av acceptance-jobbet): Playwrights `--shard=1/1` är dess no-op-shard (hela testmängden i EN shard, samma `filterForShard`-källa som riktiga shards, se ci-suite.yml:s kommentar vid jobbet) — den lokala FULLA körningen fortsätter alltså täcka HELA klassen i EN process, den prövar bara aldrig CI:s tre-parallella-jobb-topologi. Varje ANNAT `${{ ... }}`-uttryck som dyker upp i ett härlett steg (nytt secret, ny input) fäller skriptet fail-closed i stället för att köras blint — se scripts/verify-ci-parity.mjs:s hittaOhanteradeUttryck()/losStegEnv()."
}
