# shellcheck shell=bash
# shellcheck disable=SC2034
# Variablerna här sourcas och konsumeras av scripts/check-codeql-d0-kodfri.sh.
# Verktyget kan inte följa cross-file-användning, så SC2034 ("appears unused")
# är falskt positiv. Disable är fil-nivå och avsiktlig — samma form som
# .listparitet-policy.conf och .backlog-closure-policy.conf.
#
# RADEN OVAN BÖRJAR MED "Verktyget", INTE med verktygets namn — se
# .listparitet-policy.conf:s egen kommentar om SC1072/SC1073-fällan.

# .codeql-d0-kodfri-policy.conf
#
# Per-projekt config för scripts/check-codeql-d0-kodfri.sh.
# TASK-464.2, review runda 1 fynd 1 (warning): CodeQL:s D0-undantag
# (.github/workflows/codeql.yml) återanvänder ci.yml:s D0-allowlist på
# premissen att sökvägarna är kodfria. Stickprov (orkestreraren,
# 2026-09-19, `git ls-files 'tasks/**' 'docs/**' '.claude/**' | grep -E
# '\.(m?js|cjs|ts|tsx|jsx)$'`) fann åtta spårade filer som falsifierar
# premissen rakt av; review runda 2 utökade grindens § ANALYSERBARHET mot
# CodeQL:s egen extraktor-dokumentation och fann en NIONDE (en HTML-fil med
# ett äkta `<script>`-block, se posten nedan). Denna fil bär de VERIFIERADE
# undantagen — filer som är kod till formen men vars körning/deploy aldrig
# sker via npm, en workflow eller vår servade app, alltså säkra att låta
# CodeQL hoppa.
#
# ═══════════════════════════════════════════════════════════════════════════
# FORMEN
# ═══════════════════════════════════════════════════════════════════════════
#
# En rad per fil, TVÅ fält, separator `:::`
#
#   <sökväg, exakt som git ls-files skriver den>:::<skäl>
#
# Skälet ska vara VERIFIERAT, inte antaget. Körs filen av ett npm-skript
# eller en workflow hör den INTE hemma i en dokumentkatalog — flytta den
# i stället för att undanta den (grinden fäller ändå tills den flyttas).
#
# ═══════════════════════════════════════════════════════════════════════════
# DE ÅTTA POSTERNA, VAR OCH EN VERIFIERAD 2026-09-19
# ═══════════════════════════════════════════════════════════════════════════
#
# Verifieringsmetod, samma för samtliga åtta:
#   grep -n "<filnamn-utan-ändelse>" package.json           → 0 träffar
#   grep -rln "<filnamn-utan-ändelse>" .github/workflows/    → 0 träffar
#   plus filens egen källkodskommentar, läst för kontext.
#
# docs/backfill/segment-export/{export,segments,skool-partition}.mjs
#   S60 Steg 4 (2026-07-09) — engångs-backfill/CSV-export mot prod-basen
#   (app8uGPrVCVOm6LfD) via AIRTABLE_API_KEY i miljön, körd EN gång för
#   hand av Marcus/agent. Ingen npm-post, ingen workflow-referens. Nämns i
#   tasks/sessions/archive/2026-07/2026-07-08-session-60.md,
#   tasks/threads/T171-personuppgifter-i-publikt-repo.md och
#   docs/BUILD-LOG.md — enbart som historisk narrativ, inte
#   exekveringsinstruktion.
#
# docs/reference/automation-scripts/a1-eventmatchning-vakt.js
#   Frusen kopia av Airtable-automationens skriptkod (TASK-284.2,
#   ADR-122). Filens EGEN källkodskommentar säger det rakt ut: "Körs i
#   Airtable som ett customScript-steg i automation wflDCKPAv2P6Yu9U6" —
#   exekveringsmiljön är Airtables automationsmotor, aldrig detta repos
#   CI eller npm. Samma klass som docs/reference/schema_reference.md:s
#   A1–A11-skriptkod (CLAUDE.md § Prod-basens UI-/automations-lager).
#
# tasks/sessions/bilagor/s121-bekraftelsesteget-konvergens/riggen-proto-shot.mjs
#   Fristående Playwright-skärmdumpsrigg, körd manuellt via
#   `node proto-shot.mjs <utkatalog> <namn>=<sökväg>...` under en enskild
#   sessions konvergens-pass (S121). Sessionsbilaga — arkiverat
#   engångsverktyg, ingen npm/workflow-referens.
#
# tasks/sessions/bilagor/s122-pushback-bank/{bygg-bank,extrahera-pushbacks,steg3-klassificering}.mjs
#   Manuell, programmatisk pushback-extraktionspipeline (S122) — tre
#   skript som importerar varandra (bygg-bank.mjs importerar
#   steg3-klassificering.mjs) men körs som en ren Node-pipeline för hand,
#   aldrig via npm eller CI. Sessionsbilaga, samma klass som ovan.
#
# Alla åtta ligger under `tasks/sessions/bilagor/**`, `docs/backfill/**`
# eller `docs/reference/automation-scripts/**` — arkivkataloger, inte
# källkodskataloger. Ingen av dem importeras av något i `src/`,
# `scripts/` eller `supabase/functions/` (verifierat: `grep -rln` på
# respektive filnamn över de tre katalogerna gav noll träffar).
#
# ═══════════════════════════════════════════════════════════════════════════
# DEN NIONDE POSTEN — TILLAGD review runda 2 fynd 1, 2026-09-19
# ═══════════════════════════════════════════════════════════════════════════
#
# docs/design/farg-atlas.html — hittad av grindens utökade § ANALYSERBARHET
# (skriptets eget filhuvud): filen bär ett äkta `<script>`-block (4 rader,
# en temaväxlare som läser `?theme=`-querysträngen). Till skillnad från de
# åtta ovan är den INTE ett engångsskript — den REGENERERAS av `npm run
# atlas` (`scripts/build-farg-atlas.mjs`), och en separat verifieringsscript
# (`scripts/verifiera-farg-atlas.mjs`) läser dess TEXT (inte dess körning)
# för att pröva kontrastformler. Undantags-kriteriet är därför inte
# "körs aldrig av npm" (den GENERERAS av ett npm-skript) utan review runda
# 2:s uttryckliga alternativa kriterium: DEPLOYAS/SERVERAS den aldrig?
# Verifierat mot disk, 2026-09-19:
#   - vercel.json: `"outputDirectory": "dist"` — bara Vites byggutdata
#     deployas, `docs/` ingår inte.
#   - vite.config.ts: ingen `publicDir`-override, ingen referens till
#     `docs/` eller `farg-atlas` någonstans i filen.
#   - public/: filens innehåll listat, inga docs/design-filer eller
#     symlänkar dit.
#   - `.github/workflows/*.yml`: `grep -n "atlas\|farg-atlas"` → 0 träffar
#     — `npm run atlas` körs ALDRIG automatiskt, bara manuellt av en
#     designer som regenererar paletten.
# Filen är alltså en ren lokal referensartefakt (öppnas i en webbläsare för
# hand), aldrig servad över nätet av vår app. `<script>`-blocket har därför
# aldrig ett attack-ytesammanhang att verka i.
CODEQL_D0_UNDANTAG="
docs/backfill/segment-export/export.mjs:::S60 Steg 4 (2026-07-09), engångs-backfill mot prod via AIRTABLE_API_KEY. Ingen npm-post, ingen workflow-referens (grep, 0 träffar).
docs/backfill/segment-export/segments.mjs:::Samma S60 Steg 4-familj som export.mjs — engångs-backfill, ingen npm/workflow-referens.
docs/backfill/segment-export/skool-partition.mjs:::Samma S60 Steg 4-familj — engångs-backfill, ingen npm/workflow-referens.
docs/reference/automation-scripts/a1-eventmatchning-vakt.js:::Körs i Airtable som customScript-steg i automation wflDCKPAv2P6Yu9U6 (filens egen källkodskommentar) — aldrig i detta repos CI/npm.
tasks/sessions/bilagor/s121-bekraftelsesteget-konvergens/riggen-proto-shot.mjs:::Fristående Playwright-skärmdumpsrigg, körd manuellt en gång under S121:s konvergens-pass. Ingen npm/workflow-referens.
tasks/sessions/bilagor/s122-pushback-bank/bygg-bank.mjs:::Manuell S122-pushback-pipeline, körd en gång för hand. Ingen npm/workflow-referens.
tasks/sessions/bilagor/s122-pushback-bank/extrahera-pushbacks.mjs:::Samma S122-pipeline-familj som bygg-bank.mjs — manuell engångskörning, ingen npm/workflow-referens.
tasks/sessions/bilagor/s122-pushback-bank/steg3-klassificering.mjs:::Samma S122-pipeline-familj — manuell engångskörning, ingen npm/workflow-referens.
docs/design/farg-atlas.html:::Regenereras av 'npm run atlas' men deployas/servas ALDRIG (vercel.json outputDirectory=dist, ingen publicDir-koppling, 0 workflow-referenser) — ren lokal referensartefakt, script-blocket har inget attack-ytesammanhang.
"
