node_modules
dist
dist-ssr
*.local

# Environment
.env
.env.*
!.env.example
!.env.test.example
!.env.seed.example
# Committade mode-filer (ADR-061 Pelare 1): endast publika VITE_-vars (URL +
# anon/publishable-key, RLS-grindade). Cred-bärande .env.local + .env.test FÖRBLIR ignorerade.
!.env.development
!.env.staging
!.env.production

# macOS / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Vite / TypeScript
*.tsbuildinfo

# TanStack Router — genererad
src/routeTree.gen.ts

# TanStack devtools/plugin — lokal cache
.tanstack/

# Playwright
/playwright-report
/blob-report
/test-results
/playwright/.cache
# Playwright MCP-serverns artefakt-katalog (browser-verktyget) — aldrig committad,
# rapporterad ospårad i åtta sessioner (task-36.3 hygien-post, L321-klassen: durabel
# bärare = denna rad, inte en end-pass-anteckning som dör med sessionen)
.playwright-mcp/

# Playwright auth state — innehåller TEST_USER session-tokens, ALDRIG committa
playwright/.auth/*
!playwright/.auth/.gitkeep
!playwright/.auth/README.md

# Logs
logs
*.log
npm-debug.log*

# Codex scratch
.codex-scratch/

# Supabase CLI state (link, project-ref cache)
supabase/.temp/

# Odoo migration workbench — separat test-migrationsexperiment, ej del av app-bygget
odoo-migration-workbench/
package-lock.json.pre-k0åg

# Lychee — lokal länk-cache vid --cache-körning (CI cachar via actions/cache)
.lycheecache

# Segment-export: utdata bär deltagares e-postadresser — committas ALDRIG (T73)
docs/backfill/segment-export/segment-export.json
docs/backfill/segment-export/lista-*.csv

# Visuell regression (task-36.7, AC 3): ENDAST linux-baselines checkas in —
# baselines föds i CI (rätt miljö), aldrig lokalt. Lokalt genererade
# plattformsbilder är personliga jämförelse-baselines och ignoreras.
tests/visual/__screenshots__/**/*-darwin.png
tests/visual/__screenshots__/**/*-win32.png
