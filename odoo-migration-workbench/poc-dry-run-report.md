# POC Dry Run Report

Dry-run execute mot Odoo kördes inte eftersom Odoo credentials, `ODOO_TARGET_MODE=test`, `ODOO_ALLOW_WRITES=true`, `ODOO_POC_LABEL` och verifierade fält saknas.

Scripten finns för framtida gated körning.
